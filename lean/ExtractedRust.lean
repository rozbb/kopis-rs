-- THIS FILE WAS AUTOMATICALLY GENERATED BY AENEAS
-- [kopis]
import Aeneas
open Aeneas Aeneas.Std Result ControlFlow Error
set_option linter.dupNamespace false
set_option linter.hashCommand false
set_option linter.unusedVariables false

/- You can set the `maxHeartbeats` value with the `-max-heartbeats` CLI option -/
set_option maxHeartbeats 1000000

/- You can set the `maxRecDepth` value with the `-max-recdepth` CLI option -/
set_option maxRecDepth 2048

/- You can remove the following line by using the CLI option `-all-computable`: -/
noncomputable section

namespace RustKopis

/-- Trait implementation: [core::array::equality::{impl core::cmp::PartialEq<[U; N]> for [T; N]}]
    Source: '/rustc/library/core/src/array/equality.rs', lines 5:0-7:28
    Name pattern: [core::cmp::PartialEq<[@T; @N], [@U; @N]>] -/
@[reducible, rust_trait_impl "core::cmp::PartialEq<[@T; @N], [@U; @N]>"]
def Array.Insts.CoreCmpPartialEqArray {T : Type} {U : Type} (N : Std.Usize)
  (cmpPartialEqInst : core.cmp.PartialEq T U) : core.cmp.PartialEq (Array T N)
  (Array U N) := {
  eq := core.array.equality.PartialEqArray.eq cmpPartialEqInst
}

/-- [core::num::{i64}::wrapping_neg]:
    Source: '/rustc/library/core/src/num/int_macros.rs', lines 2363:8-2363:47
    Name pattern: [core::num::{i64}::wrapping_neg]
    Visibility: public -/
@[rust_fun "core::num::{i64}::wrapping_neg"]
axiom core.num.I64.wrapping_neg : Std.I64 → Result Std.I64

/-- [core::num::{u32}::count_ones]:
    Source: '/rustc/library/core/src/num/uint_macros.rs', lines 84:8-84:44
    Name pattern: [core::num::{u32}::count_ones]
    Visibility: public -/
@[rust_fun "core::num::{u32}::count_ones"]
axiom core.num.U32.count_ones : Std.U32 → Result Std.U32

/-- [core::num::{u8}::count_ones]:
    Source: '/rustc/library/core/src/num/uint_macros.rs', lines 84:8-84:44
    Name pattern: [core::num::{u8}::count_ones]
    Visibility: public -/
@[rust_fun "core::num::{u8}::count_ones"]
axiom core.num.U8.count_ones : Std.U8 → Result Std.U32

/-- Trait declaration: [core::ops::arith::Add]
    Source: '/rustc/library/core/src/ops/arith.rs', lines 76:0-76:31
    Name pattern: [core::ops::arith::Add]
    Visibility: public -/
@[rust_trait "core::ops::arith::Add"]
structure core.ops.arith.Add (Self : Type) (Rhs : Type) (Self_Output : Type)
  where
  add : Self → Rhs → Result Self_Output

/-- Trait declaration: [core::ops::arith::Sub]
    Source: '/rustc/library/core/src/ops/arith.rs', lines 188:0-188:31
    Name pattern: [core::ops::arith::Sub]
    Visibility: public -/
@[rust_trait "core::ops::arith::Sub"]
structure core.ops.arith.Sub (Self : Type) (Rhs : Type) (Self_Output : Type)
  where
  sub : Self → Rhs → Result Self_Output

/-- Trait declaration: [core::ops::arith::Mul]
    Source: '/rustc/library/core/src/ops/arith.rs', lines 322:0-322:31
    Name pattern: [core::ops::arith::Mul]
    Visibility: public -/
@[rust_trait "core::ops::arith::Mul"]
structure core.ops.arith.Mul (Self : Type) (Rhs : Type) (Self_Output : Type)
  where
  mul : Self → Rhs → Result Self_Output

/-- Trait declaration: [rand_core::RngCore]
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/rand_core-0.9.5/src/lib.rs', lines 130:0-130:17
    Name pattern: [rand_core::RngCore]
    Visibility: public -/
@[rust_trait "rand_core::RngCore"]
structure rand_core.RngCore (Self : Type) where
  next_u32 : Self → Result (Std.U32 × Self)
  next_u64 : Self → Result (Std.U64 × Self)
  fill_bytes : Self → Slice Std.U8 → Result (Self × (Slice Std.U8))

/-- Trait declaration: [rand_core::CryptoRng]
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/rand_core-0.9.5/src/lib.rs', lines 204:0-204:28
    Name pattern: [rand_core::CryptoRng]
    Visibility: public -/
@[rust_trait "rand_core::CryptoRng" (parentClauses := ["RngCoreInst"])]
structure rand_core.CryptoRng (Self : Type) where
  RngCoreInst : rand_core.RngCore Self

/-- [subtle::Choice]
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/subtle-2.6.1/src/lib.rs', lines 120:0-120:17
    Name pattern: [subtle::Choice]
    Visibility: public -/
@[rust_type "subtle::Choice"]
axiom subtle.Choice : Type

/-- Trait declaration: [subtle::ConstantTimeEq]
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/subtle-2.6.1/src/lib.rs', lines 262:0-262:24
    Name pattern: [subtle::ConstantTimeEq]
    Visibility: public -/
@[rust_trait "subtle::ConstantTimeEq"]
structure subtle.ConstantTimeEq (Self : Type) where
  ct_eq : Self → Self → Result subtle.Choice

/-- [subtle::{impl subtle::ConstantTimeEq for [T]}::ct_eq]:
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/subtle-2.6.1/src/lib.rs', lines 313:4-313:41
    Name pattern: [subtle::{subtle::ConstantTimeEq<[@T]>}::ct_eq]
    Visibility: public -/
@[rust_fun "subtle::{subtle::ConstantTimeEq<[@T]>}::ct_eq"]
axiom Slice.Insts.SubtleConstantTimeEq.ct_eq
  {T : Type} (ConstantTimeEqInst : subtle.ConstantTimeEq T) :
  Slice T → Slice T → Result subtle.Choice

/-- [subtle::{impl subtle::ConstantTimeEq for u8}::ct_eq]:
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/subtle-2.6.1/src/lib.rs', lines 348:12-348:51
    Name pattern: [subtle::{subtle::ConstantTimeEq<u8>}::ct_eq]
    Visibility: public -/
@[rust_fun "subtle::{subtle::ConstantTimeEq<u8>}::ct_eq"]
axiom U8.Insts.SubtleConstantTimeEq.ct_eq
  : Std.U8 → Std.U8 → Result subtle.Choice

/-- Trait implementation: [subtle::{impl subtle::ConstantTimeEq for u8}]
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/subtle-2.6.1/src/lib.rs', lines 346:8-346:36
    Name pattern: [subtle::ConstantTimeEq<u8>] -/
@[reducible, rust_trait_impl "subtle::ConstantTimeEq<u8>"]
def U8.Insts.SubtleConstantTimeEq : subtle.ConstantTimeEq Std.U8 := {
  ct_eq := U8.Insts.SubtleConstantTimeEq.ct_eq
}

/-- Trait declaration: [subtle::ConditionallySelectable]
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/subtle-2.6.1/src/lib.rs', lines 393:0-393:39
    Name pattern: [subtle::ConditionallySelectable]
    Visibility: public -/
@[rust_trait "subtle::ConditionallySelectable"
  (parentClauses := ["coremarkerCopyInst"])]
structure subtle.ConditionallySelectable (Self : Type) where
  coremarkerCopyInst : core.marker.Copy Self
  conditional_select : Self → Self → subtle.Choice → Result Self

/-- [subtle::{impl subtle::ConditionallySelectable for u8}::conditional_select]:
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/subtle-2.6.1/src/lib.rs', lines 513:12-513:77
    Name pattern: [subtle::{subtle::ConditionallySelectable<u8>}::conditional_select]
    Visibility: public -/
@[rust_fun "subtle::{subtle::ConditionallySelectable<u8>}::conditional_select"]
axiom U8.Insts.SubtleConditionallySelectable.conditional_select
  : Std.U8 → Std.U8 → subtle.Choice → Result Std.U8

/-- Trait implementation: [subtle::{impl subtle::ConditionallySelectable for u8}]
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/subtle-2.6.1/src/lib.rs', lines 511:8-537:10
    Name pattern: [subtle::ConditionallySelectable<u8>] -/
@[reducible, rust_trait_impl "subtle::ConditionallySelectable<u8>"]
def U8.Insts.SubtleConditionallySelectable : subtle.ConditionallySelectable
  Std.U8 := {
  coremarkerCopyInst := core.marker.CopyU8
  conditional_select :=
    U8.Insts.SubtleConditionallySelectable.conditional_select
}

/-- [subtle::{impl subtle::ConditionallySelectable for [T; N]}::conditional_select]:
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/subtle-2.6.1/src/lib.rs', lines 581:4-581:69
    Name pattern: [subtle::{subtle::ConditionallySelectable<[@T; @N]>}::conditional_select]
    Visibility: public -/
@[rust_fun
  "subtle::{subtle::ConditionallySelectable<[@T; @N]>}::conditional_select"]
axiom Array.Insts.SubtleConditionallySelectable.conditional_select
  {T : Type} {N : Std.Usize} (ConditionallySelectableInst :
  subtle.ConditionallySelectable T) :
  Array T N → Array T N → subtle.Choice → Result (Array T N)

/-- [turboshake::TurboShake]
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/turboshake-0.7.1/src/lib.rs', lines 35:0-35:54
    Name pattern: [turboshake::TurboShake]
    Visibility: public -/
@[rust_type "turboshake::TurboShake"]
axiom turboshake.TurboShake (RATE : Std.Usize) (DS : Std.U8) : Type

/-- [turboshake::{impl core::default::Default for turboshake::TurboShake<RATE, DS>}::default]:
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/turboshake-0.7.1/src/lib.rs', lines 43:4-43:24
    Name pattern: [turboshake::{core::default::Default<turboshake::TurboShake<@RATE, @DS>>}::default]
    Visibility: public -/
@[rust_fun
  "turboshake::{core::default::Default<turboshake::TurboShake<@RATE, @DS>>}::default"]
axiom turboshake.TurboShake.Insts.CoreDefaultDefault.default
  (RATE : Std.Usize) (DS : Std.U8) : Result (turboshake.TurboShake RATE DS)

/-- [turboshake::{impl digest::Update for turboshake::TurboShake<RATE, DS>}::update]:
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/turboshake-0.7.1/src/lib.rs', lines 60:4-60:37
    Name pattern: [turboshake::{digest::Update<turboshake::TurboShake<@RATE, @DS>>}::update]
    Visibility: public -/
@[rust_fun
  "turboshake::{digest::Update<turboshake::TurboShake<@RATE, @DS>>}::update"]
axiom turboshake.TurboShake.Insts.DigestUpdate.update
  {RATE : Std.Usize} {DS : Std.U8} :
  turboshake.TurboShake RATE DS → Slice Std.U8 → Result
    (turboshake.TurboShake RATE DS)

/-- [turboshake::TurboShakeReader]
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/turboshake-0.7.1/src/lib.rs', lines 153:0-153:46
    Name pattern: [turboshake::TurboShakeReader]
    Visibility: public -/
@[rust_type "turboshake::TurboShakeReader"]
axiom turboshake.TurboShakeReader (RATE : Std.Usize) : Type

/-- [turboshake::{impl digest::ExtendableOutput<turboshake::TurboShakeReader<RATE>> for turboshake::TurboShake<RATE, DS>}::finalize_xof]:
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/turboshake-0.7.1/src/lib.rs', lines 83:4-83:45
    Name pattern: [turboshake::{digest::ExtendableOutput<turboshake::TurboShake<@RATE, @DS>, turboshake::TurboShakeReader<@RATE>>}::finalize_xof]
    Visibility: public -/
@[rust_fun
  "turboshake::{digest::ExtendableOutput<turboshake::TurboShake<@RATE, @DS>, turboshake::TurboShakeReader<@RATE>>}::finalize_xof"]
axiom
  turboshake.TurboShake.Insts.DigestExtendableOutputTurboShakeReader.finalize_xof
  {RATE : Std.Usize} {DS : Std.U8} :
  turboshake.TurboShake RATE DS → Result (turboshake.TurboShakeReader RATE)

/-- [turboshake::{impl digest::XofReader for turboshake::TurboShakeReader<RATE>}::read]:
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/turboshake-0.7.1/src/lib.rs', lines 161:4-161:38
    Name pattern: [turboshake::{digest::XofReader<turboshake::TurboShakeReader<@RATE>>}::read]
    Visibility: public -/
@[rust_fun
  "turboshake::{digest::XofReader<turboshake::TurboShakeReader<@RATE>>}::read"]
axiom turboshake.TurboShakeReader.Insts.DigestXofReader.read
  {RATE : Std.Usize} :
  turboshake.TurboShakeReader RATE → Slice Std.U8 → Result
    ((turboshake.TurboShakeReader RATE) × (Slice Std.U8))

/-- Trait declaration: [zeroize::Zeroize]
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/zeroize-1.9.0/src/lib.rs', lines 245:0-245:17
    Name pattern: [zeroize::Zeroize]
    Visibility: public -/
@[rust_trait "zeroize::Zeroize"]
structure zeroize.Zeroize (Self : Type) where
  zeroize : Self → Result Self

/-- Trait declaration: [zeroize::ZeroizeOnDrop]
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/zeroize-1.9.0/src/lib.rs', lines 252:0-252:23
    Name pattern: [zeroize::ZeroizeOnDrop]
    Visibility: public -/
@[rust_trait "zeroize::ZeroizeOnDrop"]
structure zeroize.ZeroizeOnDrop (Self : Type) where

/-- Trait declaration: [zeroize::DefaultIsZeroes]
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/zeroize-1.9.0/src/lib.rs', lines 255:0-255:49
    Name pattern: [zeroize::DefaultIsZeroes]
    Visibility: public -/
@[rust_trait "zeroize::DefaultIsZeroes"
  (parentClauses := ["coremarkerCopyInst", "coredefaultDefaultInst"])]
structure zeroize.DefaultIsZeroes (Self : Type) where
  coremarkerCopyInst : core.marker.Copy Self
  coredefaultDefaultInst : core.default.Default Self

/-- [zeroize::{impl zeroize::Zeroize for Z}::zeroize]:
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/zeroize-1.9.0/src/lib.rs', lines 274:4-274:25
    Name pattern: [zeroize::{zeroize::Zeroize<@Z>}::zeroize]
    Visibility: public -/
@[rust_fun "zeroize::{zeroize::Zeroize<@Z>}::zeroize"]
axiom zeroize.Zeroize.Blanket.zeroize
  {Z : Type} (DefaultIsZeroesInst : zeroize.DefaultIsZeroes Z) : Z → Result Z

/-- Trait implementation: [zeroize::{impl zeroize::Zeroize for Z}]
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/zeroize-1.9.0/src/lib.rs', lines 270:0-272:23
    Name pattern: [zeroize::Zeroize<@Z>] -/
@[reducible, rust_trait_impl "zeroize::Zeroize<@Z>"]
def zeroize.Zeroize.Blanket {Z : Type} (DefaultIsZeroesInst :
  zeroize.DefaultIsZeroes Z) : zeroize.Zeroize Z := {
  zeroize := zeroize.Zeroize.Blanket.zeroize DefaultIsZeroesInst
}

/-- Trait implementation: [zeroize::{impl zeroize::DefaultIsZeroes for u8}]
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/zeroize-1.9.0/src/lib.rs', lines 282:10-282:40
    Name pattern: [zeroize::DefaultIsZeroes<u8>] -/
@[reducible, rust_trait_impl "zeroize::DefaultIsZeroes<u8>"]
def U8.Insts.ZeroizeDefaultIsZeroes : zeroize.DefaultIsZeroes Std.U8 := {
  coremarkerCopyInst := core.marker.CopyU8
  coredefaultDefaultInst := core.default.DefaultU8
}

/-- Trait implementation: [zeroize::{impl zeroize::DefaultIsZeroes for i32}]
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/zeroize-1.9.0/src/lib.rs', lines 282:10-282:40
    Name pattern: [zeroize::DefaultIsZeroes<i32>] -/
@[reducible, rust_trait_impl "zeroize::DefaultIsZeroes<i32>"]
def I32.Insts.ZeroizeDefaultIsZeroes : zeroize.DefaultIsZeroes Std.I32 := {
  coremarkerCopyInst := core.marker.CopyI32
  coredefaultDefaultInst := core.default.DefaultI32
}

/-- Trait implementation: [zeroize::{impl zeroize::DefaultIsZeroes for u16}]
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/zeroize-1.9.0/src/lib.rs', lines 282:10-282:40
    Name pattern: [zeroize::DefaultIsZeroes<u16>] -/
@[reducible, rust_trait_impl "zeroize::DefaultIsZeroes<u16>"]
def U16.Insts.ZeroizeDefaultIsZeroes : zeroize.DefaultIsZeroes Std.U16 := {
  coremarkerCopyInst := core.marker.CopyU16
  coredefaultDefaultInst := core.default.DefaultU16
}

/-- [zeroize::{impl zeroize::Zeroize for [Z; N]}::zeroize]:
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/zeroize-1.9.0/src/lib.rs', lines 346:4-346:25
    Name pattern: [zeroize::{zeroize::Zeroize<[@Z; @N]>}::zeroize]
    Visibility: public -/
@[rust_fun "zeroize::{zeroize::Zeroize<[@Z; @N]>}::zeroize"]
axiom Array.Insts.ZeroizeZeroize.zeroize
  {Z : Type} {N : Std.Usize} (ZeroizeInst : zeroize.Zeroize Z) :
  Array Z N → Result (Array Z N)

/-- Trait implementation: [zeroize::{impl zeroize::Zeroize for [Z; N]}]
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/zeroize-1.9.0/src/lib.rs', lines 342:0-344:15
    Name pattern: [zeroize::Zeroize<[@Z; @N]>] -/
@[reducible, rust_trait_impl "zeroize::Zeroize<[@Z; @N]>"]
def Array.Insts.ZeroizeZeroize {Z : Type} (N : Std.Usize) (ZeroizeInst :
  zeroize.Zeroize Z) : zeroize.Zeroize (Array Z N) := {
  zeroize := Array.Insts.ZeroizeZeroize.zeroize ZeroizeInst
}

/-- [zeroize::__internal::{impl zeroize::__internal::AssertZeroizeOnDrop for &'_0 &'_1 mut T}::zeroize_or_on_drop]:
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/zeroize-1.9.0/src/lib.rs', lines 837:8-837:35
    Name pattern: [zeroize::__internal::{zeroize::__internal::AssertZeroizeOnDrop<&'0 &'1 mut @T>}::zeroize_or_on_drop]
    Visibility: public -/
@[rust_fun
  "zeroize::__internal::{zeroize::__internal::AssertZeroizeOnDrop<&'0 &'1 mut @T>}::zeroize_or_on_drop"]
axiom
  Shared0Mut1T.Insts.Zeroize__internalAssertZeroizeOnDrop.zeroize_or_on_drop
  {T : Type} (ZeroizeOnDropInst : zeroize.ZeroizeOnDrop T) : T → Result Unit

/-- [zeroize::__internal::{impl zeroize::__internal::AssertZeroize for T}::zeroize_or_on_drop]:
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/zeroize-1.9.0/src/lib.rs', lines 846:8-846:40
    Name pattern: [zeroize::__internal::{zeroize::__internal::AssertZeroize<@T>}::zeroize_or_on_drop]
    Visibility: public -/
@[rust_fun
  "zeroize::__internal::{zeroize::__internal::AssertZeroize<@T>}::zeroize_or_on_drop"]
axiom zeroize.__internal.AssertZeroize.Blanket.zeroize_or_on_drop
  {T : Type} (ZeroizeInst : zeroize.Zeroize T) : T → Result T

/-- [kopis::arithmetic::ring_arith::RingElem]
    Source: 'src/arithmetic/ring_arith.rs', lines 16:0-16:48
    Visibility: public -/
@[reducible]
def arithmetic.ring_arith.RingElem := Array Std.U16 256#usize

/-- [kopis::arithmetic::matrix_arith::Matrix]
    Source: 'src/arithmetic/matrix_arith.rs', lines 13:0-13:88 -/
@[reducible]
def arithmetic.matrix_arith.Matrix (X : Std.Usize) (Y : Std.Usize) :=
  Array (Array arithmetic.ring_arith.RingElem Y) X

/-- [kopis::arithmetic::ring_arith::{impl core::cmp::PartialEq<kopis::arithmetic::ring_arith::RingElem> for kopis::arithmetic::ring_arith::RingElem}::eq]:
    Source: 'src/arithmetic/ring_arith.rs', lines 15:13-15:22
    Visibility: public -/
def arithmetic.ring_arith.RingElem.Insts.CoreCmpPartialEqRingElem.eq
  (self : arithmetic.ring_arith.RingElem)
  (other : arithmetic.ring_arith.RingElem) :
  Result Bool
  := do
  core.array.equality.PartialEqArray.eq core.cmp.PartialEqU16 self other

/-- Trait implementation: [kopis::arithmetic::ring_arith::{impl core::cmp::PartialEq<kopis::arithmetic::ring_arith::RingElem> for kopis::arithmetic::ring_arith::RingElem}]
    Source: 'src/arithmetic/ring_arith.rs', lines 15:13-15:22 -/
@[reducible]
def arithmetic.ring_arith.RingElem.Insts.CoreCmpPartialEqRingElem :
  core.cmp.PartialEq arithmetic.ring_arith.RingElem
  arithmetic.ring_arith.RingElem := {
  eq := arithmetic.ring_arith.RingElem.Insts.CoreCmpPartialEqRingElem.eq
}

/-- [kopis::arithmetic::matrix_arith::{impl core::cmp::PartialEq<kopis::arithmetic::matrix_arith::Matrix<X, Y>> for kopis::arithmetic::matrix_arith::Matrix<X, Y>}::eq]:
    Source: 'src/arithmetic/matrix_arith.rs', lines 12:13-12:22
    Visibility: public -/
def arithmetic.matrix_arith.Matrix.Insts.CoreCmpPartialEqMatrix.eq
  {X : Std.Usize} {Y : Std.Usize} (self : arithmetic.matrix_arith.Matrix X Y)
  (other : arithmetic.matrix_arith.Matrix X Y) :
  Result Bool
  := do
  core.array.equality.PartialEqArray.eq (Array.Insts.CoreCmpPartialEqArray Y
    arithmetic.ring_arith.RingElem.Insts.CoreCmpPartialEqRingElem) self other

/-- Trait implementation: [kopis::arithmetic::matrix_arith::{impl core::cmp::PartialEq<kopis::arithmetic::matrix_arith::Matrix<X, Y>> for kopis::arithmetic::matrix_arith::Matrix<X, Y>}]
    Source: 'src/arithmetic/matrix_arith.rs', lines 12:13-12:22 -/
@[reducible]
def arithmetic.matrix_arith.Matrix.Insts.CoreCmpPartialEqMatrix (X : Std.Usize)
  (Y : Std.Usize) : core.cmp.PartialEq (arithmetic.matrix_arith.Matrix X Y)
  (arithmetic.matrix_arith.Matrix X Y) := {
  eq := arithmetic.matrix_arith.Matrix.Insts.CoreCmpPartialEqMatrix.eq
}

/-- [kopis::arithmetic::matrix_arith::{impl core::cmp::Eq for kopis::arithmetic::matrix_arith::Matrix<X, Y>}::assert_fields_are_eq]:
    Source: 'src/arithmetic/matrix_arith.rs', lines 12:9-12:11
    Visibility: public -/
def arithmetic.matrix_arith.Matrix.Insts.CoreCmpEq.assert_fields_are_eq
  {X : Std.Usize} {Y : Std.Usize} (self : arithmetic.matrix_arith.Matrix X Y) :
  Result Unit
  := do
  ok ()

/-- Trait implementation: [kopis::arithmetic::matrix_arith::{impl core::cmp::Eq for kopis::arithmetic::matrix_arith::Matrix<X, Y>}]
    Source: 'src/arithmetic/matrix_arith.rs', lines 12:9-12:11 -/
@[reducible]
def arithmetic.matrix_arith.Matrix.Insts.CoreCmpEq (X : Std.Usize) (Y :
  Std.Usize) : core.cmp.Eq (arithmetic.matrix_arith.Matrix X Y) := {
  partialEqInst := arithmetic.matrix_arith.Matrix.Insts.CoreCmpPartialEqMatrix
    X Y
  assert_fields_are_eq :=
    arithmetic.matrix_arith.Matrix.Insts.CoreCmpEq.assert_fields_are_eq
}

/-- Trait implementation: [kopis::arithmetic::matrix_arith::{impl core::marker::StructuralPartialEq for kopis::arithmetic::matrix_arith::Matrix<X, Y>}]
    Source: 'src/arithmetic/matrix_arith.rs', lines 12:13-12:22 -/
@[reducible]
def arithmetic.matrix_arith.Matrix.Insts.CoreMarkerStructuralPartialEq (X :
  Std.Usize) (Y : Std.Usize) : core.marker.StructuralPartialEq
  (arithmetic.matrix_arith.Matrix X Y) := {
}

/-- [kopis::arithmetic::ring_arith::{impl core::fmt::Debug for kopis::arithmetic::ring_arith::RingElem}::fmt]:
    Source: 'src/arithmetic/ring_arith.rs', lines 15:24-15:29
    Visibility: public -/
def arithmetic.ring_arith.RingElem.Insts.CoreFmtDebug.fmt
  (self : arithmetic.ring_arith.RingElem) (f : core.fmt.Formatter) :
  Result ((core.result.Result Unit core.fmt.Error) × core.fmt.Formatter)
  := do
  let dyn :=
    Dyn.mk _ (core.fmt.DebugShared (Array.Insts.CoreFmtDebug 256#usize
      core.fmt.DebugU16)) self
  core.fmt.Formatter.debug_tuple_field1_finish f (toStr "RingElem") dyn

/-- Trait implementation: [kopis::arithmetic::ring_arith::{impl core::fmt::Debug for kopis::arithmetic::ring_arith::RingElem}]
    Source: 'src/arithmetic/ring_arith.rs', lines 15:24-15:29 -/
@[reducible]
def arithmetic.ring_arith.RingElem.Insts.CoreFmtDebug : core.fmt.Debug
  arithmetic.ring_arith.RingElem := {
  fmt := arithmetic.ring_arith.RingElem.Insts.CoreFmtDebug.fmt
}

/-- [kopis::arithmetic::matrix_arith::{impl core::fmt::Debug for kopis::arithmetic::matrix_arith::Matrix<X, Y>}::fmt]:
    Source: 'src/arithmetic/matrix_arith.rs', lines 12:24-12:29
    Visibility: public -/
def arithmetic.matrix_arith.Matrix.Insts.CoreFmtDebug.fmt
  {X : Std.Usize} {Y : Std.Usize} (self : arithmetic.matrix_arith.Matrix X Y)
  (f : core.fmt.Formatter) :
  Result ((core.result.Result Unit core.fmt.Error) × core.fmt.Formatter)
  := do
  let dyn :=
    Dyn.mk _ (core.fmt.DebugShared (Array.Insts.CoreFmtDebug X
      (Array.Insts.CoreFmtDebug Y
      arithmetic.ring_arith.RingElem.Insts.CoreFmtDebug))) self
  core.fmt.Formatter.debug_tuple_field1_finish f (toStr "Matrix") dyn

/-- Trait implementation: [kopis::arithmetic::matrix_arith::{impl core::fmt::Debug for kopis::arithmetic::matrix_arith::Matrix<X, Y>}]
    Source: 'src/arithmetic/matrix_arith.rs', lines 12:24-12:29 -/
@[reducible]
def arithmetic.matrix_arith.Matrix.Insts.CoreFmtDebug (X : Std.Usize) (Y :
  Std.Usize) : core.fmt.Debug (arithmetic.matrix_arith.Matrix X Y) := {
  fmt := arithmetic.matrix_arith.Matrix.Insts.CoreFmtDebug.fmt
}

/-- [kopis::arithmetic::ring_arith::{impl core::clone::Clone for kopis::arithmetic::ring_arith::RingElem}::clone]:
    Source: 'src/arithmetic/ring_arith.rs', lines 15:31-15:36
    Visibility: public -/
def arithmetic.ring_arith.RingElem.Insts.CoreCloneClone.clone
  (self : arithmetic.ring_arith.RingElem) :
  Result arithmetic.ring_arith.RingElem
  := do
  ok self

/-- Trait implementation: [kopis::arithmetic::ring_arith::{impl core::clone::Clone for kopis::arithmetic::ring_arith::RingElem}]
    Source: 'src/arithmetic/ring_arith.rs', lines 15:31-15:36 -/
@[reducible]
def arithmetic.ring_arith.RingElem.Insts.CoreCloneClone : core.clone.Clone
  arithmetic.ring_arith.RingElem := {
  clone := arithmetic.ring_arith.RingElem.Insts.CoreCloneClone.clone
}

/-- [kopis::arithmetic::matrix_arith::{impl core::clone::Clone for kopis::arithmetic::matrix_arith::Matrix<X, Y>}::clone]:
    Source: 'src/arithmetic/matrix_arith.rs', lines 12:31-12:36
    Visibility: public -/
def arithmetic.matrix_arith.Matrix.Insts.CoreCloneClone.clone
  {X : Std.Usize} {Y : Std.Usize} (self : arithmetic.matrix_arith.Matrix X Y) :
  Result (arithmetic.matrix_arith.Matrix X Y)
  := do
  let a ←
    core.array.CloneArray.clone (core.clone.CloneArray Y
      arithmetic.ring_arith.RingElem.Insts.CoreCloneClone) self
  ok a

/-- Trait implementation: [kopis::arithmetic::matrix_arith::{impl core::clone::Clone for kopis::arithmetic::matrix_arith::Matrix<X, Y>}]
    Source: 'src/arithmetic/matrix_arith.rs', lines 12:31-12:36 -/
@[reducible]
def arithmetic.matrix_arith.Matrix.Insts.CoreCloneClone (X : Std.Usize) (Y :
  Std.Usize) : core.clone.Clone (arithmetic.matrix_arith.Matrix X Y) := {
  clone := arithmetic.matrix_arith.Matrix.Insts.CoreCloneClone.clone
}

/-- [kopis::arithmetic::ring_arith::{impl zeroize::Zeroize for kopis::arithmetic::ring_arith::RingElem}::zeroize]:
    Source: 'src/arithmetic/ring_arith.rs', lines 15:44-15:51
    Visibility: public -/
def arithmetic.ring_arith.RingElem.Insts.ZeroizeZeroize.zeroize
  (self : arithmetic.ring_arith.RingElem) :
  Result arithmetic.ring_arith.RingElem
  := do
  let __zeroize_field_0 ←
    Array.Insts.ZeroizeZeroize.zeroize (zeroize.Zeroize.Blanket
      U16.Insts.ZeroizeDefaultIsZeroes) self
  ok __zeroize_field_0

/-- Trait implementation: [kopis::arithmetic::ring_arith::{impl zeroize::Zeroize for kopis::arithmetic::ring_arith::RingElem}]
    Source: 'src/arithmetic/ring_arith.rs', lines 15:44-15:51 -/
@[reducible]
def arithmetic.ring_arith.RingElem.Insts.ZeroizeZeroize : zeroize.Zeroize
  arithmetic.ring_arith.RingElem := {
  zeroize := arithmetic.ring_arith.RingElem.Insts.ZeroizeZeroize.zeroize
}

/-- [kopis::arithmetic::matrix_arith::{impl zeroize::Zeroize for kopis::arithmetic::matrix_arith::Matrix<X, Y>}::zeroize]:
    Source: 'src/arithmetic/matrix_arith.rs', lines 12:38-12:45
    Visibility: public -/
def arithmetic.matrix_arith.Matrix.Insts.ZeroizeZeroize.zeroize
  {X : Std.Usize} {Y : Std.Usize} (self : arithmetic.matrix_arith.Matrix X Y) :
  Result (arithmetic.matrix_arith.Matrix X Y)
  := do
  let __zeroize_field_0 ←
    Array.Insts.ZeroizeZeroize.zeroize (Array.Insts.ZeroizeZeroize Y
      arithmetic.ring_arith.RingElem.Insts.ZeroizeZeroize) self
  ok __zeroize_field_0

/-- Trait implementation: [kopis::arithmetic::matrix_arith::{impl zeroize::Zeroize for kopis::arithmetic::matrix_arith::Matrix<X, Y>}]
    Source: 'src/arithmetic/matrix_arith.rs', lines 12:38-12:45 -/
@[reducible]
def arithmetic.matrix_arith.Matrix.Insts.ZeroizeZeroize (X : Std.Usize) (Y :
  Std.Usize) : zeroize.Zeroize (arithmetic.matrix_arith.Matrix X Y) := {
  zeroize := arithmetic.matrix_arith.Matrix.Insts.ZeroizeZeroize.zeroize
}

/-- [kopis::arithmetic::ring_arith::{impl core::default::Default for kopis::arithmetic::ring_arith::RingElem}::default]:
    Source: 'src/arithmetic/ring_arith.rs', lines 19:4-21:5
    Visibility: public -/
def arithmetic.ring_arith.RingElem.Insts.CoreDefaultDefault.default
  : Result arithmetic.ring_arith.RingElem := do
  let a := Array.repeat 256#usize 0#u16
  ok a

/-- [kopis::arithmetic::matrix_arith::{impl core::default::Default for kopis::arithmetic::matrix_arith::Matrix<X, Y>}::default]:
    Source: 'src/arithmetic/matrix_arith.rs', lines 16:4-18:5
    Visibility: public -/
def arithmetic.matrix_arith.Matrix.Insts.CoreDefaultDefault.default
  (X : Std.Usize) (Y : Std.Usize) :
  Result (arithmetic.matrix_arith.Matrix X Y)
  := do
  let re ← arithmetic.ring_arith.RingElem.Insts.CoreDefaultDefault.default
  let a := Array.repeat Y re
  let a1 := Array.repeat X a
  ok a1

/-- Trait implementation: [kopis::arithmetic::matrix_arith::{impl core::default::Default for kopis::arithmetic::matrix_arith::Matrix<X, Y>}]
    Source: 'src/arithmetic/matrix_arith.rs', lines 15:0-19:1 -/
@[reducible]
def arithmetic.matrix_arith.Matrix.Insts.CoreDefaultDefault (X : Std.Usize) (Y
  : Std.Usize) : core.default.Default (arithmetic.matrix_arith.Matrix X Y) := {
  default := arithmetic.matrix_arith.Matrix.Insts.CoreDefaultDefault.default X
    Y
}

/-- [kopis::arithmetic::ring_arith::{kopis::arithmetic::ring_arith::RingElem}::shift_right]: loop 0:
    Source: 'src/arithmetic/ring_arith.rs', lines 86:8-88:9 -/
@[rust_loop]
def arithmetic.ring_arith.RingElem.shift_right_loop
  (iter : core.slice.iter.IterMut Std.U16)
  (back : core.slice.iter.IterMut Std.U16 → core.slice.iter.IterMut Std.U16)
  (shift : Std.Usize) :
  Result (core.slice.iter.IterMut Std.U16)
  := do
  let (o, iter1, next_back) ← core.slice.iter.IteratorIterMut.next iter
  match o with
  | none => ok (let im := next_back iter1 none
                back im)
  | some coeff =>
    let coeff1 ← coeff >>> shift
    arithmetic.ring_arith.RingElem.shift_right_loop iter1
      (fun im => let im1 := next_back im (some coeff1)
                 back im1) shift
partial_fixpoint

/-- [kopis::arithmetic::ring_arith::{kopis::arithmetic::ring_arith::RingElem}::shift_right]:
    Source: 'src/arithmetic/ring_arith.rs', lines 85:4-89:5 -/
def arithmetic.ring_arith.RingElem.shift_right
  (self : arithmetic.ring_arith.RingElem) (shift : Std.Usize) :
  Result arithmetic.ring_arith.RingElem
  := do
  let (s, to_slice_mut_back) ← lift (Array.to_slice_mut self)
  let (iter, iter_mut_back) ← core.slice.Slice.iter_mut s
  let back ←
    arithmetic.ring_arith.RingElem.shift_right_loop iter (fun im => im) shift
  let s1 := iter_mut_back back
  let a := to_slice_mut_back s1
  ok a

/-- [kopis::arithmetic::matrix_arith::{kopis::arithmetic::matrix_arith::Matrix<X, Y>}::shift_right]: loop 1:
    Source: 'src/arithmetic/matrix_arith.rs', lines 25:12-27:13 -/
@[rust_loop]
def arithmetic.matrix_arith.Matrix.shift_right_loop0_loop0
  (iter : core.slice.iter.IterMut arithmetic.ring_arith.RingElem)
  (back : core.slice.iter.IterMut arithmetic.ring_arith.RingElem →
  core.slice.iter.IterMut arithmetic.ring_arith.RingElem) (shift : Std.Usize) :
  Result ((core.slice.iter.IterMut arithmetic.ring_arith.RingElem) ×
    (core.slice.iter.IterMut arithmetic.ring_arith.RingElem →
    core.slice.iter.IterMut arithmetic.ring_arith.RingElem))
  := do
  let (o, iter1, next_back) ← core.slice.iter.IteratorIterMut.next iter
  match o with
  | none => ok (iter1, fun im => let im1 := next_back im none
                                 back im1)
  | some elem =>
    let elem1 ← arithmetic.ring_arith.RingElem.shift_right elem shift
    arithmetic.matrix_arith.Matrix.shift_right_loop0_loop0 iter1
      (fun im => let im1 := next_back im (some elem1)
                 back im1) shift
partial_fixpoint

/-- [kopis::arithmetic::matrix_arith::{kopis::arithmetic::matrix_arith::Matrix<X, Y>}::shift_right]: loop 0:
    Source: 'src/arithmetic/matrix_arith.rs', lines 24:8-28:9 -/
@[rust_loop]
def arithmetic.matrix_arith.Matrix.shift_right_loop0
  {Y : Std.Usize}
  (iter : core.slice.iter.IterMut (Array arithmetic.ring_arith.RingElem Y))
  (back : core.slice.iter.IterMut (Array arithmetic.ring_arith.RingElem Y) →
  core.slice.iter.IterMut (Array arithmetic.ring_arith.RingElem Y))
  (shift : Std.Usize) :
  Result (core.slice.iter.IterMut (Array arithmetic.ring_arith.RingElem Y))
  := do
  let (o, iter1, next_back) ← core.slice.iter.IteratorIterMut.next iter
  match o with
  | none => ok (let im := next_back iter1 none
                back im)
  | some row =>
    let (s, to_slice_mut_back) ← lift (Array.to_slice_mut row)
    let (iter2, iter_mut_back) ← core.slice.Slice.iter_mut s
    let (im, back1) ←
      arithmetic.matrix_arith.Matrix.shift_right_loop0_loop0 iter2
        (fun im1 => im1) shift
    arithmetic.matrix_arith.Matrix.shift_right_loop0 iter1
      (fun im1 =>
        let im2 := back1 im
        let s1 := iter_mut_back im2
        let a := to_slice_mut_back s1
        let im3 := next_back im1 (some a)
        back im3) shift
partial_fixpoint

/-- [kopis::arithmetic::matrix_arith::{kopis::arithmetic::matrix_arith::Matrix<X, Y>}::shift_right]:
    Source: 'src/arithmetic/matrix_arith.rs', lines 23:4-29:5 -/
def arithmetic.matrix_arith.Matrix.shift_right
  {X : Std.Usize} {Y : Std.Usize} (self : arithmetic.matrix_arith.Matrix X Y)
  (shift : Std.Usize) :
  Result (arithmetic.matrix_arith.Matrix X Y)
  := do
  let (s, to_slice_mut_back) ← lift (Array.to_slice_mut self)
  let (iter, iter_mut_back) ← core.slice.Slice.iter_mut s
  let back ←
    arithmetic.matrix_arith.Matrix.shift_right_loop0 iter (fun im => im) shift
  let s1 := iter_mut_back back
  let a := to_slice_mut_back s1
  ok a

/-- [kopis::consts::RING_DEG]
    Source: 'src/consts.rs', lines 6:0-6:39 -/
@[global_simps, irreducible] def consts.RING_DEG : Std.Usize := 256#usize

/-- [kopis::arithmetic::ring_arith::HALF]
    Source: 'src/arithmetic/ring_arith.rs', lines 121:0-121:33 -/
@[global_simps, irreducible]
def arithmetic.ring_arith.HALF : Result Std.Usize := consts.RING_DEG / 2#usize

/-- [kopis::arithmetic::ring_arith::schoolbook_128]: loop 1:
    Source: 'src/arithmetic/ring_arith.rs', lines 135:8-137:9 -/
@[rust_loop]
def arithmetic.ring_arith.schoolbook_128_loop0_loop0
  (iter : core.ops.range.Range Std.Usize) (out : Array Std.U16 256#usize)
  (b : Array Std.U16 128#usize) (i : Std.Usize) (ai : Std.U16) :
  Result (Array Std.U16 256#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok out
  | some j =>
    let i1 ← i + j
    let i2 ← Array.index_usize out i1
    let i3 ← Array.index_usize b j
    let i4 ← lift (core.num.U16.wrapping_mul ai i3)
    let i5 ← lift (core.num.U16.wrapping_add i2 i4)
    let a ← Array.update out i1 i5
    arithmetic.ring_arith.schoolbook_128_loop0_loop0 iter1 a b i ai
partial_fixpoint

/-- [kopis::arithmetic::ring_arith::schoolbook_128]: loop 0:
    Source: 'src/arithmetic/ring_arith.rs', lines 133:4-138:5 -/
@[rust_loop]
def arithmetic.ring_arith.schoolbook_128_loop0
  (i : Std.Usize) (iter : core.ops.range.Range Std.Usize)
  (out : Array Std.U16 256#usize) (a : Array Std.U16 128#usize)
  (b : Array Std.U16 128#usize) :
  Result (Array Std.U16 256#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok out
  | some i1 =>
    let ai ← Array.index_usize a i1
    let out1 ←
      arithmetic.ring_arith.schoolbook_128_loop0_loop0
        { start := 0#usize, «end» := i } out b i1 ai
    arithmetic.ring_arith.schoolbook_128_loop0 i iter1 out1 a b
partial_fixpoint

/-- [kopis::arithmetic::ring_arith::schoolbook_128]:
    Source: 'src/arithmetic/ring_arith.rs', lines 130:0-139:1 -/
def arithmetic.ring_arith.schoolbook_128
  (out : Array Std.U16 256#usize) (a : Array Std.U16 128#usize)
  (b : Array Std.U16 128#usize) :
  Result (Array Std.U16 256#usize)
  := do
  let i ← arithmetic.ring_arith.HALF
  arithmetic.ring_arith.schoolbook_128_loop0 i
    { start := 0#usize, «end» := i } out a b

/-- [kopis::arithmetic::ring_arith::ring_mul_acc]: loop 0:
    Source: 'src/arithmetic/ring_arith.rs', lines 175:4-178:5 -/
@[rust_loop]
def arithmetic.ring_arith.ring_mul_acc_loop0
  (iter : core.ops.range.Range Std.Usize) (a_lo : Array Std.U16 128#usize)
  (a_hi : Array Std.U16 128#usize) (b_lo : Array Std.U16 128#usize)
  (b_hi : Array Std.U16 128#usize) (a_sum : Array Std.U16 128#usize)
  (b_sum : Array Std.U16 128#usize) :
  Result ((Array Std.U16 128#usize) × (Array Std.U16 128#usize))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (a_sum, b_sum)
  | some i =>
    let i1 ← Array.index_usize a_lo i
    let i2 ← Array.index_usize a_hi i
    let i3 ← lift (core.num.U16.wrapping_add i1 i2)
    let a ← Array.update a_sum i i3
    let i4 ← Array.index_usize b_lo i
    let i5 ← Array.index_usize b_hi i
    let i6 ← lift (core.num.U16.wrapping_add i4 i5)
    let a1 ← Array.update b_sum i i6
    arithmetic.ring_arith.ring_mul_acc_loop0 iter1 a_lo a_hi b_lo b_hi a a1
partial_fixpoint

/-- [kopis::arithmetic::ring_arith::ring_mul_acc]: loop 1:
    Source: 'src/arithmetic/ring_arith.rs', lines 184:4-204:5 -/
@[rust_loop]
def arithmetic.ring_arith.ring_mul_acc_loop1
  (i : Std.Usize) (iter : core.ops.range.Range Std.Usize)
  (acc : arithmetic.ring_arith.RingElem) (z0 : Array Std.U16 256#usize)
  (z2 : Array Std.U16 256#usize) (z3 : Array Std.U16 256#usize) :
  Result arithmetic.ring_arith.RingElem
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok acc
  | some j =>
    let i1 ← j + i
    let i2 ← Array.index_usize z3 i1
    let i3 ← j + i
    let i4 ← Array.index_usize z0 i3
    let i5 ← lift (core.num.U16.wrapping_sub i2 i4)
    let i6 ← j + i
    let i7 ← Array.index_usize z2 i6
    let z1_wrap ← lift (core.num.U16.wrapping_sub i5 i7)
    let i8 ← Array.index_usize acc j
    let i9 ← Array.index_usize z0 j
    let i10 ← lift (core.num.U16.wrapping_add i8 i9)
    let i11 ← Array.index_usize z2 j
    let i12 ← lift (core.num.U16.wrapping_sub i10 i11)
    let i13 ← lift (core.num.U16.wrapping_sub i12 z1_wrap)
    let a ← Array.update acc j i13
    let i14 ← Array.index_usize z3 j
    let i15 ← lift (core.num.U16.wrapping_sub i14 i9)
    let z1_direct ← lift (core.num.U16.wrapping_sub i15 i11)
    let i16 ← j + i
    let i17 ← Array.index_usize a i16
    let i18 ← j + i
    let i19 ← Array.index_usize z0 i18
    let i20 ← lift (core.num.U16.wrapping_add i17 i19)
    let i21 ← j + i
    let i22 ← Array.index_usize z2 i21
    let i23 ← lift (core.num.U16.wrapping_sub i20 i22)
    let i24 ← lift (core.num.U16.wrapping_add i23 z1_direct)
    let i25 ← j + i
    let a1 ← Array.update a i25 i24
    arithmetic.ring_arith.ring_mul_acc_loop1 i iter1 a1 z0 z2 z3
partial_fixpoint

/-- [kopis::arithmetic::ring_arith::ring_mul_acc]:
    Source: 'src/arithmetic/ring_arith.rs', lines 146:0-205:1 -/
def arithmetic.ring_arith.ring_mul_acc
  (acc : arithmetic.ring_arith.RingElem) (a : arithmetic.ring_arith.RingElem)
  (b : arithmetic.ring_arith.RingElem) :
  Result arithmetic.ring_arith.RingElem
  := do
  let i ← arithmetic.ring_arith.HALF
  let s ←
    core.array.Array.index (core.ops.index.IndexSlice
      (core.slice.index.SliceIndexRangeToUsizeSlice Std.U16)) a
      { «end» := i }
  let r ← core.array.TryFromSharedArraySlice.try_from 128#usize s
  let a_lo ← core.result.Result.unwrap core.fmt.DebugTryFromSliceError r
  let s1 ←
    core.array.Array.index (core.ops.index.IndexSlice
      (core.slice.index.SliceIndexRangeFromUsizeSlice Std.U16)) a
      { start := i }
  let r1 ← core.array.TryFromSharedArraySlice.try_from 128#usize s1
  let a_hi ← core.result.Result.unwrap core.fmt.DebugTryFromSliceError r1
  let s2 ←
    core.array.Array.index (core.ops.index.IndexSlice
      (core.slice.index.SliceIndexRangeToUsizeSlice Std.U16)) b
      { «end» := i }
  let r2 ← core.array.TryFromSharedArraySlice.try_from 128#usize s2
  let b_lo ← core.result.Result.unwrap core.fmt.DebugTryFromSliceError r2
  let s3 ←
    core.array.Array.index (core.ops.index.IndexSlice
      (core.slice.index.SliceIndexRangeFromUsizeSlice Std.U16)) b
      { start := i }
  let r3 ← core.array.TryFromSharedArraySlice.try_from 128#usize s3
  let b_hi ← core.result.Result.unwrap core.fmt.DebugTryFromSliceError r3
  let z0 := Array.repeat 256#usize 0#u16
  let z2 := Array.repeat 256#usize 0#u16
  let z01 ← arithmetic.ring_arith.schoolbook_128 z0 a_lo b_lo
  let z21 ← arithmetic.ring_arith.schoolbook_128 z2 a_hi b_hi
  let a_sum := Array.repeat 128#usize 0#u16
  let b_sum := Array.repeat 128#usize 0#u16
  let (a_sum1, b_sum1) ←
    arithmetic.ring_arith.ring_mul_acc_loop0 { start := 0#usize, «end» := i }
      a_lo a_hi b_lo b_hi a_sum b_sum
  let z3 := Array.repeat 256#usize 0#u16
  let z31 ← arithmetic.ring_arith.schoolbook_128 z3 a_sum1 b_sum1
  arithmetic.ring_arith.ring_mul_acc_loop1 i { start := 0#usize, «end» := i }
    acc z01 z21 z31

/-- [kopis::arithmetic::matrix_arith::{kopis::arithmetic::matrix_arith::Matrix<X, Y>}::mul]: loop 2:
    Source: 'src/arithmetic/matrix_arith.rs', lines 45:16-47:17 -/
@[rust_loop]
def arithmetic.matrix_arith.Matrix.mul_loop0_loop0_loop0
  {X : Std.Usize} {Y : Std.Usize} {Z : Std.Usize}
  (iter : core.ops.range.Range Std.Usize)
  (self : arithmetic.matrix_arith.Matrix X Y)
  (other : arithmetic.matrix_arith.Matrix Y Z)
  (result : arithmetic.matrix_arith.Matrix X Z) (i : Std.Usize) (j : Std.Usize)
  :
  Result ((arithmetic.matrix_arith.Matrix X Y) ×
    (arithmetic.matrix_arith.Matrix Y Z) × (arithmetic.matrix_arith.Matrix X
    Z))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (self, other, result)
  | some k =>
    let (a, index_mut_back) ← Array.index_mut_usize result i
    let (re, index_mut_back1) ← Array.index_mut_usize a k
    let a1 ← Array.index_usize self i
    let re1 ← Array.index_usize a1 j
    let a2 ← Array.index_usize other j
    let re2 ← Array.index_usize a2 k
    let re3 ← arithmetic.ring_arith.ring_mul_acc re re1 re2
    let a3 := index_mut_back1 re3
    let a4 := index_mut_back a3
    arithmetic.matrix_arith.Matrix.mul_loop0_loop0_loop0 iter1 self other a4 i
      j
partial_fixpoint

/-- [kopis::arithmetic::matrix_arith::{kopis::arithmetic::matrix_arith::Matrix<X, Y>}::mul]: loop 1:
    Source: 'src/arithmetic/matrix_arith.rs', lines 44:12-48:13 -/
@[rust_loop]
def arithmetic.matrix_arith.Matrix.mul_loop0_loop0
  {X : Std.Usize} {Y : Std.Usize} {Z : Std.Usize}
  (iter : core.ops.range.Range Std.Usize)
  (self : arithmetic.matrix_arith.Matrix X Y)
  (other : arithmetic.matrix_arith.Matrix Y Z)
  (result : arithmetic.matrix_arith.Matrix X Z) (i : Std.Usize) :
  Result ((arithmetic.matrix_arith.Matrix X Y) ×
    (arithmetic.matrix_arith.Matrix Y Z) × (arithmetic.matrix_arith.Matrix X
    Z))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (self, other, result)
  | some j =>
    let (self1, other1, result1) ←
      arithmetic.matrix_arith.Matrix.mul_loop0_loop0_loop0
        { start := 0#usize, «end» := Z } self other result i j
    arithmetic.matrix_arith.Matrix.mul_loop0_loop0 iter1 self1 other1 result1 i
partial_fixpoint

/-- [kopis::arithmetic::matrix_arith::{kopis::arithmetic::matrix_arith::Matrix<X, Y>}::mul]: loop 0:
    Source: 'src/arithmetic/matrix_arith.rs', lines 43:8-49:9 -/
@[rust_loop]
def arithmetic.matrix_arith.Matrix.mul_loop0
  {X : Std.Usize} {Y : Std.Usize} {Z : Std.Usize}
  (iter : core.ops.range.Range Std.Usize)
  (self : arithmetic.matrix_arith.Matrix X Y)
  (other : arithmetic.matrix_arith.Matrix Y Z)
  (result : arithmetic.matrix_arith.Matrix X Z) :
  Result (arithmetic.matrix_arith.Matrix X Z)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok result
  | some i =>
    let (self1, other1, result1) ←
      arithmetic.matrix_arith.Matrix.mul_loop0_loop0
        { start := 0#usize, «end» := Y } self other result i
    arithmetic.matrix_arith.Matrix.mul_loop0 iter1 self1 other1 result1
partial_fixpoint

/-- [kopis::arithmetic::matrix_arith::{kopis::arithmetic::matrix_arith::Matrix<X, Y>}::mul]:
    Source: 'src/arithmetic/matrix_arith.rs', lines 41:4-52:5 -/
def arithmetic.matrix_arith.Matrix.mul
  {X : Std.Usize} {Y : Std.Usize} {Z : Std.Usize}
  (self : arithmetic.matrix_arith.Matrix X Y)
  (other : arithmetic.matrix_arith.Matrix Y Z) :
  Result (arithmetic.matrix_arith.Matrix X Z)
  := do
  let result ←
    arithmetic.matrix_arith.Matrix.Insts.CoreDefaultDefault.default X Z
  arithmetic.matrix_arith.Matrix.mul_loop0 { start := 0#usize, «end» := X }
    self other result

/-- [kopis::arithmetic::matrix_arith::{kopis::arithmetic::matrix_arith::Matrix<X, Y>}::mul_transpose]: loop 2:
    Source: 'src/arithmetic/matrix_arith.rs', lines 62:16-64:17 -/
@[rust_loop]
def arithmetic.matrix_arith.Matrix.mul_transpose_loop0_loop0_loop0
  {X : Std.Usize} {Y : Std.Usize} {Z : Std.Usize}
  (iter : core.ops.range.Range Std.Usize)
  (self : arithmetic.matrix_arith.Matrix X Y)
  (other : arithmetic.matrix_arith.Matrix X Z)
  (result : arithmetic.matrix_arith.Matrix Y Z) (i : Std.Usize) (j : Std.Usize)
  :
  Result ((arithmetic.matrix_arith.Matrix X Y) ×
    (arithmetic.matrix_arith.Matrix X Z) × (arithmetic.matrix_arith.Matrix Y
    Z))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (self, other, result)
  | some k =>
    let (a, index_mut_back) ← Array.index_mut_usize result j
    let (re, index_mut_back1) ← Array.index_mut_usize a k
    let a1 ← Array.index_usize self i
    let re1 ← Array.index_usize a1 j
    let a2 ← Array.index_usize other i
    let re2 ← Array.index_usize a2 k
    let re3 ← arithmetic.ring_arith.ring_mul_acc re re1 re2
    let a3 := index_mut_back1 re3
    let a4 := index_mut_back a3
    arithmetic.matrix_arith.Matrix.mul_transpose_loop0_loop0_loop0 iter1 self
      other a4 i j
partial_fixpoint

/-- [kopis::arithmetic::matrix_arith::{kopis::arithmetic::matrix_arith::Matrix<X, Y>}::mul_transpose]: loop 1:
    Source: 'src/arithmetic/matrix_arith.rs', lines 61:12-65:13 -/
@[rust_loop]
def arithmetic.matrix_arith.Matrix.mul_transpose_loop0_loop0
  {X : Std.Usize} {Y : Std.Usize} {Z : Std.Usize}
  (iter : core.ops.range.Range Std.Usize)
  (self : arithmetic.matrix_arith.Matrix X Y)
  (other : arithmetic.matrix_arith.Matrix X Z)
  (result : arithmetic.matrix_arith.Matrix Y Z) (i : Std.Usize) :
  Result ((arithmetic.matrix_arith.Matrix X Y) ×
    (arithmetic.matrix_arith.Matrix X Z) × (arithmetic.matrix_arith.Matrix Y
    Z))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (self, other, result)
  | some j =>
    let (self1, other1, result1) ←
      arithmetic.matrix_arith.Matrix.mul_transpose_loop0_loop0_loop0
        { start := 0#usize, «end» := Z } self other result i j
    arithmetic.matrix_arith.Matrix.mul_transpose_loop0_loop0 iter1 self1 other1
      result1 i
partial_fixpoint

/-- [kopis::arithmetic::matrix_arith::{kopis::arithmetic::matrix_arith::Matrix<X, Y>}::mul_transpose]: loop 0:
    Source: 'src/arithmetic/matrix_arith.rs', lines 60:8-66:9 -/
@[rust_loop]
def arithmetic.matrix_arith.Matrix.mul_transpose_loop0
  {X : Std.Usize} {Y : Std.Usize} {Z : Std.Usize}
  (iter : core.ops.range.Range Std.Usize)
  (self : arithmetic.matrix_arith.Matrix X Y)
  (other : arithmetic.matrix_arith.Matrix X Z)
  (result : arithmetic.matrix_arith.Matrix Y Z) :
  Result (arithmetic.matrix_arith.Matrix Y Z)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok result
  | some i =>
    let (self1, other1, result1) ←
      arithmetic.matrix_arith.Matrix.mul_transpose_loop0_loop0
        { start := 0#usize, «end» := Y } self other result i
    arithmetic.matrix_arith.Matrix.mul_transpose_loop0 iter1 self1 other1
      result1
partial_fixpoint

/-- [kopis::arithmetic::matrix_arith::{kopis::arithmetic::matrix_arith::Matrix<X, Y>}::mul_transpose]:
    Source: 'src/arithmetic/matrix_arith.rs', lines 58:4-69:5 -/
def arithmetic.matrix_arith.Matrix.mul_transpose
  {X : Std.Usize} {Y : Std.Usize} {Z : Std.Usize}
  (self : arithmetic.matrix_arith.Matrix X Y)
  (other : arithmetic.matrix_arith.Matrix X Z) :
  Result (arithmetic.matrix_arith.Matrix Y Z)
  := do
  let result ←
    arithmetic.matrix_arith.Matrix.Insts.CoreDefaultDefault.default Y Z
  arithmetic.matrix_arith.Matrix.mul_transpose_loop0
    { start := 0#usize, «end» := X } self other result

/-- [kopis::arithmetic::ring_arith::{kopis::arithmetic::ring_arith::RingElem}::wrapping_add_to_all]: loop 0:
    Source: 'src/arithmetic/ring_arith.rs', lines 102:8-104:9 -/
@[rust_loop]
def arithmetic.ring_arith.RingElem.wrapping_add_to_all_loop
  (iter : core.slice.iter.IterMut Std.U16)
  (back : core.slice.iter.IterMut Std.U16 → core.slice.iter.IterMut Std.U16)
  (val : Std.U16) :
  Result (core.slice.iter.IterMut Std.U16)
  := do
  let (o, iter1, next_back) ← core.slice.iter.IteratorIterMut.next iter
  match o with
  | none => ok (let im := next_back iter1 none
                back im)
  | some coeff =>
    let coeff1 ← lift (core.num.U16.wrapping_add coeff val)
    arithmetic.ring_arith.RingElem.wrapping_add_to_all_loop iter1
      (fun im => let im1 := next_back im (some coeff1)
                 back im1) val
partial_fixpoint

/-- [kopis::arithmetic::ring_arith::{kopis::arithmetic::ring_arith::RingElem}::wrapping_add_to_all]:
    Source: 'src/arithmetic/ring_arith.rs', lines 101:4-105:5 -/
def arithmetic.ring_arith.RingElem.wrapping_add_to_all
  (self : arithmetic.ring_arith.RingElem) (val : Std.U16) :
  Result arithmetic.ring_arith.RingElem
  := do
  let (s, to_slice_mut_back) ← lift (Array.to_slice_mut self)
  let (iter, iter_mut_back) ← core.slice.Slice.iter_mut s
  let back ←
    arithmetic.ring_arith.RingElem.wrapping_add_to_all_loop iter (fun im => im)
      val
  let s1 := iter_mut_back back
  let a := to_slice_mut_back s1
  ok a

/-- [kopis::arithmetic::matrix_arith::{kopis::arithmetic::matrix_arith::Matrix<X, Y>}::wrapping_add_to_all]: loop 1:
    Source: 'src/arithmetic/matrix_arith.rs', lines 74:12-76:13 -/
@[rust_loop]
def arithmetic.matrix_arith.Matrix.wrapping_add_to_all_loop0_loop0
  (iter : core.slice.iter.IterMut arithmetic.ring_arith.RingElem)
  (back : core.slice.iter.IterMut arithmetic.ring_arith.RingElem →
  core.slice.iter.IterMut arithmetic.ring_arith.RingElem) (val : Std.U16) :
  Result ((core.slice.iter.IterMut arithmetic.ring_arith.RingElem) ×
    (core.slice.iter.IterMut arithmetic.ring_arith.RingElem →
    core.slice.iter.IterMut arithmetic.ring_arith.RingElem))
  := do
  let (o, iter1, next_back) ← core.slice.iter.IteratorIterMut.next iter
  match o with
  | none => ok (iter1, fun im => let im1 := next_back im none
                                 back im1)
  | some elem =>
    let elem1 ← arithmetic.ring_arith.RingElem.wrapping_add_to_all elem val
    arithmetic.matrix_arith.Matrix.wrapping_add_to_all_loop0_loop0 iter1
      (fun im => let im1 := next_back im (some elem1)
                 back im1) val
partial_fixpoint

/-- [kopis::arithmetic::matrix_arith::{kopis::arithmetic::matrix_arith::Matrix<X, Y>}::wrapping_add_to_all]: loop 0:
    Source: 'src/arithmetic/matrix_arith.rs', lines 73:8-77:9 -/
@[rust_loop]
def arithmetic.matrix_arith.Matrix.wrapping_add_to_all_loop0
  {Y : Std.Usize}
  (iter : core.slice.iter.IterMut (Array arithmetic.ring_arith.RingElem Y))
  (back : core.slice.iter.IterMut (Array arithmetic.ring_arith.RingElem Y) →
  core.slice.iter.IterMut (Array arithmetic.ring_arith.RingElem Y))
  (val : Std.U16) :
  Result (core.slice.iter.IterMut (Array arithmetic.ring_arith.RingElem Y))
  := do
  let (o, iter1, next_back) ← core.slice.iter.IteratorIterMut.next iter
  match o with
  | none => ok (let im := next_back iter1 none
                back im)
  | some row =>
    let (s, to_slice_mut_back) ← lift (Array.to_slice_mut row)
    let (iter2, iter_mut_back) ← core.slice.Slice.iter_mut s
    let (im, back1) ←
      arithmetic.matrix_arith.Matrix.wrapping_add_to_all_loop0_loop0 iter2
        (fun im1 => im1) val
    arithmetic.matrix_arith.Matrix.wrapping_add_to_all_loop0 iter1
      (fun im1 =>
        let im2 := back1 im
        let s1 := iter_mut_back im2
        let a := to_slice_mut_back s1
        let im3 := next_back im1 (some a)
        back im3) val
partial_fixpoint

/-- [kopis::arithmetic::matrix_arith::{kopis::arithmetic::matrix_arith::Matrix<X, Y>}::wrapping_add_to_all]:
    Source: 'src/arithmetic/matrix_arith.rs', lines 72:4-78:5 -/
def arithmetic.matrix_arith.Matrix.wrapping_add_to_all
  {X : Std.Usize} {Y : Std.Usize} (self : arithmetic.matrix_arith.Matrix X Y)
  (val : Std.U16) :
  Result (arithmetic.matrix_arith.Matrix X Y)
  := do
  let (s, to_slice_mut_back) ← lift (Array.to_slice_mut self)
  let (iter, iter_mut_back) ← core.slice.Slice.iter_mut s
  let back ←
    arithmetic.matrix_arith.Matrix.wrapping_add_to_all_loop0 iter
      (fun im => im) val
  let s1 := iter_mut_back back
  let a := to_slice_mut_back s1
  ok a

/-- [kopis::ser::serialize]: loop 1:
    Source: 'src/ser.rs', lines 117:8-122:9 -/
@[rust_loop]
def ser.serialize_loop0_loop0
  (out_buf : Slice Std.U8) (window : Std.U32) (bits_in_window : Std.Usize)
  (byte_pos : Std.Usize) :
  Result ((Slice Std.U8) × Std.U32 × Std.Usize × Std.Usize)
  := do
  if bits_in_window >= 8#usize
  then
    let i ← lift (UScalar.cast .U8 window)
    let s ← Slice.update out_buf byte_pos i
    let window1 ← window >>> 8#i32
    let bits_in_window1 ← bits_in_window - 8#usize
    let byte_pos1 ← byte_pos + 1#usize
    ser.serialize_loop0_loop0 s window1 bits_in_window1 byte_pos1
  else ok (out_buf, window, bits_in_window, byte_pos)
partial_fixpoint

/-- [kopis::ser::serialize]: loop 0:
    Source: 'src/ser.rs', lines 111:4-123:5 -/
@[rust_loop]
def ser.serialize_loop0
  (iter : core.slice.iter.Iter Std.U16) (out_buf : Slice Std.U8)
  (bits_per_elem : Std.Usize) (bitmask : Std.U32) (window : Std.U32)
  (bits_in_window : Std.Usize) (byte_pos : Std.Usize) :
  Result ((Slice Std.U8) × Std.Usize)
  := do
  let (o, iter1) ← core.slice.iter.IteratorSliceIter.next iter
  match o with
  | none => ok (out_buf, bits_in_window)
  | some elem =>
    let i ← lift (UScalar.cast .U32 elem)
    let i1 ← lift (i &&& bitmask)
    let i2 ← i1 <<< bits_in_window
    let window1 ← lift (window ||| i2)
    let bits_in_window1 ← bits_in_window + bits_per_elem
    let (out_buf1, window2, bits_in_window2, byte_pos1) ←
      ser.serialize_loop0_loop0 out_buf window1 bits_in_window1 byte_pos
    ser.serialize_loop0 iter1 out_buf1 bits_per_elem bitmask window2
      bits_in_window2 byte_pos1
partial_fixpoint

/-- [kopis::ser::serialize]:
    Source: 'src/ser.rs', lines 100:0-127:1 -/
def ser.serialize
  (data : Slice Std.U16) (out_buf : Slice Std.U8) (bits_per_elem : Std.Usize) :
  Result (Slice Std.U8)
  := do
  let left_val := Slice.len out_buf
  let i := Slice.len data
  let i1 ← bits_per_elem * i
  let right_val ← i1 / 8#usize
  massert (left_val = right_val)
  let i2 ← 1#u32 <<< bits_per_elem
  let bitmask ← i2 - 1#u32
  let iter ← core.slice.Slice.iter data
  let (out_buf1, bits_in_window) ←
    ser.serialize_loop0 iter out_buf bits_per_elem bitmask 0#u32 0#usize
      0#usize
  massert (bits_in_window = 0#usize)
  ok out_buf1

/-- [kopis::ser::serialize_10::closure]
    Source: 'src/ser.rs', lines 87:16-87:46 -/
def ser.serialize_10.closure := Array Std.U16 256#usize × Std.Usize

/-- [kopis::ser::serialize_10::{impl core::ops::function::Fn<(usize,), u16> for kopis::ser::serialize_10::closure<'_0, '_1>}::call]:
    Source: 'src/ser.rs', lines 87:16-87:46 -/
def ser.serialize_10.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call
  (c : ser.serialize_10.closure) (tupled_args : Std.Usize) :
  Result Std.U16
  := do
  let (a, i) := c
  let i1 ← i + tupled_args
  let i2 ← Array.index_usize a i1
  ok (i2 &&& 1023#u16)

/-- [kopis::ser::serialize_10]: loop 0:
    Source: 'src/ser.rs', lines 83:4-94:5 -/
@[rust_loop]
def ser.serialize_10_loop
  (iter : core.ops.range.Range Std.Usize) (data : Array Std.U16 256#usize)
  (out_buf : Array Std.U8 320#usize) :
  Result (Array Std.U8 320#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok out_buf
  | some g =>
    let o1 ← 4#usize * g
    let b ← 5#usize * g
    let i ←
      ser.serialize_10.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call (data,
        o1) 0#usize
    let i1 ← lift (UScalar.cast .U8 i)
    let out_buf1 ← Array.update out_buf b i1
    let i2 ← i >>> 8#i32
    let i3 ←
      ser.serialize_10.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call (data,
        o1) 1#usize
    let i4 ← i3 <<< 2#i32
    let i5 ← lift (i2 ||| i4)
    let i6 ← b + 1#usize
    let i7 ← lift (UScalar.cast .U8 i5)
    let out_buf2 ← Array.update out_buf1 i6 i7
    let i8 ← i3 >>> 6#i32
    let i9 ←
      ser.serialize_10.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call (data,
        o1) 2#usize
    let i10 ← i9 <<< 4#i32
    let i11 ← lift (i8 ||| i10)
    let i12 ← b + 2#usize
    let i13 ← lift (UScalar.cast .U8 i11)
    let out_buf3 ← Array.update out_buf2 i12 i13
    let i14 ← i9 >>> 4#i32
    let i15 ←
      ser.serialize_10.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call (data,
        o1) 3#usize
    let i16 ← i15 <<< 6#i32
    let i17 ← lift (i14 ||| i16)
    let i18 ← b + 3#usize
    let i19 ← lift (UScalar.cast .U8 i17)
    let out_buf4 ← Array.update out_buf3 i18 i19
    let i20 ← i15 >>> 2#i32
    let i21 ← b + 4#usize
    let i22 ← lift (UScalar.cast .U8 i20)
    let a ← Array.update out_buf4 i21 i22
    ser.serialize_10_loop iter1 data a
partial_fixpoint

/-- [kopis::ser::serialize_10]:
    Source: 'src/ser.rs', lines 81:0-95:1 -/
@[reducible]
def ser.serialize_10
  (data : Array Std.U16 256#usize) (out_buf : Array Std.U8 320#usize) :
  Result (Array Std.U8 320#usize)
  := do
  ser.serialize_10_loop { start := 0#usize, «end» := 64#usize } data out_buf

/-- [kopis::consts::MODULUS_P_BITS]
    Source: 'src/consts.rs', lines 4:0-4:44 -/
@[global_simps, irreducible] def consts.MODULUS_P_BITS : Std.Usize := 10#usize

/-- [kopis::arithmetic::ring_arith::{kopis::arithmetic::ring_arith::RingElem}::serialize]:
    Source: 'src/arithmetic/ring_arith.rs', lines 70:4-80:5 -/
def arithmetic.ring_arith.RingElem.serialize
  (self : arithmetic.ring_arith.RingElem) (out_buf : Slice Std.U8)
  (bits_per_elem : Std.Usize) :
  Result (Slice Std.U8)
  := do
  let left_val := Slice.len out_buf
  let i ← bits_per_elem * consts.RING_DEG
  let right_val ← i / 8#usize
  massert (left_val = right_val)
  if bits_per_elem = consts.MODULUS_P_BITS
  then
    let (r, try_from_back) ←
      core.array.TryFromMutArraySlice.try_from 320#usize out_buf
    let (arr, unwrap_back) ←
      core.result.Result.unwrap.mut core.fmt.DebugTryFromSliceError r
    let arr1 ← ser.serialize_10 self arr
    let r1 := unwrap_back arr1
    ok (try_from_back r1)
  else
    let s ← lift (Array.to_slice self)
    ser.serialize s out_buf bits_per_elem

/-- [kopis::arithmetic::matrix_arith::{kopis::arithmetic::matrix_arith::Matrix<X, Y>}::serialize]: loop 1:
    Source: 'src/arithmetic/matrix_arith.rs', lines 98:12-102:13 -/
@[rust_loop]
def arithmetic.matrix_arith.Matrix.serialize_loop0_loop0
  {X : Std.Usize} {Y : Std.Usize} (iter : core.ops.range.Range Std.Usize)
  (self : arithmetic.matrix_arith.Matrix X Y) (out_buf : Slice Std.U8)
  (bits_per_elem : Std.Usize) (chunk_len : Std.Usize) (i : Std.Usize) :
  Result ((arithmetic.matrix_arith.Matrix X Y) × (Slice Std.U8))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (self, out_buf)
  | some j =>
    let i1 ← i * Y
    let idx ← i1 + j
    let i2 ← idx * chunk_len
    let i3 ← idx + 1#usize
    let i4 ← i3 * chunk_len
    let (out_chunk, index_mut_back) ←
      core.slice.index.Slice.index_mut
        (core.slice.index.SliceIndexRangeUsizeSlice Std.U8) out_buf
        { start := i2, «end» := i4 }
    let a ← Array.index_usize self i
    let re ← Array.index_usize a j
    let out_chunk1 ←
      arithmetic.ring_arith.RingElem.serialize re out_chunk bits_per_elem
    let out_buf1 := index_mut_back out_chunk1
    arithmetic.matrix_arith.Matrix.serialize_loop0_loop0 iter1 self out_buf1
      bits_per_elem chunk_len i
partial_fixpoint

/-- [kopis::arithmetic::matrix_arith::{kopis::arithmetic::matrix_arith::Matrix<X, Y>}::serialize]: loop 0:
    Source: 'src/arithmetic/matrix_arith.rs', lines 97:8-103:9 -/
@[rust_loop]
def arithmetic.matrix_arith.Matrix.serialize_loop0
  {X : Std.Usize} {Y : Std.Usize} (iter : core.ops.range.Range Std.Usize)
  (self : arithmetic.matrix_arith.Matrix X Y) (out_buf : Slice Std.U8)
  (bits_per_elem : Std.Usize) (chunk_len : Std.Usize) :
  Result (Slice Std.U8)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok out_buf
  | some i =>
    let (self1, out_buf1) ←
      arithmetic.matrix_arith.Matrix.serialize_loop0_loop0
        { start := 0#usize, «end» := Y } self out_buf bits_per_elem chunk_len
        i
    arithmetic.matrix_arith.Matrix.serialize_loop0 iter1 self1 out_buf1
      bits_per_elem chunk_len
partial_fixpoint

/-- [kopis::arithmetic::matrix_arith::{kopis::arithmetic::matrix_arith::Matrix<X, Y>}::serialize]:
    Source: 'src/arithmetic/matrix_arith.rs', lines 83:4-104:5 -/
def arithmetic.matrix_arith.Matrix.serialize
  {X : Std.Usize} {Y : Std.Usize} (self : arithmetic.matrix_arith.Matrix X Y)
  (out_buf : Slice Std.U8) (bits_per_elem : Std.Usize) :
  Result (Slice Std.U8)
  := do
  let left_val := Slice.len out_buf
  let i ← X * Y
  let i1 ← i * bits_per_elem
  let i2 ← i1 * consts.RING_DEG
  let right_val ← i2 / 8#usize
  massert (left_val = right_val)
  let i3 ← bits_per_elem * consts.RING_DEG
  let chunk_len ← i3 / 8#usize
  arithmetic.matrix_arith.Matrix.serialize_loop0
    { start := 0#usize, «end» := X } self out_buf bits_per_elem chunk_len

/-- [kopis::ser::deserialize_generic]: loop 1:
    Source: 'src/ser.rs', lines 63:8-67:9 -/
@[rust_loop]
def ser.deserialize_generic_loop0_loop0
  (bytes : Slice Std.U8) (bits_per_elem : Std.Usize) (window : Std.U32)
  (bits_in_window : Std.Usize) (byte_pos : Std.Usize) :
  Result (Std.U32 × Std.Usize × Std.Usize)
  := do
  if bits_in_window < bits_per_elem
  then
    let i ← Slice.index_usize bytes byte_pos
    let i1 ← lift (UScalar.cast .U32 i)
    let i2 ← i1 <<< bits_in_window
    let window1 ← lift (window ||| i2)
    let byte_pos1 ← byte_pos + 1#usize
    let bits_in_window1 ← bits_in_window + 8#usize
    ser.deserialize_generic_loop0_loop0 bytes bits_per_elem window1
      bits_in_window1 byte_pos1
  else ok (window, bits_in_window, byte_pos)
partial_fixpoint

/-- [kopis::ser::deserialize_generic]: loop 0:
    Source: 'src/ser.rs', lines 61:4-73:5 -/
@[rust_loop]
def ser.deserialize_generic_loop0
  {N : Std.Usize} (iter : core.ops.range.Range Std.Usize)
  (bytes : Slice Std.U8) (bits_per_elem : Std.Usize) (out : Array Std.U16 N)
  (bitmask : Std.U32) (window : Std.U32) (bits_in_window : Std.Usize)
  (byte_pos : Std.Usize) :
  Result (Array Std.U16 N)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok out
  | some idx =>
    let (window1, bits_in_window1, byte_pos1) ←
      ser.deserialize_generic_loop0_loop0 bytes bits_per_elem window
        bits_in_window byte_pos
    let i ← lift (window1 &&& bitmask)
    let i1 ← lift (UScalar.cast .U16 i)
    let a ← Array.update out idx i1
    let window2 ← window1 >>> bits_per_elem
    let bits_in_window2 ← bits_in_window1 - bits_per_elem
    ser.deserialize_generic_loop0 iter1 bytes bits_per_elem a bitmask window2
      bits_in_window2 byte_pos1
partial_fixpoint

/-- [kopis::ser::deserialize_generic]:
    Source: 'src/ser.rs', lines 47:0-76:1 -/
def ser.deserialize_generic
  (N : Std.Usize) (bytes : Slice Std.U8) (bits_per_elem : Std.Usize) :
  Result (Array Std.U16 N)
  := do
  let i ← bits_per_elem * N
  let left_val ← i % 8#usize
  massert (left_val = 0#usize)
  let left_val1 := Slice.len bytes
  let right_val ← i / 8#usize
  massert (left_val1 = right_val)
  let out := Array.repeat N 0#u16
  let i1 ← 1#u32 <<< bits_per_elem
  let bitmask ← i1 - 1#u32
  ser.deserialize_generic_loop0 { start := 0#usize, «end» := N } bytes
    bits_per_elem out bitmask 0#u32 0#usize 0#usize

/-- [kopis::ser::deserialize_10::closure]
    Source: 'src/ser.rs', lines 34:16-34:38 -/
@[reducible]
def ser.deserialize_10.closure := Slice Std.U8

/-- [kopis::ser::deserialize_10::{impl core::ops::function::Fn<(usize,), u16> for kopis::ser::deserialize_10::closure<'_0>}::call]:
    Source: 'src/ser.rs', lines 34:16-34:38 -/
def ser.deserialize_10.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call
  (c : ser.deserialize_10.closure) (tupled_args : Std.Usize) :
  Result Std.U16
  := do
  let i ← Slice.index_usize c tupled_args
  ok (UScalar.cast .U16 i)

/-- [kopis::ser::deserialize_10]: loop 0:
    Source: 'src/ser.rs', lines 32:4-40:5 -/
@[rust_loop]
def ser.deserialize_10_loop
  (iter : core.ops.range.Range Std.Usize) (bytes : Array Std.U8 320#usize)
  (out : Array Std.U16 256#usize) :
  Result (Array Std.U16 256#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok out
  | some g =>
    let i ← 5#usize * g
    let i1 ← i + 5#usize
    let b ←
      core.array.Array.index (core.ops.index.IndexSlice
        (core.slice.index.SliceIndexRangeUsizeSlice Std.U8)) bytes
        { start := i, «end» := i1 }
    let o1 ← 4#usize * g
    let i2 ←
      ser.deserialize_10.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call b
        0#usize
    let i3 ←
      ser.deserialize_10.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call b
        1#usize
    let i4 ← lift (i3 &&& 3#u16)
    let i5 ← i4 <<< 8#i32
    let i6 ← lift (i2 ||| i5)
    let out1 ← Array.update out o1 i6
    let i7 ← i3 >>> 2#i32
    let i8 ←
      ser.deserialize_10.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call b
        2#usize
    let i9 ← lift (i8 &&& 15#u16)
    let i10 ← i9 <<< 6#i32
    let i11 ← o1 + 1#usize
    let i12 ← lift (i7 ||| i10)
    let out2 ← Array.update out1 i11 i12
    let i13 ← i8 >>> 4#i32
    let i14 ←
      ser.deserialize_10.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call b
        3#usize
    let i15 ← lift (i14 &&& 63#u16)
    let i16 ← i15 <<< 4#i32
    let i17 ← o1 + 2#usize
    let i18 ← lift (i13 ||| i16)
    let out3 ← Array.update out2 i17 i18
    let i19 ← i14 >>> 6#i32
    let i20 ←
      ser.deserialize_10.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call b
        4#usize
    let i21 ← i20 <<< 2#i32
    let i22 ← o1 + 3#usize
    let i23 ← lift (i19 ||| i21)
    let a ← Array.update out3 i22 i23
    ser.deserialize_10_loop iter1 bytes a
partial_fixpoint

/-- [kopis::ser::deserialize_10]:
    Source: 'src/ser.rs', lines 29:0-42:1 -/
def ser.deserialize_10
  (bytes : Array Std.U8 320#usize) : Result (Array Std.U16 256#usize) := do
  let out := Array.repeat 256#usize 0#u16
  ser.deserialize_10_loop { start := 0#usize, «end» := 64#usize } bytes out

/-- [kopis::ser::deserialize_13::closure]
    Source: 'src/ser.rs', lines 12:16-12:38 -/
@[reducible]
def ser.deserialize_13.closure := Slice Std.U8

/-- [kopis::ser::deserialize_13::{impl core::ops::function::Fn<(usize,), u16> for kopis::ser::deserialize_13::closure<'_0>}::call]:
    Source: 'src/ser.rs', lines 12:16-12:38 -/
def ser.deserialize_13.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call
  (c : ser.deserialize_13.closure) (tupled_args : Std.Usize) :
  Result Std.U16
  := do
  let i ← Slice.index_usize c tupled_args
  ok (UScalar.cast .U16 i)

/-- [kopis::ser::deserialize_13]: loop 0:
    Source: 'src/ser.rs', lines 9:4-22:5 -/
@[rust_loop]
def ser.deserialize_13_loop
  (iter : core.ops.range.Range Std.Usize) (bytes : Array Std.U8 416#usize)
  (out : Array Std.U16 256#usize) :
  Result (Array Std.U16 256#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok out
  | some g =>
    let i ← 13#usize * g
    let i1 ← i + 13#usize
    let b ←
      core.array.Array.index (core.ops.index.IndexSlice
        (core.slice.index.SliceIndexRangeUsizeSlice Std.U8)) bytes
        { start := i, «end» := i1 }
    let o1 ← 8#usize * g
    let i2 ←
      ser.deserialize_13.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call b
        0#usize
    let i3 ←
      ser.deserialize_13.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call b
        1#usize
    let i4 ← lift (i3 &&& 31#u16)
    let i5 ← i4 <<< 8#i32
    let i6 ← lift (i2 ||| i5)
    let out1 ← Array.update out o1 i6
    let i7 ← i3 >>> 5#i32
    let i8 ←
      ser.deserialize_13.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call b
        2#usize
    let i9 ← i8 <<< 3#i32
    let i10 ← lift (i7 ||| i9)
    let i11 ←
      ser.deserialize_13.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call b
        3#usize
    let i12 ← lift (i11 &&& 3#u16)
    let i13 ← i12 <<< 11#i32
    let i14 ← o1 + 1#usize
    let i15 ← lift (i10 ||| i13)
    let out2 ← Array.update out1 i14 i15
    let i16 ← i11 >>> 2#i32
    let i17 ←
      ser.deserialize_13.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call b
        4#usize
    let i18 ← lift (i17 &&& 127#u16)
    let i19 ← i18 <<< 6#i32
    let i20 ← o1 + 2#usize
    let i21 ← lift (i16 ||| i19)
    let out3 ← Array.update out2 i20 i21
    let i22 ← i17 >>> 7#i32
    let i23 ←
      ser.deserialize_13.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call b
        5#usize
    let i24 ← i23 <<< 1#i32
    let i25 ← lift (i22 ||| i24)
    let i26 ←
      ser.deserialize_13.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call b
        6#usize
    let i27 ← lift (i26 &&& 15#u16)
    let i28 ← i27 <<< 9#i32
    let i29 ← o1 + 3#usize
    let i30 ← lift (i25 ||| i28)
    let out4 ← Array.update out3 i29 i30
    let i31 ← i26 >>> 4#i32
    let i32 ←
      ser.deserialize_13.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call b
        7#usize
    let i33 ← i32 <<< 4#i32
    let i34 ← lift (i31 ||| i33)
    let i35 ←
      ser.deserialize_13.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call b
        8#usize
    let i36 ← lift (i35 &&& 1#u16)
    let i37 ← i36 <<< 12#i32
    let i38 ← o1 + 4#usize
    let i39 ← lift (i34 ||| i37)
    let out5 ← Array.update out4 i38 i39
    let i40 ← i35 >>> 1#i32
    let i41 ←
      ser.deserialize_13.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call b
        9#usize
    let i42 ← lift (i41 &&& 63#u16)
    let i43 ← i42 <<< 7#i32
    let i44 ← o1 + 5#usize
    let i45 ← lift (i40 ||| i43)
    let out6 ← Array.update out5 i44 i45
    let i46 ← i41 >>> 6#i32
    let i47 ←
      ser.deserialize_13.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call b
        10#usize
    let i48 ← i47 <<< 2#i32
    let i49 ← lift (i46 ||| i48)
    let i50 ←
      ser.deserialize_13.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call b
        11#usize
    let i51 ← lift (i50 &&& 7#u16)
    let i52 ← i51 <<< 10#i32
    let i53 ← o1 + 6#usize
    let i54 ← lift (i49 ||| i52)
    let out7 ← Array.update out6 i53 i54
    let i55 ← i50 >>> 3#i32
    let i56 ←
      ser.deserialize_13.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call b
        12#usize
    let i57 ← i56 <<< 5#i32
    let i58 ← o1 + 7#usize
    let i59 ← lift (i55 ||| i57)
    let a ← Array.update out7 i58 i59
    ser.deserialize_13_loop iter1 bytes a
partial_fixpoint

/-- [kopis::ser::deserialize_13]:
    Source: 'src/ser.rs', lines 6:0-24:1 -/
def ser.deserialize_13
  (bytes : Array Std.U8 416#usize) : Result (Array Std.U16 256#usize) := do
  let out := Array.repeat 256#usize 0#u16
  ser.deserialize_13_loop { start := 0#usize, «end» := 32#usize } bytes out

/-- [kopis::consts::MODULUS_Q_BITS]
    Source: 'src/consts.rs', lines 2:0-2:44 -/
@[global_simps, irreducible] def consts.MODULUS_Q_BITS : Std.Usize := 13#usize

/-- [kopis::arithmetic::ring_arith::{kopis::arithmetic::ring_arith::RingElem}::deserialize]:
    Source: 'src/arithmetic/ring_arith.rs', lines 41:4-65:5 -/
def arithmetic.ring_arith.RingElem.deserialize
  (bytes : Slice Std.U8) (bits_per_elem : Std.Usize) :
  Result arithmetic.ring_arith.RingElem
  := do
  let left_val := Slice.len bytes
  let i ← bits_per_elem * consts.RING_DEG
  let right_val ← i / 8#usize
  massert (left_val = right_val)
  if bits_per_elem = consts.MODULUS_Q_BITS
  then
    let r ← core.array.TryFromSharedArraySlice.try_from 416#usize bytes
    let arr ← core.result.Result.unwrap core.fmt.DebugTryFromSliceError r
    let a ← ser.deserialize_13 arr
    ok a
  else
    if bits_per_elem = consts.MODULUS_P_BITS
    then
      let r ← core.array.TryFromSharedArraySlice.try_from 320#usize bytes
      let arr ← core.result.Result.unwrap core.fmt.DebugTryFromSliceError r
      let a ← ser.deserialize_10 arr
      ok a
    else let a ← ser.deserialize_generic 256#usize bytes bits_per_elem
         ok a

/-- [kopis::arithmetic::matrix_arith::{kopis::arithmetic::matrix_arith::Matrix<X, Y>}::deserialize_10]: loop 1:
    Source: 'src/arithmetic/matrix_arith.rs', lines 125:12-129:13 -/
@[rust_loop]
def arithmetic.matrix_arith.Matrix.deserialize_10_loop0_loop0
  {X : Std.Usize} {Y : Std.Usize} (iter : core.ops.range.Range Std.Usize)
  (bytes : Slice Std.U8) (result : arithmetic.matrix_arith.Matrix X Y)
  (chunk_len : Std.Usize) (i : Std.Usize) :
  Result (arithmetic.matrix_arith.Matrix X Y)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok result
  | some j =>
    let i1 ← i * Y
    let idx ← i1 + j
    let i2 ← idx * chunk_len
    let i3 ← idx + 1#usize
    let i4 ← i3 * chunk_len
    let chunk ←
      core.slice.index.Slice.index (core.slice.index.SliceIndexRangeUsizeSlice
        Std.U8) bytes { start := i2, «end» := i4 }
    let re ← arithmetic.ring_arith.RingElem.deserialize chunk 10#usize
    let (a, index_mut_back) ← Array.index_mut_usize result i
    let a1 ← Array.update a j re
    let a2 := index_mut_back a1
    arithmetic.matrix_arith.Matrix.deserialize_10_loop0_loop0 iter1 bytes a2
      chunk_len i
partial_fixpoint

/-- [kopis::arithmetic::matrix_arith::{kopis::arithmetic::matrix_arith::Matrix<X, Y>}::deserialize_10]: loop 0:
    Source: 'src/arithmetic/matrix_arith.rs', lines 124:8-130:9 -/
@[rust_loop]
def arithmetic.matrix_arith.Matrix.deserialize_10_loop0
  {X : Std.Usize} {Y : Std.Usize} (iter : core.ops.range.Range Std.Usize)
  (bytes : Slice Std.U8) (result : arithmetic.matrix_arith.Matrix X Y)
  (chunk_len : Std.Usize) :
  Result (arithmetic.matrix_arith.Matrix X Y)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok result
  | some i =>
    let result1 ←
      arithmetic.matrix_arith.Matrix.deserialize_10_loop0_loop0
        { start := 0#usize, «end» := Y } bytes result chunk_len i
    arithmetic.matrix_arith.Matrix.deserialize_10_loop0 iter1 bytes result1
      chunk_len
partial_fixpoint

/-- [kopis::arithmetic::matrix_arith::{kopis::arithmetic::matrix_arith::Matrix<X, Y>}::deserialize_10]:
    Source: 'src/arithmetic/matrix_arith.rs', lines 107:4-133:5 -/
def arithmetic.matrix_arith.Matrix.deserialize_10
  (X : Std.Usize) (Y : Std.Usize) (bytes : Slice Std.U8) :
  Result (arithmetic.matrix_arith.Matrix X Y)
  := do
  let left_val := Slice.len bytes
  let i ← X * Y
  let i1 ← i * 10#usize
  let _ ← i1 * consts.RING_DEG
  let i2 ← lift (Std.Usize.wrapping_mul X Y)
  let i3 ← lift (Std.Usize.wrapping_mul i2 10#usize)
  let i4 ← lift (Std.Usize.wrapping_mul i3 consts.RING_DEG)
  let right_val ← i4 / 8#usize
  massert (left_val = right_val)
  let result ←
    arithmetic.matrix_arith.Matrix.Insts.CoreDefaultDefault.default X Y
  let i5 ← 10#usize * consts.RING_DEG
  let chunk_len ← i5 / 8#usize
  arithmetic.matrix_arith.Matrix.deserialize_10_loop0
    { start := 0#usize, «end» := X } bytes result chunk_len

/-- [kopis::arithmetic::ring_arith::{impl core::ops::arith::Add<&'a kopis::arithmetic::ring_arith::RingElem, kopis::arithmetic::ring_arith::RingElem> for &'a kopis::arithmetic::ring_arith::RingElem}::add]: loop 0:
    Source: 'src/arithmetic/ring_arith.rs', lines 212:8-214:9
    Visibility: public -/
@[rust_loop]
def SharedARingElem.Insts.CoreOpsArithAddSharedARingElemRingElem.add_loop
  (iter : core.ops.range.Range Std.Usize)
  (self : arithmetic.ring_arith.RingElem)
  (other : arithmetic.ring_arith.RingElem)
  (ret : arithmetic.ring_arith.RingElem) :
  Result arithmetic.ring_arith.RingElem
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok ret
  | some i =>
    let i1 ← Array.index_usize self i
    let i2 ← Array.index_usize other i
    let i3 ← lift (core.num.U16.wrapping_add i1 i2)
    let a ← Array.update ret i i3
    SharedARingElem.Insts.CoreOpsArithAddSharedARingElemRingElem.add_loop iter1
      self other a
partial_fixpoint

/-- [kopis::arithmetic::ring_arith::{impl core::ops::arith::Add<&'a kopis::arithmetic::ring_arith::RingElem, kopis::arithmetic::ring_arith::RingElem> for &'a kopis::arithmetic::ring_arith::RingElem}::add]:
    Source: 'src/arithmetic/ring_arith.rs', lines 210:4-216:5
    Visibility: public -/
def SharedARingElem.Insts.CoreOpsArithAddSharedARingElemRingElem.add
  (self : arithmetic.ring_arith.RingElem)
  (other : arithmetic.ring_arith.RingElem) :
  Result arithmetic.ring_arith.RingElem
  := do
  let ret ← arithmetic.ring_arith.RingElem.Insts.CoreDefaultDefault.default
  SharedARingElem.Insts.CoreOpsArithAddSharedARingElemRingElem.add_loop
    { start := 0#usize, «end» := consts.RING_DEG } self other ret

/-- [kopis::arithmetic::matrix_arith::{impl core::ops::arith::Add<&'a kopis::arithmetic::matrix_arith::Matrix<X, Y>, kopis::arithmetic::matrix_arith::Matrix<X, Y>> for &'a kopis::arithmetic::matrix_arith::Matrix<X, Y>}::add]: loop 1:
    Source: 'src/arithmetic/matrix_arith.rs', lines 142:12-144:13
    Visibility: public -/
@[rust_loop]
def SharedAMatrix.Insts.CoreOpsArithAddSharedAMatrixMatrix.add_loop0_loop0
  {X : Std.Usize} {Y : Std.Usize} (iter : core.ops.range.Range Std.Usize)
  (self : arithmetic.matrix_arith.Matrix X Y)
  (other : arithmetic.matrix_arith.Matrix X Y)
  (result : arithmetic.matrix_arith.Matrix X Y) (i : Std.Usize) :
  Result ((arithmetic.matrix_arith.Matrix X Y) ×
    (arithmetic.matrix_arith.Matrix X Y) × (arithmetic.matrix_arith.Matrix X
    Y))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (self, other, result)
  | some j =>
    let a ← Array.index_usize self i
    let re ← Array.index_usize a j
    let a1 ← Array.index_usize other i
    let re1 ← Array.index_usize a1 j
    let re2 ←
      SharedARingElem.Insts.CoreOpsArithAddSharedARingElemRingElem.add re re1
    let (a2, index_mut_back) ← Array.index_mut_usize result i
    let a3 ← Array.update a2 j re2
    let a4 := index_mut_back a3
    SharedAMatrix.Insts.CoreOpsArithAddSharedAMatrixMatrix.add_loop0_loop0
      iter1 self other a4 i
partial_fixpoint

/-- [kopis::arithmetic::matrix_arith::{impl core::ops::arith::Add<&'a kopis::arithmetic::matrix_arith::Matrix<X, Y>, kopis::arithmetic::matrix_arith::Matrix<X, Y>> for &'a kopis::arithmetic::matrix_arith::Matrix<X, Y>}::add]: loop 0:
    Source: 'src/arithmetic/matrix_arith.rs', lines 141:8-145:9
    Visibility: public -/
@[rust_loop]
def SharedAMatrix.Insts.CoreOpsArithAddSharedAMatrixMatrix.add_loop0
  {X : Std.Usize} {Y : Std.Usize} (iter : core.ops.range.Range Std.Usize)
  (self : arithmetic.matrix_arith.Matrix X Y)
  (other : arithmetic.matrix_arith.Matrix X Y)
  (result : arithmetic.matrix_arith.Matrix X Y) :
  Result (arithmetic.matrix_arith.Matrix X Y)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok result
  | some i =>
    let (self1, other1, result1) ←
      SharedAMatrix.Insts.CoreOpsArithAddSharedAMatrixMatrix.add_loop0_loop0
        { start := 0#usize, «end» := Y } self other result i
    SharedAMatrix.Insts.CoreOpsArithAddSharedAMatrixMatrix.add_loop0 iter1
      self1 other1 result1
partial_fixpoint

/-- [kopis::arithmetic::matrix_arith::{impl core::ops::arith::Add<&'a kopis::arithmetic::matrix_arith::Matrix<X, Y>, kopis::arithmetic::matrix_arith::Matrix<X, Y>> for &'a kopis::arithmetic::matrix_arith::Matrix<X, Y>}::add]:
    Source: 'src/arithmetic/matrix_arith.rs', lines 139:4-148:5
    Visibility: public -/
def SharedAMatrix.Insts.CoreOpsArithAddSharedAMatrixMatrix.add
  {X : Std.Usize} {Y : Std.Usize} (self : arithmetic.matrix_arith.Matrix X Y)
  (other : arithmetic.matrix_arith.Matrix X Y) :
  Result (arithmetic.matrix_arith.Matrix X Y)
  := do
  let result ←
    arithmetic.matrix_arith.Matrix.Insts.CoreDefaultDefault.default X Y
  SharedAMatrix.Insts.CoreOpsArithAddSharedAMatrixMatrix.add_loop0
    { start := 0#usize, «end» := X } self other result

/-- Trait implementation: [kopis::arithmetic::matrix_arith::{impl core::ops::arith::Add<&'a kopis::arithmetic::matrix_arith::Matrix<X, Y>, kopis::arithmetic::matrix_arith::Matrix<X, Y>> for &'a kopis::arithmetic::matrix_arith::Matrix<X, Y>}]
    Source: 'src/arithmetic/matrix_arith.rs', lines 136:0-149:1 -/
@[reducible]
def SharedAMatrix.Insts.CoreOpsArithAddSharedAMatrixMatrix (X : Std.Usize) (Y :
  Std.Usize) : core.ops.arith.Add (arithmetic.matrix_arith.Matrix X Y)
  (arithmetic.matrix_arith.Matrix X Y) (arithmetic.matrix_arith.Matrix X Y)
  := {
  add := SharedAMatrix.Insts.CoreOpsArithAddSharedAMatrixMatrix.add
}

/-- [kopis::arithmetic::ntt::P]
    Source: 'src/arithmetic/ntt.rs', lines 39:0-39:35 -/
@[global_simps, irreducible] def arithmetic.ntt.P : Std.I32 := 50330113#i32

/-- [kopis::arithmetic::ntt::P_INV]
    Source: 'src/arithmetic/ntt.rs', lines 41:0-41:41 -/
@[global_simps, irreducible]
def arithmetic.ntt.P_INV : Std.U32 := 3575907841#u32

/-- [kopis::arithmetic::ntt::BARRETT_M]
    Source: 'src/arithmetic/ntt.rs', lines 43:0-43:42 -/
@[global_simps, irreducible]
def arithmetic.ntt.BARRETT_M : Std.I64 := 5592576#i64

/-- [kopis::arithmetic::ntt::BARRETT_ROUND]
    Source: 'src/arithmetic/ntt.rs', lines 46:0-46:54 -/
@[global_simps, irreducible]
def arithmetic.ntt.BARRETT_ROUND : Std.I64 := 140737488355328#i64

/-- [kopis::arithmetic::ntt::P_HALF]
    Source: 'src/arithmetic/ntt.rs', lines 48:0-48:40 -/
@[global_simps, irreducible]
def arithmetic.ntt.P_HALF : Std.I32 := 25165056#i32

/-- [kopis::arithmetic::ntt::INVNTT_SCALE]
    Source: 'src/arithmetic/ntt.rs', lines 52:0-52:46 -/
@[global_simps, irreducible]
def arithmetic.ntt.INVNTT_SCALE : Std.I32 := 44652572#i32

/-- [kopis::arithmetic::ntt::ZETAS]
    Source: 'src/arithmetic/ntt.rs', lines 58:0-91:2 -/
@[global_simps, irreducible]
def arithmetic.ntt.ZETAS : Array Std.I32 256#usize :=
  Array.make 256#usize [
    16907691#i32, 2667794#i32, 25435945#i32, 38041794#i32, 4526213#i32,
    35360986#i32, 30701667#i32, 21078350#i32, 36956843#i32, 13677930#i32,
    22323069#i32, 13735052#i32, 16437056#i32, 5691837#i32, 29800035#i32,
    15764652#i32, 45309494#i32, 32237400#i32, 24236486#i32, 2001600#i32,
    1321324#i32, 27016828#i32, 15167304#i32, 4366839#i32, 28120194#i32,
    32421317#i32, 30261273#i32, 12292806#i32, 22968316#i32, 13702742#i32,
    937110#i32, 2965954#i32, 34335133#i32, 44939767#i32, 6512044#i32,
    26419553#i32, 19973946#i32, 49111849#i32, 40847418#i32, 20023056#i32,
    43261597#i32, 41129446#i32, 12627709#i32, 45952874#i32, 30150184#i32,
    36681813#i32, 45532529#i32, 39191671#i32, 45414311#i32, 2081056#i32,
    808565#i32, 49693071#i32, 31006016#i32, 28961757#i32, 15337390#i32,
    31683976#i32, 4955339#i32, 27911199#i32, 29647313#i32, 30189282#i32,
    40031443#i32, 49298885#i32, 46092656#i32, 18408488#i32, 245475#i32,
    31889855#i32, 39534436#i32, 41714666#i32, 8292132#i32, 21449251#i32,
    22309987#i32, 8205570#i32, 31096891#i32, 47405297#i32, 930348#i32,
    6971348#i32, 10106150#i32, 31035883#i32, 46359140#i32, 35263708#i32,
    25740623#i32, 9884444#i32, 542513#i32, 16415589#i32, 9876654#i32,
    44372807#i32, 21881921#i32, 19165831#i32, 9227072#i32, 2984297#i32,
    36824748#i32, 43900147#i32, 43917176#i32, 9030513#i32, 26543409#i32,
    34836022#i32, 45307234#i32, 35198677#i32, 14645063#i32, 2098347#i32,
    37061938#i32, 3244828#i32, 41096247#i32, 32062813#i32, 8918713#i32,
    47344810#i32, 46288223#i32, 21000897#i32, 41046191#i32, 20508021#i32,
    35242638#i32, 21557502#i32, 49459492#i32, 3469647#i32, 1704205#i32,
    29274982#i32, 27408601#i32, 6834376#i32, 3860969#i32, 50170866#i32,
    7253270#i32, 26028155#i32, 34019372#i32, 9518412#i32, 22713734#i32,
    14612739#i32, 26018446#i32, 43305093#i32, 12047271#i32, 21217146#i32,
    26069968#i32, 4256850#i32, 3639874#i32, 48691800#i32, 17320569#i32,
    3323937#i32, 32443671#i32, 42492417#i32, 9308803#i32, 33498296#i32,
    25236161#i32, 17970048#i32, 30529967#i32, 11776789#i32, 23284408#i32,
    23725761#i32, 39814592#i32, 32816444#i32, 45216211#i32, 34497791#i32,
    1878200#i32, 45702238#i32, 40014922#i32, 39122569#i32, 28779735#i32,
    44971151#i32, 12142149#i32, 25663462#i32, 31479913#i32, 19837380#i32,
    39143843#i32, 21323231#i32, 17806070#i32, 36807664#i32, 47595478#i32,
    49950288#i32, 16588490#i32, 32773564#i32, 19315478#i32, 33753056#i32,
    15093953#i32, 23446825#i32, 49044025#i32, 41815538#i32, 1288725#i32,
    23610193#i32, 2754751#i32, 40030005#i32, 13643238#i32, 20793088#i32,
    16114388#i32, 33147218#i32, 15927487#i32, 41781494#i32, 5795109#i32,
    11439966#i32, 15064762#i32, 35884894#i32, 22645018#i32, 10582966#i32,
    14900694#i32, 21660678#i32, 16765530#i32, 20825350#i32, 30320649#i32,
    42368153#i32, 39131901#i32, 42360159#i32, 6363984#i32, 35581825#i32,
    28072132#i32, 11706419#i32, 20970510#i32, 44236552#i32, 17934700#i32,
    14577479#i32, 42770390#i32, 44206858#i32, 28865919#i32, 40187227#i32,
    15964209#i32, 15444696#i32, 17019703#i32, 32900174#i32, 28423607#i32,
    42332883#i32, 32435385#i32, 40121202#i32, 37968791#i32, 33541227#i32,
    35393185#i32, 34591081#i32, 48852822#i32, 8473545#i32, 43827986#i32,
    23231608#i32, 34405552#i32, 26405625#i32, 27381101#i32, 33514230#i32,
    46158701#i32, 27741938#i32, 10409285#i32, 21224207#i32, 6796942#i32,
    35314909#i32, 49625944#i32, 20271219#i32, 6354214#i32, 38361969#i32,
    34767461#i32, 14097694#i32, 13212224#i32, 47820051#i32, 49316243#i32,
    20113174#i32, 21747171#i32, 39170867#i32, 38847587#i32, 20584729#i32,
    27124165#i32, 40492847#i32, 7742348#i32, 43534070#i32, 7422899#i32,
    12232461#i32
    ]

/-- [kopis::arithmetic::ntt::mont_reduce]:
    Source: 'src/arithmetic/ntt.rs', lines 104:0-107:1 -/
def arithmetic.ntt.mont_reduce (a : Std.I64) : Result Std.I32 := do
  let i ← lift (IScalar.hcast .U32 a)
  let i1 ← lift (core.num.U32.wrapping_mul i arithmetic.ntt.P_INV)
  let i2 ← lift (UScalar.hcast .I32 i1)
  let t ← lift (IScalar.cast .I64 i2)
  let i3 ← lift (IScalar.cast .I64 arithmetic.ntt.P)
  let i4 ← lift (core.num.I64.wrapping_mul t i3)
  let i5 ← lift (core.num.I64.wrapping_sub a i4)
  let i6 ← lift (core.num.I64.wrapping_shr i5 32#u32)
  ok (IScalar.cast .I32 i6)

/-- [kopis::arithmetic::ntt::barrett_reduce]:
    Source: 'src/arithmetic/ntt.rs', lines 111:0-117:1 -/
def arithmetic.ntt.barrett_reduce (x : Std.I32) : Result Std.I32 := do
  let i ← lift (IScalar.cast .I64 x)
  let i1 ← lift (core.num.I64.wrapping_mul i arithmetic.ntt.BARRETT_M)
  let i2 ← lift (core.num.I64.wrapping_add i1 arithmetic.ntt.BARRETT_ROUND)
  let i3 ← lift (core.num.I64.wrapping_shr i2 48#u32)
  let q ← lift (IScalar.cast .I32 i3)
  let i4 ← lift (core.num.I32.wrapping_mul q arithmetic.ntt.P)
  ok (core.num.I32.wrapping_sub x i4)

/-- [kopis::arithmetic::ntt::to_canonical]:
    Source: 'src/arithmetic/ntt.rs', lines 121:0-123:1 -/
def arithmetic.ntt.to_canonical (x : Std.I32) : Result Std.I32 := do
  let i ← lift (core.num.I32.wrapping_shr x 31#u32)
  let i1 ← lift (i &&& arithmetic.ntt.P)
  ok (core.num.I32.wrapping_add x i1)

/-- [kopis::arithmetic::ntt::to_wrapping_u16]:
    Source: 'src/arithmetic/ntt.rs', lines 129:0-133:1 -/
def arithmetic.ntt.to_wrapping_u16 (x : Std.I32) : Result Std.U16 := do
  let x1 ← arithmetic.ntt.to_canonical x
  let i ← lift (core.num.I32.wrapping_sub arithmetic.ntt.P_HALF x1)
  let i1 ← lift (core.num.I32.wrapping_shr i 31#u32)
  let i2 ← lift (arithmetic.ntt.P &&& i1)
  let x2 ← lift (core.num.I32.wrapping_sub x1 i2)
  ok (IScalar.hcast .U16 x2)

/-- [kopis::arithmetic::ntt::ntt]: loop 2:
    Source: 'src/arithmetic/ntt.rs', lines 148:12-152:13 -/
@[rust_loop]
def arithmetic.ntt.ntt_loop0_loop0_loop0
  (iter : core.ops.range.Range Std.Usize) (a : Array Std.I32 256#usize)
  (len : Std.Usize) (zeta : Std.I64) :
  Result (Array Std.I32 256#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok a
  | some j =>
    let i ← j + len
    let i1 ← Array.index_usize a i
    let i2 ← lift (IScalar.cast .I64 i1)
    let i3 ← lift (core.num.I64.wrapping_mul zeta i2)
    let t ← arithmetic.ntt.mont_reduce i3
    let i4 ← Array.index_usize a j
    let i5 ← lift (core.num.I32.wrapping_sub i4 t)
    let a1 ← Array.update a i i5
    let i6 ← Array.index_usize a1 j
    let i7 ← lift (core.num.I32.wrapping_add i6 t)
    let a2 ← Array.update a1 j i7
    arithmetic.ntt.ntt_loop0_loop0_loop0 iter1 a2 len zeta
partial_fixpoint

/-- [kopis::arithmetic::ntt::ntt]: loop 1:
    Source: 'src/arithmetic/ntt.rs', lines 145:8-154:9 -/
@[rust_loop]
def arithmetic.ntt.ntt_loop0_loop0
  (a : Array Std.I32 256#usize) (k : Std.Usize) (len : Std.Usize)
  (start : Std.Usize) :
  Result ((Array Std.I32 256#usize) × Std.Usize)
  := do
  if start < consts.RING_DEG
  then
    let k1 ← k + 1#usize
    let i ← Array.index_usize arithmetic.ntt.ZETAS k1
    let zeta ← lift (IScalar.cast .I64 i)
    let i1 ← start + len
    let a1 ←
      arithmetic.ntt.ntt_loop0_loop0_loop0 { start, «end» := i1 } a len zeta
    let i2 ← 2#usize * len
    let start1 ← start + i2
    arithmetic.ntt.ntt_loop0_loop0 a1 k1 len start1
  else ok (a, k)
partial_fixpoint

/-- [kopis::arithmetic::ntt::ntt]: loop 0:
    Source: 'src/arithmetic/ntt.rs', lines 143:4-156:5 -/
@[rust_loop]
def arithmetic.ntt.ntt_loop0
  (a : Array Std.I32 256#usize) (k : Std.Usize) (len : Std.Usize) :
  Result (Array Std.I32 256#usize)
  := do
  if len > 0#usize
  then
    let (a1, k1) ← arithmetic.ntt.ntt_loop0_loop0 a k len 0#usize
    let len1 ← len / 2#usize
    arithmetic.ntt.ntt_loop0 a1 k1 len1
  else ok a
partial_fixpoint

/-- [kopis::arithmetic::ntt::ntt]: loop 3:
    Source: 'src/arithmetic/ntt.rs', lines 158:4-160:5 -/
@[rust_loop]
def arithmetic.ntt.ntt_loop1
  (iter : core.slice.iter.IterMut Std.I32)
  (back : core.slice.iter.IterMut Std.I32 → core.slice.iter.IterMut Std.I32)
  :
  Result ((core.slice.iter.IterMut Std.I32) × (core.slice.iter.IterMut Std.I32
    → core.slice.iter.IterMut Std.I32))
  := do
  let (o, iter1, next_back) ← core.slice.iter.IteratorIterMut.next iter
  match o with
  | none => ok (iter1, fun im => let im1 := next_back im none
                                 back im1)
  | some coeff =>
    let coeff1 ← arithmetic.ntt.barrett_reduce coeff
    arithmetic.ntt.ntt_loop1 iter1
      (fun im => let im1 := next_back im (some coeff1)
                 back im1)
partial_fixpoint

/-- [kopis::arithmetic::ntt::ntt]:
    Source: 'src/arithmetic/ntt.rs', lines 140:0-161:1 -/
def arithmetic.ntt.ntt
  (a : Array Std.I32 256#usize) : Result (Array Std.I32 256#usize) := do
  let len ← consts.RING_DEG / 2#usize
  let a1 ← arithmetic.ntt.ntt_loop0 a 0#usize len
  let (s, to_slice_mut_back) ← lift (Array.to_slice_mut a1)
  let (iter, iter_mut_back) ← core.slice.Slice.iter_mut s
  let (im, back) ← arithmetic.ntt.ntt_loop1 iter (fun im1 => im1)
  let im1 := back im
  let s1 := iter_mut_back im1
  ok (to_slice_mut_back s1)

/-- [kopis::arithmetic::ntt::invntt]: loop 2:
    Source: 'src/arithmetic/ntt.rs', lines 181:12-185:13 -/
@[rust_loop]
def arithmetic.ntt.invntt_loop0_loop0_loop0
  (iter : core.ops.range.Range Std.Usize) (a : Array Std.I32 256#usize)
  (len : Std.Usize) (neg_zeta : Std.I64) :
  Result (Array Std.I32 256#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok a
  | some j =>
    let t ← Array.index_usize a j
    let i ← j + len
    let i1 ← Array.index_usize a i
    let i2 ← lift (core.num.I32.wrapping_add t i1)
    let a1 ← Array.update a j i2
    let i3 ← Array.index_usize a1 i
    let i4 ← lift (core.num.I32.wrapping_sub t i3)
    let i5 ← lift (IScalar.cast .I64 i4)
    let i6 ← lift (core.num.I64.wrapping_mul neg_zeta i5)
    let i7 ← arithmetic.ntt.mont_reduce i6
    let a2 ← Array.update a1 i i7
    arithmetic.ntt.invntt_loop0_loop0_loop0 iter1 a2 len neg_zeta
partial_fixpoint

/-- [kopis::arithmetic::ntt::invntt]: loop 1:
    Source: 'src/arithmetic/ntt.rs', lines 178:8-187:9 -/
@[rust_loop]
def arithmetic.ntt.invntt_loop0_loop0
  (a : Array Std.I32 256#usize) (k : Std.Usize) (len : Std.Usize)
  (start : Std.Usize) :
  Result ((Array Std.I32 256#usize) × Std.Usize)
  := do
  if start < consts.RING_DEG
  then
    let k1 ← k - 1#usize
    let i ← Array.index_usize arithmetic.ntt.ZETAS k1
    let i1 ← lift (IScalar.cast .I64 i)
    let neg_zeta ← core.num.I64.wrapping_neg i1
    let i2 ← start + len
    let a1 ←
      arithmetic.ntt.invntt_loop0_loop0_loop0 { start, «end» := i2 } a len
        neg_zeta
    let i3 ← 2#usize * len
    let start1 ← start + i3
    arithmetic.ntt.invntt_loop0_loop0 a1 k1 len start1
  else ok (a, k)
partial_fixpoint

/-- [kopis::arithmetic::ntt::invntt]: loop 3:
    Source: 'src/arithmetic/ntt.rs', lines 192:12-194:13 -/
@[rust_loop]
def arithmetic.ntt.invntt_loop0_loop1
  (iter : core.slice.iter.IterMut Std.I32)
  (back : core.slice.iter.IterMut Std.I32 → core.slice.iter.IterMut Std.I32)
  :
  Result ((core.slice.iter.IterMut Std.I32) × (core.slice.iter.IterMut Std.I32
    → core.slice.iter.IterMut Std.I32))
  := do
  let (o, iter1, next_back) ← core.slice.iter.IteratorIterMut.next iter
  match o with
  | none => ok (iter1, fun im => let im1 := next_back im none
                                 back im1)
  | some coeff =>
    let coeff1 ← arithmetic.ntt.barrett_reduce coeff
    arithmetic.ntt.invntt_loop0_loop1 iter1
      (fun im => let im1 := next_back im (some coeff1)
                 back im1)
partial_fixpoint

/-- [kopis::arithmetic::ntt::invntt]: loop 0:
    Source: 'src/arithmetic/ntt.rs', lines 176:4-196:5 -/
@[rust_loop]
def arithmetic.ntt.invntt_loop0
  (a : Array Std.I32 256#usize) (k : Std.Usize) (len : Std.Usize) :
  Result (Array Std.I32 256#usize)
  := do
  if len < consts.RING_DEG
  then
    let (a1, k1) ← arithmetic.ntt.invntt_loop0_loop0 a k len 0#usize
    let len1 ← len * 2#usize
    if len1 = 16#usize
    then
      let (s, to_slice_mut_back) ← lift (Array.to_slice_mut a1)
      let (iter, iter_mut_back) ← core.slice.Slice.iter_mut s
      let (im, back) ←
        arithmetic.ntt.invntt_loop0_loop1 iter (fun im1 => im1)
      let im1 := back im
      let s1 := iter_mut_back im1
      let a2 := to_slice_mut_back s1
      arithmetic.ntt.invntt_loop0 a2 k1 len1
    else arithmetic.ntt.invntt_loop0 a1 k1 len1
  else ok a
partial_fixpoint

/-- [kopis::arithmetic::ntt::invntt]: loop 4:
    Source: 'src/arithmetic/ntt.rs', lines 198:4-200:5 -/
@[rust_loop]
def arithmetic.ntt.invntt_loop1
  (iter : core.slice.iter.IterMut Std.I32)
  (back : core.slice.iter.IterMut Std.I32 → core.slice.iter.IterMut Std.I32)
  :
  Result ((core.slice.iter.IterMut Std.I32) × (core.slice.iter.IterMut Std.I32
    → core.slice.iter.IterMut Std.I32))
  := do
  let (o, iter1, next_back) ← core.slice.iter.IteratorIterMut.next iter
  match o with
  | none => ok (iter1, fun im => let im1 := next_back im none
                                 back im1)
  | some coeff =>
    let i ← lift (IScalar.cast .I64 coeff)
    let i1 ← lift (IScalar.cast .I64 arithmetic.ntt.INVNTT_SCALE)
    let i2 ← lift (core.num.I64.wrapping_mul i i1)
    let coeff1 ← arithmetic.ntt.mont_reduce i2
    arithmetic.ntt.invntt_loop1 iter1
      (fun im => let im1 := next_back im (some coeff1)
                 back im1)
partial_fixpoint

/-- [kopis::arithmetic::ntt::invntt]:
    Source: 'src/arithmetic/ntt.rs', lines 173:0-201:1 -/
def arithmetic.ntt.invntt
  (a : Array Std.I32 256#usize) : Result (Array Std.I32 256#usize) := do
  let a1 ← arithmetic.ntt.invntt_loop0 a consts.RING_DEG 1#usize
  let (s, to_slice_mut_back) ← lift (Array.to_slice_mut a1)
  let (iter, iter_mut_back) ← core.slice.Slice.iter_mut s
  let (im, back) ← arithmetic.ntt.invntt_loop1 iter (fun im1 => im1)
  let im1 := back im
  let s1 := iter_mut_back im1
  ok (to_slice_mut_back s1)

/-- [kopis::arithmetic::ntt::NttElem]
    Source: 'src/arithmetic/ntt.rs', lines 207:0-207:54 -/
@[reducible]
def arithmetic.ntt.NttElem := Array Std.I32 256#usize

/-- [kopis::arithmetic::ntt::{impl core::clone::Clone for kopis::arithmetic::ntt::NttElem}::clone]:
    Source: 'src/arithmetic/ntt.rs', lines 206:9-206:14
    Visibility: public -/
def arithmetic.ntt.NttElem.Insts.CoreCloneClone.clone
  (self : arithmetic.ntt.NttElem) : Result arithmetic.ntt.NttElem := do
  ok self

/-- Trait implementation: [kopis::arithmetic::ntt::{impl core::clone::Clone for kopis::arithmetic::ntt::NttElem}]
    Source: 'src/arithmetic/ntt.rs', lines 206:9-206:14 -/
@[reducible]
def arithmetic.ntt.NttElem.Insts.CoreCloneClone : core.clone.Clone
  arithmetic.ntt.NttElem := {
  clone := arithmetic.ntt.NttElem.Insts.CoreCloneClone.clone
}

/-- Trait implementation: [kopis::arithmetic::ntt::{impl core::marker::Copy for kopis::arithmetic::ntt::NttElem}]
    Source: 'src/arithmetic/ntt.rs', lines 206:16-206:20 -/
@[reducible]
def arithmetic.ntt.NttElem.Insts.CoreMarkerCopy : core.marker.Copy
  arithmetic.ntt.NttElem := {
  cloneInst := arithmetic.ntt.NttElem.Insts.CoreCloneClone
}

/-- [kopis::arithmetic::ntt::{impl zeroize::Zeroize for kopis::arithmetic::ntt::NttElem}::zeroize]:
    Source: 'src/arithmetic/ntt.rs', lines 206:22-206:29
    Visibility: public -/
def arithmetic.ntt.NttElem.Insts.ZeroizeZeroize.zeroize
  (self : arithmetic.ntt.NttElem) : Result arithmetic.ntt.NttElem := do
  let __zeroize_field_0 ←
    Array.Insts.ZeroizeZeroize.zeroize (zeroize.Zeroize.Blanket
      I32.Insts.ZeroizeDefaultIsZeroes) self
  ok __zeroize_field_0

/-- Trait implementation: [kopis::arithmetic::ntt::{impl zeroize::Zeroize for kopis::arithmetic::ntt::NttElem}]
    Source: 'src/arithmetic/ntt.rs', lines 206:22-206:29 -/
@[reducible]
def arithmetic.ntt.NttElem.Insts.ZeroizeZeroize : zeroize.Zeroize
  arithmetic.ntt.NttElem := {
  zeroize := arithmetic.ntt.NttElem.Insts.ZeroizeZeroize.zeroize
}

/-- [kopis::arithmetic::ntt::{impl core::default::Default for kopis::arithmetic::ntt::NttElem}::default]:
    Source: 'src/arithmetic/ntt.rs', lines 210:4-212:5
    Visibility: public -/
def arithmetic.ntt.NttElem.Insts.CoreDefaultDefault.default
  : Result arithmetic.ntt.NttElem := do
  let a := Array.repeat 256#usize 0#i32
  ok a

/-- Trait implementation: [kopis::arithmetic::ntt::{impl core::default::Default for kopis::arithmetic::ntt::NttElem}]
    Source: 'src/arithmetic/ntt.rs', lines 209:0-213:1 -/
@[reducible]
def arithmetic.ntt.NttElem.Insts.CoreDefaultDefault : core.default.Default
  arithmetic.ntt.NttElem := {
  default := arithmetic.ntt.NttElem.Insts.CoreDefaultDefault.default
}

/-- [kopis::arithmetic::ntt::{kopis::arithmetic::ntt::NttElem}::from_uniform]: loop 0:
    Source: 'src/arithmetic/ntt.rs', lines 233:8-235:9 -/
@[rust_loop]
def arithmetic.ntt.NttElem.from_uniform_loop
  (iter : core.ops.range.Range Std.Usize)
  (elem : arithmetic.ring_arith.RingElem) (a : Array Std.I32 256#usize) :
  Result (Array Std.I32 256#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok a
  | some i =>
    let i1 ← Array.index_usize elem i
    let i2 ← lift (UScalar.hcast .I32 i1)
    let a1 ← Array.update a i i2
    arithmetic.ntt.NttElem.from_uniform_loop iter1 elem a1
partial_fixpoint

/-- [kopis::arithmetic::ntt::{kopis::arithmetic::ntt::NttElem}::from_uniform]:
    Source: 'src/arithmetic/ntt.rs', lines 223:4-238:5 -/
def arithmetic.ntt.NttElem.from_uniform
  (elem : arithmetic.ring_arith.RingElem) : Result arithmetic.ntt.NttElem := do
  let a := Array.repeat 256#usize 0#i32
  let a1 ←
    arithmetic.ntt.NttElem.from_uniform_loop
      { start := 0#usize, «end» := consts.RING_DEG } elem a
  let a2 ← arithmetic.ntt.ntt a1
  ok a2

/-- [kopis::arithmetic::ntt::{kopis::arithmetic::ntt::NttElem}::from_secret]: loop 0:
    Source: 'src/arithmetic/ntt.rs', lines 254:8-256:9 -/
@[rust_loop]
def arithmetic.ntt.NttElem.from_secret_loop
  (iter : core.ops.range.Range Std.Usize)
  (elem : arithmetic.ring_arith.RingElem) (a : Array Std.I32 256#usize) :
  Result (Array Std.I32 256#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok a
  | some i =>
    let i1 ← Array.index_usize elem i
    let i2 ← lift (UScalar.hcast .I16 i1)
    let i3 ← lift (IScalar.cast .I32 i2)
    let a1 ← Array.update a i i3
    arithmetic.ntt.NttElem.from_secret_loop iter1 elem a1
partial_fixpoint

/-- [kopis::arithmetic::ntt::{kopis::arithmetic::ntt::NttElem}::from_secret]:
    Source: 'src/arithmetic/ntt.rs', lines 245:4-259:5 -/
def arithmetic.ntt.NttElem.from_secret
  (elem : arithmetic.ring_arith.RingElem) : Result arithmetic.ntt.NttElem := do
  let a := Array.repeat 256#usize 0#i32
  let a1 ←
    arithmetic.ntt.NttElem.from_secret_loop
      { start := 0#usize, «end» := consts.RING_DEG } elem a
  let a2 ← arithmetic.ntt.ntt a1
  ok a2

/-- [kopis::arithmetic::ntt::NttMatrix]
    Source: 'src/arithmetic/ntt.rs', lines 264:0-264:90 -/
@[reducible]
def arithmetic.ntt.NttMatrix (X : Std.Usize) (Y : Std.Usize) :=
  Array (Array arithmetic.ntt.NttElem Y) X

/-- [kopis::arithmetic::ntt::{impl core::clone::Clone for kopis::arithmetic::ntt::NttMatrix<X, Y>}::clone]:
    Source: 'src/arithmetic/ntt.rs', lines 263:9-263:14
    Visibility: public -/
def arithmetic.ntt.NttMatrix.Insts.CoreCloneClone.clone
  {X : Std.Usize} {Y : Std.Usize} (self : arithmetic.ntt.NttMatrix X Y) :
  Result (arithmetic.ntt.NttMatrix X Y)
  := do
  let a ←
    core.array.CloneArray.clone (core.clone.CloneArray Y
      arithmetic.ntt.NttElem.Insts.CoreCloneClone) self
  ok a

/-- Trait implementation: [kopis::arithmetic::ntt::{impl core::clone::Clone for kopis::arithmetic::ntt::NttMatrix<X, Y>}]
    Source: 'src/arithmetic/ntt.rs', lines 263:9-263:14 -/
@[reducible]
def arithmetic.ntt.NttMatrix.Insts.CoreCloneClone (X : Std.Usize) (Y :
  Std.Usize) : core.clone.Clone (arithmetic.ntt.NttMatrix X Y) := {
  clone := arithmetic.ntt.NttMatrix.Insts.CoreCloneClone.clone
}

/-- [kopis::arithmetic::ntt::{impl zeroize::Zeroize for kopis::arithmetic::ntt::NttMatrix<X, Y>}::zeroize]:
    Source: 'src/arithmetic/ntt.rs', lines 263:16-263:23
    Visibility: public -/
def arithmetic.ntt.NttMatrix.Insts.ZeroizeZeroize.zeroize
  {X : Std.Usize} {Y : Std.Usize} (self : arithmetic.ntt.NttMatrix X Y) :
  Result (arithmetic.ntt.NttMatrix X Y)
  := do
  let __zeroize_field_0 ←
    Array.Insts.ZeroizeZeroize.zeroize (Array.Insts.ZeroizeZeroize Y
      arithmetic.ntt.NttElem.Insts.ZeroizeZeroize) self
  ok __zeroize_field_0

/-- Trait implementation: [kopis::arithmetic::ntt::{impl zeroize::Zeroize for kopis::arithmetic::ntt::NttMatrix<X, Y>}]
    Source: 'src/arithmetic/ntt.rs', lines 263:16-263:23 -/
@[reducible]
def arithmetic.ntt.NttMatrix.Insts.ZeroizeZeroize (X : Std.Usize) (Y :
  Std.Usize) : zeroize.Zeroize (arithmetic.ntt.NttMatrix X Y) := {
  zeroize := arithmetic.ntt.NttMatrix.Insts.ZeroizeZeroize.zeroize
}

/-- [kopis::arithmetic::ntt::{impl core::default::Default for kopis::arithmetic::ntt::NttMatrix<X, Y>}::default]:
    Source: 'src/arithmetic/ntt.rs', lines 267:4-269:5
    Visibility: public -/
def arithmetic.ntt.NttMatrix.Insts.CoreDefaultDefault.default
  (X : Std.Usize) (Y : Std.Usize) : Result (arithmetic.ntt.NttMatrix X Y) := do
  let ne ← arithmetic.ntt.NttElem.Insts.CoreDefaultDefault.default
  let a := Array.repeat Y ne
  let a1 := Array.repeat X a
  ok a1

/-- Trait implementation: [kopis::arithmetic::ntt::{impl core::default::Default for kopis::arithmetic::ntt::NttMatrix<X, Y>}]
    Source: 'src/arithmetic/ntt.rs', lines 266:0-270:1 -/
@[reducible]
def arithmetic.ntt.NttMatrix.Insts.CoreDefaultDefault (X : Std.Usize) (Y :
  Std.Usize) : core.default.Default (arithmetic.ntt.NttMatrix X Y) := {
  default := arithmetic.ntt.NttMatrix.Insts.CoreDefaultDefault.default X Y
}

/-- [kopis::arithmetic::ntt::pointwise_mul_acc]: loop 0:
    Source: 'src/arithmetic/ntt.rs', lines 286:4-288:5 -/
@[rust_loop]
def arithmetic.ntt.pointwise_mul_acc_loop
  (iter : core.ops.range.Range Std.Usize) (acc : Array Std.I64 256#usize)
  (lhs : arithmetic.ntt.NttElem) (rhs : arithmetic.ntt.NttElem) :
  Result (Array Std.I64 256#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok acc
  | some i =>
    let i1 ← Array.index_usize acc i
    let i2 ← Array.index_usize lhs i
    let i3 ← lift (IScalar.cast .I64 i2)
    let i4 ← Array.index_usize rhs i
    let i5 ← lift (IScalar.cast .I64 i4)
    let i6 ← lift (core.num.I64.wrapping_mul i3 i5)
    let i7 ← lift (core.num.I64.wrapping_add i1 i6)
    let a ← Array.update acc i i7
    arithmetic.ntt.pointwise_mul_acc_loop iter1 a lhs rhs
partial_fixpoint

/-- [kopis::arithmetic::ntt::pointwise_mul_acc]:
    Source: 'src/arithmetic/ntt.rs', lines 277:0-289:1 -/
@[reducible]
def arithmetic.ntt.pointwise_mul_acc
  (acc : Array Std.I64 256#usize) (lhs : arithmetic.ntt.NttElem)
  (rhs : arithmetic.ntt.NttElem) :
  Result (Array Std.I64 256#usize)
  := do
  arithmetic.ntt.pointwise_mul_acc_loop
    { start := 0#usize, «end» := consts.RING_DEG } acc lhs rhs

/-- [kopis::arithmetic::ntt::reduce_invntt_to_ring_elem]: loop 0:
    Source: 'src/arithmetic/ntt.rs', lines 302:4-304:5 -/
@[rust_loop]
def arithmetic.ntt.reduce_invntt_to_ring_elem_loop0
  (iter : core.ops.range.Range Std.Usize) (acc : Array Std.I64 256#usize)
  (v : Array Std.I32 256#usize) :
  Result (Array Std.I32 256#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok v
  | some i =>
    let i1 ← Array.index_usize acc i
    let i2 ← arithmetic.ntt.mont_reduce i1
    let a ← Array.update v i i2
    arithmetic.ntt.reduce_invntt_to_ring_elem_loop0 iter1 acc a
partial_fixpoint

/-- [kopis::arithmetic::ntt::reduce_invntt_to_ring_elem]: loop 1:
    Source: 'src/arithmetic/ntt.rs', lines 308:4-310:5 -/
@[rust_loop]
def arithmetic.ntt.reduce_invntt_to_ring_elem_loop1
  (iter : core.ops.range.Range Std.Usize) (v : Array Std.I32 256#usize)
  (out : arithmetic.ring_arith.RingElem) :
  Result arithmetic.ring_arith.RingElem
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok out
  | some i =>
    let i1 ← Array.index_usize v i
    let i2 ← arithmetic.ntt.to_wrapping_u16 i1
    let a ← Array.update out i i2
    arithmetic.ntt.reduce_invntt_to_ring_elem_loop1 iter1 v a
partial_fixpoint

/-- [kopis::arithmetic::ntt::reduce_invntt_to_ring_elem]:
    Source: 'src/arithmetic/ntt.rs', lines 293:0-312:1 -/
def arithmetic.ntt.reduce_invntt_to_ring_elem
  (acc : Array Std.I64 256#usize) : Result arithmetic.ring_arith.RingElem := do
  let v := Array.repeat 256#usize 0#i32
  let v1 ←
    arithmetic.ntt.reduce_invntt_to_ring_elem_loop0
      { start := 0#usize, «end» := consts.RING_DEG } acc v
  let v2 ← arithmetic.ntt.invntt v1
  let out ← arithmetic.ring_arith.RingElem.Insts.CoreDefaultDefault.default
  arithmetic.ntt.reduce_invntt_to_ring_elem_loop1
    { start := 0#usize, «end» := consts.RING_DEG } v2 out

/-- [kopis::arithmetic::ntt::{kopis::arithmetic::ntt::NttMatrix<X, Y>}::from_uniform_matrix]: loop 1:
    Source: 'src/arithmetic/ntt.rs', lines 319:12-321:13 -/
@[rust_loop]
def arithmetic.ntt.NttMatrix.from_uniform_matrix_loop0_loop0
  {X : Std.Usize} {Y : Std.Usize} (iter : core.ops.range.Range Std.Usize)
  (mat : arithmetic.matrix_arith.Matrix X Y)
  (ret : arithmetic.ntt.NttMatrix X Y) (i : Std.Usize) :
  Result ((arithmetic.matrix_arith.Matrix X Y) × (arithmetic.ntt.NttMatrix X
    Y))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (mat, ret)
  | some j =>
    let a ← Array.index_usize mat i
    let re ← Array.index_usize a j
    let ne ← arithmetic.ntt.NttElem.from_uniform re
    let (a1, index_mut_back) ← Array.index_mut_usize ret i
    let a2 ← Array.update a1 j ne
    let a3 := index_mut_back a2
    arithmetic.ntt.NttMatrix.from_uniform_matrix_loop0_loop0 iter1 mat a3 i
partial_fixpoint

/-- [kopis::arithmetic::ntt::{kopis::arithmetic::ntt::NttMatrix<X, Y>}::from_uniform_matrix]: loop 0:
    Source: 'src/arithmetic/ntt.rs', lines 318:8-322:9 -/
@[rust_loop]
def arithmetic.ntt.NttMatrix.from_uniform_matrix_loop0
  {X : Std.Usize} {Y : Std.Usize} (iter : core.ops.range.Range Std.Usize)
  (mat : arithmetic.matrix_arith.Matrix X Y)
  (ret : arithmetic.ntt.NttMatrix X Y) :
  Result (arithmetic.ntt.NttMatrix X Y)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok ret
  | some i =>
    let (mat1, ret1) ←
      arithmetic.ntt.NttMatrix.from_uniform_matrix_loop0_loop0
        { start := 0#usize, «end» := Y } mat ret i
    arithmetic.ntt.NttMatrix.from_uniform_matrix_loop0 iter1 mat1 ret1
partial_fixpoint

/-- [kopis::arithmetic::ntt::{kopis::arithmetic::ntt::NttMatrix<X, Y>}::from_uniform_matrix]:
    Source: 'src/arithmetic/ntt.rs', lines 316:4-324:5 -/
def arithmetic.ntt.NttMatrix.from_uniform_matrix
  {X : Std.Usize} {Y : Std.Usize} (mat : arithmetic.matrix_arith.Matrix X Y) :
  Result (arithmetic.ntt.NttMatrix X Y)
  := do
  let ret ← arithmetic.ntt.NttMatrix.Insts.CoreDefaultDefault.default X Y
  arithmetic.ntt.NttMatrix.from_uniform_matrix_loop0
    { start := 0#usize, «end» := X } mat ret

/-- [kopis::arithmetic::ntt::{kopis::arithmetic::ntt::NttMatrix<X, Y>}::from_secret_matrix]: loop 1:
    Source: 'src/arithmetic/ntt.rs', lines 330:12-332:13 -/
@[rust_loop]
def arithmetic.ntt.NttMatrix.from_secret_matrix_loop0_loop0
  {X : Std.Usize} {Y : Std.Usize} (iter : core.ops.range.Range Std.Usize)
  (mat : arithmetic.matrix_arith.Matrix X Y)
  (ret : arithmetic.ntt.NttMatrix X Y) (i : Std.Usize) :
  Result ((arithmetic.matrix_arith.Matrix X Y) × (arithmetic.ntt.NttMatrix X
    Y))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (mat, ret)
  | some j =>
    let a ← Array.index_usize mat i
    let re ← Array.index_usize a j
    let ne ← arithmetic.ntt.NttElem.from_secret re
    let (a1, index_mut_back) ← Array.index_mut_usize ret i
    let a2 ← Array.update a1 j ne
    let a3 := index_mut_back a2
    arithmetic.ntt.NttMatrix.from_secret_matrix_loop0_loop0 iter1 mat a3 i
partial_fixpoint

/-- [kopis::arithmetic::ntt::{kopis::arithmetic::ntt::NttMatrix<X, Y>}::from_secret_matrix]: loop 0:
    Source: 'src/arithmetic/ntt.rs', lines 329:8-333:9 -/
@[rust_loop]
def arithmetic.ntt.NttMatrix.from_secret_matrix_loop0
  {X : Std.Usize} {Y : Std.Usize} (iter : core.ops.range.Range Std.Usize)
  (mat : arithmetic.matrix_arith.Matrix X Y)
  (ret : arithmetic.ntt.NttMatrix X Y) :
  Result (arithmetic.ntt.NttMatrix X Y)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok ret
  | some i =>
    let (mat1, ret1) ←
      arithmetic.ntt.NttMatrix.from_secret_matrix_loop0_loop0
        { start := 0#usize, «end» := Y } mat ret i
    arithmetic.ntt.NttMatrix.from_secret_matrix_loop0 iter1 mat1 ret1
partial_fixpoint

/-- [kopis::arithmetic::ntt::{kopis::arithmetic::ntt::NttMatrix<X, Y>}::from_secret_matrix]:
    Source: 'src/arithmetic/ntt.rs', lines 327:4-335:5 -/
def arithmetic.ntt.NttMatrix.from_secret_matrix
  {X : Std.Usize} {Y : Std.Usize} (mat : arithmetic.matrix_arith.Matrix X Y) :
  Result (arithmetic.ntt.NttMatrix X Y)
  := do
  let ret ← arithmetic.ntt.NttMatrix.Insts.CoreDefaultDefault.default X Y
  arithmetic.ntt.NttMatrix.from_secret_matrix_loop0
    { start := 0#usize, «end» := X } mat ret

/-- [kopis::consts::MAX_L]
    Source: 'src/consts.rs', lines 34:0-34:34 -/
@[global_simps, irreducible] def consts.MAX_L : Std.Usize := 4#usize

/-- [kopis::arithmetic::ntt::{kopis::arithmetic::ntt::NttMatrix<X, Y>}::mul]: loop 2:
    Source: 'src/arithmetic/ntt.rs', lines 347:16-349:17 -/
@[rust_loop]
def arithmetic.ntt.NttMatrix.mul_loop0_loop0_loop0
  {X : Std.Usize} {Y : Std.Usize} {Z : Std.Usize}
  (iter : core.ops.range.Range Std.Usize) (self : arithmetic.ntt.NttMatrix X Y)
  (other : arithmetic.ntt.NttMatrix Y Z) (i : Std.Usize) (k : Std.Usize)
  (acc : Array Std.I64 256#usize) :
  Result ((arithmetic.ntt.NttMatrix X Y) × (arithmetic.ntt.NttMatrix Y Z) ×
    (Array Std.I64 256#usize))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (self, other, acc)
  | some j =>
    let a ← Array.index_usize self i
    let ne ← Array.index_usize a j
    let a1 ← Array.index_usize other j
    let ne1 ← Array.index_usize a1 k
    let acc1 ← arithmetic.ntt.pointwise_mul_acc acc ne ne1
    arithmetic.ntt.NttMatrix.mul_loop0_loop0_loop0 iter1 self other i k acc1
partial_fixpoint

/-- [kopis::arithmetic::ntt::{kopis::arithmetic::ntt::NttMatrix<X, Y>}::mul]: loop 1:
    Source: 'src/arithmetic/ntt.rs', lines 345:12-351:13 -/
@[rust_loop]
def arithmetic.ntt.NttMatrix.mul_loop0_loop0
  {X : Std.Usize} {Y : Std.Usize} {Z : Std.Usize}
  (iter : core.ops.range.Range Std.Usize) (self : arithmetic.ntt.NttMatrix X Y)
  (other : arithmetic.ntt.NttMatrix Y Z)
  (result : arithmetic.matrix_arith.Matrix X Z) (i : Std.Usize) :
  Result ((arithmetic.ntt.NttMatrix X Y) × (arithmetic.ntt.NttMatrix Y Z) ×
    (arithmetic.matrix_arith.Matrix X Z))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (self, other, result)
  | some k =>
    let acc := Array.repeat 256#usize 0#i64
    let (self1, other1, acc1) ←
      arithmetic.ntt.NttMatrix.mul_loop0_loop0_loop0
        { start := 0#usize, «end» := Y } self other i k acc
    let re ← arithmetic.ntt.reduce_invntt_to_ring_elem acc1
    let (a, index_mut_back) ← Array.index_mut_usize result i
    let a1 ← Array.update a k re
    let a2 := index_mut_back a1
    arithmetic.ntt.NttMatrix.mul_loop0_loop0 iter1 self1 other1 a2 i
partial_fixpoint

/-- [kopis::arithmetic::ntt::{kopis::arithmetic::ntt::NttMatrix<X, Y>}::mul]: loop 0:
    Source: 'src/arithmetic/ntt.rs', lines 344:8-352:9 -/
@[rust_loop]
def arithmetic.ntt.NttMatrix.mul_loop0
  {X : Std.Usize} {Y : Std.Usize} {Z : Std.Usize}
  (iter : core.ops.range.Range Std.Usize) (self : arithmetic.ntt.NttMatrix X Y)
  (other : arithmetic.ntt.NttMatrix Y Z)
  (result : arithmetic.matrix_arith.Matrix X Z) :
  Result (arithmetic.matrix_arith.Matrix X Z)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok result
  | some i =>
    let (self1, other1, result1) ←
      arithmetic.ntt.NttMatrix.mul_loop0_loop0
        { start := 0#usize, «end» := Z } self other result i
    arithmetic.ntt.NttMatrix.mul_loop0 iter1 self1 other1 result1
partial_fixpoint

/-- [kopis::arithmetic::ntt::{kopis::arithmetic::ntt::NttMatrix<X, Y>}::mul]:
    Source: 'src/arithmetic/ntt.rs', lines 339:4-354:5 -/
def arithmetic.ntt.NttMatrix.mul
  {X : Std.Usize} {Y : Std.Usize} {Z : Std.Usize}
  (self : arithmetic.ntt.NttMatrix X Y) (other : arithmetic.ntt.NttMatrix Y Z)
  :
  Result (arithmetic.matrix_arith.Matrix X Z)
  := do
  massert (Y <= consts.MAX_L)
  let result ←
    arithmetic.matrix_arith.Matrix.Insts.CoreDefaultDefault.default X Z
  arithmetic.ntt.NttMatrix.mul_loop0 { start := 0#usize, «end» := X } self
    other result

/-- [kopis::arithmetic::ntt::{kopis::arithmetic::ntt::NttMatrix<X, Y>}::mul_transpose]: loop 2:
    Source: 'src/arithmetic/ntt.rs', lines 366:16-368:17 -/
@[rust_loop]
def arithmetic.ntt.NttMatrix.mul_transpose_loop0_loop0_loop0
  {X : Std.Usize} {Y : Std.Usize} {Z : Std.Usize}
  (iter : core.ops.range.Range Std.Usize) (self : arithmetic.ntt.NttMatrix X Y)
  (other : arithmetic.ntt.NttMatrix X Z) (j : Std.Usize) (k : Std.Usize)
  (acc : Array Std.I64 256#usize) :
  Result ((arithmetic.ntt.NttMatrix X Y) × (arithmetic.ntt.NttMatrix X Z) ×
    (Array Std.I64 256#usize))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (self, other, acc)
  | some i =>
    let a ← Array.index_usize self i
    let ne ← Array.index_usize a j
    let a1 ← Array.index_usize other i
    let ne1 ← Array.index_usize a1 k
    let acc1 ← arithmetic.ntt.pointwise_mul_acc acc ne ne1
    arithmetic.ntt.NttMatrix.mul_transpose_loop0_loop0_loop0 iter1 self other j
      k acc1
partial_fixpoint

/-- [kopis::arithmetic::ntt::{kopis::arithmetic::ntt::NttMatrix<X, Y>}::mul_transpose]: loop 1:
    Source: 'src/arithmetic/ntt.rs', lines 364:12-370:13 -/
@[rust_loop]
def arithmetic.ntt.NttMatrix.mul_transpose_loop0_loop0
  {X : Std.Usize} {Y : Std.Usize} {Z : Std.Usize}
  (iter : core.ops.range.Range Std.Usize) (self : arithmetic.ntt.NttMatrix X Y)
  (other : arithmetic.ntt.NttMatrix X Z)
  (result : arithmetic.matrix_arith.Matrix Y Z) (j : Std.Usize) :
  Result ((arithmetic.ntt.NttMatrix X Y) × (arithmetic.ntt.NttMatrix X Z) ×
    (arithmetic.matrix_arith.Matrix Y Z))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (self, other, result)
  | some k =>
    let acc := Array.repeat 256#usize 0#i64
    let (self1, other1, acc1) ←
      arithmetic.ntt.NttMatrix.mul_transpose_loop0_loop0_loop0
        { start := 0#usize, «end» := X } self other j k acc
    let re ← arithmetic.ntt.reduce_invntt_to_ring_elem acc1
    let (a, index_mut_back) ← Array.index_mut_usize result j
    let a1 ← Array.update a k re
    let a2 := index_mut_back a1
    arithmetic.ntt.NttMatrix.mul_transpose_loop0_loop0 iter1 self1 other1 a2 j
partial_fixpoint

/-- [kopis::arithmetic::ntt::{kopis::arithmetic::ntt::NttMatrix<X, Y>}::mul_transpose]: loop 0:
    Source: 'src/arithmetic/ntt.rs', lines 363:8-371:9 -/
@[rust_loop]
def arithmetic.ntt.NttMatrix.mul_transpose_loop0
  {X : Std.Usize} {Y : Std.Usize} {Z : Std.Usize}
  (iter : core.ops.range.Range Std.Usize) (self : arithmetic.ntt.NttMatrix X Y)
  (other : arithmetic.ntt.NttMatrix X Z)
  (result : arithmetic.matrix_arith.Matrix Y Z) :
  Result (arithmetic.matrix_arith.Matrix Y Z)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok result
  | some j =>
    let (self1, other1, result1) ←
      arithmetic.ntt.NttMatrix.mul_transpose_loop0_loop0
        { start := 0#usize, «end» := Z } self other result j
    arithmetic.ntt.NttMatrix.mul_transpose_loop0 iter1 self1 other1 result1
partial_fixpoint

/-- [kopis::arithmetic::ntt::{kopis::arithmetic::ntt::NttMatrix<X, Y>}::mul_transpose]:
    Source: 'src/arithmetic/ntt.rs', lines 358:4-373:5 -/
def arithmetic.ntt.NttMatrix.mul_transpose
  {X : Std.Usize} {Y : Std.Usize} {Z : Std.Usize}
  (self : arithmetic.ntt.NttMatrix X Y) (other : arithmetic.ntt.NttMatrix X Z)
  :
  Result (arithmetic.matrix_arith.Matrix Y Z)
  := do
  massert (X <= consts.MAX_L)
  let result ←
    arithmetic.matrix_arith.Matrix.Insts.CoreDefaultDefault.default Y Z
  arithmetic.ntt.NttMatrix.mul_transpose_loop0
    { start := 0#usize, «end» := Y } self other result

/-- [kopis::arithmetic::ring_arith::{impl core::cmp::Eq for kopis::arithmetic::ring_arith::RingElem}::assert_fields_are_eq]:
    Source: 'src/arithmetic/ring_arith.rs', lines 15:9-15:11
    Visibility: public -/
def arithmetic.ring_arith.RingElem.Insts.CoreCmpEq.assert_fields_are_eq
  (self : arithmetic.ring_arith.RingElem) : Result Unit := do
  ok ()

/-- Trait implementation: [kopis::arithmetic::ring_arith::{impl core::cmp::Eq for kopis::arithmetic::ring_arith::RingElem}]
    Source: 'src/arithmetic/ring_arith.rs', lines 15:9-15:11 -/
@[reducible]
def arithmetic.ring_arith.RingElem.Insts.CoreCmpEq : core.cmp.Eq
  arithmetic.ring_arith.RingElem := {
  partialEqInst :=
    arithmetic.ring_arith.RingElem.Insts.CoreCmpPartialEqRingElem
  assert_fields_are_eq :=
    arithmetic.ring_arith.RingElem.Insts.CoreCmpEq.assert_fields_are_eq
}

/-- Trait implementation: [kopis::arithmetic::ring_arith::{impl core::marker::StructuralPartialEq for kopis::arithmetic::ring_arith::RingElem}]
    Source: 'src/arithmetic/ring_arith.rs', lines 15:13-15:22 -/
@[reducible]
def arithmetic.ring_arith.RingElem.Insts.CoreMarkerStructuralPartialEq :
  core.marker.StructuralPartialEq arithmetic.ring_arith.RingElem := {
}

/-- Trait implementation: [kopis::arithmetic::ring_arith::{impl core::marker::Copy for kopis::arithmetic::ring_arith::RingElem}]
    Source: 'src/arithmetic/ring_arith.rs', lines 15:38-15:42 -/
@[reducible]
def arithmetic.ring_arith.RingElem.Insts.CoreMarkerCopy : core.marker.Copy
  arithmetic.ring_arith.RingElem := {
  cloneInst := arithmetic.ring_arith.RingElem.Insts.CoreCloneClone
}

/-- Trait implementation: [kopis::arithmetic::ring_arith::{impl core::default::Default for kopis::arithmetic::ring_arith::RingElem}]
    Source: 'src/arithmetic/ring_arith.rs', lines 18:0-22:1 -/
@[reducible]
def arithmetic.ring_arith.RingElem.Insts.CoreDefaultDefault :
  core.default.Default arithmetic.ring_arith.RingElem := {
  default := arithmetic.ring_arith.RingElem.Insts.CoreDefaultDefault.default
}

/-- [kopis::arithmetic::ring_arith::{kopis::arithmetic::ring_arith::RingElem}::shift_left]: loop 0:
    Source: 'src/arithmetic/ring_arith.rs', lines 95:8-97:9 -/
@[rust_loop]
def arithmetic.ring_arith.RingElem.shift_left_loop
  (iter : core.slice.iter.IterMut Std.U16)
  (back : core.slice.iter.IterMut Std.U16 → core.slice.iter.IterMut Std.U16)
  (shift : Std.Usize) :
  Result (core.slice.iter.IterMut Std.U16)
  := do
  let (o, iter1, next_back) ← core.slice.iter.IteratorIterMut.next iter
  match o with
  | none => ok (let im := next_back iter1 none
                back im)
  | some coeff =>
    let coeff1 ← coeff <<< shift
    arithmetic.ring_arith.RingElem.shift_left_loop iter1
      (fun im => let im1 := next_back im (some coeff1)
                 back im1) shift
partial_fixpoint

/-- [kopis::arithmetic::ring_arith::{kopis::arithmetic::ring_arith::RingElem}::shift_left]:
    Source: 'src/arithmetic/ring_arith.rs', lines 94:4-98:5 -/
def arithmetic.ring_arith.RingElem.shift_left
  (self : arithmetic.ring_arith.RingElem) (shift : Std.Usize) :
  Result arithmetic.ring_arith.RingElem
  := do
  let (s, to_slice_mut_back) ← lift (Array.to_slice_mut self)
  let (iter, iter_mut_back) ← core.slice.Slice.iter_mut s
  let back ←
    arithmetic.ring_arith.RingElem.shift_left_loop iter (fun im => im) shift
  let s1 := iter_mut_back back
  let a := to_slice_mut_back s1
  ok a

/-- [kopis::arithmetic::ring_arith::{impl core::ops::arith::Mul<&'a kopis::arithmetic::ring_arith::RingElem, kopis::arithmetic::ring_arith::RingElem> for &'a kopis::arithmetic::ring_arith::RingElem}::mul]:
    Source: 'src/arithmetic/ring_arith.rs', lines 111:4-115:5
    Visibility: public -/
def SharedARingElem.Insts.CoreOpsArithMulSharedARingElemRingElem.mul
  (self : arithmetic.ring_arith.RingElem)
  (other : arithmetic.ring_arith.RingElem) :
  Result arithmetic.ring_arith.RingElem
  := do
  let ret ← arithmetic.ring_arith.RingElem.Insts.CoreDefaultDefault.default
  arithmetic.ring_arith.ring_mul_acc ret self other

/-- Trait implementation: [kopis::arithmetic::ring_arith::{impl core::ops::arith::Mul<&'a kopis::arithmetic::ring_arith::RingElem, kopis::arithmetic::ring_arith::RingElem> for &'a kopis::arithmetic::ring_arith::RingElem}]
    Source: 'src/arithmetic/ring_arith.rs', lines 108:0-116:1 -/
@[reducible]
def SharedARingElem.Insts.CoreOpsArithMulSharedARingElemRingElem :
  core.ops.arith.Mul arithmetic.ring_arith.RingElem
  arithmetic.ring_arith.RingElem arithmetic.ring_arith.RingElem := {
  mul := SharedARingElem.Insts.CoreOpsArithMulSharedARingElemRingElem.mul
}

/-- Trait implementation: [kopis::arithmetic::ring_arith::{impl core::ops::arith::Add<&'a kopis::arithmetic::ring_arith::RingElem, kopis::arithmetic::ring_arith::RingElem> for &'a kopis::arithmetic::ring_arith::RingElem}]
    Source: 'src/arithmetic/ring_arith.rs', lines 207:0-217:1 -/
@[reducible]
def SharedARingElem.Insts.CoreOpsArithAddSharedARingElemRingElem :
  core.ops.arith.Add arithmetic.ring_arith.RingElem
  arithmetic.ring_arith.RingElem arithmetic.ring_arith.RingElem := {
  add := SharedARingElem.Insts.CoreOpsArithAddSharedARingElemRingElem.add
}

/-- [kopis::arithmetic::ring_arith::{impl core::ops::arith::Sub<&'a kopis::arithmetic::ring_arith::RingElem, kopis::arithmetic::ring_arith::RingElem> for &'a kopis::arithmetic::ring_arith::RingElem}::sub]: loop 0:
    Source: 'src/arithmetic/ring_arith.rs', lines 224:8-226:9
    Visibility: public -/
@[rust_loop]
def SharedARingElem.Insts.CoreOpsArithSubSharedARingElemRingElem.sub_loop
  (iter : core.ops.range.Range Std.Usize)
  (self : arithmetic.ring_arith.RingElem)
  (other : arithmetic.ring_arith.RingElem)
  (ret : arithmetic.ring_arith.RingElem) :
  Result arithmetic.ring_arith.RingElem
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok ret
  | some i =>
    let i1 ← Array.index_usize self i
    let i2 ← Array.index_usize other i
    let i3 ← lift (core.num.U16.wrapping_sub i1 i2)
    let a ← Array.update ret i i3
    SharedARingElem.Insts.CoreOpsArithSubSharedARingElemRingElem.sub_loop iter1
      self other a
partial_fixpoint

/-- [kopis::arithmetic::ring_arith::{impl core::ops::arith::Sub<&'a kopis::arithmetic::ring_arith::RingElem, kopis::arithmetic::ring_arith::RingElem> for &'a kopis::arithmetic::ring_arith::RingElem}::sub]:
    Source: 'src/arithmetic/ring_arith.rs', lines 222:4-228:5
    Visibility: public -/
def SharedARingElem.Insts.CoreOpsArithSubSharedARingElemRingElem.sub
  (self : arithmetic.ring_arith.RingElem)
  (other : arithmetic.ring_arith.RingElem) :
  Result arithmetic.ring_arith.RingElem
  := do
  let ret ← arithmetic.ring_arith.RingElem.Insts.CoreDefaultDefault.default
  SharedARingElem.Insts.CoreOpsArithSubSharedARingElemRingElem.sub_loop
    { start := 0#usize, «end» := consts.RING_DEG } self other ret

/-- Trait implementation: [kopis::arithmetic::ring_arith::{impl core::ops::arith::Sub<&'a kopis::arithmetic::ring_arith::RingElem, kopis::arithmetic::ring_arith::RingElem> for &'a kopis::arithmetic::ring_arith::RingElem}]
    Source: 'src/arithmetic/ring_arith.rs', lines 219:0-229:1 -/
@[reducible]
def SharedARingElem.Insts.CoreOpsArithSubSharedARingElemRingElem :
  core.ops.arith.Sub arithmetic.ring_arith.RingElem
  arithmetic.ring_arith.RingElem arithmetic.ring_arith.RingElem := {
  sub := SharedARingElem.Insts.CoreOpsArithSubSharedARingElemRingElem.sub
}

/-- [kopis::consts::KOPIS512_L]
    Source: 'src/consts.rs', lines 16:0-16:39 -/
@[global_simps, irreducible] def consts.KOPIS512_L : Std.Usize := 2#usize

/-- [kopis::consts::KOPIS512_T]
    Source: 'src/consts.rs', lines 17:0-17:39 -/
@[global_simps, irreducible] def consts.KOPIS512_T : Std.Usize := 3#usize

/-- [kopis::consts::KOPIS512_MU]
    Source: 'src/consts.rs', lines 18:0-18:41 -/
@[global_simps, irreducible] def consts.KOPIS512_MU : Std.Usize := 10#usize

/-- [kopis::consts::KOPIS768_L]
    Source: 'src/consts.rs', lines 20:0-20:39 -/
@[global_simps, irreducible] def consts.KOPIS768_L : Std.Usize := 3#usize

/-- [kopis::consts::KOPIS768_T]
    Source: 'src/consts.rs', lines 21:0-21:39 -/
@[global_simps, irreducible] def consts.KOPIS768_T : Std.Usize := 4#usize

/-- [kopis::consts::KOPIS768_MU]
    Source: 'src/consts.rs', lines 22:0-22:40 -/
@[global_simps, irreducible] def consts.KOPIS768_MU : Std.Usize := 8#usize

/-- [kopis::consts::KOPIS1024_L]
    Source: 'src/consts.rs', lines 24:0-24:40 -/
@[global_simps, irreducible] def consts.KOPIS1024_L : Std.Usize := 4#usize

/-- [kopis::consts::KOPIS1024_T]
    Source: 'src/consts.rs', lines 25:0-25:40 -/
@[global_simps, irreducible] def consts.KOPIS1024_T : Std.Usize := 6#usize

/-- [kopis::consts::KOPIS1024_MU]
    Source: 'src/consts.rs', lines 26:0-26:41 -/
@[global_simps, irreducible] def consts.KOPIS1024_MU : Std.Usize := 6#usize

/-- [kopis::consts::MAX_MU]
    Source: 'src/consts.rs', lines 31:0-31:36 -/
@[global_simps, irreducible] def consts.MAX_MU : Std.Usize := 10#usize

/-- [kopis::consts::MAX_T]
    Source: 'src/consts.rs', lines 37:0-37:34 -/
@[global_simps, irreducible] def consts.MAX_T : Std.Usize := 6#usize

/-- [kopis::consts::DOMSEP_KGEXPAND]
    Source: 'src/consts.rs', lines 40:0-40:44 -/
@[global_simps, irreducible] def consts.DOMSEP_KGEXPAND : Std.U8 := 1#u8

/-- [kopis::consts::DOMSEP_GENMAT]
    Source: 'src/consts.rs', lines 41:0-41:42 -/
@[global_simps, irreducible] def consts.DOMSEP_GENMAT : Std.U8 := 2#u8

/-- [kopis::consts::DOMSEP_GENSEC]
    Source: 'src/consts.rs', lines 42:0-42:42 -/
@[global_simps, irreducible] def consts.DOMSEP_GENSEC : Std.U8 := 3#u8

/-- [kopis::consts::DOMSEP_PKHASH]
    Source: 'src/consts.rs', lines 43:0-43:42 -/
@[global_simps, irreducible] def consts.DOMSEP_PKHASH : Std.U8 := 4#u8

/-- [kopis::consts::DOMSEP_FO]
    Source: 'src/consts.rs', lines 44:0-44:38 -/
@[global_simps, irreducible] def consts.DOMSEP_FO : Std.U8 := 5#u8

/-- [kopis::consts::DOMSEP_NOREJECT]
    Source: 'src/consts.rs', lines 45:0-45:44 -/
@[global_simps, irreducible] def consts.DOMSEP_NOREJECT : Std.U8 := 6#u8

/-- [kopis::impls::SharedSecret]
    Source: 'src/impls.rs', lines 15:0-15:34
    Visibility: public -/
@[reducible]
def impls.SharedSecret := Array Std.U8 32#usize

/-- [kopis::impls::{impl zeroize::Zeroize for kopis::impls::SharedSecret}::zeroize]:
    Source: 'src/impls.rs', lines 14:9-14:16
    Visibility: public -/
def impls.SharedSecret.Insts.ZeroizeZeroize.zeroize
  (self : impls.SharedSecret) : Result impls.SharedSecret := do
  let __zeroize_field_0 ←
    Array.Insts.ZeroizeZeroize.zeroize (zeroize.Zeroize.Blanket
      U8.Insts.ZeroizeDefaultIsZeroes) self
  ok __zeroize_field_0

/-- Trait implementation: [kopis::impls::{impl zeroize::Zeroize for kopis::impls::SharedSecret}]
    Source: 'src/impls.rs', lines 14:9-14:16 -/
@[reducible]
def impls.SharedSecret.Insts.ZeroizeZeroize : zeroize.Zeroize
  impls.SharedSecret := {
  zeroize := impls.SharedSecret.Insts.ZeroizeZeroize.zeroize
}

/-- [kopis::impls::{impl core::ops::drop::Drop for kopis::impls::SharedSecret}::drop]:
    Source: 'src/impls.rs', lines 14:18-14:31
    Visibility: public -/
def impls.SharedSecret.Insts.CoreOpsDropDrop.drop
  (self : impls.SharedSecret) : Result impls.SharedSecret := do
  let __zeroize_field_0 ←
    zeroize.__internal.AssertZeroize.Blanket.zeroize_or_on_drop
      (Array.Insts.ZeroizeZeroize 32#usize (zeroize.Zeroize.Blanket
      U8.Insts.ZeroizeDefaultIsZeroes)) self
  ok __zeroize_field_0

/-- Trait implementation: [kopis::impls::{impl core::ops::drop::Drop for kopis::impls::SharedSecret}]
    Source: 'src/impls.rs', lines 14:18-14:31 -/
@[reducible]
def impls.SharedSecret.Insts.CoreOpsDropDrop : core.ops.drop.Drop
  impls.SharedSecret := {
  drop := impls.SharedSecret.Insts.CoreOpsDropDrop.drop
}

/-- Trait implementation: [kopis::impls::{impl zeroize::ZeroizeOnDrop for kopis::impls::SharedSecret}]
    Source: 'src/impls.rs', lines 14:18-14:31 -/
@[reducible]
def impls.SharedSecret.Insts.ZeroizeZeroizeOnDrop : zeroize.ZeroizeOnDrop
  impls.SharedSecret := {
}

/-- [kopis::impls::{kopis::impls::SharedSecret}::as_bytes]:
    Source: 'src/impls.rs', lines 20:4-22:5
    Visibility: public -/
def impls.SharedSecret.as_bytes
  (self : impls.SharedSecret) : Result (Array Std.U8 32#usize) := do
  ok self

/-- [kopis::pke::PkePublicKey]
    Source: 'src/pke.rs', lines 38:0-53:1
    Visibility: public -/
structure pke.PkePublicKey (L : Std.Usize) where
  matrix_seed : Array Std.U8 32#usize
  mat_a_ntt : arithmetic.ntt.NttMatrix L L
  vec_bytes : Array (Array Std.U8 320#usize) L
  vec_ntt : arithmetic.ntt.NttMatrix L 1#usize

/-- [kopis::pke::PkeSecretKey]
    Source: 'src/pke.rs', lines 29:0-29:64 -/
@[reducible]
def pke.PkeSecretKey (L : Std.Usize) := arithmetic.ntt.NttMatrix L 1#usize

/-- [kopis::kem::KemSecretKey]
    Source: 'src/kem.rs', lines 55:0-68:1
    Visibility: public -/
structure kem.KemSecretKey (L : Std.Usize) where
  seed : Array Std.U8 32#usize
  z : Array Std.U8 32#usize
  pke_sk : pke.PkeSecretKey L
  pke_pk : pke.PkePublicKey L
  hash_pke_pk : Array Std.U8 32#usize

/-- [kopis::impls::kopis512::Kopis512SecretKey]
    Source: 'src/impls.rs', lines 44:12-44:65
    Visibility: public -/
@[reducible]
def impls.kopis512.Kopis512SecretKey := kem.KemSecretKey 2#usize

/-- Trait implementation: [kopis::kem::{impl zeroize::ZeroizeOnDrop for kopis::kem::KemSecretKey<L>}]
    Source: 'src/kem.rs', lines 54:9-54:22 -/
@[reducible]
def kem.KemSecretKey.Insts.ZeroizeZeroizeOnDrop (L : Std.Usize) :
  zeroize.ZeroizeOnDrop (kem.KemSecretKey L) := {
}

/-- [kopis::impls::kopis512::{impl core::ops::drop::Drop for kopis::impls::kopis512::Kopis512SecretKey}::drop]:
    Source: 'src/impls.rs', lines 43:21-43:34
    Visibility: public -/
def impls.kopis512.Kopis512SecretKey.Insts.CoreOpsDropDrop.drop
  (self : impls.kopis512.Kopis512SecretKey) :
  Result impls.kopis512.Kopis512SecretKey
  := do
  Shared0Mut1T.Insts.Zeroize__internalAssertZeroizeOnDrop.zeroize_or_on_drop
    (kem.KemSecretKey.Insts.ZeroizeZeroizeOnDrop 2#usize) self
  ok self

/-- Trait implementation: [kopis::impls::kopis512::{impl core::ops::drop::Drop for kopis::impls::kopis512::Kopis512SecretKey}]
    Source: 'src/impls.rs', lines 43:21-43:34 -/
@[reducible]
def impls.kopis512.Kopis512SecretKey.Insts.CoreOpsDropDrop : core.ops.drop.Drop
  impls.kopis512.Kopis512SecretKey := {
  drop := impls.kopis512.Kopis512SecretKey.Insts.CoreOpsDropDrop.drop
}

/-- Trait implementation: [kopis::impls::kopis512::{impl zeroize::ZeroizeOnDrop for kopis::impls::kopis512::Kopis512SecretKey}]
    Source: 'src/impls.rs', lines 43:21-43:34 -/
@[reducible]
def impls.kopis512.Kopis512SecretKey.Insts.ZeroizeZeroizeOnDrop :
  zeroize.ZeroizeOnDrop impls.kopis512.Kopis512SecretKey := {
}

/-- [kopis::impls::kopis768::Kopis768SecretKey]
    Source: 'src/impls.rs', lines 44:12-44:65
    Visibility: public -/
@[reducible]
def impls.kopis768.Kopis768SecretKey := kem.KemSecretKey 3#usize

/-- [kopis::impls::kopis768::{impl core::ops::drop::Drop for kopis::impls::kopis768::Kopis768SecretKey}::drop]:
    Source: 'src/impls.rs', lines 43:21-43:34
    Visibility: public -/
def impls.kopis768.Kopis768SecretKey.Insts.CoreOpsDropDrop.drop
  (self : impls.kopis768.Kopis768SecretKey) :
  Result impls.kopis768.Kopis768SecretKey
  := do
  Shared0Mut1T.Insts.Zeroize__internalAssertZeroizeOnDrop.zeroize_or_on_drop
    (kem.KemSecretKey.Insts.ZeroizeZeroizeOnDrop 3#usize) self
  ok self

/-- Trait implementation: [kopis::impls::kopis768::{impl core::ops::drop::Drop for kopis::impls::kopis768::Kopis768SecretKey}]
    Source: 'src/impls.rs', lines 43:21-43:34 -/
@[reducible]
def impls.kopis768.Kopis768SecretKey.Insts.CoreOpsDropDrop : core.ops.drop.Drop
  impls.kopis768.Kopis768SecretKey := {
  drop := impls.kopis768.Kopis768SecretKey.Insts.CoreOpsDropDrop.drop
}

/-- Trait implementation: [kopis::impls::kopis768::{impl zeroize::ZeroizeOnDrop for kopis::impls::kopis768::Kopis768SecretKey}]
    Source: 'src/impls.rs', lines 43:21-43:34 -/
@[reducible]
def impls.kopis768.Kopis768SecretKey.Insts.ZeroizeZeroizeOnDrop :
  zeroize.ZeroizeOnDrop impls.kopis768.Kopis768SecretKey := {
}

/-- [kopis::impls::kopis1024::Kopis1024SecretKey]
    Source: 'src/impls.rs', lines 44:12-44:65
    Visibility: public -/
@[reducible]
def impls.kopis1024.Kopis1024SecretKey := kem.KemSecretKey 4#usize

/-- [kopis::impls::kopis1024::{impl core::ops::drop::Drop for kopis::impls::kopis1024::Kopis1024SecretKey}::drop]:
    Source: 'src/impls.rs', lines 43:21-43:34
    Visibility: public -/
def impls.kopis1024.Kopis1024SecretKey.Insts.CoreOpsDropDrop.drop
  (self : impls.kopis1024.Kopis1024SecretKey) :
  Result impls.kopis1024.Kopis1024SecretKey
  := do
  Shared0Mut1T.Insts.Zeroize__internalAssertZeroizeOnDrop.zeroize_or_on_drop
    (kem.KemSecretKey.Insts.ZeroizeZeroizeOnDrop 4#usize) self
  ok self

/-- Trait implementation: [kopis::impls::kopis1024::{impl core::ops::drop::Drop for kopis::impls::kopis1024::Kopis1024SecretKey}]
    Source: 'src/impls.rs', lines 43:21-43:34 -/
@[reducible]
def impls.kopis1024.Kopis1024SecretKey.Insts.CoreOpsDropDrop :
  core.ops.drop.Drop impls.kopis1024.Kopis1024SecretKey := {
  drop := impls.kopis1024.Kopis1024SecretKey.Insts.CoreOpsDropDrop.drop
}

/-- Trait implementation: [kopis::impls::kopis1024::{impl zeroize::ZeroizeOnDrop for kopis::impls::kopis1024::Kopis1024SecretKey}]
    Source: 'src/impls.rs', lines 43:21-43:34 -/
@[reducible]
def impls.kopis1024.Kopis1024SecretKey.Insts.ZeroizeZeroizeOnDrop :
  zeroize.ZeroizeOnDrop impls.kopis1024.Kopis1024SecretKey := {
}

/-- [kopis::kem::KemPublicKey]
    Source: 'src/kem.rs', lines 16:0-21:1
    Visibility: public -/
structure kem.KemPublicKey (L : Std.Usize) where
  pke_pk : pke.PkePublicKey L
  hash_pke_pk : Array Std.U8 32#usize

/-- [kopis::impls::kopis512::Kopis512PublicKey]
    Source: 'src/impls.rs', lines 47:12-47:64
    Visibility: public -/
@[reducible]
def impls.kopis512.Kopis512PublicKey := kem.KemPublicKey 2#usize

/-- [kopis::impls::kopis768::Kopis768PublicKey]
    Source: 'src/impls.rs', lines 47:12-47:64
    Visibility: public -/
@[reducible]
def impls.kopis768.Kopis768PublicKey := kem.KemPublicKey 3#usize

/-- [kopis::impls::kopis1024::Kopis1024PublicKey]
    Source: 'src/impls.rs', lines 47:12-47:64
    Visibility: public -/
@[reducible]
def impls.kopis1024.Kopis1024PublicKey := kem.KemPublicKey 4#usize

/-- [kopis::pke::ciphertext_len]:
    Source: 'src/pke.rs', lines 128:0-131:1
    Visibility: public -/
def pke.ciphertext_len (L : Std.Usize) (T : Std.Usize) : Result Std.Usize := do
  let i ← L * consts.MODULUS_P_BITS
  let i1 ← i * consts.RING_DEG
  let i2 ← i1 / 8#usize
  let i3 ← T * consts.RING_DEG
  let i4 ← i3 / 8#usize
  i2 + i4

/-- [kopis::impls::kopis512::KOPIS512_CIPHERTEXT_LEN]
    Source: 'src/impls.rs', lines 50:12-51:69
    Visibility: public -/
@[global_simps, irreducible]
def impls.kopis512.KOPIS512_CIPHERTEXT_LEN : Result Std.Usize :=
  pke.ciphertext_len 2#usize 3#usize

/-- [kopis::impls::kopis768::KOPIS768_CIPHERTEXT_LEN]
    Source: 'src/impls.rs', lines 50:12-51:69
    Visibility: public -/
@[global_simps, irreducible]
def impls.kopis768.KOPIS768_CIPHERTEXT_LEN : Result Std.Usize :=
  pke.ciphertext_len 3#usize 4#usize

/-- [kopis::impls::kopis1024::KOPIS1024_CIPHERTEXT_LEN]
    Source: 'src/impls.rs', lines 50:12-51:69
    Visibility: public -/
@[global_simps, irreducible]
def impls.kopis1024.KOPIS1024_CIPHERTEXT_LEN : Result Std.Usize :=
  pke.ciphertext_len 4#usize 6#usize

/-- [kopis::sample::gen_matrix_from_seed]: loop 1:
    Source: 'src/sample.rs', lines 150:8-158:9 -/
@[rust_loop]
def sample.gen_matrix_from_seed_loop0_loop0
  {L : Std.Usize} (iter : core.ops.range.Range Std.Usize)
  (seed : Array Std.U8 32#usize) (mat : arithmetic.matrix_arith.Matrix L L)
  (buf : Array Std.U8 416#usize) (i : Std.Usize) :
  Result ((arithmetic.matrix_arith.Matrix L L) × (Array Std.U8 416#usize))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (mat, buf)
  | some j =>
    let hasher ←
      turboshake.TurboShake.Insts.CoreDefaultDefault.default 168#usize 2#u8
    let s ← lift (Array.to_slice seed)
    let hasher1 ← turboshake.TurboShake.Insts.DigestUpdate.update hasher s
    let i1 ← lift (UScalar.cast .U8 i)
    let s1 ← lift (Array.to_slice (Array.make 1#usize [ i1 ]))
    let hasher2 ← turboshake.TurboShake.Insts.DigestUpdate.update hasher1 s1
    let i2 ← lift (UScalar.cast .U8 j)
    let s2 ← lift (Array.to_slice (Array.make 1#usize [ i2 ]))
    let hasher3 ← turboshake.TurboShake.Insts.DigestUpdate.update hasher2 s2
    let reader ←
      turboshake.TurboShake.Insts.DigestExtendableOutputTurboShakeReader.finalize_xof
        hasher3
    let (s3, to_slice_mut_back) ← lift (Array.to_slice_mut buf)
    let (_, s4) ←
      turboshake.TurboShakeReader.Insts.DigestXofReader.read reader s3
    let buf1 := to_slice_mut_back s4
    let s5 ← lift (Array.to_slice buf1)
    let re ←
      arithmetic.ring_arith.RingElem.deserialize s5 consts.MODULUS_Q_BITS
    let (a, index_mut_back) ← Array.index_mut_usize mat i
    let a1 ← Array.update a j re
    let a2 := index_mut_back a1
    sample.gen_matrix_from_seed_loop0_loop0 iter1 seed a2 buf1 i
partial_fixpoint

/-- [kopis::sample::gen_matrix_from_seed]: loop 0:
    Source: 'src/sample.rs', lines 149:4-159:5 -/
@[rust_loop]
def sample.gen_matrix_from_seed_loop0
  {L : Std.Usize} (iter : core.ops.range.Range Std.Usize)
  (seed : Array Std.U8 32#usize) (mat : arithmetic.matrix_arith.Matrix L L)
  (buf : Array Std.U8 416#usize) :
  Result (arithmetic.matrix_arith.Matrix L L)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok mat
  | some i =>
    let (mat1, buf1) ←
      sample.gen_matrix_from_seed_loop0_loop0
        { start := 0#usize, «end» := L } seed mat buf i
    sample.gen_matrix_from_seed_loop0 iter1 seed mat1 buf1
partial_fixpoint

/-- [kopis::sample::gen_matrix_from_seed]:
    Source: 'src/sample.rs', lines 134:0-162:1 -/
def sample.gen_matrix_from_seed
  (L : Std.Usize) (seed : Array Std.U8 32#usize) :
  Result (arithmetic.matrix_arith.Matrix L L)
  := do
  let mat ←
    arithmetic.matrix_arith.Matrix.Insts.CoreDefaultDefault.default L L
  let buf := Array.repeat 416#usize 0#u8
  sample.gen_matrix_from_seed_loop0 { start := 0#usize, «end» := L } seed mat
    buf

/-- [kopis::sample::cbd_diff]:
    Source: 'src/sample.rs', lines 18:0-22:1 -/
def sample.cbd_diff
  (raw1 : Std.U32) (half : Std.U32) (mask : Std.U32) : Result Std.U16 := do
  let i ← lift (raw1 &&& mask)
  let i1 ← core.num.U32.count_ones i
  let a ← lift (UScalar.cast .U16 i1)
  let i2 ← raw1 >>> half
  let i3 ← lift (i2 &&& mask)
  let i4 ← core.num.U32.count_ones i3
  let b ← lift (UScalar.cast .U16 i4)
  ok (core.num.U16.wrapping_sub a b)

/-- [kopis::sample::cbd]: loop 0:
    Source: 'src/sample.rs', lines 34:8-38:9 -/
@[rust_loop]
def sample.cbd_loop0
  (iter : core.iter.adapters.enumerate.Enumerate (core.slice.iter.Iter Std.U8))
  (out : arithmetic.ring_arith.RingElem) :
  Result arithmetic.ring_arith.RingElem
  := do
  let (o, iter1) ←
    core.iter.adapters.enumerate.IteratorEnumerate.next
      (core.iter.traits.iterator.IteratorSliceIter Std.U8) iter
  match o with
  | none => ok out
  | some p =>
    let (i, byte) := p
    let i1 ← lift (byte &&& 15#u8)
    let i2 ← core.num.U8.count_ones i1
    let a ← lift (UScalar.cast .U16 i2)
    let i3 ← byte >>> 4#i32
    let i4 ← core.num.U8.count_ones i3
    let b ← lift (UScalar.cast .U16 i4)
    let i5 ← lift (core.num.U16.wrapping_sub a b)
    let a1 ← Array.update out i i5
    sample.cbd_loop0 iter1 a1
partial_fixpoint

/-- [kopis::sample::cbd]: loop 1:
    Source: 'src/sample.rs', lines 43:8-55:9 -/
@[rust_loop]
def sample.cbd_loop1
  (iter : core.ops.range.Range Std.Usize) (buf : Slice Std.U8)
  (out : arithmetic.ring_arith.RingElem) :
  Result arithmetic.ring_arith.RingElem
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok out
  | some g =>
    let i ← 5#usize * g
    let i1 ← Slice.index_usize buf i
    let b0 ← lift (UScalar.cast .U32 i1)
    let i2 ← i + 1#usize
    let i3 ← Slice.index_usize buf i2
    let b1 ← lift (UScalar.cast .U32 i3)
    let i4 ← i + 2#usize
    let i5 ← Slice.index_usize buf i4
    let b2 ← lift (UScalar.cast .U32 i5)
    let i6 ← i + 3#usize
    let i7 ← Slice.index_usize buf i6
    let b3 ← lift (UScalar.cast .U32 i7)
    let i8 ← i + 4#usize
    let i9 ← Slice.index_usize buf i8
    let b4 ← lift (UScalar.cast .U32 i9)
    let o1 ← 4#usize * g
    let i10 ← b1 <<< 8#i32
    let i11 ← lift (b0 ||| i10)
    let i12 ← sample.cbd_diff i11 5#u32 31#u32
    let a ← Array.update out o1 i12
    let i13 ← b2 <<< 8#i32
    let i14 ← lift (b1 ||| i13)
    let i15 ← i14 >>> 2#i32
    let i16 ← sample.cbd_diff i15 5#u32 31#u32
    let i17 ← o1 + 1#usize
    let a1 ← Array.update a i17 i16
    let i18 ← b3 <<< 8#i32
    let i19 ← lift (b2 ||| i18)
    let i20 ← i19 >>> 4#i32
    let i21 ← sample.cbd_diff i20 5#u32 31#u32
    let i22 ← o1 + 2#usize
    let a2 ← Array.update a1 i22 i21
    let i23 ← b4 <<< 8#i32
    let i24 ← lift (b3 ||| i23)
    let i25 ← i24 >>> 6#i32
    let i26 ← sample.cbd_diff i25 5#u32 31#u32
    let i27 ← o1 + 3#usize
    let a3 ← Array.update a2 i27 i26
    sample.cbd_loop1 iter1 buf a3
partial_fixpoint

/-- [kopis::sample::cbd]: loop 2:
    Source: 'src/sample.rs', lines 59:8-69:9 -/
@[rust_loop]
def sample.cbd_loop2
  (iter : core.ops.range.Range Std.Usize) (buf : Slice Std.U8)
  (out : arithmetic.ring_arith.RingElem) :
  Result arithmetic.ring_arith.RingElem
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok out
  | some g =>
    let i ← 3#usize * g
    let i1 ← Slice.index_usize buf i
    let b0 ← lift (UScalar.cast .U32 i1)
    let i2 ← i + 1#usize
    let i3 ← Slice.index_usize buf i2
    let b1 ← lift (UScalar.cast .U32 i3)
    let i4 ← i + 2#usize
    let i5 ← Slice.index_usize buf i4
    let b2 ← lift (UScalar.cast .U32 i5)
    let o1 ← 4#usize * g
    let i6 ← sample.cbd_diff b0 3#u32 7#u32
    let a ← Array.update out o1 i6
    let i7 ← b1 <<< 8#i32
    let i8 ← lift (b0 ||| i7)
    let i9 ← i8 >>> 6#i32
    let i10 ← sample.cbd_diff i9 3#u32 7#u32
    let i11 ← o1 + 1#usize
    let a1 ← Array.update a i11 i10
    let i12 ← b2 <<< 8#i32
    let i13 ← lift (b1 ||| i12)
    let i14 ← i13 >>> 4#i32
    let i15 ← sample.cbd_diff i14 3#u32 7#u32
    let i16 ← o1 + 2#usize
    let a2 ← Array.update a1 i16 i15
    let i17 ← b2 >>> 2#i32
    let i18 ← sample.cbd_diff i17 3#u32 7#u32
    let i19 ← o1 + 3#usize
    let a3 ← Array.update a2 i19 i18
    sample.cbd_loop2 iter1 buf a3
partial_fixpoint

/-- [kopis::sample::cbd]: loop 3:
    Source: 'src/sample.rs', lines 77:8-95:9 -/
@[rust_loop]
def sample.cbd_loop3
  (MU : Std.Usize) (iter : core.slice.iter.IterMut Std.U16)
  (back : core.slice.iter.IterMut Std.U16 → core.slice.iter.IterMut Std.U16)
  (buf : Slice Std.U8) (half : Std.Usize) (mask : Std.U32)
  (bit_pos : Std.Usize) :
  Result (core.slice.iter.IterMut Std.U16)
  := do
  let (o, iter1, next_back) ← core.slice.iter.IteratorIterMut.next iter
  match o with
  | none => ok (let im := next_back iter1 none
                back im)
  | some _ =>
    let byte_idx ← bit_pos / 8#usize
    let bit_in_byte ← bit_pos % 8#usize
    let i ← Slice.index_usize buf byte_idx
    let raw1 ← lift (UScalar.cast .U32 i)
    let i1 ← byte_idx + 1#usize
    let i2 := Slice.len buf
    let (raw2, back1) ←
      if i1 < i2
      then
        do
        let i3 ← Slice.index_usize buf i1
        let i4 ← lift (UScalar.cast .U32 i3)
        let i5 ← i4 <<< 8#i32
        let raw3 ← lift (raw1 ||| i5)
        ok (raw3, fun i6 im => next_back im (some i6))
      else ok (raw1, fun i3 im => next_back im (some i3))
    let i3 ← byte_idx + 2#usize
    let i4 := Slice.len buf
    let raw3 ←
      if i3 < i4
      then
        do
        let i5 ← Slice.index_usize buf i3
        let i6 ← lift (UScalar.cast .U32 i5)
        let i7 ← i6 <<< 16#i32
        ok (raw2 ||| i7)
      else ok raw2
    let raw4 ← raw3 >>> bit_in_byte
    let i5 ← lift (raw4 &&& mask)
    let i6 ← core.num.U32.count_ones i5
    let a ← lift (UScalar.cast .U16 i6)
    let i7 ← raw4 >>> half
    let i8 ← lift (i7 &&& mask)
    let i9 ← core.num.U32.count_ones i8
    let b ← lift (UScalar.cast .U16 i9)
    let coeff ← lift (core.num.U16.wrapping_sub a b)
    let bit_pos1 ← bit_pos + MU
    sample.cbd_loop3 MU iter1 (fun im => let im1 := back1 coeff im
                                         back im1) buf half mask bit_pos1
partial_fixpoint

/-- [kopis::sample::cbd]:
    Source: 'src/sample.rs', lines 25:0-97:1 -/
def sample.cbd
  (MU : Std.Usize) (buf : Slice Std.U8) (out : arithmetic.ring_arith.RingElem)
  :
  Result arithmetic.ring_arith.RingElem
  := do
  let left_val := Slice.len buf
  let _ ← consts.RING_DEG * MU
  let i ← lift (Std.Usize.wrapping_mul consts.RING_DEG MU)
  let right_val ← i / 8#usize
  massert (left_val = right_val)
  if MU = 8#usize
  then
    let i1 ← core.slice.Slice.iter buf
    let iter ←
      core.iter.traits.iterator.Iterator.enumerate.trait_default
        (core.iter.traits.iterator.IteratorSliceIter Std.U8) i1
    sample.cbd_loop0 iter out
  else
    if MU = 10#usize
    then
      let i1 ← consts.RING_DEG / 4#usize
      sample.cbd_loop1 { start := 0#usize, «end» := i1 } buf out
    else
      if MU = 6#usize
      then
        let i1 ← consts.RING_DEG / 4#usize
        sample.cbd_loop2 { start := 0#usize, «end» := i1 } buf out
      else
        let half ← MU / 2#usize
        let i1 ← 1#u32 <<< half
        let mask ← i1 - 1#u32
        let (s, to_slice_mut_back) ← lift (Array.to_slice_mut out)
        let (iter, iter_mut_back) ← core.slice.Slice.iter_mut s
        let back ←
          sample.cbd_loop3 MU iter (fun im => im) buf half mask 0#usize
        let s1 := iter_mut_back back
        let a := to_slice_mut_back s1
        ok a

/-- [kopis::sample::gen_secret_from_seed]: loop 0:
    Source: 'src/sample.rs', lines 119:4-126:5 -/
@[rust_loop]
def sample.gen_secret_from_seed_loop
  {L : Std.Usize} (MU : Std.Usize) (iter : core.ops.range.Range Std.Usize)
  (seed : Array Std.U8 32#usize)
  (secret : arithmetic.matrix_arith.Matrix L 1#usize) (buf : Slice Std.U8) :
  Result (arithmetic.matrix_arith.Matrix L 1#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok secret
  | some i =>
    let hasher ←
      turboshake.TurboShake.Insts.CoreDefaultDefault.default 136#usize 3#u8
    let s ← lift (Array.to_slice seed)
    let hasher1 ← turboshake.TurboShake.Insts.DigestUpdate.update hasher s
    let i1 ← lift (UScalar.cast .U8 i)
    let s1 ← lift (Array.to_slice (Array.make 1#usize [ i1 ]))
    let hasher2 ← turboshake.TurboShake.Insts.DigestUpdate.update hasher1 s1
    let reader ←
      turboshake.TurboShake.Insts.DigestExtendableOutputTurboShakeReader.finalize_xof
        hasher2
    let (_, buf1) ←
      turboshake.TurboShakeReader.Insts.DigestXofReader.read reader buf
    let (a, index_mut_back) ← Array.index_mut_usize secret i
    let (re, index_mut_back1) ← Array.index_mut_usize a 0#usize
    let re1 ← sample.cbd MU buf1 re
    let a1 := index_mut_back1 re1
    let a2 := index_mut_back a1
    sample.gen_secret_from_seed_loop MU iter1 seed a2 buf1
partial_fixpoint

/-- [kopis::sample::gen_secret_from_seed]:
    Source: 'src/sample.rs', lines 101:0-129:1 -/
def sample.gen_secret_from_seed
  (L : Std.Usize) (MU : Std.Usize) (seed : Array Std.U8 32#usize) :
  Result (arithmetic.matrix_arith.Matrix L 1#usize)
  := do
  let secret ←
    arithmetic.matrix_arith.Matrix.Insts.CoreDefaultDefault.default L 1#usize
  let backing_buf := Array.repeat 320#usize 0#u8
  let i ← consts.RING_DEG * MU
  let i1 ← i / 8#usize
  let (buf, _) ←
    core.array.Array.index_mut (core.ops.index.IndexMutSlice
      (core.slice.index.SliceIndexRangeToUsizeSlice Std.U8)) backing_buf
      { «end» := i1 }
  sample.gen_secret_from_seed_loop MU { start := 0#usize, «end» := L } seed
    secret buf

/-- [kopis::pke::{kopis::pke::PkePublicKey<L>}::SERIALIZED_LEN]
    Source: 'src/pke.rs', lines 56:4-56:77
    Visibility: public -/
@[global_simps, irreducible]
def pke.PkePublicKey.SERIALIZED_LEN (L : Std.Usize) : Result Std.Usize := do
  let i ← L * consts.MODULUS_P_BITS
  let i1 ← i * consts.RING_DEG
  let i2 ← i1 / 8#usize
  32#usize + i2

/-- [kopis::pke::PK_VEC_ELEM_BYTES]
    Source: 'src/pke.rs', lines 21:0-21:63 -/
@[global_simps, irreducible]
def pke.PK_VEC_ELEM_BYTES : Result Std.Usize := do
  let i ← consts.MODULUS_P_BITS * consts.RING_DEG
  i / 8#usize

/-- [kopis::pke::{kopis::pke::PkePublicKey<L>}::serialize]: loop 0:
    Source: 'src/pke.rs', lines 66:8-69:9 -/
@[rust_loop]
def pke.PkePublicKey.serialize_loop
  {L : Std.Usize} (iter : core.ops.range.Range Std.Usize)
  (self : pke.PkePublicKey L) (out_buf : Slice Std.U8) :
  Result ((pke.PkePublicKey L) × (Slice Std.U8))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (self, out_buf)
  | some i =>
    let i1 ← pke.PK_VEC_ELEM_BYTES
    let start ← i * i1
    let i2 ← start + i1
    let (s, index_mut_back) ←
      core.slice.index.Slice.index_mut
        (core.slice.index.SliceIndexRangeUsizeSlice Std.U8) out_buf
        { start, «end» := i2 }
    let a ← Array.index_usize self.vec_bytes i
    let s1 ← lift (Array.to_slice a)
    let s2 ← core.slice.Slice.copy_from_slice core.marker.CopyU8 s s1
    let out_buf1 := index_mut_back s2
    pke.PkePublicKey.serialize_loop iter1 self out_buf1
partial_fixpoint

/-- [kopis::pke::{kopis::pke::PkePublicKey<L>}::serialize]:
    Source: 'src/pke.rs', lines 61:4-71:5 -/
def pke.PkePublicKey.serialize
  {L : Std.Usize} (self : pke.PkePublicKey L) (out_buf : Slice Std.U8) :
  Result (Slice Std.U8)
  := do
  let left_val := Slice.len out_buf
  let right_val ← pke.PkePublicKey.SERIALIZED_LEN L
  massert (left_val = right_val)
  let (self1, out_buf1) ←
    pke.PkePublicKey.serialize_loop { start := 0#usize, «end» := L } self
      out_buf
  let i ← pke.PK_VEC_ELEM_BYTES
  let i1 ← L * i
  let (s, index_mut_back) ←
    core.slice.index.Slice.index_mut
      (core.slice.index.SliceIndexRangeFromUsizeSlice Std.U8) out_buf1
      { start := i1 }
  let s1 ← lift (Array.to_slice self1.matrix_seed)
  let s2 ← core.slice.Slice.copy_from_slice core.marker.CopyU8 s s1
  ok (index_mut_back s2)

/-- [kopis::turboshake256_hash]:
    Source: 'src/lib.rs', lines 38:0-48:1 -/
def turboshake256_hash
  (DS : Std.U8) (input0 : Slice Std.U8) (input1 : Slice Std.U8) :
  Result (Array Std.U8 32#usize)
  := do
  let hasher ←
    turboshake.TurboShake.Insts.CoreDefaultDefault.default 136#usize DS
  let hasher1 ← turboshake.TurboShake.Insts.DigestUpdate.update hasher input0
  let hasher2 ←
    turboshake.TurboShake.Insts.DigestUpdate.update hasher1 input1
  let out := Array.repeat 32#usize 0#u8
  let reader ←
    turboshake.TurboShake.Insts.DigestExtendableOutputTurboShakeReader.finalize_xof
      hasher2
  let (s, to_slice_mut_back) ← lift (Array.to_slice_mut out)
  let (_, s1) ←
    turboshake.TurboShakeReader.Insts.DigestXofReader.read reader s
  ok (to_slice_mut_back s1)

/-- [kopis::pke::{kopis::pke::PkePublicKey<L>}::hash]:
    Source: 'src/pke.rs', lines 105:4-111:5 -/
def pke.PkePublicKey.hash
  {L : Std.Usize} (self : pke.PkePublicKey L) :
  Result (Array Std.U8 32#usize)
  := do
  let buf := Array.repeat 1312#usize 0#u8
  let i ← pke.PkePublicKey.SERIALIZED_LEN L
  let (pk_slice, _) ←
    core.array.Array.index_mut (core.ops.index.IndexMutSlice
      (core.slice.index.SliceIndexRangeToUsizeSlice Std.U8)) buf
      { «end» := i }
  let pk_slice1 ← pke.PkePublicKey.serialize self pk_slice
  let s ← lift (Array.to_slice (Std.Array.empty Std.U8))
  turboshake256_hash 4#u8 pk_slice1 s

/-- [kopis::pke::{impl core::ops::drop::Drop for kopis::pke::PkeSecretKey<L>}::drop]:
    Source: 'src/pke.rs', lines 28:18-28:31
    Visibility: public -/
def pke.PkeSecretKey.Insts.CoreOpsDropDrop.drop
  {L : Std.Usize} (self : pke.PkeSecretKey L) :
  Result (pke.PkeSecretKey L)
  := do
  let __zeroize_field_0 ←
    zeroize.__internal.AssertZeroize.Blanket.zeroize_or_on_drop
      (arithmetic.ntt.NttMatrix.Insts.ZeroizeZeroize L 1#usize) self
  ok __zeroize_field_0

/-- [kopis::pke::H1_VAL]
    Source: 'src/pke.rs', lines 17:0-17:63 -/
@[global_simps, irreducible]
def pke.H1_VAL : Result Std.U16 := do
  let i ← consts.MODULUS_Q_BITS - consts.MODULUS_P_BITS
  let i1 ← i - 1#usize
  1#u16 <<< i1

/-- [kopis::pke::expand_decap_key]: loop 0:
    Source: 'src/pke.rs', lines 178:4-180:5 -/
@[rust_loop]
def pke.expand_decap_key_loop
  {L : Std.Usize} (iter : core.ops.range.Range Std.Usize)
  (b : arithmetic.matrix_arith.Matrix L 1#usize)
  (vec_bytes : Array (Array Std.U8 320#usize) L) :
  Result (Array (Array Std.U8 320#usize) L)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok vec_bytes
  | some i =>
    let a ← Array.index_usize b i
    let re ← Array.index_usize a 0#usize
    let (a1, index_mut_back) ← Array.index_mut_usize vec_bytes i
    let (s, to_slice_mut_back) ← lift (Array.to_slice_mut a1)
    let s1 ←
      arithmetic.ring_arith.RingElem.serialize re s consts.MODULUS_P_BITS
    let a2 := to_slice_mut_back s1
    let a3 := index_mut_back a2
    pke.expand_decap_key_loop iter1 b a3
partial_fixpoint

/-- [kopis::pke::expand_decap_key]:
    Source: 'src/pke.rs', lines 142:0-191:1 -/
def pke.expand_decap_key
  (L : Std.Usize) (MU : Std.Usize) (sk : Array Std.U8 32#usize) :
  Result ((pke.PkeSecretKey L) × (Array Std.U8 32#usize) × (pke.PkePublicKey
    L) × (Array Std.U8 32#usize))
  := do
  let mat_seed := Array.repeat 32#usize 0#u8
  let secret_seed := Array.repeat 32#usize 0#u8
  let z := Array.repeat 32#usize 0#u8
  let hasher ←
    turboshake.TurboShake.Insts.CoreDefaultDefault.default 136#usize 1#u8
  let s ← lift (Array.to_slice sk)
  let hasher1 ← turboshake.TurboShake.Insts.DigestUpdate.update hasher s
  let i ← lift (UScalar.cast .U8 L)
  let s1 ← lift (Array.to_slice (Array.make 1#usize [ i ]))
  let hasher2 ← turboshake.TurboShake.Insts.DigestUpdate.update hasher1 s1
  let xof ←
    turboshake.TurboShake.Insts.DigestExtendableOutputTurboShakeReader.finalize_xof
      hasher2
  let (s2, to_slice_mut_back) ← lift (Array.to_slice_mut mat_seed)
  let (xof1, s3) ←
    turboshake.TurboShakeReader.Insts.DigestXofReader.read xof s2
  let (s4, to_slice_mut_back1) ← lift (Array.to_slice_mut secret_seed)
  let (xof2, s5) ←
    turboshake.TurboShakeReader.Insts.DigestXofReader.read xof1 s4
  let (s6, to_slice_mut_back2) ← lift (Array.to_slice_mut z)
  let (_, s7) ←
    turboshake.TurboShakeReader.Insts.DigestXofReader.read xof2 s6
  let mat_seed1 := to_slice_mut_back s3
  let mat_a ← sample.gen_matrix_from_seed L mat_seed1
  let secret_seed1 := to_slice_mut_back1 s5
  let vec_s ← sample.gen_secret_from_seed L MU secret_seed1
  let mat_a_ntt ← arithmetic.ntt.NttMatrix.from_uniform_matrix mat_a
  let vec_s_ntt ← arithmetic.ntt.NttMatrix.from_secret_matrix vec_s
  let prod ← arithmetic.ntt.NttMatrix.mul_transpose mat_a_ntt vec_s_ntt
  let i1 ← pke.H1_VAL
  let prod1 ← arithmetic.matrix_arith.Matrix.wrapping_add_to_all prod i1
  let i2 ← consts.MODULUS_Q_BITS - consts.MODULUS_P_BITS
  let prod2 ← arithmetic.matrix_arith.Matrix.shift_right prod1 i2
  let vec_ntt ← arithmetic.ntt.NttMatrix.from_uniform_matrix prod2
  let a := Array.repeat 320#usize 0#u8
  let vec_bytes := Array.repeat L a
  let z1 := to_slice_mut_back2 s7
  let vec_bytes1 ←
    pke.expand_decap_key_loop { start := 0#usize, «end» := L } prod2
      vec_bytes
  let pkh ←
    pke.PkePublicKey.hash
      { matrix_seed := mat_seed1, mat_a_ntt, vec_bytes := vec_bytes1, vec_ntt }
  ok (vec_s_ntt, z1,
    { matrix_seed := mat_seed1, mat_a_ntt, vec_bytes := vec_bytes1, vec_ntt },
    pkh)

/-- [kopis::kem::{kopis::kem::KemSecretKey<L>}::expand_from_seed]:
    Source: 'src/kem.rs', lines 72:4-82:5
    Visibility: public -/
def kem.KemSecretKey.expand_from_seed
  (L : Std.Usize) (MU : Std.Usize) (seed : Array Std.U8 32#usize) :
  Result (kem.KemSecretKey L)
  := do
  let (pke_sk, z, pke_pk, hash_pke_pk) ← pke.expand_decap_key L MU seed
  ok { seed, z, pke_sk, pke_pk, hash_pke_pk }

/-- [kopis::pke::{impl zeroize::Zeroize for kopis::pke::PkeSecretKey<L>}::zeroize]:
    Source: 'src/pke.rs', lines 28:9-28:16
    Visibility: public -/
def pke.PkeSecretKey.Insts.ZeroizeZeroize.zeroize
  {L : Std.Usize} (self : pke.PkeSecretKey L) :
  Result (pke.PkeSecretKey L)
  := do
  let __zeroize_field_0 ←
    arithmetic.ntt.NttMatrix.Insts.ZeroizeZeroize.zeroize self
  ok __zeroize_field_0

/-- Trait implementation: [kopis::pke::{impl zeroize::Zeroize for kopis::pke::PkeSecretKey<L>}]
    Source: 'src/pke.rs', lines 28:9-28:16 -/
@[reducible]
def pke.PkeSecretKey.Insts.ZeroizeZeroize (L : Std.Usize) : zeroize.Zeroize
  (pke.PkeSecretKey L) := {
  zeroize := pke.PkeSecretKey.Insts.ZeroizeZeroize.zeroize
}

/-- [kopis::kem::{impl core::ops::drop::Drop for kopis::kem::KemSecretKey<L>}::drop]:
    Source: 'src/kem.rs', lines 54:9-54:22
    Visibility: public -/
def kem.KemSecretKey.Insts.CoreOpsDropDrop.drop
  {L : Std.Usize} (self : kem.KemSecretKey L) :
  Result (kem.KemSecretKey L)
  := do
  let seed ←
    zeroize.__internal.AssertZeroize.Blanket.zeroize_or_on_drop
      (Array.Insts.ZeroizeZeroize 32#usize (zeroize.Zeroize.Blanket
      U8.Insts.ZeroizeDefaultIsZeroes)) self.seed
  let z ←
    zeroize.__internal.AssertZeroize.Blanket.zeroize_or_on_drop
      (Array.Insts.ZeroizeZeroize 32#usize (zeroize.Zeroize.Blanket
      U8.Insts.ZeroizeDefaultIsZeroes)) self.z
  let pke_sk ←
    zeroize.__internal.AssertZeroize.Blanket.zeroize_or_on_drop
      (pke.PkeSecretKey.Insts.ZeroizeZeroize L) self.pke_sk
  ok { self with seed, z, pke_sk }

/-- [kopis::kem::{kopis::kem::KemSecretKey<L>}::generate]:
    Source: 'src/kem.rs', lines 85:4-92:5
    Visibility: public -/
def kem.KemSecretKey.generate
  {T2 : Type} (L : Std.Usize) (MU : Std.Usize) (rand_coreCryptoRngInst :
  rand_core.CryptoRng T2) (rng : T2) :
  Result ((kem.KemSecretKey L) × T2)
  := do
  let seed := Array.repeat 32#usize 0#u8
  let (s, to_slice_mut_back) ← lift (Array.to_slice_mut seed)
  let (rng1, s1) ← rand_coreCryptoRngInst.RngCoreInst.fill_bytes rng s
  let seed1 := to_slice_mut_back s1
  let out ← kem.KemSecretKey.expand_from_seed L MU seed1
  let _ ←
    Array.Insts.ZeroizeZeroize.zeroize (zeroize.Zeroize.Blanket
      U8.Insts.ZeroizeDefaultIsZeroes) seed1
  ok (out, rng1)

/-- [kopis::impls::kopis512::{kopis::impls::kopis512::Kopis512SecretKey}::generate]:
    Source: 'src/impls.rs', lines 61:16-63:17
    Visibility: public -/
def impls.kopis512.Kopis512SecretKey.generate
  {T0 : Type} (rand_coreCryptoRngInst : rand_core.CryptoRng T0) (rng : T0) :
  Result (impls.kopis512.Kopis512SecretKey × T0)
  := do
  let (ksk, rng1) ←
    kem.KemSecretKey.generate 2#usize 10#usize rand_coreCryptoRngInst rng
  ok (ksk, rng1)

/-- [kopis::impls::kopis768::{kopis::impls::kopis768::Kopis768SecretKey}::generate]:
    Source: 'src/impls.rs', lines 61:16-63:17
    Visibility: public -/
def impls.kopis768.Kopis768SecretKey.generate
  {T0 : Type} (rand_coreCryptoRngInst : rand_core.CryptoRng T0) (rng : T0) :
  Result (impls.kopis768.Kopis768SecretKey × T0)
  := do
  let (ksk, rng1) ←
    kem.KemSecretKey.generate 3#usize 8#usize rand_coreCryptoRngInst rng
  ok (ksk, rng1)

/-- [kopis::impls::kopis1024::{kopis::impls::kopis1024::Kopis1024SecretKey}::generate]:
    Source: 'src/impls.rs', lines 61:16-63:17
    Visibility: public -/
def impls.kopis1024.Kopis1024SecretKey.generate
  {T0 : Type} (rand_coreCryptoRngInst : rand_core.CryptoRng T0) (rng : T0) :
  Result (impls.kopis1024.Kopis1024SecretKey × T0)
  := do
  let (ksk, rng1) ←
    kem.KemSecretKey.generate 4#usize 6#usize rand_coreCryptoRngInst rng
  ok (ksk, rng1)

/-- [kopis::kem::{kopis::kem::KemSecretKey<L>}::seed]:
    Source: 'src/kem.rs', lines 95:4-97:5
    Visibility: public -/
def kem.KemSecretKey.impl.seed
  {L : Std.Usize} (self : kem.KemSecretKey L) :
  Result (Array Std.U8 32#usize)
  := do
  ok self.seed

/-- [kopis::impls::kopis512::{kopis::impls::kopis512::Kopis512SecretKey}::seed]:
    Source: 'src/impls.rs', lines 66:16-68:17
    Visibility: public -/
def impls.kopis512.Kopis512SecretKey.seed
  (self : impls.kopis512.Kopis512SecretKey) :
  Result (Array Std.U8 32#usize)
  := do
  kem.KemSecretKey.impl.seed self

/-- [kopis::impls::kopis768::{kopis::impls::kopis768::Kopis768SecretKey}::seed]:
    Source: 'src/impls.rs', lines 66:16-68:17
    Visibility: public -/
def impls.kopis768.Kopis768SecretKey.seed
  (self : impls.kopis768.Kopis768SecretKey) :
  Result (Array Std.U8 32#usize)
  := do
  kem.KemSecretKey.impl.seed self

/-- [kopis::impls::kopis1024::{kopis::impls::kopis1024::Kopis1024SecretKey}::seed]:
    Source: 'src/impls.rs', lines 66:16-68:17
    Visibility: public -/
def impls.kopis1024.Kopis1024SecretKey.seed
  (self : impls.kopis1024.Kopis1024SecretKey) :
  Result (Array Std.U8 32#usize)
  := do
  kem.KemSecretKey.impl.seed self

/-- [kopis::impls::kopis512::{kopis::impls::kopis512::Kopis512SecretKey}::expand_from_seed]:
    Source: 'src/impls.rs', lines 71:16-73:17
    Visibility: public -/
def impls.kopis512.Kopis512SecretKey.expand_from_seed
  (bytes : Array Std.U8 32#usize) :
  Result impls.kopis512.Kopis512SecretKey
  := do
  let ksk ← kem.KemSecretKey.expand_from_seed 2#usize 10#usize bytes
  ok ksk

/-- [kopis::impls::kopis768::{kopis::impls::kopis768::Kopis768SecretKey}::expand_from_seed]:
    Source: 'src/impls.rs', lines 71:16-73:17
    Visibility: public -/
def impls.kopis768.Kopis768SecretKey.expand_from_seed
  (bytes : Array Std.U8 32#usize) :
  Result impls.kopis768.Kopis768SecretKey
  := do
  let ksk ← kem.KemSecretKey.expand_from_seed 3#usize 8#usize bytes
  ok ksk

/-- [kopis::impls::kopis1024::{kopis::impls::kopis1024::Kopis1024SecretKey}::expand_from_seed]:
    Source: 'src/impls.rs', lines 71:16-73:17
    Visibility: public -/
def impls.kopis1024.Kopis1024SecretKey.expand_from_seed
  (bytes : Array Std.U8 32#usize) :
  Result impls.kopis1024.Kopis1024SecretKey
  := do
  let ksk ← kem.KemSecretKey.expand_from_seed 4#usize 6#usize bytes
  ok ksk

/-- [kopis::pke::{impl core::clone::Clone for kopis::pke::PkePublicKey<L>}::clone]:
    Source: 'src/pke.rs', lines 37:9-37:14
    Visibility: public -/
def pke.PkePublicKey.Insts.CoreCloneClone.clone
  {L : Std.Usize} (self : pke.PkePublicKey L) :
  Result (pke.PkePublicKey L)
  := do
  let a ← core.array.CloneArray.clone core.clone.CloneU8 self.matrix_seed
  let nm ← arithmetic.ntt.NttMatrix.Insts.CoreCloneClone.clone self.mat_a_ntt
  let a1 ←
    core.array.CloneArray.clone (core.clone.CloneArray 320#usize
      core.clone.CloneU8) self.vec_bytes
  let nm1 ← arithmetic.ntt.NttMatrix.Insts.CoreCloneClone.clone self.vec_ntt
  ok { matrix_seed := a, mat_a_ntt := nm, vec_bytes := a1, vec_ntt := nm1 }

/-- [kopis::kem::{kopis::kem::KemSecretKey<L>}::public_key]:
    Source: 'src/kem.rs', lines 99:4-104:5 -/
def kem.KemSecretKey.public_key
  {L : Std.Usize} (self : kem.KemSecretKey L) :
  Result (kem.KemPublicKey L)
  := do
  let ppk ← pke.PkePublicKey.Insts.CoreCloneClone.clone self.pke_pk
  ok { pke_pk := ppk, hash_pke_pk := self.hash_pke_pk }

/-- [kopis::impls::kopis512::{kopis::impls::kopis512::Kopis512SecretKey}::public_key]:
    Source: 'src/impls.rs', lines 76:16-78:17
    Visibility: public -/
def impls.kopis512.Kopis512SecretKey.public_key
  (self : impls.kopis512.Kopis512SecretKey) :
  Result impls.kopis512.Kopis512PublicKey
  := do
  let kpk ← kem.KemSecretKey.public_key self
  ok kpk

/-- [kopis::impls::kopis768::{kopis::impls::kopis768::Kopis768SecretKey}::public_key]:
    Source: 'src/impls.rs', lines 76:16-78:17
    Visibility: public -/
def impls.kopis768.Kopis768SecretKey.public_key
  (self : impls.kopis768.Kopis768SecretKey) :
  Result impls.kopis768.Kopis768PublicKey
  := do
  let kpk ← kem.KemSecretKey.public_key self
  ok kpk

/-- [kopis::impls::kopis1024::{kopis::impls::kopis1024::Kopis1024SecretKey}::public_key]:
    Source: 'src/impls.rs', lines 76:16-78:17
    Visibility: public -/
def impls.kopis1024.Kopis1024SecretKey.public_key
  (self : impls.kopis1024.Kopis1024SecretKey) :
  Result impls.kopis1024.Kopis1024PublicKey
  := do
  let kpk ← kem.KemSecretKey.public_key self
  ok kpk

/-- [kopis::kem::{kopis::kem::KemPublicKey<L>}::SERIALIZED_LEN]
    Source: 'src/kem.rs', lines 24:4-24:79 -/
@[global_simps, irreducible]
def kem.KemPublicKey.SERIALIZED_LEN (L : Std.Usize) : Result Std.Usize :=
  pke.PkePublicKey.SERIALIZED_LEN L

/-- [kopis::impls::kopis512::{kopis::impls::kopis512::Kopis512PublicKey}::SERIALIZED_LEN]
    Source: 'src/impls.rs', lines 83:16-83:95
    Visibility: public -/
@[global_simps, irreducible]
def impls.kopis512.Kopis512PublicKey.SERIALIZED_LEN : Result Std.Usize :=
  kem.KemPublicKey.SERIALIZED_LEN 2#usize

/-- [kopis::impls::kopis768::{kopis::impls::kopis768::Kopis768PublicKey}::SERIALIZED_LEN]
    Source: 'src/impls.rs', lines 83:16-83:95
    Visibility: public -/
@[global_simps, irreducible]
def impls.kopis768.Kopis768PublicKey.SERIALIZED_LEN : Result Std.Usize :=
  kem.KemPublicKey.SERIALIZED_LEN 3#usize

/-- [kopis::impls::kopis1024::{kopis::impls::kopis1024::Kopis1024PublicKey}::SERIALIZED_LEN]
    Source: 'src/impls.rs', lines 83:16-83:95
    Visibility: public -/
@[global_simps, irreducible]
def impls.kopis1024.Kopis1024PublicKey.SERIALIZED_LEN : Result Std.Usize :=
  kem.KemPublicKey.SERIALIZED_LEN 4#usize

/-- [kopis::kem::{kopis::kem::KemPublicKey<L>}::serialize]:
    Source: 'src/kem.rs', lines 27:4-29:5 -/
def kem.KemPublicKey.serialize
  {L : Std.Usize} (self : kem.KemPublicKey L) (out_buf : Slice Std.U8) :
  Result (Slice Std.U8)
  := do
  pke.PkePublicKey.serialize self.pke_pk out_buf

/-- [kopis::impls::kopis512::{kopis::impls::kopis512::Kopis512PublicKey}::serialize]:
    Source: 'src/impls.rs', lines 86:16-88:17
    Visibility: public -/
def impls.kopis512.Kopis512PublicKey.serialize
  (self : impls.kopis512.Kopis512PublicKey) (out_buf : Array Std.U8 672#usize)
  :
  Result (Array Std.U8 672#usize)
  := do
  let (s, to_slice_mut_back) ← lift (Array.to_slice_mut out_buf)
  let s1 ← kem.KemPublicKey.serialize self s
  ok (to_slice_mut_back s1)

/-- [kopis::impls::kopis768::{kopis::impls::kopis768::Kopis768PublicKey}::serialize]:
    Source: 'src/impls.rs', lines 86:16-88:17
    Visibility: public -/
def impls.kopis768.Kopis768PublicKey.serialize
  (self : impls.kopis768.Kopis768PublicKey) (out_buf : Array Std.U8 992#usize)
  :
  Result (Array Std.U8 992#usize)
  := do
  let (s, to_slice_mut_back) ← lift (Array.to_slice_mut out_buf)
  let s1 ← kem.KemPublicKey.serialize self s
  ok (to_slice_mut_back s1)

/-- [kopis::impls::kopis1024::{kopis::impls::kopis1024::Kopis1024PublicKey}::serialize]:
    Source: 'src/impls.rs', lines 86:16-88:17
    Visibility: public -/
def impls.kopis1024.Kopis1024PublicKey.serialize
  (self : impls.kopis1024.Kopis1024PublicKey)
  (out_buf : Array Std.U8 1312#usize) :
  Result (Array Std.U8 1312#usize)
  := do
  let (s, to_slice_mut_back) ← lift (Array.to_slice_mut out_buf)
  let s1 ← kem.KemPublicKey.serialize self s
  ok (to_slice_mut_back s1)

/-- [kopis::pke::{kopis::pke::PkePublicKey<L>}::from_bytes]: loop 0:
    Source: 'src/pke.rs', lines 89:8-92:9 -/
@[rust_loop]
def pke.PkePublicKey.from_bytes_loop
  {L : Std.Usize} (iter : core.ops.range.Range Std.Usize)
  (vec_slice : Slice Std.U8) (vec_bytes : Array (Array Std.U8 320#usize) L) :
  Result (Array (Array Std.U8 320#usize) L)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok vec_bytes
  | some i =>
    let i1 ← pke.PK_VEC_ELEM_BYTES
    let start ← i * i1
    let (a, index_mut_back) ← Array.index_mut_usize vec_bytes i
    let (s, to_slice_mut_back) ← lift (Array.to_slice_mut a)
    let i2 ← start + i1
    let s1 ←
      core.slice.index.Slice.index (core.slice.index.SliceIndexRangeUsizeSlice
        Std.U8) vec_slice { start, «end» := i2 }
    let s2 ← core.slice.Slice.copy_from_slice core.marker.CopyU8 s s1
    let a1 := to_slice_mut_back s2
    let a2 := index_mut_back a1
    pke.PkePublicKey.from_bytes_loop iter1 vec_slice a2
partial_fixpoint

/-- [kopis::pke::{kopis::pke::PkePublicKey<L>}::from_bytes]:
    Source: 'src/pke.rs', lines 75:4-102:5 -/
def pke.PkePublicKey.from_bytes
  (L : Std.Usize) (bytes : Slice Std.U8) : Result (pke.PkePublicKey L) := do
  let left_val := Slice.len bytes
  let right_val ← pke.PkePublicKey.SERIALIZED_LEN L
  massert (left_val = right_val)
  let i ← right_val - 32#usize
  let (vec_slice, seed) ← core.slice.Slice.split_at bytes i
  let r ←
    core.array.TryFromArrayCopySlice.try_from 32#usize core.marker.CopyU8 seed
  let matrix_seed ←
    core.result.Result.unwrap core.fmt.DebugTryFromSliceError r
  let vec ← arithmetic.matrix_arith.Matrix.deserialize_10 L 1#usize vec_slice
  let vec_ntt ← arithmetic.ntt.NttMatrix.from_uniform_matrix vec
  let a := Array.repeat 320#usize 0#u8
  let vec_bytes := Array.repeat L a
  let vec_bytes1 ←
    pke.PkePublicKey.from_bytes_loop { start := 0#usize, «end» := L }
      vec_slice vec_bytes
  let mat_a ← sample.gen_matrix_from_seed L matrix_seed
  let mat_a_ntt ← arithmetic.ntt.NttMatrix.from_uniform_matrix mat_a
  ok { matrix_seed, mat_a_ntt, vec_bytes := vec_bytes1, vec_ntt }

/-- [kopis::kem::{kopis::kem::KemPublicKey<L>}::from_bytes]:
    Source: 'src/kem.rs', lines 32:4-40:5 -/
def kem.KemPublicKey.from_bytes
  (L : Std.Usize) (bytes : Slice Std.U8) : Result (kem.KemPublicKey L) := do
  let pke_pk ← pke.PkePublicKey.from_bytes L bytes
  let hash_pke_pk ← pke.PkePublicKey.hash pke_pk
  ok { pke_pk, hash_pke_pk }

/-- [kopis::impls::kopis512::{kopis::impls::kopis512::Kopis512PublicKey}::from_bytes]:
    Source: 'src/impls.rs', lines 91:16-93:17
    Visibility: public -/
def impls.kopis512.Kopis512PublicKey.from_bytes
  (bytes : Array Std.U8 672#usize) :
  Result impls.kopis512.Kopis512PublicKey
  := do
  let s ← lift (Array.to_slice bytes)
  let kpk ← kem.KemPublicKey.from_bytes 2#usize s
  ok kpk

/-- [kopis::impls::kopis768::{kopis::impls::kopis768::Kopis768PublicKey}::from_bytes]:
    Source: 'src/impls.rs', lines 91:16-93:17
    Visibility: public -/
def impls.kopis768.Kopis768PublicKey.from_bytes
  (bytes : Array Std.U8 992#usize) :
  Result impls.kopis768.Kopis768PublicKey
  := do
  let s ← lift (Array.to_slice bytes)
  let kpk ← kem.KemPublicKey.from_bytes 3#usize s
  ok kpk

/-- [kopis::impls::kopis1024::{kopis::impls::kopis1024::Kopis1024PublicKey}::from_bytes]:
    Source: 'src/impls.rs', lines 91:16-93:17
    Visibility: public -/
def impls.kopis1024.Kopis1024PublicKey.from_bytes
  (bytes : Array Std.U8 1312#usize) :
  Result impls.kopis1024.Kopis1024PublicKey
  := do
  let s ← lift (Array.to_slice bytes)
  let kpk ← kem.KemPublicKey.from_bytes 4#usize s
  ok kpk

/-- [kopis::pke::encrypt_deterministic]:
    Source: 'src/pke.rs', lines 226:0-259:1 -/
def pke.encrypt_deterministic
  {L : Std.Usize} (MU : Std.Usize) (T : Std.Usize) (pk : pke.PkePublicKey L)
  (msg : Array Std.U8 32#usize) (randomness : Array Std.U8 32#usize)
  (out_buf : Slice Std.U8) :
  Result (Slice Std.U8)
  := do
  let left_val := Slice.len out_buf
  let right_val ← pke.ciphertext_len L T
  massert (left_val = right_val)
  let vec_sprime ← sample.gen_secret_from_seed L MU randomness
  let sprime_ntt ← arithmetic.ntt.NttMatrix.from_secret_matrix vec_sprime
  let prod ← arithmetic.ntt.NttMatrix.mul pk.mat_a_ntt sprime_ntt
  let i ← pke.H1_VAL
  let prod1 ← arithmetic.matrix_arith.Matrix.wrapping_add_to_all prod i
  let i1 ← consts.MODULUS_Q_BITS - consts.MODULUS_P_BITS
  let prod2 ← arithmetic.matrix_arith.Matrix.shift_right prod1 i1
  let vprime ← arithmetic.ntt.NttMatrix.mul_transpose pk.vec_ntt sprime_ntt
  let a ← Array.index_usize vprime 0#usize
  let vprime1 ← Array.index_usize a 0#usize
  let s ← lift (Array.to_slice msg)
  let a1 ← ser.deserialize_generic 256#usize s 1#usize
  let i2 ← consts.MODULUS_P_BITS - 1#usize
  let msg_polyn ← arithmetic.ring_arith.RingElem.shift_left a1 i2
  let c ←
    SharedARingElem.Insts.CoreOpsArithSubSharedARingElemRingElem.sub vprime1
      msg_polyn
  let c1 ← arithmetic.ring_arith.RingElem.wrapping_add_to_all c i
  let i3 ← consts.MODULUS_P_BITS - T
  let c2 ← arithmetic.ring_arith.RingElem.shift_right c1 i3
  let i4 ← L * consts.MODULUS_P_BITS
  let i5 ← i4 * consts.RING_DEG
  let i6 ← i5 / 8#usize
  let ((bprime_buf, c_buf), split_at_mut_back) ←
    core.slice.Slice.split_at_mut out_buf i6
  let bprime_buf1 ←
    arithmetic.matrix_arith.Matrix.serialize prod2 bprime_buf
      consts.MODULUS_P_BITS
  let c_buf1 ← arithmetic.ring_arith.RingElem.serialize c2 c_buf T
  ok (split_at_mut_back (bprime_buf1, c_buf1))

/-- [kopis::kem::encap_deterministic]:
    Source: 'src/kem.rs', lines 109:0-136:1 -/
def kem.encap_deterministic
  {L : Std.Usize} (MU : Std.Usize) (T : Std.Usize)
  (randomness : Array Std.U8 32#usize) (kem_pk : kem.KemPublicKey L)
  (out_buf : Slice Std.U8) :
  Result ((Array Std.U8 32#usize) × (Slice Std.U8))
  := do
  let k := Array.repeat 32#usize 0#u8
  let r := Array.repeat 32#usize 0#u8
  let hasher ←
    turboshake.TurboShake.Insts.CoreDefaultDefault.default 136#usize 5#u8
  let s ← lift (Array.to_slice randomness)
  let hasher1 ← turboshake.TurboShake.Insts.DigestUpdate.update hasher s
  let s1 ← lift (Array.to_slice kem_pk.hash_pke_pk)
  let hasher2 ← turboshake.TurboShake.Insts.DigestUpdate.update hasher1 s1
  let xof ←
    turboshake.TurboShake.Insts.DigestExtendableOutputTurboShakeReader.finalize_xof
      hasher2
  let (s2, to_slice_mut_back) ← lift (Array.to_slice_mut k)
  let (xof1, s3) ←
    turboshake.TurboShakeReader.Insts.DigestXofReader.read xof s2
  let (s4, to_slice_mut_back1) ← lift (Array.to_slice_mut r)
  let (_, s5) ←
    turboshake.TurboShakeReader.Insts.DigestXofReader.read xof1 s4
  let r1 := to_slice_mut_back1 s5
  let out_buf1 ←
    pke.encrypt_deterministic MU T kem_pk.pke_pk randomness r1 out_buf
  let k1 := to_slice_mut_back s3
  ok (k1, out_buf1)

/-- [kopis::impls::kopis512::{kopis::impls::kopis512::Kopis512PublicKey}::encapsulate_deterministic]:
    Source: 'src/impls.rs', lines 113:16-125:17
    Visibility: public -/
def impls.kopis512.Kopis512PublicKey.encapsulate_deterministic
  (self : impls.kopis512.Kopis512PublicKey)
  (randomness : Array Std.U8 32#usize) :
  Result ((Array Std.U8 736#usize) × impls.SharedSecret)
  := do
  let ct := Array.repeat 736#usize 0#u8
  let (s, to_slice_mut_back) ← lift (Array.to_slice_mut ct)
  let (ss, s1) ← kem.encap_deterministic 10#usize 3#usize randomness self s
  let ct1 := to_slice_mut_back s1
  ok (ct1, ss)

/-- [kopis::impls::kopis512::{kopis::impls::kopis512::Kopis512PublicKey}::encapsulate]:
    Source: 'src/impls.rs', lines 98:16-108:17
    Visibility: public -/
def impls.kopis512.Kopis512PublicKey.encapsulate
  {T0 : Type} (rand_coreCryptoRngInst : rand_core.CryptoRng T0)
  (self : impls.kopis512.Kopis512PublicKey) (rng : T0) :
  Result (((Array Std.U8 736#usize) × impls.SharedSecret) × T0)
  := do
  let randomness := Array.repeat 32#usize 0#u8
  let (s, to_slice_mut_back) ← lift (Array.to_slice_mut randomness)
  let (rng1, s1) ← rand_coreCryptoRngInst.RngCoreInst.fill_bytes rng s
  let randomness1 := to_slice_mut_back s1
  let out ←
    impls.kopis512.Kopis512PublicKey.encapsulate_deterministic self randomness1
  let _ ←
    Array.Insts.ZeroizeZeroize.zeroize (zeroize.Zeroize.Blanket
      U8.Insts.ZeroizeDefaultIsZeroes) randomness1
  ok (out, rng1)

/-- [kopis::impls::kopis768::{kopis::impls::kopis768::Kopis768PublicKey}::encapsulate_deterministic]:
    Source: 'src/impls.rs', lines 113:16-125:17
    Visibility: public -/
def impls.kopis768.Kopis768PublicKey.encapsulate_deterministic
  (self : impls.kopis768.Kopis768PublicKey)
  (randomness : Array Std.U8 32#usize) :
  Result ((Array Std.U8 1088#usize) × impls.SharedSecret)
  := do
  let ct := Array.repeat 1088#usize 0#u8
  let (s, to_slice_mut_back) ← lift (Array.to_slice_mut ct)
  let (ss, s1) ← kem.encap_deterministic 8#usize 4#usize randomness self s
  let ct1 := to_slice_mut_back s1
  ok (ct1, ss)

/-- [kopis::impls::kopis768::{kopis::impls::kopis768::Kopis768PublicKey}::encapsulate]:
    Source: 'src/impls.rs', lines 98:16-108:17
    Visibility: public -/
def impls.kopis768.Kopis768PublicKey.encapsulate
  {T0 : Type} (rand_coreCryptoRngInst : rand_core.CryptoRng T0)
  (self : impls.kopis768.Kopis768PublicKey) (rng : T0) :
  Result (((Array Std.U8 1088#usize) × impls.SharedSecret) × T0)
  := do
  let randomness := Array.repeat 32#usize 0#u8
  let (s, to_slice_mut_back) ← lift (Array.to_slice_mut randomness)
  let (rng1, s1) ← rand_coreCryptoRngInst.RngCoreInst.fill_bytes rng s
  let randomness1 := to_slice_mut_back s1
  let out ←
    impls.kopis768.Kopis768PublicKey.encapsulate_deterministic self randomness1
  let _ ←
    Array.Insts.ZeroizeZeroize.zeroize (zeroize.Zeroize.Blanket
      U8.Insts.ZeroizeDefaultIsZeroes) randomness1
  ok (out, rng1)

/-- [kopis::impls::kopis1024::{kopis::impls::kopis1024::Kopis1024PublicKey}::encapsulate_deterministic]:
    Source: 'src/impls.rs', lines 113:16-125:17
    Visibility: public -/
def impls.kopis1024.Kopis1024PublicKey.encapsulate_deterministic
  (self : impls.kopis1024.Kopis1024PublicKey)
  (randomness : Array Std.U8 32#usize) :
  Result ((Array Std.U8 1472#usize) × impls.SharedSecret)
  := do
  let ct := Array.repeat 1472#usize 0#u8
  let (s, to_slice_mut_back) ← lift (Array.to_slice_mut ct)
  let (ss, s1) ← kem.encap_deterministic 6#usize 6#usize randomness self s
  let ct1 := to_slice_mut_back s1
  ok (ct1, ss)

/-- [kopis::impls::kopis1024::{kopis::impls::kopis1024::Kopis1024PublicKey}::encapsulate]:
    Source: 'src/impls.rs', lines 98:16-108:17
    Visibility: public -/
def impls.kopis1024.Kopis1024PublicKey.encapsulate
  {T0 : Type} (rand_coreCryptoRngInst : rand_core.CryptoRng T0)
  (self : impls.kopis1024.Kopis1024PublicKey) (rng : T0) :
  Result (((Array Std.U8 1472#usize) × impls.SharedSecret) × T0)
  := do
  let randomness := Array.repeat 32#usize 0#u8
  let (s, to_slice_mut_back) ← lift (Array.to_slice_mut randomness)
  let (rng1, s1) ← rand_coreCryptoRngInst.RngCoreInst.fill_bytes rng s
  let randomness1 := to_slice_mut_back s1
  let out ←
    impls.kopis1024.Kopis1024PublicKey.encapsulate_deterministic self
      randomness1
  let _ ←
    Array.Insts.ZeroizeZeroize.zeroize (zeroize.Zeroize.Blanket
      U8.Insts.ZeroizeDefaultIsZeroes) randomness1
  ok (out, rng1)

/-- [kopis::pke::decrypt]:
    Source: 'src/pke.rs', lines 195:0-222:1 -/
def pke.decrypt
  {L : Std.Usize} (T : Std.Usize) (sk : pke.PkeSecretKey L)
  (ciphertext : Slice Std.U8) :
  Result (Array Std.U8 32#usize)
  := do
  let left_val := Slice.len ciphertext
  let right_val ← pke.ciphertext_len L T
  massert (left_val = right_val)
  let i ← L * consts.MODULUS_P_BITS
  let i1 ← i * consts.RING_DEG
  let i2 ← i1 / 8#usize
  let (bprime_bytes, c_bytes) ← core.slice.Slice.split_at ciphertext i2
  let bprime ←
    arithmetic.matrix_arith.Matrix.deserialize_10 L 1#usize bprime_bytes
  let bprime_ntt ← arithmetic.ntt.NttMatrix.from_uniform_matrix bprime
  let c ← arithmetic.ring_arith.RingElem.deserialize c_bytes T
  let i3 ← consts.MODULUS_P_BITS - T
  let c1 ← arithmetic.ring_arith.RingElem.shift_left c i3
  let v ← arithmetic.ntt.NttMatrix.mul_transpose bprime_ntt sk
  let a ← Array.index_usize v 0#usize
  let v1 ← Array.index_usize a 0#usize
  let mprime ←
    SharedARingElem.Insts.CoreOpsArithSubSharedARingElemRingElem.sub v1 c1
  let i4 ← consts.MODULUS_P_BITS - 2#usize
  let i5 ← 1#u16 <<< i4
  let i6 ← i3 - 1#usize
  let i7 ← 1#u16 <<< i6
  let i8 ← i5 - i7
  let i9 ← consts.MODULUS_Q_BITS - consts.MODULUS_P_BITS
  let i10 ← i9 - 1#usize
  let i11 ← 1#u16 <<< i10
  let h2_val ← i8 + i11
  let mprime1 ←
    arithmetic.ring_arith.RingElem.wrapping_add_to_all mprime h2_val
  let i12 ← consts.MODULUS_P_BITS - 1#usize
  let mprime2 ← arithmetic.ring_arith.RingElem.shift_right mprime1 i12
  let m := Array.repeat 32#usize 0#u8
  let (s, to_slice_mut_back) ← lift (Array.to_slice_mut m)
  let s1 ← arithmetic.ring_arith.RingElem.serialize mprime2 s 1#usize
  ok (to_slice_mut_back s1)

/-- [kopis::kem::decap]:
    Source: 'src/kem.rs', lines 141:0-174:1
    Visibility: public -/
def kem.decap
  {L : Std.Usize} (MU : Std.Usize) (T : Std.Usize) (sk : kem.KemSecretKey L)
  (ciphertext : Slice Std.U8) :
  Result (Array Std.U8 32#usize)
  := do
  let left_val := Slice.len ciphertext
  let right_val ← pke.ciphertext_len L T
  massert (left_val = right_val)
  let randomness ← pke.decrypt T sk.pke_sk ciphertext
  let k := Array.repeat 32#usize 0#u8
  let rprime := Array.repeat 32#usize 0#u8
  let hasher ←
    turboshake.TurboShake.Insts.CoreDefaultDefault.default 136#usize 5#u8
  let s ← lift (Array.to_slice randomness)
  let hasher1 ← turboshake.TurboShake.Insts.DigestUpdate.update hasher s
  let s1 ← lift (Array.to_slice sk.hash_pke_pk)
  let hasher2 ← turboshake.TurboShake.Insts.DigestUpdate.update hasher1 s1
  let xof ←
    turboshake.TurboShake.Insts.DigestExtendableOutputTurboShakeReader.finalize_xof
      hasher2
  let (s2, to_slice_mut_back) ← lift (Array.to_slice_mut k)
  let (xof1, s3) ←
    turboshake.TurboShakeReader.Insts.DigestXofReader.read xof s2
  let (s4, to_slice_mut_back1) ← lift (Array.to_slice_mut rprime)
  let (_, s5) ←
    turboshake.TurboShakeReader.Insts.DigestXofReader.read xof1 s4
  let buf := Array.repeat 1472#usize 0#u8
  let (reconstructed_ct, _) ←
    core.array.Array.index_mut (core.ops.index.IndexMutSlice
      (core.slice.index.SliceIndexRangeToUsizeSlice Std.U8)) buf
      { «end» := right_val }
  let rprime1 := to_slice_mut_back1 s5
  let reconstructed_ct1 ←
    pke.encrypt_deterministic MU T sk.pke_pk randomness rprime1
      reconstructed_ct
  let s6 ← lift (Array.to_slice sk.z)
  let reject_val ← turboshake256_hash 6#u8 s6 ciphertext
  let reconstruction_matched ←
    Slice.Insts.SubtleConstantTimeEq.ct_eq U8.Insts.SubtleConstantTimeEq
      reconstructed_ct1 ciphertext
  let k1 := to_slice_mut_back s3
  Array.Insts.SubtleConditionallySelectable.conditional_select
    U8.Insts.SubtleConditionallySelectable reject_val k1 reconstruction_matched

/-- [kopis::impls::kopis512::{kopis::impls::kopis512::Kopis512SecretKey}::decapsulate]:
    Source: 'src/impls.rs', lines 132:16-138:17
    Visibility: public -/
def impls.kopis512.Kopis512SecretKey.decapsulate
  (self : impls.kopis512.Kopis512SecretKey)
  (encapsulated_key : Array Std.U8 736#usize) :
  Result impls.SharedSecret
  := do
  let s ← lift (Array.to_slice encapsulated_key)
  let a ← kem.decap 10#usize 3#usize self s
  ok a

/-- [kopis::impls::kopis768::{kopis::impls::kopis768::Kopis768SecretKey}::decapsulate]:
    Source: 'src/impls.rs', lines 132:16-138:17
    Visibility: public -/
def impls.kopis768.Kopis768SecretKey.decapsulate
  (self : impls.kopis768.Kopis768SecretKey)
  (encapsulated_key : Array Std.U8 1088#usize) :
  Result impls.SharedSecret
  := do
  let s ← lift (Array.to_slice encapsulated_key)
  let a ← kem.decap 8#usize 4#usize self s
  ok a

/-- [kopis::impls::kopis1024::{kopis::impls::kopis1024::Kopis1024SecretKey}::decapsulate]:
    Source: 'src/impls.rs', lines 132:16-138:17
    Visibility: public -/
def impls.kopis1024.Kopis1024SecretKey.decapsulate
  (self : impls.kopis1024.Kopis1024SecretKey)
  (encapsulated_key : Array Std.U8 1472#usize) :
  Result impls.SharedSecret
  := do
  let s ← lift (Array.to_slice encapsulated_key)
  let a ← kem.decap 6#usize 6#usize self s
  ok a

/-- Trait implementation: [kopis::kem::{impl core::ops::drop::Drop for kopis::kem::KemSecretKey<L>}]
    Source: 'src/kem.rs', lines 54:9-54:22 -/
@[reducible]
def kem.KemSecretKey.Insts.CoreOpsDropDrop (L : Std.Usize) : core.ops.drop.Drop
  (kem.KemSecretKey L) := {
  drop := kem.KemSecretKey.Insts.CoreOpsDropDrop.drop
}

/-- Trait implementation: [kopis::pke::{impl core::ops::drop::Drop for kopis::pke::PkeSecretKey<L>}]
    Source: 'src/pke.rs', lines 28:18-28:31 -/
@[reducible]
def pke.PkeSecretKey.Insts.CoreOpsDropDrop (L : Std.Usize) : core.ops.drop.Drop
  (pke.PkeSecretKey L) := {
  drop := pke.PkeSecretKey.Insts.CoreOpsDropDrop.drop
}

/-- Trait implementation: [kopis::pke::{impl zeroize::ZeroizeOnDrop for kopis::pke::PkeSecretKey<L>}]
    Source: 'src/pke.rs', lines 28:18-28:31 -/
@[reducible]
def pke.PkeSecretKey.Insts.ZeroizeZeroizeOnDrop (L : Std.Usize) :
  zeroize.ZeroizeOnDrop (pke.PkeSecretKey L) := {
}

/-- Trait implementation: [kopis::pke::{impl core::clone::Clone for kopis::pke::PkePublicKey<L>}]
    Source: 'src/pke.rs', lines 37:9-37:14 -/
@[reducible]
def pke.PkePublicKey.Insts.CoreCloneClone (L : Std.Usize) : core.clone.Clone
  (pke.PkePublicKey L) := {
  clone := pke.PkePublicKey.Insts.CoreCloneClone.clone
}

/-- [kopis::pke::max_pke_pubkey_serialized_len]:
    Source: 'src/pke.rs', lines 115:0-117:1 -/
def pke.max_pke_pubkey_serialized_len : Result Std.Usize := do
  let i ← consts.MAX_L * consts.MODULUS_P_BITS
  let i1 ← i * consts.RING_DEG
  let i2 ← i1 / 8#usize
  32#usize + i2

/-- [kopis::pke::max_ciphertext_len]:
    Source: 'src/pke.rs', lines 121:0-124:1
    Visibility: public -/
def pke.max_ciphertext_len : Result Std.Usize := do
  let i ← consts.MAX_T * consts.RING_DEG
  let i1 ← i / 8#usize
  let i2 ← consts.MAX_L * consts.MODULUS_P_BITS
  let i3 ← i2 * consts.RING_DEG
  let i4 ← i3 / 8#usize
  i1 + i4

/-- [kopis::ser::deserialize_13::{impl core::ops::function::FnMut<(usize,), u16> for kopis::ser::deserialize_13::closure<'_0>}::call_mut]:
    Source: 'src/ser.rs', lines 12:16-12:38 -/
def ser.deserialize_13.closure.Insts.CoreOpsFunctionFnMutTupleUsizeU16.call_mut
  (state : ser.deserialize_13.closure) (args : Std.Usize) :
  Result (Std.U16 × ser.deserialize_13.closure)
  := do
  let i ←
    ser.deserialize_13.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call state
      args
  ok (i, state)

/-- [kopis::ser::deserialize_13::{impl core::ops::function::FnOnce<(usize,), u16> for kopis::ser::deserialize_13::closure<'_0>}::call_once]:
    Source: 'src/ser.rs', lines 12:16-12:38 -/
def
  ser.deserialize_13.closure.Insts.CoreOpsFunctionFnOnceTupleUsizeU16.call_once
  (c : ser.deserialize_13.closure) (i : Std.Usize) : Result Std.U16 := do
  let (i1, _) ←
    ser.deserialize_13.closure.Insts.CoreOpsFunctionFnMutTupleUsizeU16.call_mut
      c i
  ok i1

/-- Trait implementation: [kopis::ser::deserialize_13::{impl core::ops::function::FnOnce<(usize,), u16> for kopis::ser::deserialize_13::closure<'_0>}]
    Source: 'src/ser.rs', lines 12:16-12:38 -/
@[reducible]
def ser.deserialize_13.closure.Insts.CoreOpsFunctionFnOnceTupleUsizeU16 :
  core.ops.function.FnOnce ser.deserialize_13.closure Std.Usize Std.U16 := {
  call_once :=
    ser.deserialize_13.closure.Insts.CoreOpsFunctionFnOnceTupleUsizeU16.call_once
}

/-- Trait implementation: [kopis::ser::deserialize_13::{impl core::ops::function::FnMut<(usize,), u16> for kopis::ser::deserialize_13::closure<'_0>}]
    Source: 'src/ser.rs', lines 12:16-12:38 -/
@[reducible]
def ser.deserialize_13.closure.Insts.CoreOpsFunctionFnMutTupleUsizeU16 :
  core.ops.function.FnMut ser.deserialize_13.closure Std.Usize Std.U16 := {
  FnOnceInst :=
    ser.deserialize_13.closure.Insts.CoreOpsFunctionFnOnceTupleUsizeU16
  call_mut :=
    ser.deserialize_13.closure.Insts.CoreOpsFunctionFnMutTupleUsizeU16.call_mut
}

/-- Trait implementation: [kopis::ser::deserialize_13::{impl core::ops::function::Fn<(usize,), u16> for kopis::ser::deserialize_13::closure<'_0>}]
    Source: 'src/ser.rs', lines 12:16-12:38 -/
@[reducible]
def ser.deserialize_13.closure.Insts.CoreOpsFunctionFnTupleUsizeU16 :
  core.ops.function.Fn ser.deserialize_13.closure Std.Usize Std.U16 := {
  FnMutInst :=
    ser.deserialize_13.closure.Insts.CoreOpsFunctionFnMutTupleUsizeU16
  call := ser.deserialize_13.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call
}

/-- [kopis::ser::deserialize_10::{impl core::ops::function::FnMut<(usize,), u16> for kopis::ser::deserialize_10::closure<'_0>}::call_mut]:
    Source: 'src/ser.rs', lines 34:16-34:38 -/
def ser.deserialize_10.closure.Insts.CoreOpsFunctionFnMutTupleUsizeU16.call_mut
  (state : ser.deserialize_10.closure) (args : Std.Usize) :
  Result (Std.U16 × ser.deserialize_10.closure)
  := do
  let i ←
    ser.deserialize_10.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call state
      args
  ok (i, state)

/-- [kopis::ser::deserialize_10::{impl core::ops::function::FnOnce<(usize,), u16> for kopis::ser::deserialize_10::closure<'_0>}::call_once]:
    Source: 'src/ser.rs', lines 34:16-34:38 -/
def
  ser.deserialize_10.closure.Insts.CoreOpsFunctionFnOnceTupleUsizeU16.call_once
  (c : ser.deserialize_10.closure) (i : Std.Usize) : Result Std.U16 := do
  let (i1, _) ←
    ser.deserialize_10.closure.Insts.CoreOpsFunctionFnMutTupleUsizeU16.call_mut
      c i
  ok i1

/-- Trait implementation: [kopis::ser::deserialize_10::{impl core::ops::function::FnOnce<(usize,), u16> for kopis::ser::deserialize_10::closure<'_0>}]
    Source: 'src/ser.rs', lines 34:16-34:38 -/
@[reducible]
def ser.deserialize_10.closure.Insts.CoreOpsFunctionFnOnceTupleUsizeU16 :
  core.ops.function.FnOnce ser.deserialize_10.closure Std.Usize Std.U16 := {
  call_once :=
    ser.deserialize_10.closure.Insts.CoreOpsFunctionFnOnceTupleUsizeU16.call_once
}

/-- Trait implementation: [kopis::ser::deserialize_10::{impl core::ops::function::FnMut<(usize,), u16> for kopis::ser::deserialize_10::closure<'_0>}]
    Source: 'src/ser.rs', lines 34:16-34:38 -/
@[reducible]
def ser.deserialize_10.closure.Insts.CoreOpsFunctionFnMutTupleUsizeU16 :
  core.ops.function.FnMut ser.deserialize_10.closure Std.Usize Std.U16 := {
  FnOnceInst :=
    ser.deserialize_10.closure.Insts.CoreOpsFunctionFnOnceTupleUsizeU16
  call_mut :=
    ser.deserialize_10.closure.Insts.CoreOpsFunctionFnMutTupleUsizeU16.call_mut
}

/-- Trait implementation: [kopis::ser::deserialize_10::{impl core::ops::function::Fn<(usize,), u16> for kopis::ser::deserialize_10::closure<'_0>}]
    Source: 'src/ser.rs', lines 34:16-34:38 -/
@[reducible]
def ser.deserialize_10.closure.Insts.CoreOpsFunctionFnTupleUsizeU16 :
  core.ops.function.Fn ser.deserialize_10.closure Std.Usize Std.U16 := {
  FnMutInst :=
    ser.deserialize_10.closure.Insts.CoreOpsFunctionFnMutTupleUsizeU16
  call := ser.deserialize_10.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call
}

/-- [kopis::ser::serialize_10::{impl core::ops::function::FnMut<(usize,), u16> for kopis::ser::serialize_10::closure<'_0, '_1>}::call_mut]:
    Source: 'src/ser.rs', lines 87:16-87:46 -/
def ser.serialize_10.closure.Insts.CoreOpsFunctionFnMutTupleUsizeU16.call_mut
  (state : ser.serialize_10.closure) (args : Std.Usize) :
  Result (Std.U16 × ser.serialize_10.closure)
  := do
  let i ←
    ser.serialize_10.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call state
      args
  ok (i, state)

/-- [kopis::ser::serialize_10::{impl core::ops::function::FnOnce<(usize,), u16> for kopis::ser::serialize_10::closure<'_0, '_1>}::call_once]:
    Source: 'src/ser.rs', lines 87:16-87:46 -/
def ser.serialize_10.closure.Insts.CoreOpsFunctionFnOnceTupleUsizeU16.call_once
  (c : ser.serialize_10.closure) (i : Std.Usize) : Result Std.U16 := do
  let (i1, _) ←
    ser.serialize_10.closure.Insts.CoreOpsFunctionFnMutTupleUsizeU16.call_mut c
      i
  ok i1

/-- Trait implementation: [kopis::ser::serialize_10::{impl core::ops::function::FnOnce<(usize,), u16> for kopis::ser::serialize_10::closure<'_0, '_1>}]
    Source: 'src/ser.rs', lines 87:16-87:46 -/
@[reducible]
def ser.serialize_10.closure.Insts.CoreOpsFunctionFnOnceTupleUsizeU16 :
  core.ops.function.FnOnce ser.serialize_10.closure Std.Usize Std.U16 := {
  call_once :=
    ser.serialize_10.closure.Insts.CoreOpsFunctionFnOnceTupleUsizeU16.call_once
}

/-- Trait implementation: [kopis::ser::serialize_10::{impl core::ops::function::FnMut<(usize,), u16> for kopis::ser::serialize_10::closure<'_0, '_1>}]
    Source: 'src/ser.rs', lines 87:16-87:46 -/
@[reducible]
def ser.serialize_10.closure.Insts.CoreOpsFunctionFnMutTupleUsizeU16 :
  core.ops.function.FnMut ser.serialize_10.closure Std.Usize Std.U16 := {
  FnOnceInst :=
    ser.serialize_10.closure.Insts.CoreOpsFunctionFnOnceTupleUsizeU16
  call_mut :=
    ser.serialize_10.closure.Insts.CoreOpsFunctionFnMutTupleUsizeU16.call_mut
}

/-- Trait implementation: [kopis::ser::serialize_10::{impl core::ops::function::Fn<(usize,), u16> for kopis::ser::serialize_10::closure<'_0, '_1>}]
    Source: 'src/ser.rs', lines 87:16-87:46 -/
@[reducible]
def ser.serialize_10.closure.Insts.CoreOpsFunctionFnTupleUsizeU16 :
  core.ops.function.Fn ser.serialize_10.closure Std.Usize Std.U16 := {
  FnMutInst := ser.serialize_10.closure.Insts.CoreOpsFunctionFnMutTupleUsizeU16
  call := ser.serialize_10.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call
}

end RustKopis
