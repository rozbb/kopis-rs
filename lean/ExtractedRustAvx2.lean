-- THIS FILE WAS AUTOMATICALLY GENERATED BY AENEAS
-- [kopis]
import Aeneas
open Aeneas Aeneas.Std Result ControlFlow Error
set_option linter.dupNamespace false
set_option linter.hashCommand false
set_option linter.unusedVariables false

/- You can set the `maxHeartbeats` value with the `-max-heartbeats` CLI option -/
set_option maxHeartbeats 1000000

/- You can set the `maxRecDepth` value with the `-max-recdepth` CLI option -/
set_option maxRecDepth 2048

/- You can remove the following line by using the CLI option `-all-computable`: -/
noncomputable section

namespace RustKopisAvx2

/-- Trait implementation: [core::array::equality::{impl core::cmp::PartialEq<[U; N]> for [T; N]}]
    Source: '/rustc/library/core/src/array/equality.rs', lines 5:0-7:28
    Name pattern: [core::cmp::PartialEq<[@T; @N], [@U; @N]>] -/
@[reducible, rust_trait_impl "core::cmp::PartialEq<[@T; @N], [@U; @N]>"]
def Array.Insts.CoreCmpPartialEqArray {T : Type} {U : Type} (N : Std.Usize)
  (cmpPartialEqInst : core.cmp.PartialEq T U) : core.cmp.PartialEq (Array T N)
  (Array U N) := {
  eq := core.array.equality.PartialEqArray.eq cmpPartialEqInst
}

/-- [core::num::{u32}::count_ones]:
    Source: '/rustc/library/core/src/num/uint_macros.rs', lines 84:8-84:44
    Name pattern: [core::num::{u32}::count_ones]
    Visibility: public -/
@[rust_fun "core::num::{u32}::count_ones"]
axiom core.num.U32.count_ones : Std.U32 → Result Std.U32

/-- [core::num::{u8}::count_ones]:
    Source: '/rustc/library/core/src/num/uint_macros.rs', lines 84:8-84:44
    Name pattern: [core::num::{u8}::count_ones]
    Visibility: public -/
@[rust_fun "core::num::{u8}::count_ones"]
axiom core.num.U8.count_ones : Std.U8 → Result Std.U32

/-- Trait declaration: [core::ops::arith::Add]
    Source: '/rustc/library/core/src/ops/arith.rs', lines 76:0-76:31
    Name pattern: [core::ops::arith::Add]
    Visibility: public -/
@[rust_trait "core::ops::arith::Add"]
structure core.ops.arith.Add (Self : Type) (Rhs : Type) (Self_Output : Type)
  where
  add : Self → Rhs → Result Self_Output

/-- Trait declaration: [core::ops::arith::Sub]
    Source: '/rustc/library/core/src/ops/arith.rs', lines 188:0-188:31
    Name pattern: [core::ops::arith::Sub]
    Visibility: public -/
@[rust_trait "core::ops::arith::Sub"]
structure core.ops.arith.Sub (Self : Type) (Rhs : Type) (Self_Output : Type)
  where
  sub : Self → Rhs → Result Self_Output

/-- Trait declaration: [core::ops::arith::Mul]
    Source: '/rustc/library/core/src/ops/arith.rs', lines 322:0-322:31
    Name pattern: [core::ops::arith::Mul]
    Visibility: public -/
@[rust_trait "core::ops::arith::Mul"]
structure core.ops.arith.Mul (Self : Type) (Rhs : Type) (Self_Output : Type)
  where
  mul : Self → Rhs → Result Self_Output

/-- [core::ops::range::{core::ops::range::RangeInclusive<Idx>}::contains]:
    Source: '/rustc/library/core/src/ops/range.rs', lines 522:4-525:44
    Name pattern: [core::ops::range::{core::ops::range::RangeInclusive<@Idx>}::contains]
    Visibility: public -/
@[rust_fun
  "core::ops::range::{core::ops::range::RangeInclusive<@Idx>}::contains"]
axiom core.ops.range.RangeInclusive.contains
  {Idx : Type} {U : Type} (cmpPartialOrdInst : core.cmp.PartialOrd Idx Idx)
  (cmpPartialOrdInst1 : core.cmp.PartialOrd Idx U) (cmpPartialOrdInst2 :
  core.cmp.PartialOrd U Idx) :
  core.ops.range.RangeInclusive Idx → U → Result Bool

/-- Trait declaration: [rand_core::RngCore]
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/rand_core-0.9.5/src/lib.rs', lines 130:0-130:17
    Name pattern: [rand_core::RngCore]
    Visibility: public -/
@[rust_trait "rand_core::RngCore"]
structure rand_core.RngCore (Self : Type) where
  next_u32 : Self → Result (Std.U32 × Self)
  next_u64 : Self → Result (Std.U64 × Self)
  fill_bytes : Self → Slice Std.U8 → Result (Self × (Slice Std.U8))

/-- Trait declaration: [rand_core::CryptoRng]
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/rand_core-0.9.5/src/lib.rs', lines 204:0-204:28
    Name pattern: [rand_core::CryptoRng]
    Visibility: public -/
@[rust_trait "rand_core::CryptoRng" (parentClauses := ["RngCoreInst"])]
structure rand_core.CryptoRng (Self : Type) where
  RngCoreInst : rand_core.RngCore Self

/-- [subtle::Choice]
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/subtle-2.6.1/src/lib.rs', lines 120:0-120:17
    Name pattern: [subtle::Choice]
    Visibility: public -/
@[rust_type "subtle::Choice"]
axiom subtle.Choice : Type

/-- Trait declaration: [subtle::ConstantTimeEq]
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/subtle-2.6.1/src/lib.rs', lines 262:0-262:24
    Name pattern: [subtle::ConstantTimeEq]
    Visibility: public -/
@[rust_trait "subtle::ConstantTimeEq"]
structure subtle.ConstantTimeEq (Self : Type) where
  ct_eq : Self → Self → Result subtle.Choice

/-- [subtle::{impl subtle::ConstantTimeEq for [T]}::ct_eq]:
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/subtle-2.6.1/src/lib.rs', lines 313:4-313:41
    Name pattern: [subtle::{subtle::ConstantTimeEq<[@T]>}::ct_eq]
    Visibility: public -/
@[rust_fun "subtle::{subtle::ConstantTimeEq<[@T]>}::ct_eq"]
axiom Slice.Insts.SubtleConstantTimeEq.ct_eq
  {T : Type} (ConstantTimeEqInst : subtle.ConstantTimeEq T) :
  Slice T → Slice T → Result subtle.Choice

/-- [subtle::{impl subtle::ConstantTimeEq for u8}::ct_eq]:
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/subtle-2.6.1/src/lib.rs', lines 348:12-348:51
    Name pattern: [subtle::{subtle::ConstantTimeEq<u8>}::ct_eq]
    Visibility: public -/
@[rust_fun "subtle::{subtle::ConstantTimeEq<u8>}::ct_eq"]
axiom U8.Insts.SubtleConstantTimeEq.ct_eq
  : Std.U8 → Std.U8 → Result subtle.Choice

/-- Trait implementation: [subtle::{impl subtle::ConstantTimeEq for u8}]
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/subtle-2.6.1/src/lib.rs', lines 346:8-346:36
    Name pattern: [subtle::ConstantTimeEq<u8>] -/
@[reducible, rust_trait_impl "subtle::ConstantTimeEq<u8>"]
def U8.Insts.SubtleConstantTimeEq : subtle.ConstantTimeEq Std.U8 := {
  ct_eq := U8.Insts.SubtleConstantTimeEq.ct_eq
}

/-- Trait declaration: [subtle::ConditionallySelectable]
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/subtle-2.6.1/src/lib.rs', lines 393:0-393:39
    Name pattern: [subtle::ConditionallySelectable]
    Visibility: public -/
@[rust_trait "subtle::ConditionallySelectable"
  (parentClauses := ["coremarkerCopyInst"])]
structure subtle.ConditionallySelectable (Self : Type) where
  coremarkerCopyInst : core.marker.Copy Self
  conditional_select : Self → Self → subtle.Choice → Result Self

/-- [subtle::{impl subtle::ConditionallySelectable for u8}::conditional_select]:
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/subtle-2.6.1/src/lib.rs', lines 513:12-513:77
    Name pattern: [subtle::{subtle::ConditionallySelectable<u8>}::conditional_select]
    Visibility: public -/
@[rust_fun "subtle::{subtle::ConditionallySelectable<u8>}::conditional_select"]
axiom U8.Insts.SubtleConditionallySelectable.conditional_select
  : Std.U8 → Std.U8 → subtle.Choice → Result Std.U8

/-- Trait implementation: [subtle::{impl subtle::ConditionallySelectable for u8}]
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/subtle-2.6.1/src/lib.rs', lines 511:8-537:10
    Name pattern: [subtle::ConditionallySelectable<u8>] -/
@[reducible, rust_trait_impl "subtle::ConditionallySelectable<u8>"]
def U8.Insts.SubtleConditionallySelectable : subtle.ConditionallySelectable
  Std.U8 := {
  coremarkerCopyInst := core.marker.CopyU8
  conditional_select :=
    U8.Insts.SubtleConditionallySelectable.conditional_select
}

/-- [subtle::{impl subtle::ConditionallySelectable for [T; N]}::conditional_select]:
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/subtle-2.6.1/src/lib.rs', lines 581:4-581:69
    Name pattern: [subtle::{subtle::ConditionallySelectable<[@T; @N]>}::conditional_select]
    Visibility: public -/
@[rust_fun
  "subtle::{subtle::ConditionallySelectable<[@T; @N]>}::conditional_select"]
axiom Array.Insts.SubtleConditionallySelectable.conditional_select
  {T : Type} {N : Std.Usize} (ConditionallySelectableInst :
  subtle.ConditionallySelectable T) :
  Array T N → Array T N → subtle.Choice → Result (Array T N)

/-- [turboshake::TurboShake]
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/turboshake-0.7.1/src/lib.rs', lines 35:0-35:54
    Name pattern: [turboshake::TurboShake]
    Visibility: public -/
@[rust_type "turboshake::TurboShake"]
axiom turboshake.TurboShake (RATE : Std.Usize) (DS : Std.U8) : Type

/-- [turboshake::{impl core::default::Default for turboshake::TurboShake<RATE, DS>}::default]:
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/turboshake-0.7.1/src/lib.rs', lines 43:4-43:24
    Name pattern: [turboshake::{core::default::Default<turboshake::TurboShake<@RATE, @DS>>}::default]
    Visibility: public -/
@[rust_fun
  "turboshake::{core::default::Default<turboshake::TurboShake<@RATE, @DS>>}::default"]
axiom turboshake.TurboShake.Insts.CoreDefaultDefault.default
  (RATE : Std.Usize) (DS : Std.U8) : Result (turboshake.TurboShake RATE DS)

/-- [turboshake::{impl digest::Update for turboshake::TurboShake<RATE, DS>}::update]:
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/turboshake-0.7.1/src/lib.rs', lines 60:4-60:37
    Name pattern: [turboshake::{digest::Update<turboshake::TurboShake<@RATE, @DS>>}::update]
    Visibility: public -/
@[rust_fun
  "turboshake::{digest::Update<turboshake::TurboShake<@RATE, @DS>>}::update"]
axiom turboshake.TurboShake.Insts.DigestUpdate.update
  {RATE : Std.Usize} {DS : Std.U8} :
  turboshake.TurboShake RATE DS → Slice Std.U8 → Result
    (turboshake.TurboShake RATE DS)

/-- [turboshake::TurboShakeReader]
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/turboshake-0.7.1/src/lib.rs', lines 153:0-153:46
    Name pattern: [turboshake::TurboShakeReader]
    Visibility: public -/
@[rust_type "turboshake::TurboShakeReader"]
axiom turboshake.TurboShakeReader (RATE : Std.Usize) : Type

/-- [turboshake::{impl digest::ExtendableOutput<turboshake::TurboShakeReader<RATE>> for turboshake::TurboShake<RATE, DS>}::finalize_xof]:
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/turboshake-0.7.1/src/lib.rs', lines 83:4-83:45
    Name pattern: [turboshake::{digest::ExtendableOutput<turboshake::TurboShake<@RATE, @DS>, turboshake::TurboShakeReader<@RATE>>}::finalize_xof]
    Visibility: public -/
@[rust_fun
  "turboshake::{digest::ExtendableOutput<turboshake::TurboShake<@RATE, @DS>, turboshake::TurboShakeReader<@RATE>>}::finalize_xof"]
axiom
  turboshake.TurboShake.Insts.DigestExtendableOutputTurboShakeReader.finalize_xof
  {RATE : Std.Usize} {DS : Std.U8} :
  turboshake.TurboShake RATE DS → Result (turboshake.TurboShakeReader RATE)

/-- [turboshake::{impl digest::XofReader for turboshake::TurboShakeReader<RATE>}::read]:
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/turboshake-0.7.1/src/lib.rs', lines 161:4-161:38
    Name pattern: [turboshake::{digest::XofReader<turboshake::TurboShakeReader<@RATE>>}::read]
    Visibility: public -/
@[rust_fun
  "turboshake::{digest::XofReader<turboshake::TurboShakeReader<@RATE>>}::read"]
axiom turboshake.TurboShakeReader.Insts.DigestXofReader.read
  {RATE : Std.Usize} :
  turboshake.TurboShakeReader RATE → Slice Std.U8 → Result
    ((turboshake.TurboShakeReader RATE) × (Slice Std.U8))

/-- Trait declaration: [zeroize::Zeroize]
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/zeroize-1.9.0/src/lib.rs', lines 245:0-245:17
    Name pattern: [zeroize::Zeroize]
    Visibility: public -/
@[rust_trait "zeroize::Zeroize"]
structure zeroize.Zeroize (Self : Type) where
  zeroize : Self → Result Self

/-- Trait declaration: [zeroize::ZeroizeOnDrop]
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/zeroize-1.9.0/src/lib.rs', lines 252:0-252:23
    Name pattern: [zeroize::ZeroizeOnDrop]
    Visibility: public -/
@[rust_trait "zeroize::ZeroizeOnDrop"]
structure zeroize.ZeroizeOnDrop (Self : Type) where

/-- Trait declaration: [zeroize::DefaultIsZeroes]
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/zeroize-1.9.0/src/lib.rs', lines 255:0-255:49
    Name pattern: [zeroize::DefaultIsZeroes]
    Visibility: public -/
@[rust_trait "zeroize::DefaultIsZeroes"
  (parentClauses := ["coremarkerCopyInst", "coredefaultDefaultInst"])]
structure zeroize.DefaultIsZeroes (Self : Type) where
  coremarkerCopyInst : core.marker.Copy Self
  coredefaultDefaultInst : core.default.Default Self

/-- [zeroize::{impl zeroize::Zeroize for Z}::zeroize]:
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/zeroize-1.9.0/src/lib.rs', lines 274:4-274:25
    Name pattern: [zeroize::{zeroize::Zeroize<@Z>}::zeroize]
    Visibility: public -/
@[rust_fun "zeroize::{zeroize::Zeroize<@Z>}::zeroize"]
axiom zeroize.Zeroize.Blanket.zeroize
  {Z : Type} (DefaultIsZeroesInst : zeroize.DefaultIsZeroes Z) : Z → Result Z

/-- Trait implementation: [zeroize::{impl zeroize::Zeroize for Z}]
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/zeroize-1.9.0/src/lib.rs', lines 270:0-272:23
    Name pattern: [zeroize::Zeroize<@Z>] -/
@[reducible, rust_trait_impl "zeroize::Zeroize<@Z>"]
def zeroize.Zeroize.Blanket {Z : Type} (DefaultIsZeroesInst :
  zeroize.DefaultIsZeroes Z) : zeroize.Zeroize Z := {
  zeroize := zeroize.Zeroize.Blanket.zeroize DefaultIsZeroesInst
}

/-- Trait implementation: [zeroize::{impl zeroize::DefaultIsZeroes for u8}]
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/zeroize-1.9.0/src/lib.rs', lines 282:10-282:40
    Name pattern: [zeroize::DefaultIsZeroes<u8>] -/
@[reducible, rust_trait_impl "zeroize::DefaultIsZeroes<u8>"]
def U8.Insts.ZeroizeDefaultIsZeroes : zeroize.DefaultIsZeroes Std.U8 := {
  coremarkerCopyInst := core.marker.CopyU8
  coredefaultDefaultInst := core.default.DefaultU8
}

/-- Trait implementation: [zeroize::{impl zeroize::DefaultIsZeroes for i16}]
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/zeroize-1.9.0/src/lib.rs', lines 282:10-282:40
    Name pattern: [zeroize::DefaultIsZeroes<i16>] -/
@[reducible, rust_trait_impl "zeroize::DefaultIsZeroes<i16>"]
def I16.Insts.ZeroizeDefaultIsZeroes : zeroize.DefaultIsZeroes Std.I16 := {
  coremarkerCopyInst := core.marker.CopyI16
  coredefaultDefaultInst := core.default.DefaultI16
}

/-- Trait implementation: [zeroize::{impl zeroize::DefaultIsZeroes for u16}]
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/zeroize-1.9.0/src/lib.rs', lines 282:10-282:40
    Name pattern: [zeroize::DefaultIsZeroes<u16>] -/
@[reducible, rust_trait_impl "zeroize::DefaultIsZeroes<u16>"]
def U16.Insts.ZeroizeDefaultIsZeroes : zeroize.DefaultIsZeroes Std.U16 := {
  coremarkerCopyInst := core.marker.CopyU16
  coredefaultDefaultInst := core.default.DefaultU16
}

/-- [zeroize::{impl zeroize::Zeroize for [Z; N]}::zeroize]:
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/zeroize-1.9.0/src/lib.rs', lines 346:4-346:25
    Name pattern: [zeroize::{zeroize::Zeroize<[@Z; @N]>}::zeroize]
    Visibility: public -/
@[rust_fun "zeroize::{zeroize::Zeroize<[@Z; @N]>}::zeroize"]
axiom Array.Insts.ZeroizeZeroize.zeroize
  {Z : Type} {N : Std.Usize} (ZeroizeInst : zeroize.Zeroize Z) :
  Array Z N → Result (Array Z N)

/-- Trait implementation: [zeroize::{impl zeroize::Zeroize for [Z; N]}]
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/zeroize-1.9.0/src/lib.rs', lines 342:0-344:15
    Name pattern: [zeroize::Zeroize<[@Z; @N]>] -/
@[reducible, rust_trait_impl "zeroize::Zeroize<[@Z; @N]>"]
def Array.Insts.ZeroizeZeroize {Z : Type} (N : Std.Usize) (ZeroizeInst :
  zeroize.Zeroize Z) : zeroize.Zeroize (Array Z N) := {
  zeroize := Array.Insts.ZeroizeZeroize.zeroize ZeroizeInst
}

/-- [zeroize::__internal::{impl zeroize::__internal::AssertZeroizeOnDrop for &'_0 &'_1 mut T}::zeroize_or_on_drop]:
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/zeroize-1.9.0/src/lib.rs', lines 837:8-837:35
    Name pattern: [zeroize::__internal::{zeroize::__internal::AssertZeroizeOnDrop<&'0 &'1 mut @T>}::zeroize_or_on_drop]
    Visibility: public -/
@[rust_fun
  "zeroize::__internal::{zeroize::__internal::AssertZeroizeOnDrop<&'0 &'1 mut @T>}::zeroize_or_on_drop"]
axiom
  Shared0Mut1T.Insts.Zeroize__internalAssertZeroizeOnDrop.zeroize_or_on_drop
  {T : Type} (ZeroizeOnDropInst : zeroize.ZeroizeOnDrop T) : T → Result Unit

/-- [zeroize::__internal::{impl zeroize::__internal::AssertZeroize for T}::zeroize_or_on_drop]:
    Source: '/cargo/registry/src/index.crates.io-1949cf8c6b5b557f/zeroize-1.9.0/src/lib.rs', lines 846:8-846:40
    Name pattern: [zeroize::__internal::{zeroize::__internal::AssertZeroize<@T>}::zeroize_or_on_drop]
    Visibility: public -/
@[rust_fun
  "zeroize::__internal::{zeroize::__internal::AssertZeroize<@T>}::zeroize_or_on_drop"]
axiom zeroize.__internal.AssertZeroize.Blanket.zeroize_or_on_drop
  {T : Type} (ZeroizeInst : zeroize.Zeroize T) : T → Result T

/-- [kopis::arithmetic::ring_arith::RingElem]
    Source: 'src/arithmetic/ring_arith.rs', lines 16:0-16:48
    Visibility: public -/
@[reducible]
def arithmetic.ring_arith.RingElem := Array Std.U16 256#usize

/-- [kopis::arithmetic::matrix_arith::Matrix]
    Source: 'src/arithmetic/matrix_arith.rs', lines 13:0-13:88 -/
@[reducible]
def arithmetic.matrix_arith.Matrix (X : Std.Usize) (Y : Std.Usize) :=
  Array (Array arithmetic.ring_arith.RingElem Y) X

/-- [kopis::arithmetic::ring_arith::{impl core::cmp::PartialEq<kopis::arithmetic::ring_arith::RingElem> for kopis::arithmetic::ring_arith::RingElem}::eq]:
    Source: 'src/arithmetic/ring_arith.rs', lines 15:13-15:22
    Visibility: public -/
def arithmetic.ring_arith.RingElem.Insts.CoreCmpPartialEqRingElem.eq
  (self : arithmetic.ring_arith.RingElem)
  (other : arithmetic.ring_arith.RingElem) :
  Result Bool
  := do
  core.array.equality.PartialEqArray.eq core.cmp.PartialEqU16 self other

/-- Trait implementation: [kopis::arithmetic::ring_arith::{impl core::cmp::PartialEq<kopis::arithmetic::ring_arith::RingElem> for kopis::arithmetic::ring_arith::RingElem}]
    Source: 'src/arithmetic/ring_arith.rs', lines 15:13-15:22 -/
@[reducible]
def arithmetic.ring_arith.RingElem.Insts.CoreCmpPartialEqRingElem :
  core.cmp.PartialEq arithmetic.ring_arith.RingElem
  arithmetic.ring_arith.RingElem := {
  eq := arithmetic.ring_arith.RingElem.Insts.CoreCmpPartialEqRingElem.eq
}

/-- [kopis::arithmetic::matrix_arith::{impl core::cmp::PartialEq<kopis::arithmetic::matrix_arith::Matrix<X, Y>> for kopis::arithmetic::matrix_arith::Matrix<X, Y>}::eq]:
    Source: 'src/arithmetic/matrix_arith.rs', lines 12:13-12:22
    Visibility: public -/
def arithmetic.matrix_arith.Matrix.Insts.CoreCmpPartialEqMatrix.eq
  {X : Std.Usize} {Y : Std.Usize} (self : arithmetic.matrix_arith.Matrix X Y)
  (other : arithmetic.matrix_arith.Matrix X Y) :
  Result Bool
  := do
  core.array.equality.PartialEqArray.eq (Array.Insts.CoreCmpPartialEqArray Y
    arithmetic.ring_arith.RingElem.Insts.CoreCmpPartialEqRingElem) self other

/-- Trait implementation: [kopis::arithmetic::matrix_arith::{impl core::cmp::PartialEq<kopis::arithmetic::matrix_arith::Matrix<X, Y>> for kopis::arithmetic::matrix_arith::Matrix<X, Y>}]
    Source: 'src/arithmetic/matrix_arith.rs', lines 12:13-12:22 -/
@[reducible]
def arithmetic.matrix_arith.Matrix.Insts.CoreCmpPartialEqMatrix (X : Std.Usize)
  (Y : Std.Usize) : core.cmp.PartialEq (arithmetic.matrix_arith.Matrix X Y)
  (arithmetic.matrix_arith.Matrix X Y) := {
  eq := arithmetic.matrix_arith.Matrix.Insts.CoreCmpPartialEqMatrix.eq
}

/-- [kopis::arithmetic::matrix_arith::{impl core::cmp::Eq for kopis::arithmetic::matrix_arith::Matrix<X, Y>}::assert_fields_are_eq]:
    Source: 'src/arithmetic/matrix_arith.rs', lines 12:9-12:11
    Visibility: public -/
def arithmetic.matrix_arith.Matrix.Insts.CoreCmpEq.assert_fields_are_eq
  {X : Std.Usize} {Y : Std.Usize} (self : arithmetic.matrix_arith.Matrix X Y) :
  Result Unit
  := do
  ok ()

/-- Trait implementation: [kopis::arithmetic::matrix_arith::{impl core::cmp::Eq for kopis::arithmetic::matrix_arith::Matrix<X, Y>}]
    Source: 'src/arithmetic/matrix_arith.rs', lines 12:9-12:11 -/
@[reducible]
def arithmetic.matrix_arith.Matrix.Insts.CoreCmpEq (X : Std.Usize) (Y :
  Std.Usize) : core.cmp.Eq (arithmetic.matrix_arith.Matrix X Y) := {
  partialEqInst := arithmetic.matrix_arith.Matrix.Insts.CoreCmpPartialEqMatrix
    X Y
  assert_fields_are_eq :=
    arithmetic.matrix_arith.Matrix.Insts.CoreCmpEq.assert_fields_are_eq
}

/-- Trait implementation: [kopis::arithmetic::matrix_arith::{impl core::marker::StructuralPartialEq for kopis::arithmetic::matrix_arith::Matrix<X, Y>}]
    Source: 'src/arithmetic/matrix_arith.rs', lines 12:13-12:22 -/
@[reducible]
def arithmetic.matrix_arith.Matrix.Insts.CoreMarkerStructuralPartialEq (X :
  Std.Usize) (Y : Std.Usize) : core.marker.StructuralPartialEq
  (arithmetic.matrix_arith.Matrix X Y) := {
}

/-- [kopis::arithmetic::ring_arith::{impl core::fmt::Debug for kopis::arithmetic::ring_arith::RingElem}::fmt]:
    Source: 'src/arithmetic/ring_arith.rs', lines 15:24-15:29
    Visibility: public -/
def arithmetic.ring_arith.RingElem.Insts.CoreFmtDebug.fmt
  (self : arithmetic.ring_arith.RingElem) (f : core.fmt.Formatter) :
  Result ((core.result.Result Unit core.fmt.Error) × core.fmt.Formatter)
  := do
  let dyn :=
    Dyn.mk _ (core.fmt.DebugShared (Array.Insts.CoreFmtDebug 256#usize
      core.fmt.DebugU16)) self
  core.fmt.Formatter.debug_tuple_field1_finish f (toStr "RingElem") dyn

/-- Trait implementation: [kopis::arithmetic::ring_arith::{impl core::fmt::Debug for kopis::arithmetic::ring_arith::RingElem}]
    Source: 'src/arithmetic/ring_arith.rs', lines 15:24-15:29 -/
@[reducible]
def arithmetic.ring_arith.RingElem.Insts.CoreFmtDebug : core.fmt.Debug
  arithmetic.ring_arith.RingElem := {
  fmt := arithmetic.ring_arith.RingElem.Insts.CoreFmtDebug.fmt
}

/-- [kopis::arithmetic::matrix_arith::{impl core::fmt::Debug for kopis::arithmetic::matrix_arith::Matrix<X, Y>}::fmt]:
    Source: 'src/arithmetic/matrix_arith.rs', lines 12:24-12:29
    Visibility: public -/
def arithmetic.matrix_arith.Matrix.Insts.CoreFmtDebug.fmt
  {X : Std.Usize} {Y : Std.Usize} (self : arithmetic.matrix_arith.Matrix X Y)
  (f : core.fmt.Formatter) :
  Result ((core.result.Result Unit core.fmt.Error) × core.fmt.Formatter)
  := do
  let dyn :=
    Dyn.mk _ (core.fmt.DebugShared (Array.Insts.CoreFmtDebug X
      (Array.Insts.CoreFmtDebug Y
      arithmetic.ring_arith.RingElem.Insts.CoreFmtDebug))) self
  core.fmt.Formatter.debug_tuple_field1_finish f (toStr "Matrix") dyn

/-- Trait implementation: [kopis::arithmetic::matrix_arith::{impl core::fmt::Debug for kopis::arithmetic::matrix_arith::Matrix<X, Y>}]
    Source: 'src/arithmetic/matrix_arith.rs', lines 12:24-12:29 -/
@[reducible]
def arithmetic.matrix_arith.Matrix.Insts.CoreFmtDebug (X : Std.Usize) (Y :
  Std.Usize) : core.fmt.Debug (arithmetic.matrix_arith.Matrix X Y) := {
  fmt := arithmetic.matrix_arith.Matrix.Insts.CoreFmtDebug.fmt
}

/-- [kopis::arithmetic::ring_arith::{impl core::clone::Clone for kopis::arithmetic::ring_arith::RingElem}::clone]:
    Source: 'src/arithmetic/ring_arith.rs', lines 15:31-15:36
    Visibility: public -/
def arithmetic.ring_arith.RingElem.Insts.CoreCloneClone.clone
  (self : arithmetic.ring_arith.RingElem) :
  Result arithmetic.ring_arith.RingElem
  := do
  ok self

/-- Trait implementation: [kopis::arithmetic::ring_arith::{impl core::clone::Clone for kopis::arithmetic::ring_arith::RingElem}]
    Source: 'src/arithmetic/ring_arith.rs', lines 15:31-15:36 -/
@[reducible]
def arithmetic.ring_arith.RingElem.Insts.CoreCloneClone : core.clone.Clone
  arithmetic.ring_arith.RingElem := {
  clone := arithmetic.ring_arith.RingElem.Insts.CoreCloneClone.clone
}

/-- [kopis::arithmetic::matrix_arith::{impl core::clone::Clone for kopis::arithmetic::matrix_arith::Matrix<X, Y>}::clone]:
    Source: 'src/arithmetic/matrix_arith.rs', lines 12:31-12:36
    Visibility: public -/
def arithmetic.matrix_arith.Matrix.Insts.CoreCloneClone.clone
  {X : Std.Usize} {Y : Std.Usize} (self : arithmetic.matrix_arith.Matrix X Y) :
  Result (arithmetic.matrix_arith.Matrix X Y)
  := do
  let a ←
    core.array.CloneArray.clone (core.clone.CloneArray Y
      arithmetic.ring_arith.RingElem.Insts.CoreCloneClone) self
  ok a

/-- Trait implementation: [kopis::arithmetic::matrix_arith::{impl core::clone::Clone for kopis::arithmetic::matrix_arith::Matrix<X, Y>}]
    Source: 'src/arithmetic/matrix_arith.rs', lines 12:31-12:36 -/
@[reducible]
def arithmetic.matrix_arith.Matrix.Insts.CoreCloneClone (X : Std.Usize) (Y :
  Std.Usize) : core.clone.Clone (arithmetic.matrix_arith.Matrix X Y) := {
  clone := arithmetic.matrix_arith.Matrix.Insts.CoreCloneClone.clone
}

/-- [kopis::arithmetic::ring_arith::{impl zeroize::Zeroize for kopis::arithmetic::ring_arith::RingElem}::zeroize]:
    Source: 'src/arithmetic/ring_arith.rs', lines 15:44-15:51
    Visibility: public -/
def arithmetic.ring_arith.RingElem.Insts.ZeroizeZeroize.zeroize
  (self : arithmetic.ring_arith.RingElem) :
  Result arithmetic.ring_arith.RingElem
  := do
  let __zeroize_field_0 ←
    Array.Insts.ZeroizeZeroize.zeroize (zeroize.Zeroize.Blanket
      U16.Insts.ZeroizeDefaultIsZeroes) self
  ok __zeroize_field_0

/-- Trait implementation: [kopis::arithmetic::ring_arith::{impl zeroize::Zeroize for kopis::arithmetic::ring_arith::RingElem}]
    Source: 'src/arithmetic/ring_arith.rs', lines 15:44-15:51 -/
@[reducible]
def arithmetic.ring_arith.RingElem.Insts.ZeroizeZeroize : zeroize.Zeroize
  arithmetic.ring_arith.RingElem := {
  zeroize := arithmetic.ring_arith.RingElem.Insts.ZeroizeZeroize.zeroize
}

/-- [kopis::arithmetic::matrix_arith::{impl zeroize::Zeroize for kopis::arithmetic::matrix_arith::Matrix<X, Y>}::zeroize]:
    Source: 'src/arithmetic/matrix_arith.rs', lines 12:38-12:45
    Visibility: public -/
def arithmetic.matrix_arith.Matrix.Insts.ZeroizeZeroize.zeroize
  {X : Std.Usize} {Y : Std.Usize} (self : arithmetic.matrix_arith.Matrix X Y) :
  Result (arithmetic.matrix_arith.Matrix X Y)
  := do
  let __zeroize_field_0 ←
    Array.Insts.ZeroizeZeroize.zeroize (Array.Insts.ZeroizeZeroize Y
      arithmetic.ring_arith.RingElem.Insts.ZeroizeZeroize) self
  ok __zeroize_field_0

/-- Trait implementation: [kopis::arithmetic::matrix_arith::{impl zeroize::Zeroize for kopis::arithmetic::matrix_arith::Matrix<X, Y>}]
    Source: 'src/arithmetic/matrix_arith.rs', lines 12:38-12:45 -/
@[reducible]
def arithmetic.matrix_arith.Matrix.Insts.ZeroizeZeroize (X : Std.Usize) (Y :
  Std.Usize) : zeroize.Zeroize (arithmetic.matrix_arith.Matrix X Y) := {
  zeroize := arithmetic.matrix_arith.Matrix.Insts.ZeroizeZeroize.zeroize
}

/-- [kopis::arithmetic::ring_arith::{impl core::default::Default for kopis::arithmetic::ring_arith::RingElem}::default]:
    Source: 'src/arithmetic/ring_arith.rs', lines 19:4-21:5
    Visibility: public -/
def arithmetic.ring_arith.RingElem.Insts.CoreDefaultDefault.default
  : Result arithmetic.ring_arith.RingElem := do
  let a := Array.repeat 256#usize 0#u16
  ok a

/-- [kopis::arithmetic::matrix_arith::{impl core::default::Default for kopis::arithmetic::matrix_arith::Matrix<X, Y>}::default]:
    Source: 'src/arithmetic/matrix_arith.rs', lines 16:4-18:5
    Visibility: public -/
def arithmetic.matrix_arith.Matrix.Insts.CoreDefaultDefault.default
  (X : Std.Usize) (Y : Std.Usize) :
  Result (arithmetic.matrix_arith.Matrix X Y)
  := do
  let re ← arithmetic.ring_arith.RingElem.Insts.CoreDefaultDefault.default
  let a := Array.repeat Y re
  let a1 := Array.repeat X a
  ok a1

/-- Trait implementation: [kopis::arithmetic::matrix_arith::{impl core::default::Default for kopis::arithmetic::matrix_arith::Matrix<X, Y>}]
    Source: 'src/arithmetic/matrix_arith.rs', lines 15:0-19:1 -/
@[reducible]
def arithmetic.matrix_arith.Matrix.Insts.CoreDefaultDefault (X : Std.Usize) (Y
  : Std.Usize) : core.default.Default (arithmetic.matrix_arith.Matrix X Y) := {
  default := arithmetic.matrix_arith.Matrix.Insts.CoreDefaultDefault.default X
    Y
}

/-- [kopis::arithmetic::ring_arith::{kopis::arithmetic::ring_arith::RingElem}::shift_right]: loop 0:
    Source: 'src/arithmetic/ring_arith.rs', lines 96:8-98:9 -/
@[rust_loop]
def arithmetic.ring_arith.RingElem.shift_right_loop
  (iter : core.slice.iter.IterMut Std.U16)
  (back : core.slice.iter.IterMut Std.U16 → core.slice.iter.IterMut Std.U16)
  (shift : Std.Usize) :
  Result (core.slice.iter.IterMut Std.U16)
  := do
  let (o, iter1, next_back) ← core.slice.iter.IteratorIterMut.next iter
  match o with
  | none => ok (let im := next_back iter1 none
                back im)
  | some coeff =>
    let coeff1 ← coeff >>> shift
    arithmetic.ring_arith.RingElem.shift_right_loop iter1
      (fun im => let im1 := next_back im (some coeff1)
                 back im1) shift
partial_fixpoint

/-- [kopis::arithmetic::ring_arith::{kopis::arithmetic::ring_arith::RingElem}::shift_right]:
    Source: 'src/arithmetic/ring_arith.rs', lines 95:4-99:5 -/
def arithmetic.ring_arith.RingElem.shift_right
  (self : arithmetic.ring_arith.RingElem) (shift : Std.Usize) :
  Result arithmetic.ring_arith.RingElem
  := do
  let (s, to_slice_mut_back) ← lift (Array.to_slice_mut self)
  let (iter, iter_mut_back) ← core.slice.Slice.iter_mut s
  let back ←
    arithmetic.ring_arith.RingElem.shift_right_loop iter (fun im => im) shift
  let s1 := iter_mut_back back
  let a := to_slice_mut_back s1
  ok a

/-- [kopis::arithmetic::matrix_arith::{kopis::arithmetic::matrix_arith::Matrix<X, Y>}::shift_right]: loop 1:
    Source: 'src/arithmetic/matrix_arith.rs', lines 25:12-27:13 -/
@[rust_loop]
def arithmetic.matrix_arith.Matrix.shift_right_loop0_loop0
  (iter : core.slice.iter.IterMut arithmetic.ring_arith.RingElem)
  (back : core.slice.iter.IterMut arithmetic.ring_arith.RingElem →
  core.slice.iter.IterMut arithmetic.ring_arith.RingElem) (shift : Std.Usize) :
  Result ((core.slice.iter.IterMut arithmetic.ring_arith.RingElem) ×
    (core.slice.iter.IterMut arithmetic.ring_arith.RingElem →
    core.slice.iter.IterMut arithmetic.ring_arith.RingElem))
  := do
  let (o, iter1, next_back) ← core.slice.iter.IteratorIterMut.next iter
  match o with
  | none => ok (iter1, fun im => let im1 := next_back im none
                                 back im1)
  | some elem =>
    let elem1 ← arithmetic.ring_arith.RingElem.shift_right elem shift
    arithmetic.matrix_arith.Matrix.shift_right_loop0_loop0 iter1
      (fun im => let im1 := next_back im (some elem1)
                 back im1) shift
partial_fixpoint

/-- [kopis::arithmetic::matrix_arith::{kopis::arithmetic::matrix_arith::Matrix<X, Y>}::shift_right]: loop 0:
    Source: 'src/arithmetic/matrix_arith.rs', lines 24:8-28:9 -/
@[rust_loop]
def arithmetic.matrix_arith.Matrix.shift_right_loop0
  {Y : Std.Usize}
  (iter : core.slice.iter.IterMut (Array arithmetic.ring_arith.RingElem Y))
  (back : core.slice.iter.IterMut (Array arithmetic.ring_arith.RingElem Y) →
  core.slice.iter.IterMut (Array arithmetic.ring_arith.RingElem Y))
  (shift : Std.Usize) :
  Result (core.slice.iter.IterMut (Array arithmetic.ring_arith.RingElem Y))
  := do
  let (o, iter1, next_back) ← core.slice.iter.IteratorIterMut.next iter
  match o with
  | none => ok (let im := next_back iter1 none
                back im)
  | some row =>
    let (s, to_slice_mut_back) ← lift (Array.to_slice_mut row)
    let (iter2, iter_mut_back) ← core.slice.Slice.iter_mut s
    let (im, back1) ←
      arithmetic.matrix_arith.Matrix.shift_right_loop0_loop0 iter2
        (fun im1 => im1) shift
    arithmetic.matrix_arith.Matrix.shift_right_loop0 iter1
      (fun im1 =>
        let im2 := back1 im
        let s1 := iter_mut_back im2
        let a := to_slice_mut_back s1
        let im3 := next_back im1 (some a)
        back im3) shift
partial_fixpoint

/-- [kopis::arithmetic::matrix_arith::{kopis::arithmetic::matrix_arith::Matrix<X, Y>}::shift_right]:
    Source: 'src/arithmetic/matrix_arith.rs', lines 23:4-29:5 -/
def arithmetic.matrix_arith.Matrix.shift_right
  {X : Std.Usize} {Y : Std.Usize} (self : arithmetic.matrix_arith.Matrix X Y)
  (shift : Std.Usize) :
  Result (arithmetic.matrix_arith.Matrix X Y)
  := do
  let (s, to_slice_mut_back) ← lift (Array.to_slice_mut self)
  let (iter, iter_mut_back) ← core.slice.Slice.iter_mut s
  let back ←
    arithmetic.matrix_arith.Matrix.shift_right_loop0 iter (fun im => im) shift
  let s1 := iter_mut_back back
  let a := to_slice_mut_back s1
  ok a

/-- [kopis::consts::RING_DEG]
    Source: 'src/consts.rs', lines 6:0-6:39 -/
@[global_simps, irreducible] def consts.RING_DEG : Std.Usize := 256#usize

/-- [kopis::arithmetic::ring_arith::HALF]
    Source: 'src/arithmetic/ring_arith.rs', lines 131:0-131:33 -/
@[global_simps, irreducible]
def arithmetic.ring_arith.HALF : Result Std.Usize := consts.RING_DEG / 2#usize

/-- [kopis::arithmetic::ring_arith::schoolbook_128]: loop 1:
    Source: 'src/arithmetic/ring_arith.rs', lines 145:8-147:9 -/
@[rust_loop]
def arithmetic.ring_arith.schoolbook_128_loop0_loop0
  (iter : core.ops.range.Range Std.Usize) (out : Array Std.U16 256#usize)
  (b : Array Std.U16 128#usize) (i : Std.Usize) (ai : Std.U16) :
  Result (Array Std.U16 256#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok out
  | some j =>
    let i1 ← i + j
    let i2 ← Array.index_usize out i1
    let i3 ← Array.index_usize b j
    let i4 ← lift (core.num.U16.wrapping_mul ai i3)
    let i5 ← lift (core.num.U16.wrapping_add i2 i4)
    let a ← Array.update out i1 i5
    arithmetic.ring_arith.schoolbook_128_loop0_loop0 iter1 a b i ai
partial_fixpoint

/-- [kopis::arithmetic::ring_arith::schoolbook_128]: loop 0:
    Source: 'src/arithmetic/ring_arith.rs', lines 143:4-148:5 -/
@[rust_loop]
def arithmetic.ring_arith.schoolbook_128_loop0
  (i : Std.Usize) (iter : core.ops.range.Range Std.Usize)
  (out : Array Std.U16 256#usize) (a : Array Std.U16 128#usize)
  (b : Array Std.U16 128#usize) :
  Result (Array Std.U16 256#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok out
  | some i1 =>
    let ai ← Array.index_usize a i1
    let out1 ←
      arithmetic.ring_arith.schoolbook_128_loop0_loop0
        { start := 0#usize, «end» := i } out b i1 ai
    arithmetic.ring_arith.schoolbook_128_loop0 i iter1 out1 a b
partial_fixpoint

/-- [kopis::arithmetic::ring_arith::schoolbook_128]:
    Source: 'src/arithmetic/ring_arith.rs', lines 140:0-149:1 -/
def arithmetic.ring_arith.schoolbook_128
  (out : Array Std.U16 256#usize) (a : Array Std.U16 128#usize)
  (b : Array Std.U16 128#usize) :
  Result (Array Std.U16 256#usize)
  := do
  let i ← arithmetic.ring_arith.HALF
  arithmetic.ring_arith.schoolbook_128_loop0 i
    { start := 0#usize, «end» := i } out a b

/-- [kopis::arithmetic::ring_arith::ring_mul_acc]: loop 0:
    Source: 'src/arithmetic/ring_arith.rs', lines 185:4-188:5 -/
@[rust_loop]
def arithmetic.ring_arith.ring_mul_acc_loop0
  (iter : core.ops.range.Range Std.Usize) (a_lo : Array Std.U16 128#usize)
  (a_hi : Array Std.U16 128#usize) (b_lo : Array Std.U16 128#usize)
  (b_hi : Array Std.U16 128#usize) (a_sum : Array Std.U16 128#usize)
  (b_sum : Array Std.U16 128#usize) :
  Result ((Array Std.U16 128#usize) × (Array Std.U16 128#usize))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (a_sum, b_sum)
  | some i =>
    let i1 ← Array.index_usize a_lo i
    let i2 ← Array.index_usize a_hi i
    let i3 ← lift (core.num.U16.wrapping_add i1 i2)
    let a ← Array.update a_sum i i3
    let i4 ← Array.index_usize b_lo i
    let i5 ← Array.index_usize b_hi i
    let i6 ← lift (core.num.U16.wrapping_add i4 i5)
    let a1 ← Array.update b_sum i i6
    arithmetic.ring_arith.ring_mul_acc_loop0 iter1 a_lo a_hi b_lo b_hi a a1
partial_fixpoint

/-- [kopis::arithmetic::ring_arith::ring_mul_acc]: loop 1:
    Source: 'src/arithmetic/ring_arith.rs', lines 194:4-214:5 -/
@[rust_loop]
def arithmetic.ring_arith.ring_mul_acc_loop1
  (i : Std.Usize) (iter : core.ops.range.Range Std.Usize)
  (acc : arithmetic.ring_arith.RingElem) (z0 : Array Std.U16 256#usize)
  (z2 : Array Std.U16 256#usize) (z3 : Array Std.U16 256#usize) :
  Result arithmetic.ring_arith.RingElem
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok acc
  | some j =>
    let i1 ← j + i
    let i2 ← Array.index_usize z3 i1
    let i3 ← j + i
    let i4 ← Array.index_usize z0 i3
    let i5 ← lift (core.num.U16.wrapping_sub i2 i4)
    let i6 ← j + i
    let i7 ← Array.index_usize z2 i6
    let z1_wrap ← lift (core.num.U16.wrapping_sub i5 i7)
    let i8 ← Array.index_usize acc j
    let i9 ← Array.index_usize z0 j
    let i10 ← lift (core.num.U16.wrapping_add i8 i9)
    let i11 ← Array.index_usize z2 j
    let i12 ← lift (core.num.U16.wrapping_sub i10 i11)
    let i13 ← lift (core.num.U16.wrapping_sub i12 z1_wrap)
    let a ← Array.update acc j i13
    let i14 ← Array.index_usize z3 j
    let i15 ← lift (core.num.U16.wrapping_sub i14 i9)
    let z1_direct ← lift (core.num.U16.wrapping_sub i15 i11)
    let i16 ← j + i
    let i17 ← Array.index_usize a i16
    let i18 ← j + i
    let i19 ← Array.index_usize z0 i18
    let i20 ← lift (core.num.U16.wrapping_add i17 i19)
    let i21 ← j + i
    let i22 ← Array.index_usize z2 i21
    let i23 ← lift (core.num.U16.wrapping_sub i20 i22)
    let i24 ← lift (core.num.U16.wrapping_add i23 z1_direct)
    let i25 ← j + i
    let a1 ← Array.update a i25 i24
    arithmetic.ring_arith.ring_mul_acc_loop1 i iter1 a1 z0 z2 z3
partial_fixpoint

/-- [kopis::arithmetic::ring_arith::ring_mul_acc]:
    Source: 'src/arithmetic/ring_arith.rs', lines 156:0-215:1 -/
def arithmetic.ring_arith.ring_mul_acc
  (acc : arithmetic.ring_arith.RingElem) (a : arithmetic.ring_arith.RingElem)
  (b : arithmetic.ring_arith.RingElem) :
  Result arithmetic.ring_arith.RingElem
  := do
  let i ← arithmetic.ring_arith.HALF
  let s ←
    core.array.Array.index (core.ops.index.IndexSlice
      (core.slice.index.SliceIndexRangeToUsizeSlice Std.U16)) a
      { «end» := i }
  let r ← core.array.TryFromSharedArraySlice.try_from 128#usize s
  let a_lo ← core.result.Result.unwrap core.fmt.DebugTryFromSliceError r
  let s1 ←
    core.array.Array.index (core.ops.index.IndexSlice
      (core.slice.index.SliceIndexRangeFromUsizeSlice Std.U16)) a
      { start := i }
  let r1 ← core.array.TryFromSharedArraySlice.try_from 128#usize s1
  let a_hi ← core.result.Result.unwrap core.fmt.DebugTryFromSliceError r1
  let s2 ←
    core.array.Array.index (core.ops.index.IndexSlice
      (core.slice.index.SliceIndexRangeToUsizeSlice Std.U16)) b
      { «end» := i }
  let r2 ← core.array.TryFromSharedArraySlice.try_from 128#usize s2
  let b_lo ← core.result.Result.unwrap core.fmt.DebugTryFromSliceError r2
  let s3 ←
    core.array.Array.index (core.ops.index.IndexSlice
      (core.slice.index.SliceIndexRangeFromUsizeSlice Std.U16)) b
      { start := i }
  let r3 ← core.array.TryFromSharedArraySlice.try_from 128#usize s3
  let b_hi ← core.result.Result.unwrap core.fmt.DebugTryFromSliceError r3
  let z0 := Array.repeat 256#usize 0#u16
  let z2 := Array.repeat 256#usize 0#u16
  let z01 ← arithmetic.ring_arith.schoolbook_128 z0 a_lo b_lo
  let z21 ← arithmetic.ring_arith.schoolbook_128 z2 a_hi b_hi
  let a_sum := Array.repeat 128#usize 0#u16
  let b_sum := Array.repeat 128#usize 0#u16
  let (a_sum1, b_sum1) ←
    arithmetic.ring_arith.ring_mul_acc_loop0 { start := 0#usize, «end» := i }
      a_lo a_hi b_lo b_hi a_sum b_sum
  let z3 := Array.repeat 256#usize 0#u16
  let z31 ← arithmetic.ring_arith.schoolbook_128 z3 a_sum1 b_sum1
  arithmetic.ring_arith.ring_mul_acc_loop1 i { start := 0#usize, «end» := i }
    acc z01 z21 z31

/-- [kopis::arithmetic::matrix_arith::{kopis::arithmetic::matrix_arith::Matrix<X, Y>}::mul]: loop 2:
    Source: 'src/arithmetic/matrix_arith.rs', lines 45:16-47:17 -/
@[rust_loop]
def arithmetic.matrix_arith.Matrix.mul_loop0_loop0_loop0
  {X : Std.Usize} {Y : Std.Usize} {Z : Std.Usize}
  (iter : core.ops.range.Range Std.Usize)
  (self : arithmetic.matrix_arith.Matrix X Y)
  (other : arithmetic.matrix_arith.Matrix Y Z)
  (result : arithmetic.matrix_arith.Matrix X Z) (i : Std.Usize) (j : Std.Usize)
  :
  Result ((arithmetic.matrix_arith.Matrix X Y) ×
    (arithmetic.matrix_arith.Matrix Y Z) × (arithmetic.matrix_arith.Matrix X
    Z))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (self, other, result)
  | some k =>
    let (a, index_mut_back) ← Array.index_mut_usize result i
    let (re, index_mut_back1) ← Array.index_mut_usize a k
    let a1 ← Array.index_usize self i
    let re1 ← Array.index_usize a1 j
    let a2 ← Array.index_usize other j
    let re2 ← Array.index_usize a2 k
    let re3 ← arithmetic.ring_arith.ring_mul_acc re re1 re2
    let a3 := index_mut_back1 re3
    let a4 := index_mut_back a3
    arithmetic.matrix_arith.Matrix.mul_loop0_loop0_loop0 iter1 self other a4 i
      j
partial_fixpoint

/-- [kopis::arithmetic::matrix_arith::{kopis::arithmetic::matrix_arith::Matrix<X, Y>}::mul]: loop 1:
    Source: 'src/arithmetic/matrix_arith.rs', lines 44:12-48:13 -/
@[rust_loop]
def arithmetic.matrix_arith.Matrix.mul_loop0_loop0
  {X : Std.Usize} {Y : Std.Usize} {Z : Std.Usize}
  (iter : core.ops.range.Range Std.Usize)
  (self : arithmetic.matrix_arith.Matrix X Y)
  (other : arithmetic.matrix_arith.Matrix Y Z)
  (result : arithmetic.matrix_arith.Matrix X Z) (i : Std.Usize) :
  Result ((arithmetic.matrix_arith.Matrix X Y) ×
    (arithmetic.matrix_arith.Matrix Y Z) × (arithmetic.matrix_arith.Matrix X
    Z))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (self, other, result)
  | some j =>
    let (self1, other1, result1) ←
      arithmetic.matrix_arith.Matrix.mul_loop0_loop0_loop0
        { start := 0#usize, «end» := Z } self other result i j
    arithmetic.matrix_arith.Matrix.mul_loop0_loop0 iter1 self1 other1 result1 i
partial_fixpoint

/-- [kopis::arithmetic::matrix_arith::{kopis::arithmetic::matrix_arith::Matrix<X, Y>}::mul]: loop 0:
    Source: 'src/arithmetic/matrix_arith.rs', lines 43:8-49:9 -/
@[rust_loop]
def arithmetic.matrix_arith.Matrix.mul_loop0
  {X : Std.Usize} {Y : Std.Usize} {Z : Std.Usize}
  (iter : core.ops.range.Range Std.Usize)
  (self : arithmetic.matrix_arith.Matrix X Y)
  (other : arithmetic.matrix_arith.Matrix Y Z)
  (result : arithmetic.matrix_arith.Matrix X Z) :
  Result (arithmetic.matrix_arith.Matrix X Z)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok result
  | some i =>
    let (self1, other1, result1) ←
      arithmetic.matrix_arith.Matrix.mul_loop0_loop0
        { start := 0#usize, «end» := Y } self other result i
    arithmetic.matrix_arith.Matrix.mul_loop0 iter1 self1 other1 result1
partial_fixpoint

/-- [kopis::arithmetic::matrix_arith::{kopis::arithmetic::matrix_arith::Matrix<X, Y>}::mul]:
    Source: 'src/arithmetic/matrix_arith.rs', lines 41:4-52:5 -/
def arithmetic.matrix_arith.Matrix.mul
  {X : Std.Usize} {Y : Std.Usize} {Z : Std.Usize}
  (self : arithmetic.matrix_arith.Matrix X Y)
  (other : arithmetic.matrix_arith.Matrix Y Z) :
  Result (arithmetic.matrix_arith.Matrix X Z)
  := do
  let result ←
    arithmetic.matrix_arith.Matrix.Insts.CoreDefaultDefault.default X Z
  arithmetic.matrix_arith.Matrix.mul_loop0 { start := 0#usize, «end» := X }
    self other result

/-- [kopis::arithmetic::matrix_arith::{kopis::arithmetic::matrix_arith::Matrix<X, Y>}::mul_transpose]: loop 2:
    Source: 'src/arithmetic/matrix_arith.rs', lines 62:16-64:17 -/
@[rust_loop]
def arithmetic.matrix_arith.Matrix.mul_transpose_loop0_loop0_loop0
  {X : Std.Usize} {Y : Std.Usize} {Z : Std.Usize}
  (iter : core.ops.range.Range Std.Usize)
  (self : arithmetic.matrix_arith.Matrix X Y)
  (other : arithmetic.matrix_arith.Matrix X Z)
  (result : arithmetic.matrix_arith.Matrix Y Z) (i : Std.Usize) (j : Std.Usize)
  :
  Result ((arithmetic.matrix_arith.Matrix X Y) ×
    (arithmetic.matrix_arith.Matrix X Z) × (arithmetic.matrix_arith.Matrix Y
    Z))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (self, other, result)
  | some k =>
    let (a, index_mut_back) ← Array.index_mut_usize result j
    let (re, index_mut_back1) ← Array.index_mut_usize a k
    let a1 ← Array.index_usize self i
    let re1 ← Array.index_usize a1 j
    let a2 ← Array.index_usize other i
    let re2 ← Array.index_usize a2 k
    let re3 ← arithmetic.ring_arith.ring_mul_acc re re1 re2
    let a3 := index_mut_back1 re3
    let a4 := index_mut_back a3
    arithmetic.matrix_arith.Matrix.mul_transpose_loop0_loop0_loop0 iter1 self
      other a4 i j
partial_fixpoint

/-- [kopis::arithmetic::matrix_arith::{kopis::arithmetic::matrix_arith::Matrix<X, Y>}::mul_transpose]: loop 1:
    Source: 'src/arithmetic/matrix_arith.rs', lines 61:12-65:13 -/
@[rust_loop]
def arithmetic.matrix_arith.Matrix.mul_transpose_loop0_loop0
  {X : Std.Usize} {Y : Std.Usize} {Z : Std.Usize}
  (iter : core.ops.range.Range Std.Usize)
  (self : arithmetic.matrix_arith.Matrix X Y)
  (other : arithmetic.matrix_arith.Matrix X Z)
  (result : arithmetic.matrix_arith.Matrix Y Z) (i : Std.Usize) :
  Result ((arithmetic.matrix_arith.Matrix X Y) ×
    (arithmetic.matrix_arith.Matrix X Z) × (arithmetic.matrix_arith.Matrix Y
    Z))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (self, other, result)
  | some j =>
    let (self1, other1, result1) ←
      arithmetic.matrix_arith.Matrix.mul_transpose_loop0_loop0_loop0
        { start := 0#usize, «end» := Z } self other result i j
    arithmetic.matrix_arith.Matrix.mul_transpose_loop0_loop0 iter1 self1 other1
      result1 i
partial_fixpoint

/-- [kopis::arithmetic::matrix_arith::{kopis::arithmetic::matrix_arith::Matrix<X, Y>}::mul_transpose]: loop 0:
    Source: 'src/arithmetic/matrix_arith.rs', lines 60:8-66:9 -/
@[rust_loop]
def arithmetic.matrix_arith.Matrix.mul_transpose_loop0
  {X : Std.Usize} {Y : Std.Usize} {Z : Std.Usize}
  (iter : core.ops.range.Range Std.Usize)
  (self : arithmetic.matrix_arith.Matrix X Y)
  (other : arithmetic.matrix_arith.Matrix X Z)
  (result : arithmetic.matrix_arith.Matrix Y Z) :
  Result (arithmetic.matrix_arith.Matrix Y Z)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok result
  | some i =>
    let (self1, other1, result1) ←
      arithmetic.matrix_arith.Matrix.mul_transpose_loop0_loop0
        { start := 0#usize, «end» := Y } self other result i
    arithmetic.matrix_arith.Matrix.mul_transpose_loop0 iter1 self1 other1
      result1
partial_fixpoint

/-- [kopis::arithmetic::matrix_arith::{kopis::arithmetic::matrix_arith::Matrix<X, Y>}::mul_transpose]:
    Source: 'src/arithmetic/matrix_arith.rs', lines 58:4-69:5 -/
def arithmetic.matrix_arith.Matrix.mul_transpose
  {X : Std.Usize} {Y : Std.Usize} {Z : Std.Usize}
  (self : arithmetic.matrix_arith.Matrix X Y)
  (other : arithmetic.matrix_arith.Matrix X Z) :
  Result (arithmetic.matrix_arith.Matrix Y Z)
  := do
  let result ←
    arithmetic.matrix_arith.Matrix.Insts.CoreDefaultDefault.default Y Z
  arithmetic.matrix_arith.Matrix.mul_transpose_loop0
    { start := 0#usize, «end» := X } self other result

/-- [kopis::arithmetic::ring_arith::{kopis::arithmetic::ring_arith::RingElem}::wrapping_add_to_all]: loop 0:
    Source: 'src/arithmetic/ring_arith.rs', lines 112:8-114:9 -/
@[rust_loop]
def arithmetic.ring_arith.RingElem.wrapping_add_to_all_loop
  (iter : core.slice.iter.IterMut Std.U16)
  (back : core.slice.iter.IterMut Std.U16 → core.slice.iter.IterMut Std.U16)
  (val : Std.U16) :
  Result (core.slice.iter.IterMut Std.U16)
  := do
  let (o, iter1, next_back) ← core.slice.iter.IteratorIterMut.next iter
  match o with
  | none => ok (let im := next_back iter1 none
                back im)
  | some coeff =>
    let coeff1 ← lift (core.num.U16.wrapping_add coeff val)
    arithmetic.ring_arith.RingElem.wrapping_add_to_all_loop iter1
      (fun im => let im1 := next_back im (some coeff1)
                 back im1) val
partial_fixpoint

/-- [kopis::arithmetic::ring_arith::{kopis::arithmetic::ring_arith::RingElem}::wrapping_add_to_all]:
    Source: 'src/arithmetic/ring_arith.rs', lines 111:4-115:5 -/
def arithmetic.ring_arith.RingElem.wrapping_add_to_all
  (self : arithmetic.ring_arith.RingElem) (val : Std.U16) :
  Result arithmetic.ring_arith.RingElem
  := do
  let (s, to_slice_mut_back) ← lift (Array.to_slice_mut self)
  let (iter, iter_mut_back) ← core.slice.Slice.iter_mut s
  let back ←
    arithmetic.ring_arith.RingElem.wrapping_add_to_all_loop iter (fun im => im)
      val
  let s1 := iter_mut_back back
  let a := to_slice_mut_back s1
  ok a

/-- [kopis::arithmetic::matrix_arith::{kopis::arithmetic::matrix_arith::Matrix<X, Y>}::wrapping_add_to_all]: loop 1:
    Source: 'src/arithmetic/matrix_arith.rs', lines 74:12-76:13 -/
@[rust_loop]
def arithmetic.matrix_arith.Matrix.wrapping_add_to_all_loop0_loop0
  (iter : core.slice.iter.IterMut arithmetic.ring_arith.RingElem)
  (back : core.slice.iter.IterMut arithmetic.ring_arith.RingElem →
  core.slice.iter.IterMut arithmetic.ring_arith.RingElem) (val : Std.U16) :
  Result ((core.slice.iter.IterMut arithmetic.ring_arith.RingElem) ×
    (core.slice.iter.IterMut arithmetic.ring_arith.RingElem →
    core.slice.iter.IterMut arithmetic.ring_arith.RingElem))
  := do
  let (o, iter1, next_back) ← core.slice.iter.IteratorIterMut.next iter
  match o with
  | none => ok (iter1, fun im => let im1 := next_back im none
                                 back im1)
  | some elem =>
    let elem1 ← arithmetic.ring_arith.RingElem.wrapping_add_to_all elem val
    arithmetic.matrix_arith.Matrix.wrapping_add_to_all_loop0_loop0 iter1
      (fun im => let im1 := next_back im (some elem1)
                 back im1) val
partial_fixpoint

/-- [kopis::arithmetic::matrix_arith::{kopis::arithmetic::matrix_arith::Matrix<X, Y>}::wrapping_add_to_all]: loop 0:
    Source: 'src/arithmetic/matrix_arith.rs', lines 73:8-77:9 -/
@[rust_loop]
def arithmetic.matrix_arith.Matrix.wrapping_add_to_all_loop0
  {Y : Std.Usize}
  (iter : core.slice.iter.IterMut (Array arithmetic.ring_arith.RingElem Y))
  (back : core.slice.iter.IterMut (Array arithmetic.ring_arith.RingElem Y) →
  core.slice.iter.IterMut (Array arithmetic.ring_arith.RingElem Y))
  (val : Std.U16) :
  Result (core.slice.iter.IterMut (Array arithmetic.ring_arith.RingElem Y))
  := do
  let (o, iter1, next_back) ← core.slice.iter.IteratorIterMut.next iter
  match o with
  | none => ok (let im := next_back iter1 none
                back im)
  | some row =>
    let (s, to_slice_mut_back) ← lift (Array.to_slice_mut row)
    let (iter2, iter_mut_back) ← core.slice.Slice.iter_mut s
    let (im, back1) ←
      arithmetic.matrix_arith.Matrix.wrapping_add_to_all_loop0_loop0 iter2
        (fun im1 => im1) val
    arithmetic.matrix_arith.Matrix.wrapping_add_to_all_loop0 iter1
      (fun im1 =>
        let im2 := back1 im
        let s1 := iter_mut_back im2
        let a := to_slice_mut_back s1
        let im3 := next_back im1 (some a)
        back im3) val
partial_fixpoint

/-- [kopis::arithmetic::matrix_arith::{kopis::arithmetic::matrix_arith::Matrix<X, Y>}::wrapping_add_to_all]:
    Source: 'src/arithmetic/matrix_arith.rs', lines 72:4-78:5 -/
def arithmetic.matrix_arith.Matrix.wrapping_add_to_all
  {X : Std.Usize} {Y : Std.Usize} (self : arithmetic.matrix_arith.Matrix X Y)
  (val : Std.U16) :
  Result (arithmetic.matrix_arith.Matrix X Y)
  := do
  let (s, to_slice_mut_back) ← lift (Array.to_slice_mut self)
  let (iter, iter_mut_back) ← core.slice.Slice.iter_mut s
  let back ←
    arithmetic.matrix_arith.Matrix.wrapping_add_to_all_loop0 iter
      (fun im => im) val
  let s1 := iter_mut_back back
  let a := to_slice_mut_back s1
  ok a

/-- [kopis::ser::serialize]: loop 1:
    Source: 'src/ser.rs', lines 117:8-122:9 -/
@[rust_loop]
def ser.serialize_loop0_loop0
  (out_buf : Slice Std.U8) (window : Std.U32) (bits_in_window : Std.Usize)
  (byte_pos : Std.Usize) :
  Result ((Slice Std.U8) × Std.U32 × Std.Usize × Std.Usize)
  := do
  if bits_in_window >= 8#usize
  then
    let i ← lift (UScalar.cast .U8 window)
    let s ← Slice.update out_buf byte_pos i
    let window1 ← window >>> 8#i32
    let bits_in_window1 ← bits_in_window - 8#usize
    let byte_pos1 ← byte_pos + 1#usize
    ser.serialize_loop0_loop0 s window1 bits_in_window1 byte_pos1
  else ok (out_buf, window, bits_in_window, byte_pos)
partial_fixpoint

/-- [kopis::ser::serialize]: loop 0:
    Source: 'src/ser.rs', lines 111:4-123:5 -/
@[rust_loop]
def ser.serialize_loop0
  (iter : core.slice.iter.Iter Std.U16) (out_buf : Slice Std.U8)
  (bits_per_elem : Std.Usize) (bitmask : Std.U32) (window : Std.U32)
  (bits_in_window : Std.Usize) (byte_pos : Std.Usize) :
  Result ((Slice Std.U8) × Std.Usize)
  := do
  let (o, iter1) ← core.slice.iter.IteratorSliceIter.next iter
  match o with
  | none => ok (out_buf, bits_in_window)
  | some elem =>
    let i ← lift (UScalar.cast .U32 elem)
    let i1 ← lift (i &&& bitmask)
    let i2 ← i1 <<< bits_in_window
    let window1 ← lift (window ||| i2)
    let bits_in_window1 ← bits_in_window + bits_per_elem
    let (out_buf1, window2, bits_in_window2, byte_pos1) ←
      ser.serialize_loop0_loop0 out_buf window1 bits_in_window1 byte_pos
    ser.serialize_loop0 iter1 out_buf1 bits_per_elem bitmask window2
      bits_in_window2 byte_pos1
partial_fixpoint

/-- [kopis::ser::serialize]:
    Source: 'src/ser.rs', lines 100:0-127:1 -/
def ser.serialize
  (data : Slice Std.U16) (out_buf : Slice Std.U8) (bits_per_elem : Std.Usize) :
  Result (Slice Std.U8)
  := do
  let left_val := Slice.len out_buf
  let i := Slice.len data
  let i1 ← bits_per_elem * i
  let right_val ← i1 / 8#usize
  massert (left_val = right_val)
  let i2 ← 1#u32 <<< bits_per_elem
  let bitmask ← i2 - 1#u32
  let iter ← core.slice.Slice.iter data
  let (out_buf1, bits_in_window) ←
    ser.serialize_loop0 iter out_buf bits_per_elem bitmask 0#u32 0#usize
      0#usize
  massert (bits_in_window = 0#usize)
  ok out_buf1

/-- [kopis::ser::serialize_10::closure]
    Source: 'src/ser.rs', lines 87:16-87:46 -/
def ser.serialize_10.closure := Array Std.U16 256#usize × Std.Usize

/-- [kopis::ser::serialize_10::{impl core::ops::function::Fn<(usize,), u16> for kopis::ser::serialize_10::closure<'_0, '_1>}::call]:
    Source: 'src/ser.rs', lines 87:16-87:46 -/
def ser.serialize_10.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call
  (c : ser.serialize_10.closure) (tupled_args : Std.Usize) :
  Result Std.U16
  := do
  let (a, i) := c
  let i1 ← i + tupled_args
  let i2 ← Array.index_usize a i1
  ok (i2 &&& 1023#u16)

/-- [kopis::ser::serialize_10]: loop 0:
    Source: 'src/ser.rs', lines 83:4-94:5 -/
@[rust_loop]
def ser.serialize_10_loop
  (iter : core.ops.range.Range Std.Usize) (data : Array Std.U16 256#usize)
  (out_buf : Array Std.U8 320#usize) :
  Result (Array Std.U8 320#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok out_buf
  | some g =>
    let o1 ← 4#usize * g
    let b ← 5#usize * g
    let i ←
      ser.serialize_10.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call (data,
        o1) 0#usize
    let i1 ← lift (UScalar.cast .U8 i)
    let out_buf1 ← Array.update out_buf b i1
    let i2 ← i >>> 8#i32
    let i3 ←
      ser.serialize_10.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call (data,
        o1) 1#usize
    let i4 ← i3 <<< 2#i32
    let i5 ← lift (i2 ||| i4)
    let i6 ← b + 1#usize
    let i7 ← lift (UScalar.cast .U8 i5)
    let out_buf2 ← Array.update out_buf1 i6 i7
    let i8 ← i3 >>> 6#i32
    let i9 ←
      ser.serialize_10.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call (data,
        o1) 2#usize
    let i10 ← i9 <<< 4#i32
    let i11 ← lift (i8 ||| i10)
    let i12 ← b + 2#usize
    let i13 ← lift (UScalar.cast .U8 i11)
    let out_buf3 ← Array.update out_buf2 i12 i13
    let i14 ← i9 >>> 4#i32
    let i15 ←
      ser.serialize_10.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call (data,
        o1) 3#usize
    let i16 ← i15 <<< 6#i32
    let i17 ← lift (i14 ||| i16)
    let i18 ← b + 3#usize
    let i19 ← lift (UScalar.cast .U8 i17)
    let out_buf4 ← Array.update out_buf3 i18 i19
    let i20 ← i15 >>> 2#i32
    let i21 ← b + 4#usize
    let i22 ← lift (UScalar.cast .U8 i20)
    let a ← Array.update out_buf4 i21 i22
    ser.serialize_10_loop iter1 data a
partial_fixpoint

/-- [kopis::ser::serialize_10]:
    Source: 'src/ser.rs', lines 81:0-95:1 -/
@[reducible]
def ser.serialize_10
  (data : Array Std.U16 256#usize) (out_buf : Array Std.U8 320#usize) :
  Result (Array Std.U8 320#usize)
  := do
  ser.serialize_10_loop { start := 0#usize, «end» := 64#usize } data out_buf

/-- [kopis::consts::MODULUS_P_BITS]
    Source: 'src/consts.rs', lines 4:0-4:44 -/
@[global_simps, irreducible] def consts.MODULUS_P_BITS : Std.Usize := 10#usize

/-- [kopis::arithmetic::ring_arith::{kopis::arithmetic::ring_arith::RingElem}::serialize]:
    Source: 'src/arithmetic/ring_arith.rs', lines 80:4-90:5 -/
def arithmetic.ring_arith.RingElem.serialize
  (self : arithmetic.ring_arith.RingElem) (out_buf : Slice Std.U8)
  (bits_per_elem : Std.Usize) :
  Result (Slice Std.U8)
  := do
  let left_val := Slice.len out_buf
  let i ← bits_per_elem * consts.RING_DEG
  let right_val ← i / 8#usize
  massert (left_val = right_val)
  if bits_per_elem = consts.MODULUS_P_BITS
  then
    let (r, try_from_back) ←
      core.array.TryFromMutArraySlice.try_from 320#usize out_buf
    let (arr, unwrap_back) ←
      core.result.Result.unwrap.mut core.fmt.DebugTryFromSliceError r
    let arr1 ← ser.serialize_10 self arr
    let r1 := unwrap_back arr1
    ok (try_from_back r1)
  else
    let s ← lift (Array.to_slice self)
    ser.serialize s out_buf bits_per_elem

/-- [kopis::arithmetic::matrix_arith::{kopis::arithmetic::matrix_arith::Matrix<X, Y>}::serialize]: loop 1:
    Source: 'src/arithmetic/matrix_arith.rs', lines 98:12-102:13 -/
@[rust_loop]
def arithmetic.matrix_arith.Matrix.serialize_loop0_loop0
  {X : Std.Usize} {Y : Std.Usize} (iter : core.ops.range.Range Std.Usize)
  (self : arithmetic.matrix_arith.Matrix X Y) (out_buf : Slice Std.U8)
  (bits_per_elem : Std.Usize) (chunk_len : Std.Usize) (i : Std.Usize) :
  Result ((arithmetic.matrix_arith.Matrix X Y) × (Slice Std.U8))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (self, out_buf)
  | some j =>
    let i1 ← i * Y
    let idx ← i1 + j
    let i2 ← idx * chunk_len
    let i3 ← idx + 1#usize
    let i4 ← i3 * chunk_len
    let (out_chunk, index_mut_back) ←
      core.slice.index.Slice.index_mut
        (core.slice.index.SliceIndexRangeUsizeSlice Std.U8) out_buf
        { start := i2, «end» := i4 }
    let a ← Array.index_usize self i
    let re ← Array.index_usize a j
    let out_chunk1 ←
      arithmetic.ring_arith.RingElem.serialize re out_chunk bits_per_elem
    let out_buf1 := index_mut_back out_chunk1
    arithmetic.matrix_arith.Matrix.serialize_loop0_loop0 iter1 self out_buf1
      bits_per_elem chunk_len i
partial_fixpoint

/-- [kopis::arithmetic::matrix_arith::{kopis::arithmetic::matrix_arith::Matrix<X, Y>}::serialize]: loop 0:
    Source: 'src/arithmetic/matrix_arith.rs', lines 97:8-103:9 -/
@[rust_loop]
def arithmetic.matrix_arith.Matrix.serialize_loop0
  {X : Std.Usize} {Y : Std.Usize} (iter : core.ops.range.Range Std.Usize)
  (self : arithmetic.matrix_arith.Matrix X Y) (out_buf : Slice Std.U8)
  (bits_per_elem : Std.Usize) (chunk_len : Std.Usize) :
  Result (Slice Std.U8)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok out_buf
  | some i =>
    let (self1, out_buf1) ←
      arithmetic.matrix_arith.Matrix.serialize_loop0_loop0
        { start := 0#usize, «end» := Y } self out_buf bits_per_elem chunk_len
        i
    arithmetic.matrix_arith.Matrix.serialize_loop0 iter1 self1 out_buf1
      bits_per_elem chunk_len
partial_fixpoint

/-- [kopis::arithmetic::matrix_arith::{kopis::arithmetic::matrix_arith::Matrix<X, Y>}::serialize]:
    Source: 'src/arithmetic/matrix_arith.rs', lines 83:4-104:5 -/
def arithmetic.matrix_arith.Matrix.serialize
  {X : Std.Usize} {Y : Std.Usize} (self : arithmetic.matrix_arith.Matrix X Y)
  (out_buf : Slice Std.U8) (bits_per_elem : Std.Usize) :
  Result (Slice Std.U8)
  := do
  let left_val := Slice.len out_buf
  let i ← X * Y
  let i1 ← i * bits_per_elem
  let i2 ← i1 * consts.RING_DEG
  let right_val ← i2 / 8#usize
  massert (left_val = right_val)
  let i3 ← bits_per_elem * consts.RING_DEG
  let chunk_len ← i3 / 8#usize
  arithmetic.matrix_arith.Matrix.serialize_loop0
    { start := 0#usize, «end» := X } self out_buf bits_per_elem chunk_len

/-- [kopis::ser::deserialize_generic]: loop 1:
    Source: 'src/ser.rs', lines 63:8-67:9 -/
@[rust_loop]
def ser.deserialize_generic_loop0_loop0
  (bytes : Slice Std.U8) (bits_per_elem : Std.Usize) (window : Std.U32)
  (bits_in_window : Std.Usize) (byte_pos : Std.Usize) :
  Result (Std.U32 × Std.Usize × Std.Usize)
  := do
  if bits_in_window < bits_per_elem
  then
    let i ← Slice.index_usize bytes byte_pos
    let i1 ← lift (UScalar.cast .U32 i)
    let i2 ← i1 <<< bits_in_window
    let window1 ← lift (window ||| i2)
    let byte_pos1 ← byte_pos + 1#usize
    let bits_in_window1 ← bits_in_window + 8#usize
    ser.deserialize_generic_loop0_loop0 bytes bits_per_elem window1
      bits_in_window1 byte_pos1
  else ok (window, bits_in_window, byte_pos)
partial_fixpoint

/-- [kopis::ser::deserialize_generic]: loop 0:
    Source: 'src/ser.rs', lines 61:4-73:5 -/
@[rust_loop]
def ser.deserialize_generic_loop0
  {N : Std.Usize} (iter : core.ops.range.Range Std.Usize)
  (bytes : Slice Std.U8) (bits_per_elem : Std.Usize) (out : Array Std.U16 N)
  (bitmask : Std.U32) (window : Std.U32) (bits_in_window : Std.Usize)
  (byte_pos : Std.Usize) :
  Result (Array Std.U16 N)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok out
  | some idx =>
    let (window1, bits_in_window1, byte_pos1) ←
      ser.deserialize_generic_loop0_loop0 bytes bits_per_elem window
        bits_in_window byte_pos
    let i ← lift (window1 &&& bitmask)
    let i1 ← lift (UScalar.cast .U16 i)
    let a ← Array.update out idx i1
    let window2 ← window1 >>> bits_per_elem
    let bits_in_window2 ← bits_in_window1 - bits_per_elem
    ser.deserialize_generic_loop0 iter1 bytes bits_per_elem a bitmask window2
      bits_in_window2 byte_pos1
partial_fixpoint

/-- [kopis::ser::deserialize_generic]:
    Source: 'src/ser.rs', lines 47:0-76:1 -/
def ser.deserialize_generic
  (N : Std.Usize) (bytes : Slice Std.U8) (bits_per_elem : Std.Usize) :
  Result (Array Std.U16 N)
  := do
  let i ← bits_per_elem * N
  let left_val ← i % 8#usize
  massert (left_val = 0#usize)
  let left_val1 := Slice.len bytes
  let right_val ← i / 8#usize
  massert (left_val1 = right_val)
  let out := Array.repeat N 0#u16
  let i1 ← 1#u32 <<< bits_per_elem
  let bitmask ← i1 - 1#u32
  ser.deserialize_generic_loop0 { start := 0#usize, «end» := N } bytes
    bits_per_elem out bitmask 0#u32 0#usize 0#usize

/-- [kopis::ser::deserialize_10::closure]
    Source: 'src/ser.rs', lines 34:16-34:38 -/
@[reducible]
def ser.deserialize_10.closure := Slice Std.U8

/-- [kopis::ser::deserialize_10::{impl core::ops::function::Fn<(usize,), u16> for kopis::ser::deserialize_10::closure<'_0>}::call]:
    Source: 'src/ser.rs', lines 34:16-34:38 -/
def ser.deserialize_10.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call
  (c : ser.deserialize_10.closure) (tupled_args : Std.Usize) :
  Result Std.U16
  := do
  let i ← Slice.index_usize c tupled_args
  ok (UScalar.cast .U16 i)

/-- [kopis::ser::deserialize_10]: loop 0:
    Source: 'src/ser.rs', lines 32:4-40:5 -/
@[rust_loop]
def ser.deserialize_10_loop
  (iter : core.ops.range.Range Std.Usize) (bytes : Array Std.U8 320#usize)
  (out : Array Std.U16 256#usize) :
  Result (Array Std.U16 256#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok out
  | some g =>
    let i ← 5#usize * g
    let i1 ← i + 5#usize
    let b ←
      core.array.Array.index (core.ops.index.IndexSlice
        (core.slice.index.SliceIndexRangeUsizeSlice Std.U8)) bytes
        { start := i, «end» := i1 }
    let o1 ← 4#usize * g
    let i2 ←
      ser.deserialize_10.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call b
        0#usize
    let i3 ←
      ser.deserialize_10.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call b
        1#usize
    let i4 ← lift (i3 &&& 3#u16)
    let i5 ← i4 <<< 8#i32
    let i6 ← lift (i2 ||| i5)
    let out1 ← Array.update out o1 i6
    let i7 ← i3 >>> 2#i32
    let i8 ←
      ser.deserialize_10.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call b
        2#usize
    let i9 ← lift (i8 &&& 15#u16)
    let i10 ← i9 <<< 6#i32
    let i11 ← o1 + 1#usize
    let i12 ← lift (i7 ||| i10)
    let out2 ← Array.update out1 i11 i12
    let i13 ← i8 >>> 4#i32
    let i14 ←
      ser.deserialize_10.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call b
        3#usize
    let i15 ← lift (i14 &&& 63#u16)
    let i16 ← i15 <<< 4#i32
    let i17 ← o1 + 2#usize
    let i18 ← lift (i13 ||| i16)
    let out3 ← Array.update out2 i17 i18
    let i19 ← i14 >>> 6#i32
    let i20 ←
      ser.deserialize_10.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call b
        4#usize
    let i21 ← i20 <<< 2#i32
    let i22 ← o1 + 3#usize
    let i23 ← lift (i19 ||| i21)
    let a ← Array.update out3 i22 i23
    ser.deserialize_10_loop iter1 bytes a
partial_fixpoint

/-- [kopis::ser::deserialize_10]:
    Source: 'src/ser.rs', lines 29:0-42:1 -/
def ser.deserialize_10
  (bytes : Array Std.U8 320#usize) : Result (Array Std.U16 256#usize) := do
  let out := Array.repeat 256#usize 0#u16
  ser.deserialize_10_loop { start := 0#usize, «end» := 64#usize } bytes out

/-- [kopis::ser::deserialize_13::closure]
    Source: 'src/ser.rs', lines 12:16-12:38 -/
@[reducible]
def ser.deserialize_13.closure := Slice Std.U8

/-- [kopis::ser::deserialize_13::{impl core::ops::function::Fn<(usize,), u16> for kopis::ser::deserialize_13::closure<'_0>}::call]:
    Source: 'src/ser.rs', lines 12:16-12:38 -/
def ser.deserialize_13.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call
  (c : ser.deserialize_13.closure) (tupled_args : Std.Usize) :
  Result Std.U16
  := do
  let i ← Slice.index_usize c tupled_args
  ok (UScalar.cast .U16 i)

/-- [kopis::ser::deserialize_13]: loop 0:
    Source: 'src/ser.rs', lines 9:4-22:5 -/
@[rust_loop]
def ser.deserialize_13_loop
  (iter : core.ops.range.Range Std.Usize) (bytes : Array Std.U8 416#usize)
  (out : Array Std.U16 256#usize) :
  Result (Array Std.U16 256#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok out
  | some g =>
    let i ← 13#usize * g
    let i1 ← i + 13#usize
    let b ←
      core.array.Array.index (core.ops.index.IndexSlice
        (core.slice.index.SliceIndexRangeUsizeSlice Std.U8)) bytes
        { start := i, «end» := i1 }
    let o1 ← 8#usize * g
    let i2 ←
      ser.deserialize_13.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call b
        0#usize
    let i3 ←
      ser.deserialize_13.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call b
        1#usize
    let i4 ← lift (i3 &&& 31#u16)
    let i5 ← i4 <<< 8#i32
    let i6 ← lift (i2 ||| i5)
    let out1 ← Array.update out o1 i6
    let i7 ← i3 >>> 5#i32
    let i8 ←
      ser.deserialize_13.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call b
        2#usize
    let i9 ← i8 <<< 3#i32
    let i10 ← lift (i7 ||| i9)
    let i11 ←
      ser.deserialize_13.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call b
        3#usize
    let i12 ← lift (i11 &&& 3#u16)
    let i13 ← i12 <<< 11#i32
    let i14 ← o1 + 1#usize
    let i15 ← lift (i10 ||| i13)
    let out2 ← Array.update out1 i14 i15
    let i16 ← i11 >>> 2#i32
    let i17 ←
      ser.deserialize_13.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call b
        4#usize
    let i18 ← lift (i17 &&& 127#u16)
    let i19 ← i18 <<< 6#i32
    let i20 ← o1 + 2#usize
    let i21 ← lift (i16 ||| i19)
    let out3 ← Array.update out2 i20 i21
    let i22 ← i17 >>> 7#i32
    let i23 ←
      ser.deserialize_13.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call b
        5#usize
    let i24 ← i23 <<< 1#i32
    let i25 ← lift (i22 ||| i24)
    let i26 ←
      ser.deserialize_13.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call b
        6#usize
    let i27 ← lift (i26 &&& 15#u16)
    let i28 ← i27 <<< 9#i32
    let i29 ← o1 + 3#usize
    let i30 ← lift (i25 ||| i28)
    let out4 ← Array.update out3 i29 i30
    let i31 ← i26 >>> 4#i32
    let i32 ←
      ser.deserialize_13.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call b
        7#usize
    let i33 ← i32 <<< 4#i32
    let i34 ← lift (i31 ||| i33)
    let i35 ←
      ser.deserialize_13.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call b
        8#usize
    let i36 ← lift (i35 &&& 1#u16)
    let i37 ← i36 <<< 12#i32
    let i38 ← o1 + 4#usize
    let i39 ← lift (i34 ||| i37)
    let out5 ← Array.update out4 i38 i39
    let i40 ← i35 >>> 1#i32
    let i41 ←
      ser.deserialize_13.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call b
        9#usize
    let i42 ← lift (i41 &&& 63#u16)
    let i43 ← i42 <<< 7#i32
    let i44 ← o1 + 5#usize
    let i45 ← lift (i40 ||| i43)
    let out6 ← Array.update out5 i44 i45
    let i46 ← i41 >>> 6#i32
    let i47 ←
      ser.deserialize_13.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call b
        10#usize
    let i48 ← i47 <<< 2#i32
    let i49 ← lift (i46 ||| i48)
    let i50 ←
      ser.deserialize_13.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call b
        11#usize
    let i51 ← lift (i50 &&& 7#u16)
    let i52 ← i51 <<< 10#i32
    let i53 ← o1 + 6#usize
    let i54 ← lift (i49 ||| i52)
    let out7 ← Array.update out6 i53 i54
    let i55 ← i50 >>> 3#i32
    let i56 ←
      ser.deserialize_13.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call b
        12#usize
    let i57 ← i56 <<< 5#i32
    let i58 ← o1 + 7#usize
    let i59 ← lift (i55 ||| i57)
    let a ← Array.update out7 i58 i59
    ser.deserialize_13_loop iter1 bytes a
partial_fixpoint

/-- [kopis::ser::deserialize_13]:
    Source: 'src/ser.rs', lines 6:0-24:1 -/
def ser.deserialize_13
  (bytes : Array Std.U8 416#usize) : Result (Array Std.U16 256#usize) := do
  let out := Array.repeat 256#usize 0#u16
  ser.deserialize_13_loop { start := 0#usize, «end» := 32#usize } bytes out

/-- [kopis::consts::MODULUS_Q_BITS]
    Source: 'src/consts.rs', lines 2:0-2:44 -/
@[global_simps, irreducible] def consts.MODULUS_Q_BITS : Std.Usize := 13#usize

/-- [kopis::backend::avx2::ser::Plan]
    Source: 'src/backend/avx2/ser.rs', lines 40:0-43:1 -/
structure backend.avx2.ser.Plan where
  shuffle : Array Std.U8 32#usize
  shift : Array Std.I32 8#usize

/-- [kopis::backend::avx2::ser::plan]: loop 1:
    Source: 'src/backend/avx2/ser.rs', lines 54:8-60:9 -/
@[rust_loop]
def backend.avx2.ser.plan_loop0_loop0
  (shuffle : Array Std.U8 32#usize) (k : Std.Usize) (offset : Std.Usize)
  (byte : Std.Usize) :
  Result (Array Std.U8 32#usize)
  := do
  if byte < 4#usize
  then
    let i ← offset + byte
    let i1 ← 4#usize * k
    let i2 ← i1 + byte
    let i3 ← lift (UScalar.cast .U8 i)
    let a ← Array.update shuffle i2 i3
    let byte1 ← byte + 1#usize
    backend.avx2.ser.plan_loop0_loop0 a k offset byte1
  else ok shuffle
partial_fixpoint

/-- [kopis::backend::avx2::ser::plan]: loop 0:
    Source: 'src/backend/avx2/ser.rs', lines 50:4-62:5 -/
@[rust_loop]
def backend.avx2.ser.plan_loop0
  (bits : Std.Usize) (shuffle : Array Std.U8 32#usize)
  (shift : Array Std.I32 8#usize) (k : Std.Usize) :
  Result ((Array Std.U8 32#usize) × (Array Std.I32 8#usize))
  := do
  if k < 8#usize
  then
    let i ← k * bits
    let offset ← i / 8#usize
    let i1 ← i % 8#usize
    let i2 ← lift (UScalar.hcast .I32 i1)
    let a ← Array.update shift k i2
    let shuffle1 ← backend.avx2.ser.plan_loop0_loop0 shuffle k offset 0#usize
    let k1 ← k + 1#usize
    backend.avx2.ser.plan_loop0 bits shuffle1 a k1
  else ok (shuffle, shift)
partial_fixpoint

/-- [kopis::backend::avx2::ser::plan]:
    Source: 'src/backend/avx2/ser.rs', lines 46:0-64:1 -/
def backend.avx2.ser.plan
  (bits : Std.Usize) : Result backend.avx2.ser.Plan := do
  let shuffle := Array.repeat 32#usize 0#u8
  let shift := Array.repeat 8#usize 0#i32
  let (shuffle1, shift1) ←
    backend.avx2.ser.plan_loop0 bits shuffle shift 0#usize
  ok { shuffle := shuffle1, shift := shift1 }

/-- [kopis::backend::avx2::ser::MAX_BITS]
    Source: 'src/backend/avx2/ser.rs', lines 28:0-28:27 -/
@[global_simps, irreducible]
def backend.avx2.ser.MAX_BITS : Std.Usize := 13#usize

/-- [kopis::backend::avx2::ser::plans]: loop 0:
    Source: 'src/backend/avx2/ser.rs', lines 74:4-77:5 -/
@[rust_loop]
def backend.avx2.ser.plans_loop
  (plans : Array backend.avx2.ser.Plan 14#usize) (bits : Std.Usize) :
  Result (Array backend.avx2.ser.Plan 14#usize)
  := do
  if bits <= backend.avx2.ser.MAX_BITS
  then
    let p ← backend.avx2.ser.plan bits
    let a ← Array.update plans bits p
    let bits1 ← bits + 1#usize
    backend.avx2.ser.plans_loop a bits1
  else ok plans
partial_fixpoint

/-- [kopis::backend::avx2::ser::plans]:
    Source: 'src/backend/avx2/ser.rs', lines 71:0-79:1 -/
def backend.avx2.ser.plans
  : Result (Array backend.avx2.ser.Plan 14#usize) := do
  let p ← backend.avx2.ser.plan 0#usize
  let plans := Array.repeat 14#usize p
  backend.avx2.ser.plans_loop plans 1#usize

/-- [kopis::backend::avx2::ser::PLANS]
    Source: 'src/backend/avx2/ser.rs', lines 82:0-82:44 -/
@[global_simps, irreducible]
def backend.avx2.ser.PLANS : Result (Array backend.avx2.ser.Plan 14#usize) :=
  backend.avx2.ser.plans

/-- [kopis::backend::avx2::ser::GROUPS]
    Source: 'src/backend/avx2/ser.rs', lines 31:0-31:35 -/
@[global_simps, irreducible]
def backend.avx2.ser.GROUPS : Result Std.Usize := consts.RING_DEG / 8#usize

/-- [kopis::backend::avx2::intrinsics::Vec128]
    Source: 'src/backend/avx2/intrinsics.rs', lines 56:0-56:34 -/
axiom backend.avx2.intrinsics.Vec128 : Type

/-- [kopis::backend::avx2::intrinsics::load_u8x16]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 446:0-450:1 -/
axiom backend.avx2.intrinsics.load_u8x16
  : Slice Std.U8 → Std.Usize → Result backend.avx2.intrinsics.Vec128

/-- [kopis::backend::avx2::intrinsics::Vec256]
    Source: 'src/backend/avx2/intrinsics.rs', lines 51:0-51:34 -/
axiom backend.avx2.intrinsics.Vec256 : Type

/-- [kopis::backend::avx2::intrinsics::load_u8]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 434:0-438:1 -/
axiom backend.avx2.intrinsics.load_u8
  {N : Std.Usize} :
  Array Std.U8 N → Std.Usize → Result backend.avx2.intrinsics.Vec256

/-- [kopis::backend::avx2::intrinsics::load_i32]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 416:0-420:1 -/
axiom backend.avx2.intrinsics.load_i32
  {N : Std.Usize} :
  Array Std.I32 N → Std.Usize → Result backend.avx2.intrinsics.Vec256

/-- [kopis::backend::avx2::intrinsics::store_u16]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 407:0-411:1 -/
axiom backend.avx2.intrinsics.store_u16
  {N : Std.Usize} :
  Array Std.U16 N → Std.Usize → backend.avx2.intrinsics.Vec256 → Result
    (Array Std.U16 N)

/-- [kopis::backend::avx2::intrinsics::broadcastsi128_si256]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 364:0-366:1 -/
axiom backend.avx2.intrinsics.broadcastsi128_si256
  : backend.avx2.intrinsics.Vec128 → Result backend.avx2.intrinsics.Vec256

/-- [kopis::backend::avx2::intrinsics::packus_epi32]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 336:0-338:1 -/
axiom backend.avx2.intrinsics.packus_epi32
  :
  backend.avx2.intrinsics.Vec256 → backend.avx2.intrinsics.Vec256 → Result
    backend.avx2.intrinsics.Vec256

/-- [kopis::backend::avx2::intrinsics::permute4x64_epi64]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 320:0-322:1 -/
axiom backend.avx2.intrinsics.permute4x64_epi64
  (IMM : Std.I32) :
  backend.avx2.intrinsics.Vec256 → Result backend.avx2.intrinsics.Vec256

/-- [kopis::backend::avx2::intrinsics::shuffle_epi8]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 263:0-265:1 -/
axiom backend.avx2.intrinsics.shuffle_epi8
  :
  backend.avx2.intrinsics.Vec256 → backend.avx2.intrinsics.Vec256 → Result
    backend.avx2.intrinsics.Vec256

/-- [kopis::backend::avx2::intrinsics::srlv_epi32]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 251:0-253:1 -/
axiom backend.avx2.intrinsics.srlv_epi32
  :
  backend.avx2.intrinsics.Vec256 → backend.avx2.intrinsics.Vec256 → Result
    backend.avx2.intrinsics.Vec256

/-- [kopis::backend::avx2::intrinsics::and_si256]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 100:0-102:1 -/
axiom backend.avx2.intrinsics.and_si256
  :
  backend.avx2.intrinsics.Vec256 → backend.avx2.intrinsics.Vec256 → Result
    backend.avx2.intrinsics.Vec256

/-- [kopis::backend::avx2::intrinsics::setzero_si256]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 86:0-88:1 -/
axiom backend.avx2.intrinsics.setzero_si256
  : Result backend.avx2.intrinsics.Vec256

/-- [kopis::backend::avx2::intrinsics::set1_epi32]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 72:0-74:1 -/
axiom backend.avx2.intrinsics.set1_epi32
  : Std.I32 → Result backend.avx2.intrinsics.Vec256

/-- [kopis::backend::avx2::ser::deserialize]: loop 1:
    Source: 'src/backend/avx2/ser.rs', lines 115:8-128:9 -/
@[rust_loop]
def backend.avx2.ser.deserialize_loop0_loop0
  (iter : core.ops.range.Range Std.Usize) (bytes : Slice Std.U8)
  (bits : Std.Usize) (shuffle : backend.avx2.intrinsics.Vec256)
  (shift : backend.avx2.intrinsics.Vec256)
  (mask : backend.avx2.intrinsics.Vec256) (head_groups : Std.Usize)
  (tail_start : Std.Usize) (tail : Array Std.U8 32#usize) (pair : Std.Usize)
  (wide : Array backend.avx2.intrinsics.Vec256 2#usize) :
  Result (Array backend.avx2.intrinsics.Vec256 2#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok wide
  | some half =>
    let i ← 2#usize * pair
    let group ← i + half
    let raw1 ←
      if group < head_groups
      then
        do
        let i1 ← group * bits
        let v ← backend.avx2.intrinsics.load_u8x16 bytes i1
        backend.avx2.intrinsics.broadcastsi128_si256 v
      else
        do
        let s ← lift (Array.to_slice tail)
        let i1 ← group * bits
        let i2 ← i1 - tail_start
        let v ← backend.avx2.intrinsics.load_u8x16 s i2
        backend.avx2.intrinsics.broadcastsi128_si256 v
    let windows ← backend.avx2.intrinsics.shuffle_epi8 raw1 shuffle
    let v ← backend.avx2.intrinsics.srlv_epi32 windows shift
    let v1 ← backend.avx2.intrinsics.and_si256 v mask
    let a ← Array.update wide half v1
    backend.avx2.ser.deserialize_loop0_loop0 iter1 bytes bits shuffle shift
      mask head_groups tail_start tail pair a
partial_fixpoint

/-- [kopis::backend::avx2::ser::deserialize]: loop 0:
    Source: 'src/backend/avx2/ser.rs', lines 113:4-135:5 -/
@[rust_loop]
def backend.avx2.ser.deserialize_loop0
  (iter : core.ops.range.Range Std.Usize) (bytes : Slice Std.U8)
  (bits : Std.Usize) (shuffle : backend.avx2.intrinsics.Vec256)
  (shift : backend.avx2.intrinsics.Vec256)
  (mask : backend.avx2.intrinsics.Vec256) (head_groups : Std.Usize)
  (tail_start : Std.Usize) (tail : Array Std.U8 32#usize)
  (out : Array Std.U16 256#usize) :
  Result (Array Std.U16 256#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok out
  | some pair =>
    let v ← backend.avx2.intrinsics.setzero_si256
    let wide := Array.repeat 2#usize v
    let wide1 ←
      backend.avx2.ser.deserialize_loop0_loop0
        { start := 0#usize, «end» := 2#usize } bytes bits shuffle shift mask
        head_groups tail_start tail pair wide
    let v1 ← Array.index_usize wide1 0#usize
    let v2 ← Array.index_usize wide1 1#usize
    let packed ← backend.avx2.intrinsics.packus_epi32 v1 v2
    let v3 ← backend.avx2.intrinsics.permute4x64_epi64 216#i32 packed
    let out1 ← backend.avx2.intrinsics.store_u16 out pair v3
    backend.avx2.ser.deserialize_loop0 iter1 bytes bits shuffle shift mask
      head_groups tail_start tail out1
partial_fixpoint

/-- [kopis::backend::avx2::ser::deserialize]:
    Source: 'src/backend/avx2/ser.rs', lines 94:0-138:1 -/
def backend.avx2.ser.deserialize
  (bytes : Slice Std.U8) (bits : Std.Usize) :
  Result (Array Std.U16 256#usize)
  := do
  let a ← backend.avx2.ser.PLANS
  let plan ← Array.index_usize a bits
  let shuffle ← backend.avx2.intrinsics.load_u8 plan.shuffle 0#usize
  let shift ← backend.avx2.intrinsics.load_i32 plan.shift 0#usize
  let i ← 1#i32 <<< bits
  let i1 ← i - 1#i32
  let mask ← backend.avx2.intrinsics.set1_epi32 i1
  let tail_groups ← 15#usize / bits
  let i2 ← backend.avx2.ser.GROUPS
  let head_groups ← i2 - tail_groups
  let tail_start ← head_groups * bits
  let tail := Array.repeat 32#usize 0#u8
  let i3 := Slice.len bytes
  let i4 ← i3 - tail_start
  let (s, index_mut_back) ←
    core.array.Array.index_mut (core.ops.index.IndexMutSlice
      (core.slice.index.SliceIndexRangeToUsizeSlice Std.U8)) tail
      { «end» := i4 }
  let s1 ←
    core.slice.index.Slice.index
      (core.slice.index.SliceIndexRangeFromUsizeSlice Std.U8) bytes
      { start := tail_start }
  let s2 ← core.slice.Slice.copy_from_slice core.marker.CopyU8 s s1
  let out := Array.repeat 256#usize 0#u16
  let i5 ← i2 / 2#usize
  let tail1 := index_mut_back s2
  backend.avx2.ser.deserialize_loop0 { start := 0#usize, «end» := i5 } bytes
    bits shuffle shift mask head_groups tail_start tail1 out

/-- [kopis::backend::avx2::cpu::available]:
    Source: 'src/backend/avx2/cpu.rs', lines 30:0-44:1 -/
axiom backend.avx2.cpu.available : Result Bool

/-- [kopis::arithmetic::ring_arith::{kopis::arithmetic::ring_arith::RingElem}::deserialize]:
    Source: 'src/arithmetic/ring_arith.rs', lines 41:4-75:5 -/
def arithmetic.ring_arith.RingElem.deserialize
  (bytes : Slice Std.U8) (bits_per_elem : Std.Usize) :
  Result arithmetic.ring_arith.RingElem
  := do
  let left_val := Slice.len bytes
  let i ← bits_per_elem * consts.RING_DEG
  let right_val ← i / 8#usize
  massert (left_val = right_val)
  let ri ← core.ops.range.RangeInclusive.new 1#usize 13#usize
  let b ←
    core.ops.range.RangeInclusive.contains core.cmp.PartialOrdUsize
      core.cmp.PartialOrdUsize core.cmp.PartialOrdUsize ri bits_per_elem
  if b
  then
    let b1 ← backend.avx2.cpu.available
    if b1
    then let a ← backend.avx2.ser.deserialize bytes bits_per_elem
         ok a
    else
      if bits_per_elem = consts.MODULUS_Q_BITS
      then
        let r ← core.array.TryFromSharedArraySlice.try_from 416#usize bytes
        let arr ← core.result.Result.unwrap core.fmt.DebugTryFromSliceError r
        let a ← ser.deserialize_13 arr
        ok a
      else
        if bits_per_elem = consts.MODULUS_P_BITS
        then
          let r ← core.array.TryFromSharedArraySlice.try_from 320#usize bytes
          let arr ←
            core.result.Result.unwrap core.fmt.DebugTryFromSliceError r
          let a ← ser.deserialize_10 arr
          ok a
        else
          let a ← ser.deserialize_generic 256#usize bytes bits_per_elem
          ok a
  else
    if bits_per_elem = consts.MODULUS_Q_BITS
    then
      let r ← core.array.TryFromSharedArraySlice.try_from 416#usize bytes
      let arr ← core.result.Result.unwrap core.fmt.DebugTryFromSliceError r
      let a ← ser.deserialize_13 arr
      ok a
    else
      if bits_per_elem = consts.MODULUS_P_BITS
      then
        let r ← core.array.TryFromSharedArraySlice.try_from 320#usize bytes
        let arr ← core.result.Result.unwrap core.fmt.DebugTryFromSliceError r
        let a ← ser.deserialize_10 arr
        ok a
      else let a ← ser.deserialize_generic 256#usize bytes bits_per_elem
           ok a

/-- [kopis::arithmetic::matrix_arith::{kopis::arithmetic::matrix_arith::Matrix<X, Y>}::deserialize_10]: loop 1:
    Source: 'src/arithmetic/matrix_arith.rs', lines 125:12-129:13 -/
@[rust_loop]
def arithmetic.matrix_arith.Matrix.deserialize_10_loop0_loop0
  {X : Std.Usize} {Y : Std.Usize} (iter : core.ops.range.Range Std.Usize)
  (bytes : Slice Std.U8) (result : arithmetic.matrix_arith.Matrix X Y)
  (chunk_len : Std.Usize) (i : Std.Usize) :
  Result (arithmetic.matrix_arith.Matrix X Y)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok result
  | some j =>
    let i1 ← i * Y
    let idx ← i1 + j
    let i2 ← idx * chunk_len
    let i3 ← idx + 1#usize
    let i4 ← i3 * chunk_len
    let chunk ←
      core.slice.index.Slice.index (core.slice.index.SliceIndexRangeUsizeSlice
        Std.U8) bytes { start := i2, «end» := i4 }
    let re ← arithmetic.ring_arith.RingElem.deserialize chunk 10#usize
    let (a, index_mut_back) ← Array.index_mut_usize result i
    let a1 ← Array.update a j re
    let a2 := index_mut_back a1
    arithmetic.matrix_arith.Matrix.deserialize_10_loop0_loop0 iter1 bytes a2
      chunk_len i
partial_fixpoint

/-- [kopis::arithmetic::matrix_arith::{kopis::arithmetic::matrix_arith::Matrix<X, Y>}::deserialize_10]: loop 0:
    Source: 'src/arithmetic/matrix_arith.rs', lines 124:8-130:9 -/
@[rust_loop]
def arithmetic.matrix_arith.Matrix.deserialize_10_loop0
  {X : Std.Usize} {Y : Std.Usize} (iter : core.ops.range.Range Std.Usize)
  (bytes : Slice Std.U8) (result : arithmetic.matrix_arith.Matrix X Y)
  (chunk_len : Std.Usize) :
  Result (arithmetic.matrix_arith.Matrix X Y)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok result
  | some i =>
    let result1 ←
      arithmetic.matrix_arith.Matrix.deserialize_10_loop0_loop0
        { start := 0#usize, «end» := Y } bytes result chunk_len i
    arithmetic.matrix_arith.Matrix.deserialize_10_loop0 iter1 bytes result1
      chunk_len
partial_fixpoint

/-- [kopis::arithmetic::matrix_arith::{kopis::arithmetic::matrix_arith::Matrix<X, Y>}::deserialize_10]:
    Source: 'src/arithmetic/matrix_arith.rs', lines 107:4-133:5 -/
def arithmetic.matrix_arith.Matrix.deserialize_10
  (X : Std.Usize) (Y : Std.Usize) (bytes : Slice Std.U8) :
  Result (arithmetic.matrix_arith.Matrix X Y)
  := do
  let left_val := Slice.len bytes
  let i ← X * Y
  let i1 ← i * 10#usize
  let _ ← i1 * consts.RING_DEG
  let i2 ← lift (Std.Usize.wrapping_mul X Y)
  let i3 ← lift (Std.Usize.wrapping_mul i2 10#usize)
  let i4 ← lift (Std.Usize.wrapping_mul i3 consts.RING_DEG)
  let right_val ← i4 / 8#usize
  massert (left_val = right_val)
  let result ←
    arithmetic.matrix_arith.Matrix.Insts.CoreDefaultDefault.default X Y
  let i5 ← 10#usize * consts.RING_DEG
  let chunk_len ← i5 / 8#usize
  arithmetic.matrix_arith.Matrix.deserialize_10_loop0
    { start := 0#usize, «end» := X } bytes result chunk_len

/-- [kopis::arithmetic::ring_arith::{impl core::ops::arith::Add<&'a kopis::arithmetic::ring_arith::RingElem, kopis::arithmetic::ring_arith::RingElem> for &'a kopis::arithmetic::ring_arith::RingElem}::add]: loop 0:
    Source: 'src/arithmetic/ring_arith.rs', lines 222:8-224:9
    Visibility: public -/
@[rust_loop]
def SharedARingElem.Insts.CoreOpsArithAddSharedARingElemRingElem.add_loop
  (iter : core.ops.range.Range Std.Usize)
  (self : arithmetic.ring_arith.RingElem)
  (other : arithmetic.ring_arith.RingElem)
  (ret : arithmetic.ring_arith.RingElem) :
  Result arithmetic.ring_arith.RingElem
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok ret
  | some i =>
    let i1 ← Array.index_usize self i
    let i2 ← Array.index_usize other i
    let i3 ← lift (core.num.U16.wrapping_add i1 i2)
    let a ← Array.update ret i i3
    SharedARingElem.Insts.CoreOpsArithAddSharedARingElemRingElem.add_loop iter1
      self other a
partial_fixpoint

/-- [kopis::arithmetic::ring_arith::{impl core::ops::arith::Add<&'a kopis::arithmetic::ring_arith::RingElem, kopis::arithmetic::ring_arith::RingElem> for &'a kopis::arithmetic::ring_arith::RingElem}::add]:
    Source: 'src/arithmetic/ring_arith.rs', lines 220:4-226:5
    Visibility: public -/
def SharedARingElem.Insts.CoreOpsArithAddSharedARingElemRingElem.add
  (self : arithmetic.ring_arith.RingElem)
  (other : arithmetic.ring_arith.RingElem) :
  Result arithmetic.ring_arith.RingElem
  := do
  let ret ← arithmetic.ring_arith.RingElem.Insts.CoreDefaultDefault.default
  SharedARingElem.Insts.CoreOpsArithAddSharedARingElemRingElem.add_loop
    { start := 0#usize, «end» := consts.RING_DEG } self other ret

/-- [kopis::arithmetic::matrix_arith::{impl core::ops::arith::Add<&'a kopis::arithmetic::matrix_arith::Matrix<X, Y>, kopis::arithmetic::matrix_arith::Matrix<X, Y>> for &'a kopis::arithmetic::matrix_arith::Matrix<X, Y>}::add]: loop 1:
    Source: 'src/arithmetic/matrix_arith.rs', lines 142:12-144:13
    Visibility: public -/
@[rust_loop]
def SharedAMatrix.Insts.CoreOpsArithAddSharedAMatrixMatrix.add_loop0_loop0
  {X : Std.Usize} {Y : Std.Usize} (iter : core.ops.range.Range Std.Usize)
  (self : arithmetic.matrix_arith.Matrix X Y)
  (other : arithmetic.matrix_arith.Matrix X Y)
  (result : arithmetic.matrix_arith.Matrix X Y) (i : Std.Usize) :
  Result ((arithmetic.matrix_arith.Matrix X Y) ×
    (arithmetic.matrix_arith.Matrix X Y) × (arithmetic.matrix_arith.Matrix X
    Y))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (self, other, result)
  | some j =>
    let a ← Array.index_usize self i
    let re ← Array.index_usize a j
    let a1 ← Array.index_usize other i
    let re1 ← Array.index_usize a1 j
    let re2 ←
      SharedARingElem.Insts.CoreOpsArithAddSharedARingElemRingElem.add re re1
    let (a2, index_mut_back) ← Array.index_mut_usize result i
    let a3 ← Array.update a2 j re2
    let a4 := index_mut_back a3
    SharedAMatrix.Insts.CoreOpsArithAddSharedAMatrixMatrix.add_loop0_loop0
      iter1 self other a4 i
partial_fixpoint

/-- [kopis::arithmetic::matrix_arith::{impl core::ops::arith::Add<&'a kopis::arithmetic::matrix_arith::Matrix<X, Y>, kopis::arithmetic::matrix_arith::Matrix<X, Y>> for &'a kopis::arithmetic::matrix_arith::Matrix<X, Y>}::add]: loop 0:
    Source: 'src/arithmetic/matrix_arith.rs', lines 141:8-145:9
    Visibility: public -/
@[rust_loop]
def SharedAMatrix.Insts.CoreOpsArithAddSharedAMatrixMatrix.add_loop0
  {X : Std.Usize} {Y : Std.Usize} (iter : core.ops.range.Range Std.Usize)
  (self : arithmetic.matrix_arith.Matrix X Y)
  (other : arithmetic.matrix_arith.Matrix X Y)
  (result : arithmetic.matrix_arith.Matrix X Y) :
  Result (arithmetic.matrix_arith.Matrix X Y)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok result
  | some i =>
    let (self1, other1, result1) ←
      SharedAMatrix.Insts.CoreOpsArithAddSharedAMatrixMatrix.add_loop0_loop0
        { start := 0#usize, «end» := Y } self other result i
    SharedAMatrix.Insts.CoreOpsArithAddSharedAMatrixMatrix.add_loop0 iter1
      self1 other1 result1
partial_fixpoint

/-- [kopis::arithmetic::matrix_arith::{impl core::ops::arith::Add<&'a kopis::arithmetic::matrix_arith::Matrix<X, Y>, kopis::arithmetic::matrix_arith::Matrix<X, Y>> for &'a kopis::arithmetic::matrix_arith::Matrix<X, Y>}::add]:
    Source: 'src/arithmetic/matrix_arith.rs', lines 139:4-148:5
    Visibility: public -/
def SharedAMatrix.Insts.CoreOpsArithAddSharedAMatrixMatrix.add
  {X : Std.Usize} {Y : Std.Usize} (self : arithmetic.matrix_arith.Matrix X Y)
  (other : arithmetic.matrix_arith.Matrix X Y) :
  Result (arithmetic.matrix_arith.Matrix X Y)
  := do
  let result ←
    arithmetic.matrix_arith.Matrix.Insts.CoreDefaultDefault.default X Y
  SharedAMatrix.Insts.CoreOpsArithAddSharedAMatrixMatrix.add_loop0
    { start := 0#usize, «end» := X } self other result

/-- Trait implementation: [kopis::arithmetic::matrix_arith::{impl core::ops::arith::Add<&'a kopis::arithmetic::matrix_arith::Matrix<X, Y>, kopis::arithmetic::matrix_arith::Matrix<X, Y>> for &'a kopis::arithmetic::matrix_arith::Matrix<X, Y>}]
    Source: 'src/arithmetic/matrix_arith.rs', lines 136:0-149:1 -/
@[reducible]
def SharedAMatrix.Insts.CoreOpsArithAddSharedAMatrixMatrix (X : Std.Usize) (Y :
  Std.Usize) : core.ops.arith.Add (arithmetic.matrix_arith.Matrix X Y)
  (arithmetic.matrix_arith.Matrix X Y) (arithmetic.matrix_arith.Matrix X Y)
  := {
  add := SharedAMatrix.Insts.CoreOpsArithAddSharedAMatrixMatrix.add
}

/-- [kopis::arithmetic::ntt::NttElem]
    Source: 'src/arithmetic/ntt.rs', lines 242:0-242:58 -/
@[reducible]
def arithmetic.ntt.NttElem := Array Std.I16 512#usize

/-- [kopis::arithmetic::ntt::{impl core::clone::Clone for kopis::arithmetic::ntt::NttElem}::clone]:
    Source: 'src/arithmetic/ntt.rs', lines 241:9-241:14
    Visibility: public -/
def arithmetic.ntt.NttElem.Insts.CoreCloneClone.clone
  (self : arithmetic.ntt.NttElem) : Result arithmetic.ntt.NttElem := do
  ok self

/-- Trait implementation: [kopis::arithmetic::ntt::{impl core::clone::Clone for kopis::arithmetic::ntt::NttElem}]
    Source: 'src/arithmetic/ntt.rs', lines 241:9-241:14 -/
@[reducible]
def arithmetic.ntt.NttElem.Insts.CoreCloneClone : core.clone.Clone
  arithmetic.ntt.NttElem := {
  clone := arithmetic.ntt.NttElem.Insts.CoreCloneClone.clone
}

/-- Trait implementation: [kopis::arithmetic::ntt::{impl core::marker::Copy for kopis::arithmetic::ntt::NttElem}]
    Source: 'src/arithmetic/ntt.rs', lines 241:16-241:20 -/
@[reducible]
def arithmetic.ntt.NttElem.Insts.CoreMarkerCopy : core.marker.Copy
  arithmetic.ntt.NttElem := {
  cloneInst := arithmetic.ntt.NttElem.Insts.CoreCloneClone
}

/-- [kopis::arithmetic::ntt::{impl zeroize::Zeroize for kopis::arithmetic::ntt::NttElem}::zeroize]:
    Source: 'src/arithmetic/ntt.rs', lines 241:22-241:29
    Visibility: public -/
def arithmetic.ntt.NttElem.Insts.ZeroizeZeroize.zeroize
  (self : arithmetic.ntt.NttElem) : Result arithmetic.ntt.NttElem := do
  let __zeroize_field_0 ←
    Array.Insts.ZeroizeZeroize.zeroize (zeroize.Zeroize.Blanket
      I16.Insts.ZeroizeDefaultIsZeroes) self
  ok __zeroize_field_0

/-- Trait implementation: [kopis::arithmetic::ntt::{impl zeroize::Zeroize for kopis::arithmetic::ntt::NttElem}]
    Source: 'src/arithmetic/ntt.rs', lines 241:22-241:29 -/
@[reducible]
def arithmetic.ntt.NttElem.Insts.ZeroizeZeroize : zeroize.Zeroize
  arithmetic.ntt.NttElem := {
  zeroize := arithmetic.ntt.NttElem.Insts.ZeroizeZeroize.zeroize
}

/-- [kopis::arithmetic::ntt::{impl core::default::Default for kopis::arithmetic::ntt::NttElem}::default]:
    Source: 'src/arithmetic/ntt.rs', lines 245:4-247:5
    Visibility: public -/
def arithmetic.ntt.NttElem.Insts.CoreDefaultDefault.default
  : Result arithmetic.ntt.NttElem := do
  let a := Array.repeat 512#usize 0#i16
  ok a

/-- Trait implementation: [kopis::arithmetic::ntt::{impl core::default::Default for kopis::arithmetic::ntt::NttElem}]
    Source: 'src/arithmetic/ntt.rs', lines 244:0-248:1 -/
@[reducible]
def arithmetic.ntt.NttElem.Insts.CoreDefaultDefault : core.default.Default
  arithmetic.ntt.NttElem := {
  default := arithmetic.ntt.NttElem.Insts.CoreDefaultDefault.default
}

/-- [kopis::arithmetic::ntt::ACC_ZERO]
    Source: 'src/arithmetic/ntt.rs', lines 255:0-255:54 -/
@[global_simps, irreducible]
def arithmetic.ntt.ACC_ZERO : Array Std.I32 512#usize :=
  Array.repeat 512#usize 0#i32

/-- [kopis::backend::crt::Q2_BARRETT_M]
    Source: 'src/backend/crt.rs', lines 90:0-90:43 -/
@[global_simps, irreducible]
def backend.crt.Q2_BARRETT_M : Std.I16 := 12482#i16

/-- [kopis::backend::crt::Q1_BARRETT_M]
    Source: 'src/backend/crt.rs', lines 88:0-88:43 -/
@[global_simps, irreducible]
def backend.crt.Q1_BARRETT_M : Std.I16 := 17474#i16

/-- [kopis::backend::crt::barrett_m]:
    Source: 'src/backend/crt.rs', lines 199:0-201:1 -/
def backend.crt.barrett_m (SECOND : Bool) : Result Std.I16 := do
  if SECOND
  then ok backend.crt.Q2_BARRETT_M
  else ok backend.crt.Q1_BARRETT_M

/-- [kopis::backend::crt::Q2]
    Source: 'src/backend/crt.rs', lines 82:0-82:33 -/
@[global_simps, irreducible] def backend.crt.Q2 : Std.I16 := 10753#i16

/-- [kopis::backend::crt::Q1]
    Source: 'src/backend/crt.rs', lines 80:0-80:32 -/
@[global_simps, irreducible] def backend.crt.Q1 : Std.I16 := 7681#i16

/-- [kopis::backend::crt::q]:
    Source: 'src/backend/crt.rs', lines 189:0-191:1 -/
def backend.crt.q (SECOND : Bool) : Result Std.I16 := do
  if SECOND
  then ok backend.crt.Q2
  else ok backend.crt.Q1

/-- [kopis::backend::crt::BARRETT_SH]
    Source: 'src/backend/crt.rs', lines 96:0-96:38 -/
@[global_simps, irreducible] def backend.crt.BARRETT_SH : Std.I32 := 11#i32

/-- [kopis::backend::crt::zetas_qinv]: loop 0:
    Source: 'src/backend/crt.rs', lines 159:4-162:5 -/
@[rust_loop]
def backend.crt.zetas_qinv_loop
  (zetas : Array Std.I16 256#usize) (qinv : Std.I16)
  (table : Array Std.I16 256#usize) (k : Std.Usize) :
  Result (Array Std.I16 256#usize)
  := do
  if k < 256#usize
  then
    let i ← Array.index_usize zetas k
    let i1 ← lift (core.num.I16.wrapping_mul i qinv)
    let a ← Array.update table k i1
    let k1 ← k + 1#usize
    backend.crt.zetas_qinv_loop zetas qinv a k1
  else ok table
partial_fixpoint

/-- [kopis::backend::crt::zetas_qinv]:
    Source: 'src/backend/crt.rs', lines 156:0-164:1 -/
def backend.crt.zetas_qinv
  (zetas : Array Std.I16 256#usize) (qinv : Std.I16) :
  Result (Array Std.I16 256#usize)
  := do
  let table := Array.repeat 256#usize 0#i16
  backend.crt.zetas_qinv_loop zetas qinv table 0#usize

/-- [kopis::backend::crt::ZETAS_Q2]
    Source: 'src/backend/crt.rs', lines 135:0-152:2 -/
@[global_simps, irreducible]
def backend.crt.ZETAS_Q2 : Array Std.I16 256#usize :=
  Array.make 256#usize [
    1018#i16, 223#i16, 4188#i16, (-3688)#i16, 2413#i16, (-3686)#i16, 357#i16,
    (-376)#i16, 2695#i16, (-730)#i16, 4855#i16, 2236#i16, (-425)#i16, 4544#i16,
    3364#i16, (-3784)#i16, 4875#i16, (-1520)#i16, (-5063)#i16, (-4035)#i16,
    2503#i16, 918#i16, (-3012)#i16, 4347#i16, 1931#i16, (-1341)#i16,
    (-3823)#i16, (-341)#i16, (-4095)#i16, (-5175)#i16, (-2629)#i16,
    (-5213)#i16, (-3091)#i16, 4129#i16, (-2935)#i16, 2790#i16, 268#i16,
    1284#i16, 4#i16, 3550#i16, 2982#i16, 1287#i16, 205#i16, 4513#i16,
    (-2565)#i16, (-2178)#i16, 4616#i16, (-193)#i16, (-4102)#i16, 4742#i16,
    (-4876)#i16, (-4744)#i16, (-2984)#i16, (-3062)#i16, (-847)#i16,
    (-4379)#i16, (-2388)#i16, (-1009)#i16, (-3085)#i16, (-1299)#i16,
    (-2576)#i16, 4189#i16, 1085#i16, 544#i16, 5023#i16, 794#i16, (-567)#i16,
    (-3198)#i16, 4734#i16, (-2998)#i16, 3441#i16, (-5341)#i16, 675#i16,
    2271#i16, 1615#i16, (-2213)#i16, 512#i16, 2774#i16, 3057#i16, (-2045)#i16,
    3615#i16, (-1458)#i16, (-909)#i16, 5114#i16, 2981#i16, (-4977)#i16,
    (-116)#i16, 4580#i16, (-454)#i16, (-5064)#i16, 4808#i16, (-1841)#i16,
    (-886)#i16, (-1356)#i16, (-4828)#i16, (-5156)#i16, 2737#i16, 4286#i16,
    (-3169)#i16, (-578)#i16, 5294#i16, (-636)#i16, 400#i16, 151#i16,
    (-2884)#i16, (-336)#i16, (-1006)#i16, (-326)#i16, 1572#i16, (-2740)#i16,
    (-779)#i16, 2206#i16, (-1586)#i16, 1068#i16, (-3715)#i16, (-1268)#i16,
    2684#i16, (-5116)#i16, 1324#i16, 2973#i16, (-2234)#i16, (-4123)#i16,
    3337#i16, (-864)#i16, 472#i16, (-467)#i16, 970#i16, 635#i16, (-573)#i16,
    2230#i16, (-1132)#i16, (-4621)#i16, 2624#i16, (-4601)#i16, 3570#i16,
    (-3760)#i16, (-5309)#i16, 3453#i16, (-5215)#i16, 854#i16, (-4250)#i16,
    2428#i16, 1381#i16, 5172#i16, (-5015)#i16, (-4447)#i16, 3135#i16, 2662#i16,
    3524#i16, (-1573)#i16, 2139#i16, 458#i16, (-2196)#i16, (-2657)#i16,
    4782#i16, (-3410)#i16, 2062#i16, 2015#i16, (-4784)#i16, 1635#i16, 1349#i16,
    (-1722)#i16, 2909#i16, (-4359)#i16, 2680#i16, 2087#i16, 40#i16, 3241#i16,
    (-2439)#i16, 2117#i16, 2050#i16, 2118#i16, (-4144)#i16, (-274)#i16,
    3148#i16, (-1930)#i16, 1992#i16, 4408#i16, 5005#i16, (-4428)#i16, 2419#i16,
    1639#i16, 2283#i16, (-778)#i16, (-2374)#i16, 663#i16, 1409#i16,
    (-2237)#i16, (-4254)#i16, (-1122)#i16, 97#i16, (-5313)#i16, (-3535)#i16,
    (-2813)#i16, 5083#i16, 279#i16, 4328#i16, 2279#i16, 2151#i16, 355#i16,
    (-4003)#i16, 1204#i16, (-5356)#i16, (-624)#i16, 5120#i16, (-4519)#i16,
    (-1689)#i16, 1056#i16, 3891#i16, (-3827)#i16, 1663#i16, (-2625)#i16,
    (-2449)#i16, 3995#i16, (-1160)#i16, 2788#i16, (-4540)#i16, 3125#i16,
    5068#i16, 3096#i16, 1893#i16, (-2807)#i16, (-5268)#i16, 2205#i16,
    (-4889)#i16, (-152)#i16, 569#i16, 4973#i16, (-825)#i16, 4393#i16, 4000#i16,
    1510#i16, 3419#i16, (-3360)#i16, 693#i16, (-3260)#i16, 4967#i16, 4859#i16,
    2963#i16, 554#i16, (-5107)#i16, (-73)#i16, (-4891)#i16, (-1927)#i16,
    5334#i16, 2605#i16, 2487#i16, (-2529)#i16, (-834)#i16, 1782#i16, 1111#i16,
    2113#i16, 4720#i16, (-4670)#i16, (-1053)#i16, (-4403)#i16
    ]

/-- [kopis::backend::crt::Q2_INV]
    Source: 'src/backend/crt.rs', lines 86:0-86:38 -/
@[global_simps, irreducible] def backend.crt.Q2_INV : Std.I16 := (-10751)#i16

/-- [kopis::backend::crt::ZETAS_Q2_QINV]
    Source: 'src/backend/crt.rs', lines 167:0-167:75 -/
@[global_simps, irreducible]
def backend.crt.ZETAS_Q2_QINV : Result (Array Std.I16 256#usize) :=
  backend.crt.zetas_qinv backend.crt.ZETAS_Q2 backend.crt.Q2_INV

/-- [kopis::backend::crt::ZETAS_Q1]
    Source: 'src/backend/crt.rs', lines 114:0-131:2 -/
@[global_simps, irreducible]
def backend.crt.ZETAS_Q1 : Array Std.I16 256#usize :=
  Array.make 256#usize [
    (-3593)#i16, 3777#i16, (-3182)#i16, 3625#i16, (-3696)#i16, (-1100)#i16,
    2456#i16, 2194#i16, 121#i16, (-2250)#i16, 834#i16, (-2495)#i16,
    (-2319)#i16, 2876#i16, (-1701)#i16, 1414#i16, 2816#i16, (-2088)#i16,
    (-2237)#i16, 1986#i16, (-1599)#i16, 1993#i16, 3706#i16, (-2006)#i16,
    (-1525)#i16, (-2557)#i16, 1296#i16, 1483#i16, (-2830)#i16, 3364#i16,
    617#i16, 1921#i16, (-3689)#i16, (-1738)#i16, 3266#i16, (-3600)#i16,
    810#i16, 1887#i16, (-638)#i16, (-7)#i16, (-438)#i16, (-679)#i16,
    (-1305)#i16, (-1760)#i16, 396#i16, (-3174)#i16, (-3555)#i16, (-1881)#i16,
    3772#i16, (-2535)#i16, (-2440)#i16, (-2555)#i16, 1535#i16, (-549)#i16,
    3153#i16, 2310#i16, (-1399)#i16, 1321#i16, 514#i16, (-2956)#i16,
    (-103)#i16, 2804#i16, (-2043)#i16, (-1431)#i16, (-1054)#i16, 1698#i16,
    (-3456)#i16, 1166#i16, 2426#i16, 3831#i16, 915#i16, (-2)#i16, (-3417)#i16,
    (-194)#i16, 2919#i16, 2789#i16, 3405#i16, 2385#i16, (-2113)#i16,
    (-2732)#i16, 2175#i16, 373#i16, 3692#i16, (-730)#i16, (-1756)#i16,
    3135#i16, (-2391)#i16, 660#i16, (-1497)#i16, 2572#i16, (-3145)#i16,
    1350#i16, (-2224)#i16, (-3588)#i16, (-1681)#i16, 2883#i16, (-1390)#i16,
    1598#i16, 3750#i16, 2762#i16, 2835#i16, 2764#i16, (-2233)#i16, 3816#i16,
    (-1533)#i16, 1464#i16, (-727)#i16, 1521#i16, 1386#i16, (-3428)#i16,
    (-921)#i16, (-2743)#i16, (-2160)#i16, 2649#i16, (-859)#i16, 2579#i16,
    1532#i16, 1919#i16, (-486)#i16, 404#i16, (-1056)#i16, 783#i16, 1799#i16,
    (-2665)#i16, 3480#i16, 2133#i16, (-3310)#i16, (-1168)#i16, (-17)#i16,
    3744#i16, 2422#i16, 2001#i16, 1278#i16, 929#i16, (-1348)#i16, (-2230)#i16,
    (-179)#i16, (-1242)#i16, (-2059)#i16, (-1070)#i16, 2161#i16, 1649#i16,
    2072#i16, 3177#i16, (-2071)#i16, 1121#i16, (-436)#i16, 236#i16, 715#i16,
    670#i16, (-658)#i16, (-1476)#i16, (-2378)#i16, 2767#i16, 3542#i16,
    (-226)#i16, 1203#i16, 1181#i16, (-151)#i16, (-3794)#i16, 1712#i16,
    (-222)#i16, 2786#i16, (-451)#i16, (-3547)#i16, 1779#i16, (-1151)#i16,
    (-434)#i16, 3568#i16, (-3693)#i16, 3581#i16, (-1586)#i16, 1509#i16,
    2918#i16, 2339#i16, (-1407)#i16, 3434#i16, (-3550)#i16, 2340#i16, 2891#i16,
    2998#i16, (-3314)#i16, 3461#i16, (-2719)#i16, (-2247)#i16, (-2589)#i16,
    1144#i16, 1072#i16, 1295#i16, (-2815)#i16, (-3770)#i16, 3450#i16, 3781#i16,
    (-2258)#i16, 796#i16, 3163#i16, (-3208)#i16, (-589)#i16, 2963#i16,
    (-124)#i16, 3214#i16, 3334#i16, (-3366)#i16, (-3745)#i16, 3723#i16,
    1931#i16, (-429)#i16, (-402)#i16, (-3408)#i16, 83#i16, (-1526)#i16,
    826#i16, (-1338)#i16, 2345#i16, (-2303)#i16, 2515#i16, (-642)#i16,
    (-1837)#i16, (-2965)#i16, (-791)#i16, 370#i16, 293#i16, 3312#i16, 2083#i16,
    (-1689)#i16, (-777)#i16, 2070#i16, 2262#i16, (-893)#i16, 2386#i16,
    (-188)#i16, (-1519)#i16, (-2874)#i16, (-1404)#i16, 1012#i16, 2130#i16,
    1441#i16, 2532#i16, (-3335)#i16, (-1084)#i16, (-3343)#i16, 2937#i16,
    509#i16, (-1403)#i16, 2812#i16, 3763#i16, 592#i16, 2005#i16, 3657#i16,
    2460#i16, (-3677)#i16, 3752#i16, 692#i16, 1669#i16, 2167#i16, (-3287)#i16
    ]

/-- [kopis::backend::crt::Q1_INV]
    Source: 'src/backend/crt.rs', lines 84:0-84:37 -/
@[global_simps, irreducible] def backend.crt.Q1_INV : Std.I16 := (-7679)#i16

/-- [kopis::backend::crt::ZETAS_Q1_QINV]
    Source: 'src/backend/crt.rs', lines 166:0-166:75 -/
@[global_simps, irreducible]
def backend.crt.ZETAS_Q1_QINV : Result (Array Std.I16 256#usize) :=
  backend.crt.zetas_qinv backend.crt.ZETAS_Q1 backend.crt.Q1_INV

/-- [kopis::backend::crt::zeta_q]:
    Source: 'src/backend/crt.rs', lines 218:0-224:1 -/
def backend.crt.zeta_q (SECOND : Bool) (k : Std.Usize) : Result Std.I16 := do
  if SECOND
  then let a ← backend.crt.ZETAS_Q2_QINV
       Array.index_usize a k
  else let a ← backend.crt.ZETAS_Q1_QINV
       Array.index_usize a k

/-- [kopis::backend::crt::zeta]:
    Source: 'src/backend/crt.rs', lines 213:0-215:1 -/
def backend.crt.zeta (SECOND : Bool) (k : Std.Usize) : Result Std.I16 := do
  if SECOND
  then Array.index_usize backend.crt.ZETAS_Q2 k
  else Array.index_usize backend.crt.ZETAS_Q1 k

/-- [kopis::backend::avx2::intrinsics::unpackhi_epi64]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 305:0-307:1 -/
axiom backend.avx2.intrinsics.unpackhi_epi64
  :
  backend.avx2.intrinsics.Vec256 → backend.avx2.intrinsics.Vec256 → Result
    backend.avx2.intrinsics.Vec256

/-- [kopis::backend::avx2::intrinsics::unpacklo_epi64]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 298:0-300:1 -/
axiom backend.avx2.intrinsics.unpacklo_epi64
  :
  backend.avx2.intrinsics.Vec256 → backend.avx2.intrinsics.Vec256 → Result
    backend.avx2.intrinsics.Vec256

/-- [kopis::backend::avx2::intrinsics::unpackhi_epi32]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 291:0-293:1 -/
axiom backend.avx2.intrinsics.unpackhi_epi32
  :
  backend.avx2.intrinsics.Vec256 → backend.avx2.intrinsics.Vec256 → Result
    backend.avx2.intrinsics.Vec256

/-- [kopis::backend::avx2::intrinsics::unpacklo_epi32]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 284:0-286:1 -/
axiom backend.avx2.intrinsics.unpacklo_epi32
  :
  backend.avx2.intrinsics.Vec256 → backend.avx2.intrinsics.Vec256 → Result
    backend.avx2.intrinsics.Vec256

/-- [kopis::backend::avx2::intrinsics::unpackhi_epi16]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 277:0-279:1 -/
axiom backend.avx2.intrinsics.unpackhi_epi16
  :
  backend.avx2.intrinsics.Vec256 → backend.avx2.intrinsics.Vec256 → Result
    backend.avx2.intrinsics.Vec256

/-- [kopis::backend::avx2::intrinsics::unpacklo_epi16]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 270:0-272:1 -/
axiom backend.avx2.intrinsics.unpacklo_epi16
  :
  backend.avx2.intrinsics.Vec256 → backend.avx2.intrinsics.Vec256 → Result
    backend.avx2.intrinsics.Vec256

/-- [kopis::backend::avx2::ntt::inlane_transpose8]:
    Source: 'src/backend/avx2/ntt.rs', lines 273:0-300:1 -/
def backend.avx2.ntt.inlane_transpose8
  (v : Array backend.avx2.intrinsics.Vec256 8#usize) :
  Result (Array backend.avx2.intrinsics.Vec256 8#usize)
  := do
  let v1 ← Array.index_usize v 0#usize
  let v2 ← Array.index_usize v 1#usize
  let a0 ← backend.avx2.intrinsics.unpacklo_epi16 v1 v2
  let a1 ← backend.avx2.intrinsics.unpackhi_epi16 v1 v2
  let v3 ← Array.index_usize v 2#usize
  let v4 ← Array.index_usize v 3#usize
  let a2 ← backend.avx2.intrinsics.unpacklo_epi16 v3 v4
  let a3 ← backend.avx2.intrinsics.unpackhi_epi16 v3 v4
  let v5 ← Array.index_usize v 4#usize
  let v6 ← Array.index_usize v 5#usize
  let a4 ← backend.avx2.intrinsics.unpacklo_epi16 v5 v6
  let a5 ← backend.avx2.intrinsics.unpackhi_epi16 v5 v6
  let v7 ← Array.index_usize v 6#usize
  let v8 ← Array.index_usize v 7#usize
  let a6 ← backend.avx2.intrinsics.unpacklo_epi16 v7 v8
  let a7 ← backend.avx2.intrinsics.unpackhi_epi16 v7 v8
  let b0 ← backend.avx2.intrinsics.unpacklo_epi32 a0 a2
  let b1 ← backend.avx2.intrinsics.unpackhi_epi32 a0 a2
  let b2 ← backend.avx2.intrinsics.unpacklo_epi32 a1 a3
  let b3 ← backend.avx2.intrinsics.unpackhi_epi32 a1 a3
  let b4 ← backend.avx2.intrinsics.unpacklo_epi32 a4 a6
  let b5 ← backend.avx2.intrinsics.unpackhi_epi32 a4 a6
  let b6 ← backend.avx2.intrinsics.unpacklo_epi32 a5 a7
  let b7 ← backend.avx2.intrinsics.unpackhi_epi32 a5 a7
  let v9 ← backend.avx2.intrinsics.unpacklo_epi64 b0 b4
  let v10 ← Array.update v 0#usize v9
  let v11 ← backend.avx2.intrinsics.unpackhi_epi64 b0 b4
  let v12 ← Array.update v10 1#usize v11
  let v13 ← backend.avx2.intrinsics.unpacklo_epi64 b1 b5
  let v14 ← Array.update v12 2#usize v13
  let v15 ← backend.avx2.intrinsics.unpackhi_epi64 b1 b5
  let v16 ← Array.update v14 3#usize v15
  let v17 ← backend.avx2.intrinsics.unpacklo_epi64 b2 b6
  let v18 ← Array.update v16 4#usize v17
  let v19 ← backend.avx2.intrinsics.unpackhi_epi64 b2 b6
  let v20 ← Array.update v18 5#usize v19
  let v21 ← backend.avx2.intrinsics.unpacklo_epi64 b3 b7
  let v22 ← Array.update v20 6#usize v21
  let v23 ← backend.avx2.intrinsics.unpackhi_epi64 b3 b7
  Array.update v22 7#usize v23

/-- [kopis::backend::avx2::intrinsics::store_i16]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 389:0-393:1 -/
axiom backend.avx2.intrinsics.store_i16
  {N : Std.Usize} :
  Array Std.I16 N → Std.Usize → backend.avx2.intrinsics.Vec256 → Result
    (Array Std.I16 N)

/-- [kopis::backend::avx2::intrinsics::load_i16]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 380:0-384:1 -/
axiom backend.avx2.intrinsics.load_i16
  {N : Std.Usize} :
  Array Std.I16 N → Std.Usize → Result backend.avx2.intrinsics.Vec256

/-- [kopis::backend::avx2::intrinsics::permute2x128_si256]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 313:0-315:1 -/
axiom backend.avx2.intrinsics.permute2x128_si256
  (IMM : Std.I32) :
  backend.avx2.intrinsics.Vec256 → backend.avx2.intrinsics.Vec256 → Result
    backend.avx2.intrinsics.Vec256

/-- [kopis::backend::avx2::ntt::transpose16]: loop 1:
    Source: 'src/backend/avx2/ntt.rs', lines 327:8-329:9 -/
@[rust_loop]
def backend.avx2.ntt.transpose16_loop0_loop0
  (iter : core.ops.range.Range Std.Usize) (scratch : Array Std.I16 256#usize)
  (half : Std.Usize) (v : Array backend.avx2.intrinsics.Vec256 8#usize) :
  Result (Array Std.I16 256#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok scratch
  | some j =>
    let i ← 8#usize * half
    let i1 ← i + j
    let v1 ← Array.index_usize v j
    let scratch1 ← backend.avx2.intrinsics.store_i16 scratch i1 v1
    backend.avx2.ntt.transpose16_loop0_loop0 iter1 scratch1 half v
partial_fixpoint

/-- [kopis::backend::avx2::ntt::transpose16]: loop 0:
    Source: 'src/backend/avx2/ntt.rs', lines 315:4-330:5 -/
@[rust_loop]
def backend.avx2.ntt.transpose16_loop0
  (iter : core.ops.range.Range Std.Usize) (b : Array Std.I16 256#usize)
  (scratch : Array Std.I16 256#usize) :
  Result (Array Std.I16 256#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok scratch
  | some half =>
    let i ← 8#usize * half
    let v ← backend.avx2.intrinsics.load_i16 b i
    let i1 ← i + 1#usize
    let v1 ← backend.avx2.intrinsics.load_i16 b i1
    let i2 ← i + 2#usize
    let v2 ← backend.avx2.intrinsics.load_i16 b i2
    let i3 ← i + 3#usize
    let v3 ← backend.avx2.intrinsics.load_i16 b i3
    let i4 ← i + 4#usize
    let v4 ← backend.avx2.intrinsics.load_i16 b i4
    let i5 ← i + 5#usize
    let v5 ← backend.avx2.intrinsics.load_i16 b i5
    let i6 ← i + 6#usize
    let v6 ← backend.avx2.intrinsics.load_i16 b i6
    let i7 ← i + 7#usize
    let v7 ← backend.avx2.intrinsics.load_i16 b i7
    let v8 ←
      backend.avx2.ntt.inlane_transpose8
        (Array.make 8#usize [ v, v1, v2, v3, v4, v5, v6, v7 ])
    let scratch1 ←
      backend.avx2.ntt.transpose16_loop0_loop0
        { start := 0#usize, «end» := 8#usize } scratch half v8
    backend.avx2.ntt.transpose16_loop0 iter1 b scratch1
partial_fixpoint

/-- [kopis::backend::avx2::ntt::transpose16]: loop 2:
    Source: 'src/backend/avx2/ntt.rs', lines 332:4-337:5 -/
@[rust_loop]
def backend.avx2.ntt.transpose16_loop1
  (iter : core.ops.range.Range Std.Usize) (b : Array Std.I16 256#usize)
  (scratch : Array Std.I16 256#usize) :
  Result (Array Std.I16 256#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok b
  | some r =>
    let a ← backend.avx2.intrinsics.load_i16 scratch r
    let i ← 8#usize + r
    let c ← backend.avx2.intrinsics.load_i16 scratch i
    let v ← backend.avx2.intrinsics.permute2x128_si256 32#i32 a c
    let b1 ← backend.avx2.intrinsics.store_i16 b r v
    let v1 ← backend.avx2.intrinsics.permute2x128_si256 49#i32 a c
    let b2 ← backend.avx2.intrinsics.store_i16 b1 i v1
    backend.avx2.ntt.transpose16_loop1 iter1 b2 scratch
partial_fixpoint

/-- [kopis::backend::avx2::ntt::transpose16]:
    Source: 'src/backend/avx2/ntt.rs', lines 313:0-338:1 -/
def backend.avx2.ntt.transpose16
  (b : Array Std.I16 256#usize) : Result (Array Std.I16 256#usize) := do
  let scratch := Array.repeat 256#usize 0#i16
  let scratch1 ←
    backend.avx2.ntt.transpose16_loop0 { start := 0#usize, «end» := 2#usize }
      b scratch
  backend.avx2.ntt.transpose16_loop1 { start := 0#usize, «end» := 8#usize } b
    scratch1

/-- [kopis::backend::avx2::intrinsics::srai_epi16]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 195:0-197:1 -/
axiom backend.avx2.intrinsics.srai_epi16
  (IMM : Std.I32) :
  backend.avx2.intrinsics.Vec256 → Result backend.avx2.intrinsics.Vec256

/-- [kopis::backend::avx2::intrinsics::mulhi_epi16]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 170:0-172:1 -/
axiom backend.avx2.intrinsics.mulhi_epi16
  :
  backend.avx2.intrinsics.Vec256 → backend.avx2.intrinsics.Vec256 → Result
    backend.avx2.intrinsics.Vec256

/-- [kopis::backend::avx2::intrinsics::mullo_epi16]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 163:0-165:1 -/
axiom backend.avx2.intrinsics.mullo_epi16
  :
  backend.avx2.intrinsics.Vec256 → backend.avx2.intrinsics.Vec256 → Result
    backend.avx2.intrinsics.Vec256

/-- [kopis::backend::avx2::intrinsics::sub_epi16]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 142:0-144:1 -/
axiom backend.avx2.intrinsics.sub_epi16
  :
  backend.avx2.intrinsics.Vec256 → backend.avx2.intrinsics.Vec256 → Result
    backend.avx2.intrinsics.Vec256

/-- [kopis::backend::avx2::intrinsics::add_epi16]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 135:0-137:1 -/
axiom backend.avx2.intrinsics.add_epi16
  :
  backend.avx2.intrinsics.Vec256 → backend.avx2.intrinsics.Vec256 → Result
    backend.avx2.intrinsics.Vec256

/-- [kopis::backend::avx2::ntt::barrett]:
    Source: 'src/backend/avx2/ntt.rs', lines 233:0-237:1 -/
def backend.avx2.ntt.barrett
  (x : backend.avx2.intrinsics.Vec256) (m : backend.avx2.intrinsics.Vec256)
  (round : backend.avx2.intrinsics.Vec256) (q : backend.avx2.intrinsics.Vec256)
  :
  Result backend.avx2.intrinsics.Vec256
  := do
  let v ← backend.avx2.intrinsics.mulhi_epi16 x m
  let t ← backend.avx2.intrinsics.add_epi16 v round
  let t1 ← backend.avx2.intrinsics.srai_epi16 11#i32 t
  let v1 ← backend.avx2.intrinsics.mullo_epi16 t1 q
  backend.avx2.intrinsics.sub_epi16 x v1

/-- [kopis::backend::avx2::ntt::barrett_block]: loop 0:
    Source: 'src/backend/avx2/ntt.rs', lines 262:4-264:5 -/
@[rust_loop]
def backend.avx2.ntt.barrett_block_loop
  (iter : core.ops.range.Range Std.Usize) (b : Array Std.I16 256#usize)
  (m : backend.avx2.intrinsics.Vec256) (round : backend.avx2.intrinsics.Vec256)
  (q : backend.avx2.intrinsics.Vec256) :
  Result (Array Std.I16 256#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok b
  | some i =>
    let v ← backend.avx2.intrinsics.load_i16 b i
    let v1 ← backend.avx2.ntt.barrett v m round q
    let b1 ← backend.avx2.intrinsics.store_i16 b i v1
    backend.avx2.ntt.barrett_block_loop iter1 b1 m round q
partial_fixpoint

/-- [kopis::backend::avx2::ntt::barrett_block]:
    Source: 'src/backend/avx2/ntt.rs', lines 261:0-265:1 -/
@[reducible]
def backend.avx2.ntt.barrett_block
  (b : Array Std.I16 256#usize) (m : backend.avx2.intrinsics.Vec256)
  (round : backend.avx2.intrinsics.Vec256) (q : backend.avx2.intrinsics.Vec256)
  :
  Result (Array Std.I16 256#usize)
  := do
  backend.avx2.ntt.barrett_block_loop { start := 0#usize, «end» := 16#usize }
    b m round q

/-- [kopis::backend::avx2::ntt::mont_mul]:
    Source: 'src/backend/avx2/ntt.rs', lines 221:0-224:1 -/
def backend.avx2.ntt.mont_mul
  (a : backend.avx2.intrinsics.Vec256) (z : backend.avx2.intrinsics.Vec256)
  (zq : backend.avx2.intrinsics.Vec256) (q : backend.avx2.intrinsics.Vec256) :
  Result backend.avx2.intrinsics.Vec256
  := do
  let t ← backend.avx2.intrinsics.mullo_epi16 a zq
  let v ← backend.avx2.intrinsics.mulhi_epi16 a z
  let v1 ← backend.avx2.intrinsics.mulhi_epi16 t q
  backend.avx2.intrinsics.sub_epi16 v v1

/-- [kopis::backend::avx2::ntt::ct_butterfly]:
    Source: 'src/backend/avx2/ntt.rs', lines 242:0-246:1 -/
def backend.avx2.ntt.ct_butterfly
  (lo : backend.avx2.intrinsics.Vec256) (hi : backend.avx2.intrinsics.Vec256)
  (z : backend.avx2.intrinsics.Vec256) (zq : backend.avx2.intrinsics.Vec256)
  (q : backend.avx2.intrinsics.Vec256) :
  Result (backend.avx2.intrinsics.Vec256 × backend.avx2.intrinsics.Vec256)
  := do
  let t ← backend.avx2.ntt.mont_mul hi z zq q
  let hi1 ← backend.avx2.intrinsics.sub_epi16 lo t
  let lo1 ← backend.avx2.intrinsics.add_epi16 lo t
  ok (lo1, hi1)

/-- [kopis::backend::avx2::ntt::Tbl]
    Source: 'src/backend/avx2/ntt.rs', lines 131:0-134:1 -/
structure backend.avx2.ntt.Tbl (N : Std.Usize) where
  z : Array Std.I16 N
  zq : Array Std.I16 N

/-- [kopis::backend::avx2::ntt::ld_tbl]:
    Source: 'src/backend/avx2/ntt.rs', lines 209:0-211:1 -/
def backend.avx2.ntt.ld_tbl
  {N : Std.Usize} (table : backend.avx2.ntt.Tbl N) (h : Std.Usize) :
  Result (backend.avx2.intrinsics.Vec256 × backend.avx2.intrinsics.Vec256)
  := do
  let v ← backend.avx2.intrinsics.load_i16 table.z h
  let v1 ← backend.avx2.intrinsics.load_i16 table.zq h
  ok (v, v1)

/-- [kopis::backend::avx2::ntt::lane_tbl]: loop 1:
    Source: 'src/backend/avx2/ntt.rs', lines 151:8-158:9 -/
@[rust_loop]
def backend.avx2.ntt.lane_tbl_loop0_loop0
  {N : Std.Usize} (zetas : Array Std.I16 256#usize) (qinv : Std.I16)
  (base : Std.Isize) (h_stride : Std.Isize) (m_stride : Std.Isize) (neg : Bool)
  (z : Array Std.I16 N) (zq : Array Std.I16 N) (h : Std.Usize) (m : Std.Usize)
  :
  Result ((Array Std.I16 N) × (Array Std.I16 N))
  := do
  if m < 16#usize
  then
    let i ← lift (UScalar.hcast .Isize h)
    let i1 ← i * h_stride
    let i2 ← base + i1
    let i3 ← lift (UScalar.hcast .Isize m)
    let i4 ← i3 * m_stride
    let idx ← i2 + i4
    let i5 ← lift (IScalar.hcast .Usize idx)
    let value ← Array.index_usize zetas i5
    let value1 ← if neg
                   then -. value
                   else ok value
    let i6 ← h * 16#usize
    let i7 ← i6 + m
    let a ← Array.update z i7 value1
    let i8 ← lift (core.num.I16.wrapping_mul value1 qinv)
    let i9 ← i6 + m
    let a1 ← Array.update zq i9 i8
    let m1 ← m + 1#usize
    backend.avx2.ntt.lane_tbl_loop0_loop0 zetas qinv base h_stride m_stride neg
      a a1 h m1
  else ok (z, zq)
partial_fixpoint

/-- [kopis::backend::avx2::ntt::lane_tbl]: loop 0:
    Source: 'src/backend/avx2/ntt.rs', lines 149:4-160:5 -/
@[rust_loop]
def backend.avx2.ntt.lane_tbl_loop0
  {N : Std.Usize} (zetas : Array Std.I16 256#usize) (qinv : Std.I16)
  (base : Std.Isize) (h_stride : Std.Isize) (m_stride : Std.Isize) (neg : Bool)
  (z : Array Std.I16 N) (zq : Array Std.I16 N) (h : Std.Usize) :
  Result ((Array Std.I16 N) × (Array Std.I16 N))
  := do
  let i ← h * 16#usize
  if i < N
  then
    let (z1, zq1) ←
      backend.avx2.ntt.lane_tbl_loop0_loop0 zetas qinv base h_stride m_stride
        neg z zq h 0#usize
    let h1 ← h + 1#usize
    backend.avx2.ntt.lane_tbl_loop0 zetas qinv base h_stride m_stride neg z1
      zq1 h1
  else ok (z, zq)
partial_fixpoint

/-- [kopis::backend::avx2::ntt::lane_tbl]:
    Source: 'src/backend/avx2/ntt.rs', lines 138:0-162:1 -/
def backend.avx2.ntt.lane_tbl
  (N : Std.Usize) (zetas : Array Std.I16 256#usize) (qinv : Std.I16)
  (base : Std.Isize) (h_stride : Std.Isize) (m_stride : Std.Isize) (neg : Bool)
  :
  Result (backend.avx2.ntt.Tbl N)
  := do
  let z := Array.repeat N 0#i16
  let zq := Array.repeat N 0#i16
  let (z1, zq1) ←
    backend.avx2.ntt.lane_tbl_loop0 zetas qinv base h_stride m_stride neg z zq
      0#usize
  ok { z := z1, zq := zq1 }

/-- [kopis::backend::avx2::ntt::FWD1_Q2]
    Source: 'src/backend/avx2/ntt.rs', lines 189:0-189:73 -/
@[global_simps, irreducible]
def backend.avx2.ntt.FWD1_Q2 : Result (backend.avx2.ntt.Tbl 128#usize) :=
  backend.avx2.ntt.lane_tbl 128#usize backend.crt.ZETAS_Q2 backend.crt.Q2_INV
    128#isize 1#isize 8#isize false

/-- [kopis::backend::avx2::ntt::FWD2_Q2]
    Source: 'src/backend/avx2/ntt.rs', lines 188:0-188:71 -/
@[global_simps, irreducible]
def backend.avx2.ntt.FWD2_Q2 : Result (backend.avx2.ntt.Tbl 64#usize) :=
  backend.avx2.ntt.lane_tbl 64#usize backend.crt.ZETAS_Q2 backend.crt.Q2_INV
    64#isize 1#isize 4#isize false

/-- [kopis::backend::avx2::ntt::FWD4_Q2]
    Source: 'src/backend/avx2/ntt.rs', lines 187:0-187:71 -/
@[global_simps, irreducible]
def backend.avx2.ntt.FWD4_Q2 : Result (backend.avx2.ntt.Tbl 32#usize) :=
  backend.avx2.ntt.lane_tbl 32#usize backend.crt.ZETAS_Q2 backend.crt.Q2_INV
    32#isize 1#isize 2#isize false

/-- [kopis::backend::avx2::ntt::FWD8_Q2]
    Source: 'src/backend/avx2/ntt.rs', lines 186:0-186:71 -/
@[global_simps, irreducible]
def backend.avx2.ntt.FWD8_Q2 : Result (backend.avx2.ntt.Tbl 16#usize) :=
  backend.avx2.ntt.lane_tbl 16#usize backend.crt.ZETAS_Q2 backend.crt.Q2_INV
    16#isize 0#isize 1#isize false

/-- [kopis::backend::avx2::ntt::FWD1_Q1]
    Source: 'src/backend/avx2/ntt.rs', lines 176:0-176:73 -/
@[global_simps, irreducible]
def backend.avx2.ntt.FWD1_Q1 : Result (backend.avx2.ntt.Tbl 128#usize) :=
  backend.avx2.ntt.lane_tbl 128#usize backend.crt.ZETAS_Q1 backend.crt.Q1_INV
    128#isize 1#isize 8#isize false

/-- [kopis::backend::avx2::ntt::FWD2_Q1]
    Source: 'src/backend/avx2/ntt.rs', lines 175:0-175:71 -/
@[global_simps, irreducible]
def backend.avx2.ntt.FWD2_Q1 : Result (backend.avx2.ntt.Tbl 64#usize) :=
  backend.avx2.ntt.lane_tbl 64#usize backend.crt.ZETAS_Q1 backend.crt.Q1_INV
    64#isize 1#isize 4#isize false

/-- [kopis::backend::avx2::ntt::FWD4_Q1]
    Source: 'src/backend/avx2/ntt.rs', lines 174:0-174:71 -/
@[global_simps, irreducible]
def backend.avx2.ntt.FWD4_Q1 : Result (backend.avx2.ntt.Tbl 32#usize) :=
  backend.avx2.ntt.lane_tbl 32#usize backend.crt.ZETAS_Q1 backend.crt.Q1_INV
    32#isize 1#isize 2#isize false

/-- [kopis::backend::avx2::ntt::FWD8_Q1]
    Source: 'src/backend/avx2/ntt.rs', lines 173:0-173:71 -/
@[global_simps, irreducible]
def backend.avx2.ntt.FWD8_Q1 : Result (backend.avx2.ntt.Tbl 16#usize) :=
  backend.avx2.ntt.lane_tbl 16#usize backend.crt.ZETAS_Q1 backend.crt.Q1_INV
    16#isize 0#isize 1#isize false

/-- [kopis::backend::avx2::intrinsics::set1_epi16]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 65:0-67:1 -/
axiom backend.avx2.intrinsics.set1_epi16
  : Std.I16 → Result backend.avx2.intrinsics.Vec256

/-- [kopis::backend::avx2::ntt::ntt_block]: loop 2:
    Source: 'src/backend/avx2/ntt.rs', lines 367:12-374:13 -/
@[rust_loop]
def backend.avx2.ntt.ntt_block_loop0_loop0_loop0
  (b : Array Std.I16 256#usize) (q : backend.avx2.intrinsics.Vec256)
  (half : Std.Usize) (start : Std.Usize) (z : backend.avx2.intrinsics.Vec256)
  (zq : backend.avx2.intrinsics.Vec256) (i : Std.Usize) :
  Result (Array Std.I16 256#usize)
  := do
  let i1 ← start + half
  if i < i1
  then
    let lo ← backend.avx2.intrinsics.load_i16 b i
    let i2 ← i + half
    let hi ← backend.avx2.intrinsics.load_i16 b i2
    let (lo1, hi1) ← backend.avx2.ntt.ct_butterfly lo hi z zq q
    let b1 ← backend.avx2.intrinsics.store_i16 b i lo1
    let b2 ← backend.avx2.intrinsics.store_i16 b1 i2 hi1
    let i3 ← i + 1#usize
    backend.avx2.ntt.ntt_block_loop0_loop0_loop0 b2 q half start z zq i3
  else ok b
partial_fixpoint

/-- [kopis::backend::avx2::ntt::ntt_block]: loop 1:
    Source: 'src/backend/avx2/ntt.rs', lines 362:8-376:9 -/
@[rust_loop]
def backend.avx2.ntt.ntt_block_loop0_loop0
  (SECOND : Bool) (b : Array Std.I16 256#usize)
  (q : backend.avx2.intrinsics.Vec256) (k : Std.Usize) (half : Std.Usize)
  (start : Std.Usize) :
  Result ((Array Std.I16 256#usize) × Std.Usize)
  := do
  if start < 16#usize
  then
    let k1 ← k + 1#usize
    let i ← backend.crt.zeta SECOND k1
    let z ← backend.avx2.intrinsics.set1_epi16 i
    let i1 ← backend.crt.zeta_q SECOND k1
    let zq ← backend.avx2.intrinsics.set1_epi16 i1
    let b1 ←
      backend.avx2.ntt.ntt_block_loop0_loop0_loop0 b q half start z zq start
    let i2 ← 2#usize * half
    let start1 ← start + i2
    backend.avx2.ntt.ntt_block_loop0_loop0 SECOND b1 q k1 half start1
  else ok (b, k)
partial_fixpoint

/-- [kopis::backend::avx2::ntt::ntt_block]: loop 0:
    Source: 'src/backend/avx2/ntt.rs', lines 360:4-383:5 -/
@[rust_loop]
def backend.avx2.ntt.ntt_block_loop0
  (SECOND : Bool) (b : Array Std.I16 256#usize)
  (q : backend.avx2.intrinsics.Vec256) (bm : backend.avx2.intrinsics.Vec256)
  (round : backend.avx2.intrinsics.Vec256) (k : Std.Usize) (half : Std.Usize)
  (level : Std.Usize) :
  Result (Array Std.I16 256#usize)
  := do
  if half >= 1#usize
  then
    let (b1, k1) ←
      backend.avx2.ntt.ntt_block_loop0_loop0 SECOND b q k half 0#usize
    let b2 ←
      if level = 2#usize
      then backend.avx2.ntt.barrett_block b1 bm round q
      else ok b1
    let half1 ← half / 2#usize
    let level1 ← level + 1#usize
    backend.avx2.ntt.ntt_block_loop0 SECOND b2 q bm round k1 half1 level1
  else ok b
partial_fixpoint

/-- [kopis::backend::avx2::ntt::ntt_block]: loop 3:
    Source: 'src/backend/avx2/ntt.rs', lines 392:74-408:5 -/
@[rust_loop]
def backend.avx2.ntt.ntt_block_loop1
  (SECOND : Bool) (iter : core.ops.range.Range Std.Usize)
  (b : Array Std.I16 256#usize) (q : backend.avx2.intrinsics.Vec256) :
  Result (Array Std.I16 256#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok b
  | some j =>
    let (z, zq) ←
      if SECOND
      then
        do
        let t ← backend.avx2.ntt.FWD8_Q2
        backend.avx2.ntt.ld_tbl t 0#usize
      else
        do
        let t ← backend.avx2.ntt.FWD8_Q1
        backend.avx2.ntt.ld_tbl t 0#usize
    let lo ← backend.avx2.intrinsics.load_i16 b j
    let i ← j + 8#usize
    let hi ← backend.avx2.intrinsics.load_i16 b i
    let (lo1, hi1) ← backend.avx2.ntt.ct_butterfly lo hi z zq q
    let b1 ← backend.avx2.intrinsics.store_i16 b j lo1
    let b2 ← backend.avx2.intrinsics.store_i16 b1 i hi1
    backend.avx2.ntt.ntt_block_loop1 SECOND iter1 b2 q
partial_fixpoint

/-- [kopis::backend::avx2::ntt::ntt_block]: loop 5:
    Source: 'src/backend/avx2/ntt.rs', lines 392:74-419:9 -/
@[rust_loop]
def backend.avx2.ntt.ntt_block_loop2_loop0
  (SECOND : Bool) (iter : core.ops.range.Range Std.Usize)
  (b : Array Std.I16 256#usize) (q : backend.avx2.intrinsics.Vec256)
  (h : Std.Usize) :
  Result (Array Std.I16 256#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok b
  | some j =>
    let (z, zq) ←
      if SECOND
      then do
           let t ← backend.avx2.ntt.FWD4_Q2
           backend.avx2.ntt.ld_tbl t h
      else do
           let t ← backend.avx2.ntt.FWD4_Q1
           backend.avx2.ntt.ld_tbl t h
    let i ← 8#usize * h
    let i1 ← i + j
    let lo ← backend.avx2.intrinsics.load_i16 b i1
    let i2 ← i + j
    let i3 ← i2 + 4#usize
    let hi ← backend.avx2.intrinsics.load_i16 b i3
    let (lo1, hi1) ← backend.avx2.ntt.ct_butterfly lo hi z zq q
    let i4 ← i + j
    let b1 ← backend.avx2.intrinsics.store_i16 b i4 lo1
    let i5 ← i + j
    let i6 ← i5 + 4#usize
    let b2 ← backend.avx2.intrinsics.store_i16 b1 i6 hi1
    backend.avx2.ntt.ntt_block_loop2_loop0 SECOND iter1 b2 q h
partial_fixpoint

/-- [kopis::backend::avx2::ntt::ntt_block]: loop 4:
    Source: 'src/backend/avx2/ntt.rs', lines 392:74-420:5 -/
@[rust_loop]
def backend.avx2.ntt.ntt_block_loop2
  (SECOND : Bool) (iter : core.ops.range.Range Std.Usize)
  (b : Array Std.I16 256#usize) (q : backend.avx2.intrinsics.Vec256) :
  Result (Array Std.I16 256#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok b
  | some h =>
    let b1 ←
      backend.avx2.ntt.ntt_block_loop2_loop0 SECOND
        { start := 0#usize, «end» := 4#usize } b q h
    backend.avx2.ntt.ntt_block_loop2 SECOND iter1 b1 q
partial_fixpoint

/-- [kopis::backend::avx2::ntt::ntt_block]: loop 7:
    Source: 'src/backend/avx2/ntt.rs', lines 392:74-431:9 -/
@[rust_loop]
def backend.avx2.ntt.ntt_block_loop3_loop0
  (SECOND : Bool) (iter : core.ops.range.Range Std.Usize)
  (b : Array Std.I16 256#usize) (q : backend.avx2.intrinsics.Vec256)
  (h : Std.Usize) :
  Result (Array Std.I16 256#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok b
  | some j =>
    let (z, zq) ←
      if SECOND
      then do
           let t ← backend.avx2.ntt.FWD2_Q2
           backend.avx2.ntt.ld_tbl t h
      else do
           let t ← backend.avx2.ntt.FWD2_Q1
           backend.avx2.ntt.ld_tbl t h
    let i ← 4#usize * h
    let i1 ← i + j
    let lo ← backend.avx2.intrinsics.load_i16 b i1
    let i2 ← i + j
    let i3 ← i2 + 2#usize
    let hi ← backend.avx2.intrinsics.load_i16 b i3
    let (lo1, hi1) ← backend.avx2.ntt.ct_butterfly lo hi z zq q
    let i4 ← i + j
    let b1 ← backend.avx2.intrinsics.store_i16 b i4 lo1
    let i5 ← i + j
    let i6 ← i5 + 2#usize
    let b2 ← backend.avx2.intrinsics.store_i16 b1 i6 hi1
    backend.avx2.ntt.ntt_block_loop3_loop0 SECOND iter1 b2 q h
partial_fixpoint

/-- [kopis::backend::avx2::ntt::ntt_block]: loop 6:
    Source: 'src/backend/avx2/ntt.rs', lines 392:74-432:5 -/
@[rust_loop]
def backend.avx2.ntt.ntt_block_loop3
  (SECOND : Bool) (iter : core.ops.range.Range Std.Usize)
  (b : Array Std.I16 256#usize) (q : backend.avx2.intrinsics.Vec256) :
  Result (Array Std.I16 256#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok b
  | some h =>
    let b1 ←
      backend.avx2.ntt.ntt_block_loop3_loop0 SECOND
        { start := 0#usize, «end» := 2#usize } b q h
    backend.avx2.ntt.ntt_block_loop3 SECOND iter1 b1 q
partial_fixpoint

/-- [kopis::backend::avx2::ntt::ntt_block]: loop 8:
    Source: 'src/backend/avx2/ntt.rs', lines 392:74-439:5 -/
@[rust_loop]
def backend.avx2.ntt.ntt_block_loop4
  (SECOND : Bool) (iter : core.ops.range.Range Std.Usize)
  (b : Array Std.I16 256#usize) (q : backend.avx2.intrinsics.Vec256) :
  Result (Array Std.I16 256#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok b
  | some h =>
    let (z, zq) ←
      if SECOND
      then do
           let t ← backend.avx2.ntt.FWD1_Q2
           backend.avx2.ntt.ld_tbl t h
      else do
           let t ← backend.avx2.ntt.FWD1_Q1
           backend.avx2.ntt.ld_tbl t h
    let i ← 2#usize * h
    let lo ← backend.avx2.intrinsics.load_i16 b i
    let i1 ← i + 1#usize
    let hi ← backend.avx2.intrinsics.load_i16 b i1
    let (lo1, hi1) ← backend.avx2.ntt.ct_butterfly lo hi z zq q
    let b1 ← backend.avx2.intrinsics.store_i16 b i lo1
    let i2 ← i + 1#usize
    let b2 ← backend.avx2.intrinsics.store_i16 b1 i2 hi1
    backend.avx2.ntt.ntt_block_loop4 SECOND iter1 b2 q
partial_fixpoint

/-- [kopis::backend::avx2::ntt::ntt_block]:
    Source: 'src/backend/avx2/ntt.rs', lines 350:0-445:1 -/
def backend.avx2.ntt.ntt_block
  (SECOND : Bool) (b : Array Std.I16 256#usize) :
  Result (Array Std.I16 256#usize)
  := do
  let i ← backend.crt.q SECOND
  let q ← backend.avx2.intrinsics.set1_epi16 i
  let i1 ← backend.crt.barrett_m SECOND
  let bm ← backend.avx2.intrinsics.set1_epi16 i1
  let i2 ← backend.crt.BARRETT_SH - 1#i32
  let i3 ← 1#i16 <<< i2
  let round ← backend.avx2.intrinsics.set1_epi16 i3
  let b1 ←
    backend.avx2.ntt.ntt_block_loop0 SECOND b q bm round 0#usize 8#usize
      0#usize
  let b2 ← backend.avx2.ntt.transpose16 b1
  let b3 ←
    backend.avx2.ntt.ntt_block_loop1 SECOND
      { start := 0#usize, «end» := 8#usize } b2 q
  let b4 ←
    backend.avx2.ntt.ntt_block_loop2 SECOND
      { start := 0#usize, «end» := 2#usize } b3 q
  let b5 ←
    backend.avx2.ntt.ntt_block_loop3 SECOND
      { start := 0#usize, «end» := 4#usize } b4 q
  let b6 ← backend.avx2.ntt.barrett_block b5 bm round q
  let b7 ←
    backend.avx2.ntt.ntt_block_loop4 SECOND
      { start := 0#usize, «end» := 8#usize } b6 q
  let b8 ← backend.avx2.ntt.transpose16 b7
  backend.avx2.ntt.barrett_block b8 bm round q

/-- [kopis::backend::avx2::intrinsics::load_u16]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 398:0-402:1 -/
axiom backend.avx2.intrinsics.load_u16
  {N : Std.Usize} :
  Array Std.U16 N → Std.Usize → Result backend.avx2.intrinsics.Vec256

/-- [kopis::backend::avx2::ntt::split_and_transform]: loop 0:
    Source: 'src/backend/avx2/ntt.rs', lines 564:4-571:5 -/
@[rust_loop]
def backend.avx2.ntt.split_and_transform_loop
  (REDUCE : Bool) (iter : core.ops.range.Range Std.Usize)
  (elem : Array Std.U16 256#usize) (b : Array Std.I16 256#usize)
  (q : backend.avx2.intrinsics.Vec256) (bm : backend.avx2.intrinsics.Vec256)
  (round : backend.avx2.intrinsics.Vec256) :
  Result (Array Std.I16 256#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok b
  | some i =>
    let x ← backend.avx2.intrinsics.load_u16 elem i
    let x1 ← if REDUCE
               then backend.avx2.ntt.barrett x bm round q
               else ok x
    let b1 ← backend.avx2.intrinsics.store_i16 b i x1
    backend.avx2.ntt.split_and_transform_loop REDUCE iter1 elem b1 q bm round
partial_fixpoint

/-- [kopis::backend::avx2::ntt::split_and_transform]:
    Source: 'src/backend/avx2/ntt.rs', lines 557:0-574:1 -/
def backend.avx2.ntt.split_and_transform
  (SECOND : Bool) (REDUCE : Bool) (elem : Array Std.U16 256#usize)
  (b : Array Std.I16 256#usize) :
  Result (Array Std.I16 256#usize)
  := do
  let i ← backend.crt.q SECOND
  let q ← backend.avx2.intrinsics.set1_epi16 i
  let i1 ← backend.crt.barrett_m SECOND
  let bm ← backend.avx2.intrinsics.set1_epi16 i1
  let i2 ← backend.crt.BARRETT_SH - 1#i32
  let i3 ← 1#i16 <<< i2
  let round ← backend.avx2.intrinsics.set1_epi16 i3
  let b1 ←
    backend.avx2.ntt.split_and_transform_loop REDUCE
      { start := 0#usize, «end» := 16#usize } elem b q bm round
  backend.avx2.ntt.ntt_block SECOND b1

/-- [kopis::backend::avx2::ntt::from_ring_elem]: loop 0:
    Source: 'src/backend/avx2/ntt.rs', lines 592:4-594:5 -/
@[rust_loop]
def backend.avx2.ntt.from_ring_elem_loop0
  (iter : core.ops.range.Range Std.Usize) (out : Array Std.I16 512#usize)
  (b : Array Std.I16 256#usize) :
  Result (Array Std.I16 512#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok out
  | some i =>
    let v ← backend.avx2.intrinsics.load_i16 b i
    let out1 ← backend.avx2.intrinsics.store_i16 out i v
    backend.avx2.ntt.from_ring_elem_loop0 iter1 out1 b
partial_fixpoint

/-- [kopis::backend::avx2::ntt::from_ring_elem]: loop 1:
    Source: 'src/backend/avx2/ntt.rs', lines 597:4-599:5 -/
@[rust_loop]
def backend.avx2.ntt.from_ring_elem_loop1
  (iter : core.ops.range.Range Std.Usize) (out : Array Std.I16 512#usize)
  (b : Array Std.I16 256#usize) :
  Result (Array Std.I16 512#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok out
  | some i =>
    let i1 ← 16#usize + i
    let v ← backend.avx2.intrinsics.load_i16 b i
    let out1 ← backend.avx2.intrinsics.store_i16 out i1 v
    backend.avx2.ntt.from_ring_elem_loop1 iter1 out1 b
partial_fixpoint

/-- [kopis::backend::avx2::ntt::from_ring_elem]:
    Source: 'src/backend/avx2/ntt.rs', lines 587:0-602:1 -/
def backend.avx2.ntt.from_ring_elem
  (REDUCE : Bool) (elem : Array Std.U16 256#usize) :
  Result (Array Std.I16 512#usize)
  := do
  let out := Array.repeat 512#usize 0#i16
  let b := Array.repeat 256#usize 0#i16
  let b1 ← backend.avx2.ntt.split_and_transform false REDUCE elem b
  let out1 ←
    backend.avx2.ntt.from_ring_elem_loop0
      { start := 0#usize, «end» := 16#usize } out b1
  let b2 ← backend.avx2.ntt.split_and_transform true REDUCE elem b1
  backend.avx2.ntt.from_ring_elem_loop1
    { start := 0#usize, «end» := 16#usize } out1 b2

/-- [kopis::backend::avx2::ntt::from_uniform]:
    Source: 'src/backend/avx2/ntt.rs', lines 610:0-612:1 -/
def backend.avx2.ntt.from_uniform
  (elem : Array Std.U16 256#usize) : Result (Array Std.I16 512#usize) := do
  backend.avx2.ntt.from_ring_elem true elem

/-- [kopis::arithmetic::ntt_crt::mont_mul]:
    Source: 'src/arithmetic/ntt_crt.rs', lines 74:0-79:1 -/
def arithmetic.ntt_crt.mont_mul
  (a : Std.I16) (z : Std.I16) (zq : Std.I16) (q : Std.I16) :
  Result Std.I16
  := do
  let t ← lift (core.num.I16.wrapping_mul a zq)
  let i ← lift (IScalar.cast .I32 a)
  let i1 ← lift (IScalar.cast .I32 z)
  let i2 ← lift (core.num.I32.wrapping_mul i i1)
  let i3 ← i2 >>> 16#i32
  let hi ← lift (IScalar.cast .I16 i3)
  let i4 ← lift (IScalar.cast .I32 t)
  let i5 ← lift (IScalar.cast .I32 q)
  let i6 ← lift (core.num.I32.wrapping_mul i4 i5)
  let i7 ← i6 >>> 16#i32
  let th ← lift (IScalar.cast .I16 i7)
  ok (core.num.I16.wrapping_sub hi th)

/-- [kopis::arithmetic::ntt_crt::ct_level]: loop 1:
    Source: 'src/arithmetic/ntt_crt.rs', lines 115:8-119:9 -/
@[rust_loop]
def arithmetic.ntt_crt.ct_level_loop0_loop0
  (LEN : Std.Usize) (iter : core.ops.range.Range Std.Usize)
  (b : Array Std.I16 256#usize) (q : Std.I16) (z : Std.I16) (zq : Std.I16) :
  Result (Array Std.I16 256#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok b
  | some j =>
    let i ← j + LEN
    let i1 ← Array.index_usize b i
    let t ← arithmetic.ntt_crt.mont_mul i1 z zq q
    let i2 ← Array.index_usize b j
    let i3 ← lift (core.num.I16.wrapping_sub i2 t)
    let b1 ← Array.update b i i3
    let i4 ← Array.index_usize b1 j
    let i5 ← lift (core.num.I16.wrapping_add i4 t)
    let a ← Array.update b1 j i5
    arithmetic.ntt_crt.ct_level_loop0_loop0 LEN iter1 a q z zq
partial_fixpoint

/-- [kopis::arithmetic::ntt_crt::ct_level]: loop 0:
    Source: 'src/arithmetic/ntt_crt.rs', lines 111:4-121:5 -/
@[rust_loop]
def arithmetic.ntt_crt.ct_level_loop0
  (LEN : Std.Usize) (SECOND : Bool) (b : Array Std.I16 256#usize)
  (k : Std.Usize) (q : Std.I16) (start : Std.Usize) :
  Result ((Array Std.I16 256#usize) × Std.Usize)
  := do
  if start < consts.RING_DEG
  then
    let k1 ← k + 1#usize
    let z ← backend.crt.zeta SECOND k1
    let zq ← backend.crt.zeta_q SECOND k1
    let i ← start + LEN
    let b1 ←
      arithmetic.ntt_crt.ct_level_loop0_loop0 LEN { start, «end» := i } b q z
        zq
    let i1 ← 2#usize * LEN
    let start1 ← start + i1
    arithmetic.ntt_crt.ct_level_loop0 LEN SECOND b1 k1 q start1
  else ok (b, k)
partial_fixpoint

/-- [kopis::arithmetic::ntt_crt::ct_level]:
    Source: 'src/arithmetic/ntt_crt.rs', lines 108:0-122:1 -/
def arithmetic.ntt_crt.ct_level
  (LEN : Std.Usize) (SECOND : Bool) (b : Array Std.I16 256#usize)
  (k : Std.Usize) :
  Result ((Array Std.I16 256#usize) × Std.Usize)
  := do
  let q ← backend.crt.q SECOND
  arithmetic.ntt_crt.ct_level_loop0 LEN SECOND b k q 0#usize

/-- [kopis::arithmetic::ntt_crt::barrett]:
    Source: 'src/arithmetic/ntt_crt.rs', lines 87:0-91:1 -/
def arithmetic.ntt_crt.barrett
  (x : Std.I16) (m : Std.I16) (q : Std.I16) : Result Std.I16 := do
  let i ← lift (IScalar.cast .I32 x)
  let i1 ← lift (IScalar.cast .I32 m)
  let i2 ← lift (core.num.I32.wrapping_mul i i1)
  let i3 ← i2 >>> 16#i32
  let t ← lift (IScalar.cast .I16 i3)
  let i4 ← backend.crt.BARRETT_SH - 1#i32
  let i5 ← 1#i16 <<< i4
  let i6 ← lift (core.num.I16.wrapping_add t i5)
  let t1 ← i6 >>> backend.crt.BARRETT_SH
  let i7 ← lift (core.num.I16.wrapping_mul t1 q)
  ok (core.num.I16.wrapping_sub x i7)

/-- [kopis::arithmetic::ntt_crt::barrett_block]: loop 0:
    Source: 'src/arithmetic/ntt_crt.rs', lines 97:4-99:5 -/
@[rust_loop]
def arithmetic.ntt_crt.barrett_block_loop
  (iter : core.ops.range.Range Std.Usize) (b : Array Std.I16 256#usize)
  (q : Std.I16) (m : Std.I16) :
  Result (Array Std.I16 256#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok b
  | some i =>
    let i1 ← Array.index_usize b i
    let i2 ← arithmetic.ntt_crt.barrett i1 m q
    let a ← Array.update b i i2
    arithmetic.ntt_crt.barrett_block_loop iter1 a q m
partial_fixpoint

/-- [kopis::arithmetic::ntt_crt::barrett_block]:
    Source: 'src/arithmetic/ntt_crt.rs', lines 94:0-100:1 -/
def arithmetic.ntt_crt.barrett_block
  (SECOND : Bool) (b : Array Std.I16 256#usize) :
  Result (Array Std.I16 256#usize)
  := do
  let q ← backend.crt.q SECOND
  let m ← backend.crt.barrett_m SECOND
  arithmetic.ntt_crt.barrett_block_loop
    { start := 0#usize, «end» := consts.RING_DEG } b q m

/-- [kopis::arithmetic::ntt_crt::ntt_block]:
    Source: 'src/arithmetic/ntt_crt.rs', lines 152:0-167:1 -/
def arithmetic.ntt_crt.ntt_block
  (SECOND : Bool) (b : Array Std.I16 256#usize) :
  Result (Array Std.I16 256#usize)
  := do
  let (b1, k) ← arithmetic.ntt_crt.ct_level 128#usize SECOND b 0#usize
  let (b2, k1) ← arithmetic.ntt_crt.ct_level 64#usize SECOND b1 k
  let (b3, k2) ← arithmetic.ntt_crt.ct_level 32#usize SECOND b2 k1
  let b4 ← arithmetic.ntt_crt.barrett_block SECOND b3
  let (b5, k3) ← arithmetic.ntt_crt.ct_level 16#usize SECOND b4 k2
  let (b6, k4) ← arithmetic.ntt_crt.ct_level 8#usize SECOND b5 k3
  let (b7, k5) ← arithmetic.ntt_crt.ct_level 4#usize SECOND b6 k4
  let b8 ← arithmetic.ntt_crt.barrett_block SECOND b7
  let (b9, k6) ← arithmetic.ntt_crt.ct_level 2#usize SECOND b8 k5
  let (b10, _) ← arithmetic.ntt_crt.ct_level 1#usize SECOND b9 k6
  arithmetic.ntt_crt.barrett_block SECOND b10

/-- [kopis::arithmetic::ntt_crt::split_and_transform]: loop 0:
    Source: 'src/arithmetic/ntt_crt.rs', lines 208:4-211:5 -/
@[rust_loop]
def arithmetic.ntt_crt.split_and_transform_loop
  (REDUCE : Bool) (iter : core.ops.range.Range Std.Usize)
  (elem : Array Std.U16 256#usize) (b : Array Std.I16 256#usize) (q : Std.I16)
  (m : Std.I16) :
  Result (Array Std.I16 256#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok b
  | some i =>
    let i1 ← Array.index_usize elem i
    let x ← lift (UScalar.hcast .I16 i1)
    let x1 ← if REDUCE
               then arithmetic.ntt_crt.barrett x m q
               else ok x
    let a ← Array.update b i x1
    arithmetic.ntt_crt.split_and_transform_loop REDUCE iter1 elem a q m
partial_fixpoint

/-- [kopis::arithmetic::ntt_crt::split_and_transform]:
    Source: 'src/arithmetic/ntt_crt.rs', lines 202:0-213:1 -/
def arithmetic.ntt_crt.split_and_transform
  (SECOND : Bool) (REDUCE : Bool) (elem : Array Std.U16 256#usize)
  (b : Array Std.I16 256#usize) :
  Result (Array Std.I16 256#usize)
  := do
  let q ← backend.crt.q SECOND
  let m ← backend.crt.barrett_m SECOND
  let b1 ←
    arithmetic.ntt_crt.split_and_transform_loop REDUCE
      { start := 0#usize, «end» := consts.RING_DEG } elem b q m
  arithmetic.ntt_crt.ntt_block SECOND b1

/-- [kopis::arithmetic::ntt_crt::from_ring_elem]:
    Source: 'src/arithmetic/ntt_crt.rs', lines 217:0-228:1 -/
def arithmetic.ntt_crt.from_ring_elem
  (REDUCE : Bool) (elem : Array Std.U16 256#usize) :
  Result (Array Std.I16 512#usize)
  := do
  let out := Array.repeat 512#usize 0#i16
  let b := Array.repeat 256#usize 0#i16
  let b1 ← arithmetic.ntt_crt.split_and_transform false REDUCE elem b
  let (s, index_mut_back) ←
    core.array.Array.index_mut (core.ops.index.IndexMutSlice
      (core.slice.index.SliceIndexRangeToUsizeSlice Std.I16)) out
      { «end» := consts.RING_DEG }
  let s1 ← lift (Array.to_slice b1)
  let s2 ← core.slice.Slice.copy_from_slice core.marker.CopyI16 s s1
  let b2 ← arithmetic.ntt_crt.split_and_transform true REDUCE elem b1
  let out1 := index_mut_back s2
  let (s3, index_mut_back1) ←
    core.array.Array.index_mut (core.ops.index.IndexMutSlice
      (core.slice.index.SliceIndexRangeFromUsizeSlice Std.I16)) out1
      { start := consts.RING_DEG }
  let s4 ← lift (Array.to_slice b2)
  let s5 ← core.slice.Slice.copy_from_slice core.marker.CopyI16 s3 s4
  ok (index_mut_back1 s5)

/-- [kopis::arithmetic::ntt_crt::from_uniform]:
    Source: 'src/arithmetic/ntt_crt.rs', lines 231:0-233:1 -/
def arithmetic.ntt_crt.from_uniform
  (elem : Array Std.U16 256#usize) : Result (Array Std.I16 512#usize) := do
  arithmetic.ntt_crt.from_ring_elem true elem

/-- [kopis::arithmetic::ntt::{kopis::arithmetic::ntt::NttElem}::from_uniform]:
    Source: 'src/arithmetic/ntt.rs', lines 265:4-283:5 -/
def arithmetic.ntt.NttElem.from_uniform
  (elem : arithmetic.ring_arith.RingElem) : Result arithmetic.ntt.NttElem := do
  let b ← backend.avx2.cpu.available
  if b
  then let a ← backend.avx2.ntt.from_uniform elem
       ok a
  else let a ← arithmetic.ntt_crt.from_uniform elem
       ok a

/-- [kopis::backend::avx2::ntt::from_secret]:
    Source: 'src/backend/avx2/ntt.rs', lines 620:0-622:1 -/
def backend.avx2.ntt.from_secret
  (elem : Array Std.U16 256#usize) : Result (Array Std.I16 512#usize) := do
  backend.avx2.ntt.from_ring_elem false elem

/-- [kopis::arithmetic::ntt_crt::from_secret]:
    Source: 'src/arithmetic/ntt_crt.rs', lines 236:0-238:1 -/
def arithmetic.ntt_crt.from_secret
  (elem : Array Std.U16 256#usize) : Result (Array Std.I16 512#usize) := do
  arithmetic.ntt_crt.from_ring_elem false elem

/-- [kopis::arithmetic::ntt::{kopis::arithmetic::ntt::NttElem}::from_secret]:
    Source: 'src/arithmetic/ntt.rs', lines 290:4-306:5 -/
def arithmetic.ntt.NttElem.from_secret
  (elem : arithmetic.ring_arith.RingElem) : Result arithmetic.ntt.NttElem := do
  let b ← backend.avx2.cpu.available
  if b
  then let a ← backend.avx2.ntt.from_secret elem
       ok a
  else let a ← arithmetic.ntt_crt.from_secret elem
       ok a

/-- [kopis::arithmetic::ntt::NttMatrix]
    Source: 'src/arithmetic/ntt.rs', lines 311:0-311:90 -/
@[reducible]
def arithmetic.ntt.NttMatrix (X : Std.Usize) (Y : Std.Usize) :=
  Array (Array arithmetic.ntt.NttElem Y) X

/-- [kopis::arithmetic::ntt::{impl core::clone::Clone for kopis::arithmetic::ntt::NttMatrix<X, Y>}::clone]:
    Source: 'src/arithmetic/ntt.rs', lines 310:9-310:14
    Visibility: public -/
def arithmetic.ntt.NttMatrix.Insts.CoreCloneClone.clone
  {X : Std.Usize} {Y : Std.Usize} (self : arithmetic.ntt.NttMatrix X Y) :
  Result (arithmetic.ntt.NttMatrix X Y)
  := do
  let a ←
    core.array.CloneArray.clone (core.clone.CloneArray Y
      arithmetic.ntt.NttElem.Insts.CoreCloneClone) self
  ok a

/-- Trait implementation: [kopis::arithmetic::ntt::{impl core::clone::Clone for kopis::arithmetic::ntt::NttMatrix<X, Y>}]
    Source: 'src/arithmetic/ntt.rs', lines 310:9-310:14 -/
@[reducible]
def arithmetic.ntt.NttMatrix.Insts.CoreCloneClone (X : Std.Usize) (Y :
  Std.Usize) : core.clone.Clone (arithmetic.ntt.NttMatrix X Y) := {
  clone := arithmetic.ntt.NttMatrix.Insts.CoreCloneClone.clone
}

/-- [kopis::arithmetic::ntt::{impl zeroize::Zeroize for kopis::arithmetic::ntt::NttMatrix<X, Y>}::zeroize]:
    Source: 'src/arithmetic/ntt.rs', lines 310:16-310:23
    Visibility: public -/
def arithmetic.ntt.NttMatrix.Insts.ZeroizeZeroize.zeroize
  {X : Std.Usize} {Y : Std.Usize} (self : arithmetic.ntt.NttMatrix X Y) :
  Result (arithmetic.ntt.NttMatrix X Y)
  := do
  let __zeroize_field_0 ←
    Array.Insts.ZeroizeZeroize.zeroize (Array.Insts.ZeroizeZeroize Y
      arithmetic.ntt.NttElem.Insts.ZeroizeZeroize) self
  ok __zeroize_field_0

/-- Trait implementation: [kopis::arithmetic::ntt::{impl zeroize::Zeroize for kopis::arithmetic::ntt::NttMatrix<X, Y>}]
    Source: 'src/arithmetic/ntt.rs', lines 310:16-310:23 -/
@[reducible]
def arithmetic.ntt.NttMatrix.Insts.ZeroizeZeroize (X : Std.Usize) (Y :
  Std.Usize) : zeroize.Zeroize (arithmetic.ntt.NttMatrix X Y) := {
  zeroize := arithmetic.ntt.NttMatrix.Insts.ZeroizeZeroize.zeroize
}

/-- [kopis::arithmetic::ntt::{impl core::default::Default for kopis::arithmetic::ntt::NttMatrix<X, Y>}::default]:
    Source: 'src/arithmetic/ntt.rs', lines 314:4-316:5
    Visibility: public -/
def arithmetic.ntt.NttMatrix.Insts.CoreDefaultDefault.default
  (X : Std.Usize) (Y : Std.Usize) : Result (arithmetic.ntt.NttMatrix X Y) := do
  let ne ← arithmetic.ntt.NttElem.Insts.CoreDefaultDefault.default
  let a := Array.repeat Y ne
  let a1 := Array.repeat X a
  ok a1

/-- Trait implementation: [kopis::arithmetic::ntt::{impl core::default::Default for kopis::arithmetic::ntt::NttMatrix<X, Y>}]
    Source: 'src/arithmetic/ntt.rs', lines 313:0-317:1 -/
@[reducible]
def arithmetic.ntt.NttMatrix.Insts.CoreDefaultDefault (X : Std.Usize) (Y :
  Std.Usize) : core.default.Default (arithmetic.ntt.NttMatrix X Y) := {
  default := arithmetic.ntt.NttMatrix.Insts.CoreDefaultDefault.default X Y
}

/-- [kopis::backend::avx2::intrinsics::store_i32]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 425:0-429:1 -/
axiom backend.avx2.intrinsics.store_i32
  {N : Std.Usize} :
  Array Std.I32 N → Std.Usize → backend.avx2.intrinsics.Vec256 → Result
    (Array Std.I32 N)

/-- [kopis::backend::avx2::intrinsics::add_epi32]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 149:0-151:1 -/
axiom backend.avx2.intrinsics.add_epi32
  :
  backend.avx2.intrinsics.Vec256 → backend.avx2.intrinsics.Vec256 → Result
    backend.avx2.intrinsics.Vec256

/-- [kopis::backend::avx2::ntt::pointwise_mul_acc]: loop 1:
    Source: 'src/backend/avx2/ntt.rs', lines 641:8-661:9 -/
@[rust_loop]
def backend.avx2.ntt.pointwise_mul_acc_loop0_loop0
  (iter : core.ops.range.Range Std.Usize) (acc : Array Std.I32 512#usize)
  (lhs : Array Std.I16 512#usize) (rhs : Array Std.I16 512#usize)
  (block : Std.Usize) :
  Result (Array Std.I32 512#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok acc
  | some i =>
    let i1 ← 16#usize * block
    let i2 ← i1 + i
    let l ← backend.avx2.intrinsics.load_i16 lhs i2
    let i3 ← i1 + i
    let r ← backend.avx2.intrinsics.load_i16 rhs i3
    let lo ← backend.avx2.intrinsics.mullo_epi16 l r
    let hi ← backend.avx2.intrinsics.mulhi_epi16 l r
    let p0 ← backend.avx2.intrinsics.unpacklo_epi16 lo hi
    let p1 ← backend.avx2.intrinsics.unpackhi_epi16 lo hi
    let first ← backend.avx2.intrinsics.permute2x128_si256 32#i32 p0 p1
    let second ← backend.avx2.intrinsics.permute2x128_si256 49#i32 p0 p1
    let i4 ← 32#usize * block
    let i5 ← 2#usize * i
    let a0 ← i4 + i5
    let a1 ← a0 + 1#usize
    let v ← backend.avx2.intrinsics.load_i32 acc a0
    let v1 ← backend.avx2.intrinsics.add_epi32 v first
    let acc1 ← backend.avx2.intrinsics.store_i32 acc a0 v1
    let v2 ← backend.avx2.intrinsics.load_i32 acc1 a1
    let v3 ← backend.avx2.intrinsics.add_epi32 v2 second
    let acc2 ← backend.avx2.intrinsics.store_i32 acc1 a1 v3
    backend.avx2.ntt.pointwise_mul_acc_loop0_loop0 iter1 acc2 lhs rhs block
partial_fixpoint

/-- [kopis::backend::avx2::ntt::pointwise_mul_acc]: loop 0:
    Source: 'src/backend/avx2/ntt.rs', lines 640:4-662:5 -/
@[rust_loop]
def backend.avx2.ntt.pointwise_mul_acc_loop0
  (iter : core.ops.range.Range Std.Usize) (acc : Array Std.I32 512#usize)
  (lhs : Array Std.I16 512#usize) (rhs : Array Std.I16 512#usize) :
  Result (Array Std.I32 512#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok acc
  | some block =>
    let acc1 ←
      backend.avx2.ntt.pointwise_mul_acc_loop0_loop0
        { start := 0#usize, «end» := 16#usize } acc lhs rhs block
    backend.avx2.ntt.pointwise_mul_acc_loop0 iter1 acc1 lhs rhs
partial_fixpoint

/-- [kopis::backend::avx2::ntt::pointwise_mul_acc]:
    Source: 'src/backend/avx2/ntt.rs', lines 635:0-663:1 -/
@[reducible]
def backend.avx2.ntt.pointwise_mul_acc
  (acc : Array Std.I32 512#usize) (lhs : Array Std.I16 512#usize)
  (rhs : Array Std.I16 512#usize) :
  Result (Array Std.I32 512#usize)
  := do
  backend.avx2.ntt.pointwise_mul_acc_loop0
    { start := 0#usize, «end» := 2#usize } acc lhs rhs

/-- [kopis::arithmetic::ntt_crt::pointwise_mul_acc]: loop 0:
    Source: 'src/arithmetic/ntt_crt.rs', lines 250:4-252:5 -/
@[rust_loop]
def arithmetic.ntt_crt.pointwise_mul_acc_loop
  (iter : core.ops.range.Range Std.Usize) (acc : Array Std.I32 512#usize)
  (lhs : Array Std.I16 512#usize) (rhs : Array Std.I16 512#usize) :
  Result (Array Std.I32 512#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok acc
  | some i =>
    let i1 ← Array.index_usize acc i
    let i2 ← Array.index_usize lhs i
    let i3 ← lift (IScalar.cast .I32 i2)
    let i4 ← Array.index_usize rhs i
    let i5 ← lift (IScalar.cast .I32 i4)
    let i6 ← lift (core.num.I32.wrapping_mul i3 i5)
    let i7 ← lift (core.num.I32.wrapping_add i1 i6)
    let a ← Array.update acc i i7
    arithmetic.ntt_crt.pointwise_mul_acc_loop iter1 a lhs rhs
partial_fixpoint

/-- [kopis::arithmetic::ntt_crt::pointwise_mul_acc]:
    Source: 'src/arithmetic/ntt_crt.rs', lines 245:0-253:1 -/
def arithmetic.ntt_crt.pointwise_mul_acc
  (acc : Array Std.I32 512#usize) (lhs : Array Std.I16 512#usize)
  (rhs : Array Std.I16 512#usize) :
  Result (Array Std.I32 512#usize)
  := do
  let i ← 2#usize * consts.RING_DEG
  arithmetic.ntt_crt.pointwise_mul_acc_loop { start := 0#usize, «end» := i }
    acc lhs rhs

/-- [kopis::arithmetic::ntt::pointwise_mul_acc]:
    Source: 'src/arithmetic/ntt.rs', lines 324:0-342:1 -/
def arithmetic.ntt.pointwise_mul_acc
  (acc : Array Std.I32 512#usize) (lhs : arithmetic.ntt.NttElem)
  (rhs : arithmetic.ntt.NttElem) :
  Result (Array Std.I32 512#usize)
  := do
  let b ← backend.avx2.cpu.available
  if b
  then backend.avx2.ntt.pointwise_mul_acc acc lhs rhs
  else arithmetic.ntt_crt.pointwise_mul_acc acc lhs rhs

/-- [kopis::backend::crt::CRT_Q1_INV_MONT]
    Source: 'src/backend/crt.rs', lines 107:0-107:45 -/
@[global_simps, irreducible]
def backend.crt.CRT_Q1_INV_MONT : Std.I16 := 3563#i16

/-- [kopis::backend::crt::CRT_Q_HALF]
    Source: 'src/backend/crt.rs', lines 105:0-105:44 -/
@[global_simps, irreducible]
def backend.crt.CRT_Q_HALF : Std.I32 := 41296896#i32

/-- [kopis::backend::crt::CRT_Q]
    Source: 'src/backend/crt.rs', lines 103:0-103:39 -/
@[global_simps, irreducible] def backend.crt.CRT_Q : Std.I32 := 82593793#i32

/-- [kopis::backend::crt::qinv]:
    Source: 'src/backend/crt.rs', lines 194:0-196:1 -/
def backend.crt.qinv (SECOND : Bool) : Result Std.I16 := do
  if SECOND
  then ok backend.crt.Q2_INV
  else ok backend.crt.Q1_INV

/-- [kopis::backend::crt::INVNTT_SCALE_2]
    Source: 'src/backend/crt.rs', lines 101:0-101:44 -/
@[global_simps, irreducible]
def backend.crt.INVNTT_SCALE_2 : Std.I16 := 2536#i16

/-- [kopis::backend::crt::INVNTT_SCALE_1]
    Source: 'src/backend/crt.rs', lines 99:0-99:44 -/
@[global_simps, irreducible]
def backend.crt.INVNTT_SCALE_1 : Std.I16 := 1912#i16

/-- [kopis::backend::crt::invntt_scale]:
    Source: 'src/backend/crt.rs', lines 204:0-210:1 -/
def backend.crt.invntt_scale (SECOND : Bool) : Result Std.I16 := do
  if SECOND
  then ok backend.crt.INVNTT_SCALE_2
  else ok backend.crt.INVNTT_SCALE_1

/-- [kopis::backend::avx2::ntt::gs_butterfly]:
    Source: 'src/backend/avx2/ntt.rs', lines 252:0-256:1 -/
def backend.avx2.ntt.gs_butterfly
  (lo : backend.avx2.intrinsics.Vec256) (hi : backend.avx2.intrinsics.Vec256)
  (z : backend.avx2.intrinsics.Vec256) (zq : backend.avx2.intrinsics.Vec256)
  (q : backend.avx2.intrinsics.Vec256) :
  Result (backend.avx2.intrinsics.Vec256 × backend.avx2.intrinsics.Vec256)
  := do
  let diff ← backend.avx2.intrinsics.sub_epi16 lo hi
  let lo1 ← backend.avx2.intrinsics.add_epi16 lo hi
  let hi1 ← backend.avx2.ntt.mont_mul diff z zq q
  ok (lo1, hi1)

/-- [kopis::backend::avx2::ntt::INV8_Q2]
    Source: 'src/backend/avx2/ntt.rs', lines 194:0-194:71 -/
@[global_simps, irreducible]
def backend.avx2.ntt.INV8_Q2 : Result (backend.avx2.ntt.Tbl 16#usize) :=
  backend.avx2.ntt.lane_tbl 16#usize backend.crt.ZETAS_Q2 backend.crt.Q2_INV
    31#isize 0#isize (-1)#isize true

/-- [kopis::backend::avx2::ntt::INV4_Q2]
    Source: 'src/backend/avx2/ntt.rs', lines 193:0-193:72 -/
@[global_simps, irreducible]
def backend.avx2.ntt.INV4_Q2 : Result (backend.avx2.ntt.Tbl 32#usize) :=
  backend.avx2.ntt.lane_tbl 32#usize backend.crt.ZETAS_Q2 backend.crt.Q2_INV
    63#isize (-1)#isize (-2)#isize true

/-- [kopis::backend::avx2::ntt::INV2_Q2]
    Source: 'src/backend/avx2/ntt.rs', lines 192:0-192:73 -/
@[global_simps, irreducible]
def backend.avx2.ntt.INV2_Q2 : Result (backend.avx2.ntt.Tbl 64#usize) :=
  backend.avx2.ntt.lane_tbl 64#usize backend.crt.ZETAS_Q2 backend.crt.Q2_INV
    127#isize (-1)#isize (-4)#isize true

/-- [kopis::backend::avx2::ntt::INV1_Q2]
    Source: 'src/backend/avx2/ntt.rs', lines 191:0-191:74 -/
@[global_simps, irreducible]
def backend.avx2.ntt.INV1_Q2 : Result (backend.avx2.ntt.Tbl 128#usize) :=
  backend.avx2.ntt.lane_tbl 128#usize backend.crt.ZETAS_Q2 backend.crt.Q2_INV
    255#isize (-1)#isize (-8)#isize true

/-- [kopis::backend::avx2::ntt::INV8_Q1]
    Source: 'src/backend/avx2/ntt.rs', lines 184:0-184:71 -/
@[global_simps, irreducible]
def backend.avx2.ntt.INV8_Q1 : Result (backend.avx2.ntt.Tbl 16#usize) :=
  backend.avx2.ntt.lane_tbl 16#usize backend.crt.ZETAS_Q1 backend.crt.Q1_INV
    31#isize 0#isize (-1)#isize true

/-- [kopis::backend::avx2::ntt::INV4_Q1]
    Source: 'src/backend/avx2/ntt.rs', lines 183:0-183:72 -/
@[global_simps, irreducible]
def backend.avx2.ntt.INV4_Q1 : Result (backend.avx2.ntt.Tbl 32#usize) :=
  backend.avx2.ntt.lane_tbl 32#usize backend.crt.ZETAS_Q1 backend.crt.Q1_INV
    63#isize (-1)#isize (-2)#isize true

/-- [kopis::backend::avx2::ntt::INV2_Q1]
    Source: 'src/backend/avx2/ntt.rs', lines 182:0-182:73 -/
@[global_simps, irreducible]
def backend.avx2.ntt.INV2_Q1 : Result (backend.avx2.ntt.Tbl 64#usize) :=
  backend.avx2.ntt.lane_tbl 64#usize backend.crt.ZETAS_Q1 backend.crt.Q1_INV
    127#isize (-1)#isize (-4)#isize true

/-- [kopis::backend::avx2::ntt::INV1_Q1]
    Source: 'src/backend/avx2/ntt.rs', lines 181:0-181:74 -/
@[global_simps, irreducible]
def backend.avx2.ntt.INV1_Q1 : Result (backend.avx2.ntt.Tbl 128#usize) :=
  backend.avx2.ntt.lane_tbl 128#usize backend.crt.ZETAS_Q1 backend.crt.Q1_INV
    255#isize (-1)#isize (-8)#isize true

/-- [kopis::backend::avx2::ntt::invntt_block]: loop 0:
    Source: 'src/backend/avx2/ntt.rs', lines 462:62-478:5 -/
@[rust_loop]
def backend.avx2.ntt.invntt_block_loop0
  (SECOND : Bool) (iter : core.ops.range.Range Std.Usize)
  (b : Array Std.I16 256#usize) (q : backend.avx2.intrinsics.Vec256) :
  Result (Array Std.I16 256#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok b
  | some h =>
    let (z, zq) ←
      if SECOND
      then do
           let t ← backend.avx2.ntt.INV1_Q2
           backend.avx2.ntt.ld_tbl t h
      else do
           let t ← backend.avx2.ntt.INV1_Q1
           backend.avx2.ntt.ld_tbl t h
    let i ← 2#usize * h
    let lo ← backend.avx2.intrinsics.load_i16 b i
    let i1 ← i + 1#usize
    let hi ← backend.avx2.intrinsics.load_i16 b i1
    let (lo1, hi1) ← backend.avx2.ntt.gs_butterfly lo hi z zq q
    let b1 ← backend.avx2.intrinsics.store_i16 b i lo1
    let i2 ← i + 1#usize
    let b2 ← backend.avx2.intrinsics.store_i16 b1 i2 hi1
    backend.avx2.ntt.invntt_block_loop0 SECOND iter1 b2 q
partial_fixpoint

/-- [kopis::backend::avx2::ntt::invntt_block]: loop 2:
    Source: 'src/backend/avx2/ntt.rs', lines 462:62-482:9 -/
@[rust_loop]
def backend.avx2.ntt.invntt_block_loop1_loop0
  (SECOND : Bool) (iter : core.ops.range.Range Std.Usize)
  (b : Array Std.I16 256#usize) (q : backend.avx2.intrinsics.Vec256)
  (h : Std.Usize) :
  Result (Array Std.I16 256#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok b
  | some j =>
    let (z, zq) ←
      if SECOND
      then do
           let t ← backend.avx2.ntt.INV2_Q2
           backend.avx2.ntt.ld_tbl t h
      else do
           let t ← backend.avx2.ntt.INV2_Q1
           backend.avx2.ntt.ld_tbl t h
    let i ← 4#usize * h
    let i1 ← i + j
    let lo ← backend.avx2.intrinsics.load_i16 b i1
    let i2 ← i + j
    let i3 ← i2 + 2#usize
    let hi ← backend.avx2.intrinsics.load_i16 b i3
    let (lo1, hi1) ← backend.avx2.ntt.gs_butterfly lo hi z zq q
    let i4 ← i + j
    let b1 ← backend.avx2.intrinsics.store_i16 b i4 lo1
    let i5 ← i + j
    let i6 ← i5 + 2#usize
    let b2 ← backend.avx2.intrinsics.store_i16 b1 i6 hi1
    backend.avx2.ntt.invntt_block_loop1_loop0 SECOND iter1 b2 q h
partial_fixpoint

/-- [kopis::backend::avx2::ntt::invntt_block]: loop 1:
    Source: 'src/backend/avx2/ntt.rs', lines 462:62-483:5 -/
@[rust_loop]
def backend.avx2.ntt.invntt_block_loop1
  (SECOND : Bool) (iter : core.ops.range.Range Std.Usize)
  (b : Array Std.I16 256#usize) (q : backend.avx2.intrinsics.Vec256) :
  Result (Array Std.I16 256#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok b
  | some h =>
    let b1 ←
      backend.avx2.ntt.invntt_block_loop1_loop0 SECOND
        { start := 0#usize, «end» := 2#usize } b q h
    backend.avx2.ntt.invntt_block_loop1 SECOND iter1 b1 q
partial_fixpoint

/-- [kopis::backend::avx2::ntt::invntt_block]: loop 4:
    Source: 'src/backend/avx2/ntt.rs', lines 462:62-490:9 -/
@[rust_loop]
def backend.avx2.ntt.invntt_block_loop2_loop0
  (SECOND : Bool) (iter : core.ops.range.Range Std.Usize)
  (b : Array Std.I16 256#usize) (q : backend.avx2.intrinsics.Vec256)
  (h : Std.Usize) :
  Result (Array Std.I16 256#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok b
  | some j =>
    let (z, zq) ←
      if SECOND
      then do
           let t ← backend.avx2.ntt.INV4_Q2
           backend.avx2.ntt.ld_tbl t h
      else do
           let t ← backend.avx2.ntt.INV4_Q1
           backend.avx2.ntt.ld_tbl t h
    let i ← 8#usize * h
    let i1 ← i + j
    let lo ← backend.avx2.intrinsics.load_i16 b i1
    let i2 ← i + j
    let i3 ← i2 + 4#usize
    let hi ← backend.avx2.intrinsics.load_i16 b i3
    let (lo1, hi1) ← backend.avx2.ntt.gs_butterfly lo hi z zq q
    let i4 ← i + j
    let b1 ← backend.avx2.intrinsics.store_i16 b i4 lo1
    let i5 ← i + j
    let i6 ← i5 + 4#usize
    let b2 ← backend.avx2.intrinsics.store_i16 b1 i6 hi1
    backend.avx2.ntt.invntt_block_loop2_loop0 SECOND iter1 b2 q h
partial_fixpoint

/-- [kopis::backend::avx2::ntt::invntt_block]: loop 3:
    Source: 'src/backend/avx2/ntt.rs', lines 462:62-491:5 -/
@[rust_loop]
def backend.avx2.ntt.invntt_block_loop2
  (SECOND : Bool) (iter : core.ops.range.Range Std.Usize)
  (b : Array Std.I16 256#usize) (q : backend.avx2.intrinsics.Vec256) :
  Result (Array Std.I16 256#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok b
  | some h =>
    let b1 ←
      backend.avx2.ntt.invntt_block_loop2_loop0 SECOND
        { start := 0#usize, «end» := 4#usize } b q h
    backend.avx2.ntt.invntt_block_loop2 SECOND iter1 b1 q
partial_fixpoint

/-- [kopis::backend::avx2::ntt::invntt_block]: loop 5:
    Source: 'src/backend/avx2/ntt.rs', lines 462:62-494:5 -/
@[rust_loop]
def backend.avx2.ntt.invntt_block_loop3
  (SECOND : Bool) (iter : core.ops.range.Range Std.Usize)
  (b : Array Std.I16 256#usize) (q : backend.avx2.intrinsics.Vec256) :
  Result (Array Std.I16 256#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok b
  | some j =>
    let (z, zq) ←
      if SECOND
      then
        do
        let t ← backend.avx2.ntt.INV8_Q2
        backend.avx2.ntt.ld_tbl t 0#usize
      else
        do
        let t ← backend.avx2.ntt.INV8_Q1
        backend.avx2.ntt.ld_tbl t 0#usize
    let lo ← backend.avx2.intrinsics.load_i16 b j
    let i ← j + 8#usize
    let hi ← backend.avx2.intrinsics.load_i16 b i
    let (lo1, hi1) ← backend.avx2.ntt.gs_butterfly lo hi z zq q
    let b1 ← backend.avx2.intrinsics.store_i16 b j lo1
    let b2 ← backend.avx2.intrinsics.store_i16 b1 i hi1
    backend.avx2.ntt.invntt_block_loop3 SECOND iter1 b2 q
partial_fixpoint

/-- [kopis::backend::avx2::ntt::invntt_block]: loop 8:
    Source: 'src/backend/avx2/ntt.rs', lines 516:12-523:13 -/
@[rust_loop]
def backend.avx2.ntt.invntt_block_loop4_loop0_loop0
  (b : Array Std.I16 256#usize) (q : backend.avx2.intrinsics.Vec256)
  (half : Std.Usize) (start : Std.Usize) (z : backend.avx2.intrinsics.Vec256)
  (zq : backend.avx2.intrinsics.Vec256) (i : Std.Usize) :
  Result (Array Std.I16 256#usize)
  := do
  let i1 ← start + half
  if i < i1
  then
    let lo ← backend.avx2.intrinsics.load_i16 b i
    let i2 ← i + half
    let hi ← backend.avx2.intrinsics.load_i16 b i2
    let (lo1, hi1) ← backend.avx2.ntt.gs_butterfly lo hi z zq q
    let b1 ← backend.avx2.intrinsics.store_i16 b i lo1
    let b2 ← backend.avx2.intrinsics.store_i16 b1 i2 hi1
    let i3 ← i + 1#usize
    backend.avx2.ntt.invntt_block_loop4_loop0_loop0 b2 q half start z zq i3
  else ok b
partial_fixpoint

/-- [kopis::backend::avx2::ntt::invntt_block]: loop 7:
    Source: 'src/backend/avx2/ntt.rs', lines 505:8-525:9 -/
@[rust_loop]
def backend.avx2.ntt.invntt_block_loop4_loop0
  (SECOND : Bool) (b : Array Std.I16 256#usize)
  (q : backend.avx2.intrinsics.Vec256) (k : Std.Usize) (half : Std.Usize)
  (start : Std.Usize) :
  Result ((Array Std.I16 256#usize) × Std.Usize)
  := do
  if start < 16#usize
  then
    let k1 ← k - 1#usize
    let i ← backend.crt.zeta SECOND k1
    let neg_zeta ← lift (core.num.I16.wrapping_sub 0#i16 i)
    let z ← backend.avx2.intrinsics.set1_epi16 neg_zeta
    let i1 ← backend.crt.qinv SECOND
    let i2 ← lift (core.num.I16.wrapping_mul neg_zeta i1)
    let zq ← backend.avx2.intrinsics.set1_epi16 i2
    let b1 ←
      backend.avx2.ntt.invntt_block_loop4_loop0_loop0 b q half start z zq start
    let i3 ← 2#usize * half
    let start1 ← start + i3
    backend.avx2.ntt.invntt_block_loop4_loop0 SECOND b1 q k1 half start1
  else ok (b, k)
partial_fixpoint

/-- [kopis::backend::avx2::ntt::invntt_block]: loop 6:
    Source: 'src/backend/avx2/ntt.rs', lines 503:4-531:5 -/
@[rust_loop]
def backend.avx2.ntt.invntt_block_loop4
  (SECOND : Bool) (b : Array Std.I16 256#usize)
  (q : backend.avx2.intrinsics.Vec256) (bm : backend.avx2.intrinsics.Vec256)
  (round : backend.avx2.intrinsics.Vec256) (k : Std.Usize) (half : Std.Usize) :
  Result (Array Std.I16 256#usize)
  := do
  if half < 16#usize
  then
    let (b1, k1) ←
      backend.avx2.ntt.invntt_block_loop4_loop0 SECOND b q k half 0#usize
    let half1 ← half * 2#usize
    if half1 = 4#usize
    then
      let b2 ← backend.avx2.ntt.barrett_block b1 bm round q
      backend.avx2.ntt.invntt_block_loop4 SECOND b2 q bm round k1 half1
    else backend.avx2.ntt.invntt_block_loop4 SECOND b1 q bm round k1 half1
  else ok b
partial_fixpoint

/-- [kopis::backend::avx2::ntt::invntt_block]: loop 9:
    Source: 'src/backend/avx2/ntt.rs', lines 536:4-538:5 -/
@[rust_loop]
def backend.avx2.ntt.invntt_block_loop5
  (iter : core.ops.range.Range Std.Usize) (b : Array Std.I16 256#usize)
  (q : backend.avx2.intrinsics.Vec256) (scale : backend.avx2.intrinsics.Vec256)
  (scale_q : backend.avx2.intrinsics.Vec256) :
  Result (Array Std.I16 256#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok b
  | some i =>
    let v ← backend.avx2.intrinsics.load_i16 b i
    let v1 ← backend.avx2.ntt.mont_mul v scale scale_q q
    let b1 ← backend.avx2.intrinsics.store_i16 b i v1
    backend.avx2.ntt.invntt_block_loop5 iter1 b1 q scale scale_q
partial_fixpoint

/-- [kopis::backend::avx2::ntt::invntt_block]:
    Source: 'src/backend/avx2/ntt.rs', lines 454:0-539:1 -/
def backend.avx2.ntt.invntt_block
  (SECOND : Bool) (b : Array Std.I16 256#usize) :
  Result (Array Std.I16 256#usize)
  := do
  let i ← backend.crt.q SECOND
  let q ← backend.avx2.intrinsics.set1_epi16 i
  let i1 ← backend.crt.barrett_m SECOND
  let bm ← backend.avx2.intrinsics.set1_epi16 i1
  let i2 ← backend.crt.BARRETT_SH - 1#i32
  let i3 ← 1#i16 <<< i2
  let round ← backend.avx2.intrinsics.set1_epi16 i3
  let b1 ← backend.avx2.ntt.transpose16 b
  let b2 ←
    backend.avx2.ntt.invntt_block_loop0 SECOND
      { start := 0#usize, «end» := 8#usize } b1 q
  let b3 ←
    backend.avx2.ntt.invntt_block_loop1 SECOND
      { start := 0#usize, «end» := 4#usize } b2 q
  let b4 ← backend.avx2.ntt.barrett_block b3 bm round q
  let b5 ←
    backend.avx2.ntt.invntt_block_loop2 SECOND
      { start := 0#usize, «end» := 2#usize } b4 q
  let b6 ←
    backend.avx2.ntt.invntt_block_loop3 SECOND
      { start := 0#usize, «end» := 8#usize } b5 q
  let b7 ← backend.avx2.ntt.barrett_block b6 bm round q
  let b8 ← backend.avx2.ntt.transpose16 b7
  let b9 ←
    backend.avx2.ntt.invntt_block_loop4 SECOND b8 q bm round 16#usize 1#usize
  let i4 ← backend.crt.invntt_scale SECOND
  let scale ← backend.avx2.intrinsics.set1_epi16 i4
  let i5 ← backend.crt.qinv SECOND
  let i6 ← lift (core.num.I16.wrapping_mul i4 i5)
  let scale_q ← backend.avx2.intrinsics.set1_epi16 i6
  backend.avx2.ntt.invntt_block_loop5 { start := 0#usize, «end» := 16#usize }
    b9 q scale scale_q

/-- [kopis::backend::avx2::intrinsics::packs_epi32]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 328:0-330:1 -/
axiom backend.avx2.intrinsics.packs_epi32
  :
  backend.avx2.intrinsics.Vec256 → backend.avx2.intrinsics.Vec256 → Result
    backend.avx2.intrinsics.Vec256

/-- [kopis::backend::avx2::intrinsics::slli_epi32]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 216:0-218:1 -/
axiom backend.avx2.intrinsics.slli_epi32
  (IMM : Std.I32) :
  backend.avx2.intrinsics.Vec256 → Result backend.avx2.intrinsics.Vec256

/-- [kopis::backend::avx2::intrinsics::srai_epi32]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 202:0-204:1 -/
axiom backend.avx2.intrinsics.srai_epi32
  (IMM : Std.I32) :
  backend.avx2.intrinsics.Vec256 → Result backend.avx2.intrinsics.Vec256

/-- [kopis::backend::avx2::ntt::reduce_block]: loop 0:
    Source: 'src/backend/avx2/ntt.rs', lines 678:4-693:5 -/
@[rust_loop]
def backend.avx2.ntt.reduce_block_loop
  (iter : core.ops.range.Range Std.Usize) (acc : Array Std.I32 512#usize)
  (base : Std.Usize) (b : Array Std.I16 256#usize)
  (q : backend.avx2.intrinsics.Vec256) (qinv : backend.avx2.intrinsics.Vec256)
  :
  Result (Array Std.I16 256#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok b
  | some i =>
    let i1 ← 2#usize * i
    let i2 ← base + i1
    let a0 ← backend.avx2.intrinsics.load_i32 acc i2
    let i3 ← base + i1
    let i4 ← i3 + 1#usize
    let a1 ← backend.avx2.intrinsics.load_i32 acc i4
    let v ← backend.avx2.intrinsics.slli_epi32 16#i32 a0
    let v1 ← backend.avx2.intrinsics.srai_epi32 16#i32 v
    let v2 ← backend.avx2.intrinsics.slli_epi32 16#i32 a1
    let v3 ← backend.avx2.intrinsics.srai_epi32 16#i32 v2
    let v4 ← backend.avx2.intrinsics.packs_epi32 v1 v3
    let lo ← backend.avx2.intrinsics.permute4x64_epi64 216#i32 v4
    let v5 ← backend.avx2.intrinsics.srai_epi32 16#i32 a0
    let v6 ← backend.avx2.intrinsics.srai_epi32 16#i32 a1
    let v7 ← backend.avx2.intrinsics.packs_epi32 v5 v6
    let hi ← backend.avx2.intrinsics.permute4x64_epi64 216#i32 v7
    let t ← backend.avx2.intrinsics.mullo_epi16 lo qinv
    let v8 ← backend.avx2.intrinsics.mulhi_epi16 t q
    let v9 ← backend.avx2.intrinsics.sub_epi16 hi v8
    let b1 ← backend.avx2.intrinsics.store_i16 b i v9
    backend.avx2.ntt.reduce_block_loop iter1 acc base b1 q qinv
partial_fixpoint

/-- [kopis::backend::avx2::ntt::reduce_block]:
    Source: 'src/backend/avx2/ntt.rs', lines 674:0-697:1 -/
def backend.avx2.ntt.reduce_block
  (SECOND : Bool) (acc : Array Std.I32 512#usize) (base : Std.Usize)
  (b : Array Std.I16 256#usize) :
  Result (Array Std.I16 256#usize)
  := do
  let i ← backend.crt.q SECOND
  let q ← backend.avx2.intrinsics.set1_epi16 i
  let i1 ← backend.crt.qinv SECOND
  let qinv ← backend.avx2.intrinsics.set1_epi16 i1
  let b1 ←
    backend.avx2.ntt.reduce_block_loop
      { start := 0#usize, «end» := 16#usize } acc base b q qinv
  backend.avx2.ntt.invntt_block SECOND b1

/-- [kopis::backend::avx2::intrinsics::extracti128_si256]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 357:0-359:1 -/
axiom backend.avx2.intrinsics.extracti128_si256
  (IMM : Std.I32) :
  backend.avx2.intrinsics.Vec256 → Result backend.avx2.intrinsics.Vec128

/-- [kopis::backend::avx2::intrinsics::castsi256_si128]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 350:0-352:1 -/
axiom backend.avx2.intrinsics.castsi256_si128
  : backend.avx2.intrinsics.Vec256 → Result backend.avx2.intrinsics.Vec128

/-- [kopis::backend::avx2::intrinsics::cvtepu16_epi32]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 343:0-345:1 -/
axiom backend.avx2.intrinsics.cvtepu16_epi32
  : backend.avx2.intrinsics.Vec128 → Result backend.avx2.intrinsics.Vec256

/-- [kopis::backend::avx2::intrinsics::cmpgt_epi32]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 184:0-186:1 -/
axiom backend.avx2.intrinsics.cmpgt_epi32
  :
  backend.avx2.intrinsics.Vec256 → backend.avx2.intrinsics.Vec256 → Result
    backend.avx2.intrinsics.Vec256

/-- [kopis::backend::avx2::intrinsics::mullo_epi32]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 177:0-179:1 -/
axiom backend.avx2.intrinsics.mullo_epi32
  :
  backend.avx2.intrinsics.Vec256 → backend.avx2.intrinsics.Vec256 → Result
    backend.avx2.intrinsics.Vec256

/-- [kopis::backend::avx2::intrinsics::sub_epi32]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 156:0-158:1 -/
axiom backend.avx2.intrinsics.sub_epi32
  :
  backend.avx2.intrinsics.Vec256 → backend.avx2.intrinsics.Vec256 → Result
    backend.avx2.intrinsics.Vec256

/-- [kopis::backend::avx2::ntt::reduce_invntt]: loop 0:
    Source: 'src/backend/avx2/ntt.rs', lines 731:16-765:5 -/
@[rust_loop]
def backend.avx2.ntt.reduce_invntt_loop
  (iter : core.ops.range.Range Std.Usize) (v1 : Array Std.I16 256#usize)
  (v2 : Array Std.I16 256#usize) (q1 : backend.avx2.intrinsics.Vec256)
  (q2 : backend.avx2.intrinsics.Vec256)
  (q1_inv_mont : backend.avx2.intrinsics.Vec256)
  (q1_inv_mont_q : backend.avx2.intrinsics.Vec256)
  (q1_wide : backend.avx2.intrinsics.Vec256)
  (crt_q : backend.avx2.intrinsics.Vec256)
  (crt_q_half : backend.avx2.intrinsics.Vec256)
  (low16 : backend.avx2.intrinsics.Vec256) (out : Array Std.U16 256#usize) :
  Result (Array Std.U16 256#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok out
  | some i =>
    let r1 ← backend.avx2.intrinsics.load_i16 v1 i
    let r2 ← backend.avx2.intrinsics.load_i16 v2 i
    let v ← backend.avx2.intrinsics.srai_epi16 15#i32 r1
    let v3 ← backend.avx2.intrinsics.and_si256 v q1
    let a1 ← backend.avx2.intrinsics.add_epi16 r1 v3
    let v4 ← backend.avx2.intrinsics.srai_epi16 15#i32 r2
    let v5 ← backend.avx2.intrinsics.and_si256 v4 q2
    let a2 ← backend.avx2.intrinsics.add_epi16 r2 v5
    let v6 ← backend.avx2.intrinsics.sub_epi16 a2 a1
    let t ← backend.avx2.ntt.mont_mul v6 q1_inv_mont q1_inv_mont_q q2
    let v7 ← backend.avx2.intrinsics.srai_epi16 15#i32 t
    let v8 ← backend.avx2.intrinsics.and_si256 v7 q2
    let t1 ← backend.avx2.intrinsics.add_epi16 t v8
    let v9 ← backend.avx2.intrinsics.castsi256_si128 a1
    let v10 ← backend.avx2.intrinsics.cvtepu16_epi32 v9
    let v11 ← backend.avx2.intrinsics.castsi256_si128 t1
    let v12 ← backend.avx2.intrinsics.cvtepu16_epi32 v11
    let v13 ← backend.avx2.intrinsics.mullo_epi32 v12 q1_wide
    let x ← backend.avx2.intrinsics.add_epi32 v10 v13
    let over ← backend.avx2.intrinsics.cmpgt_epi32 x crt_q_half
    let v14 ← backend.avx2.intrinsics.and_si256 crt_q over
    let v15 ← backend.avx2.intrinsics.sub_epi32 x v14
    let first ← backend.avx2.intrinsics.and_si256 v15 low16
    let v16 ← backend.avx2.intrinsics.extracti128_si256 1#i32 a1
    let v17 ← backend.avx2.intrinsics.cvtepu16_epi32 v16
    let v18 ← backend.avx2.intrinsics.extracti128_si256 1#i32 t1
    let v19 ← backend.avx2.intrinsics.cvtepu16_epi32 v18
    let v20 ← backend.avx2.intrinsics.mullo_epi32 v19 q1_wide
    let x1 ← backend.avx2.intrinsics.add_epi32 v17 v20
    let over1 ← backend.avx2.intrinsics.cmpgt_epi32 x1 crt_q_half
    let v21 ← backend.avx2.intrinsics.and_si256 crt_q over1
    let v22 ← backend.avx2.intrinsics.sub_epi32 x1 v21
    let second ← backend.avx2.intrinsics.and_si256 v22 low16
    let v23 ← backend.avx2.intrinsics.packus_epi32 first second
    let packed ← backend.avx2.intrinsics.permute4x64_epi64 216#i32 v23
    let out1 ← backend.avx2.intrinsics.store_u16 out i packed
    backend.avx2.ntt.reduce_invntt_loop iter1 v1 v2 q1 q2 q1_inv_mont
      q1_inv_mont_q q1_wide crt_q crt_q_half low16 out1
partial_fixpoint

/-- [kopis::backend::avx2::ntt::reduce_invntt]:
    Source: 'src/backend/avx2/ntt.rs', lines 706:0-767:1 -/
def backend.avx2.ntt.reduce_invntt
  (acc : Array Std.I32 512#usize) : Result (Array Std.U16 256#usize) := do
  let v1 := Array.repeat 256#usize 0#i16
  let v2 := Array.repeat 256#usize 0#i16
  let v11 ← backend.avx2.ntt.reduce_block false acc 0#usize v1
  let v21 ← backend.avx2.ntt.reduce_block true acc 32#usize v2
  let q1 ← backend.avx2.intrinsics.set1_epi16 backend.crt.Q1
  let q2 ← backend.avx2.intrinsics.set1_epi16 backend.crt.Q2
  let q1_inv_mont ←
    backend.avx2.intrinsics.set1_epi16 backend.crt.CRT_Q1_INV_MONT
  let i ←
    lift (core.num.I16.wrapping_mul backend.crt.CRT_Q1_INV_MONT
      backend.crt.Q2_INV)
  let q1_inv_mont_q ← backend.avx2.intrinsics.set1_epi16 i
  let i1 ← lift (IScalar.cast .I32 backend.crt.Q1)
  let q1_wide ← backend.avx2.intrinsics.set1_epi32 i1
  let crt_q ← backend.avx2.intrinsics.set1_epi32 backend.crt.CRT_Q
  let crt_q_half ← backend.avx2.intrinsics.set1_epi32 backend.crt.CRT_Q_HALF
  let low16 ← backend.avx2.intrinsics.set1_epi32 65535#i32
  let out := Array.repeat 256#usize 0#u16
  backend.avx2.ntt.reduce_invntt_loop { start := 0#usize, «end» := 16#usize }
    v11 v21 q1 q2 q1_inv_mont q1_inv_mont_q q1_wide crt_q crt_q_half low16 out

/-- [kopis::arithmetic::ntt_crt::reduce_block]: loop 0:
    Source: 'src/arithmetic/ntt_crt.rs', lines 263:4-268:5 -/
@[rust_loop]
def arithmetic.ntt_crt.reduce_block_loop
  (iter : core.ops.range.Range Std.Usize) (acc : Slice Std.I32)
  (b : Array Std.I16 256#usize) (q : Std.I16) (qinv : Std.I16) :
  Result (Array Std.I16 256#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok b
  | some i =>
    let a ← Slice.index_usize acc i
    let i1 ← lift (IScalar.cast .I16 a)
    let t ← lift (core.num.I16.wrapping_mul i1 qinv)
    let i2 ← lift (IScalar.cast .I32 t)
    let i3 ← lift (IScalar.cast .I32 q)
    let i4 ← lift (core.num.I32.wrapping_mul i2 i3)
    let i5 ← i4 >>> 16#i32
    let th ← lift (IScalar.cast .I16 i5)
    let i6 ← a >>> 16#i32
    let i7 ← lift (IScalar.cast .I16 i6)
    let i8 ← lift (core.num.I16.wrapping_sub i7 th)
    let a1 ← Array.update b i i8
    arithmetic.ntt_crt.reduce_block_loop iter1 acc a1 q qinv
partial_fixpoint

/-- [kopis::arithmetic::ntt_crt::reduce_block]:
    Source: 'src/arithmetic/ntt_crt.rs', lines 260:0-269:1 -/
def arithmetic.ntt_crt.reduce_block
  (SECOND : Bool) (acc : Slice Std.I32) (b : Array Std.I16 256#usize) :
  Result (Array Std.I16 256#usize)
  := do
  let q ← backend.crt.q SECOND
  let qinv ← backend.crt.qinv SECOND
  arithmetic.ntt_crt.reduce_block_loop
    { start := 0#usize, «end» := consts.RING_DEG } acc b q qinv

/-- [kopis::arithmetic::ntt_crt::gs_level]: loop 1:
    Source: 'src/arithmetic/ntt_crt.rs', lines 141:8-146:9 -/
@[rust_loop]
def arithmetic.ntt_crt.gs_level_loop0_loop0
  (LEN : Std.Usize) (iter : core.ops.range.Range Std.Usize)
  (b : Array Std.I16 256#usize) (q : Std.I16) (z : Std.I16) (zq : Std.I16) :
  Result (Array Std.I16 256#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok b
  | some j =>
    let lo ← Array.index_usize b j
    let i ← j + LEN
    let hi ← Array.index_usize b i
    let i1 ← lift (core.num.I16.wrapping_add lo hi)
    let b1 ← Array.update b j i1
    let i2 ← lift (core.num.I16.wrapping_sub lo hi)
    let i3 ← arithmetic.ntt_crt.mont_mul i2 z zq q
    let a ← Array.update b1 i i3
    arithmetic.ntt_crt.gs_level_loop0_loop0 LEN iter1 a q z zq
partial_fixpoint

/-- [kopis::arithmetic::ntt_crt::gs_level]: loop 0:
    Source: 'src/arithmetic/ntt_crt.rs', lines 130:4-148:5 -/
@[rust_loop]
def arithmetic.ntt_crt.gs_level_loop0
  (LEN : Std.Usize) (SECOND : Bool) (b : Array Std.I16 256#usize)
  (k : Std.Usize) (q : Std.I16) (qinv : Std.I16) (start : Std.Usize) :
  Result ((Array Std.I16 256#usize) × Std.Usize)
  := do
  if start < consts.RING_DEG
  then
    let k1 ← k - 1#usize
    let i ← backend.crt.zeta SECOND k1
    let z ← lift (core.num.I16.wrapping_sub 0#i16 i)
    let zq ← lift (core.num.I16.wrapping_mul z qinv)
    let i1 ← start + LEN
    let b1 ←
      arithmetic.ntt_crt.gs_level_loop0_loop0 LEN { start, «end» := i1 } b q
        z zq
    let i2 ← 2#usize * LEN
    let start1 ← start + i2
    arithmetic.ntt_crt.gs_level_loop0 LEN SECOND b1 k1 q qinv start1
  else ok (b, k)
partial_fixpoint

/-- [kopis::arithmetic::ntt_crt::gs_level]:
    Source: 'src/arithmetic/ntt_crt.rs', lines 126:0-149:1 -/
def arithmetic.ntt_crt.gs_level
  (LEN : Std.Usize) (SECOND : Bool) (b : Array Std.I16 256#usize)
  (k : Std.Usize) :
  Result ((Array Std.I16 256#usize) × Std.Usize)
  := do
  let q ← backend.crt.q SECOND
  let qinv ← backend.crt.qinv SECOND
  arithmetic.ntt_crt.gs_level_loop0 LEN SECOND b k q qinv 0#usize

/-- [kopis::arithmetic::ntt_crt::invntt_block]: loop 0:
    Source: 'src/arithmetic/ntt_crt.rs', lines 190:4-192:5 -/
@[rust_loop]
def arithmetic.ntt_crt.invntt_block_loop
  (iter : core.ops.range.Range Std.Usize) (b : Array Std.I16 256#usize)
  (q : Std.I16) (scale : Std.I16) (scale_q : Std.I16) :
  Result (Array Std.I16 256#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok b
  | some i =>
    let i1 ← Array.index_usize b i
    let i2 ← arithmetic.ntt_crt.mont_mul i1 scale scale_q q
    let a ← Array.update b i i2
    arithmetic.ntt_crt.invntt_block_loop iter1 a q scale scale_q
partial_fixpoint

/-- [kopis::arithmetic::ntt_crt::invntt_block]:
    Source: 'src/arithmetic/ntt_crt.rs', lines 170:0-193:1 -/
def arithmetic.ntt_crt.invntt_block
  (SECOND : Bool) (b : Array Std.I16 256#usize) :
  Result (Array Std.I16 256#usize)
  := do
  let q ← backend.crt.q SECOND
  let scale ← backend.crt.invntt_scale SECOND
  let i ← backend.crt.qinv SECOND
  let scale_q ← lift (core.num.I16.wrapping_mul scale i)
  let (b1, k) ← arithmetic.ntt_crt.gs_level 1#usize SECOND b consts.RING_DEG
  let (b2, k1) ← arithmetic.ntt_crt.gs_level 2#usize SECOND b1 k
  let b3 ← arithmetic.ntt_crt.barrett_block SECOND b2
  let (b4, k2) ← arithmetic.ntt_crt.gs_level 4#usize SECOND b3 k1
  let (b5, k3) ← arithmetic.ntt_crt.gs_level 8#usize SECOND b4 k2
  let b6 ← arithmetic.ntt_crt.barrett_block SECOND b5
  let (b7, k4) ← arithmetic.ntt_crt.gs_level 16#usize SECOND b6 k3
  let (b8, k5) ← arithmetic.ntt_crt.gs_level 32#usize SECOND b7 k4
  let b9 ← arithmetic.ntt_crt.barrett_block SECOND b8
  let (b10, k6) ← arithmetic.ntt_crt.gs_level 64#usize SECOND b9 k5
  let (b11, _) ← arithmetic.ntt_crt.gs_level 128#usize SECOND b10 k6
  arithmetic.ntt_crt.invntt_block_loop
    { start := 0#usize, «end» := consts.RING_DEG } b11 q scale scale_q

/-- [kopis::arithmetic::ntt_crt::reduce_invntt]: loop 0:
    Source: 'src/arithmetic/ntt_crt.rs', lines 289:4-305:5 -/
@[rust_loop]
def arithmetic.ntt_crt.reduce_invntt_loop
  (iter : core.ops.range.Range Std.Usize) (v1 : Array Std.I16 256#usize)
  (v2 : Array Std.I16 256#usize) (q1_inv_mont_q : Std.I16)
  (out : Array Std.U16 256#usize) :
  Result (Array Std.U16 256#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok out
  | some i =>
    let r1 ← Array.index_usize v1 i
    let r2 ← Array.index_usize v2 i
    let i1 ← r1 >>> 15#i32
    let i2 ← lift (i1 &&& backend.crt.Q1)
    let a1 ← lift (core.num.I16.wrapping_add r1 i2)
    let i3 ← r2 >>> 15#i32
    let i4 ← lift (i3 &&& backend.crt.Q2)
    let a2 ← lift (core.num.I16.wrapping_add r2 i4)
    let i5 ← lift (core.num.I16.wrapping_sub a2 a1)
    let t ←
      arithmetic.ntt_crt.mont_mul i5 backend.crt.CRT_Q1_INV_MONT q1_inv_mont_q
        backend.crt.Q2
    let i6 ← t >>> 15#i32
    let i7 ← lift (i6 &&& backend.crt.Q2)
    let t1 ← lift (core.num.I16.wrapping_add t i7)
    let i8 ← lift (IScalar.cast .I32 a1)
    let i9 ← lift (IScalar.cast .I32 t1)
    let i10 ← lift (IScalar.cast .I32 backend.crt.Q1)
    let i11 ← lift (core.num.I32.wrapping_mul i9 i10)
    let x ← lift (core.num.I32.wrapping_add i8 i11)
    let i12 ← lift (core.num.I32.wrapping_sub backend.crt.CRT_Q_HALF x)
    let over ← i12 >>> 31#i32
    let i13 ← lift (backend.crt.CRT_Q &&& over)
    let i14 ← lift (core.num.I32.wrapping_sub x i13)
    let i15 ← lift (IScalar.hcast .U16 i14)
    let a ← Array.update out i i15
    arithmetic.ntt_crt.reduce_invntt_loop iter1 v1 v2 q1_inv_mont_q a
partial_fixpoint

/-- [kopis::arithmetic::ntt_crt::reduce_invntt]:
    Source: 'src/arithmetic/ntt_crt.rs', lines 273:0-307:1 -/
def arithmetic.ntt_crt.reduce_invntt
  (acc : Array Std.I32 512#usize) : Result (Array Std.U16 256#usize) := do
  let v1 := Array.repeat 256#usize 0#i16
  let v2 := Array.repeat 256#usize 0#i16
  let s ←
    core.array.Array.index (core.ops.index.IndexSlice
      (core.slice.index.SliceIndexRangeToUsizeSlice Std.I32)) acc
      { «end» := consts.RING_DEG }
  let v11 ← arithmetic.ntt_crt.reduce_block false s v1
  let s1 ←
    core.array.Array.index (core.ops.index.IndexSlice
      (core.slice.index.SliceIndexRangeFromUsizeSlice Std.I32)) acc
      { start := consts.RING_DEG }
  let v21 ← arithmetic.ntt_crt.reduce_block true s1 v2
  let v12 ← arithmetic.ntt_crt.invntt_block false v11
  let v22 ← arithmetic.ntt_crt.invntt_block true v21
  let q1_inv_mont_q ←
    lift (core.num.I16.wrapping_mul backend.crt.CRT_Q1_INV_MONT
      backend.crt.Q2_INV)
  let out := Array.repeat 256#usize 0#u16
  arithmetic.ntt_crt.reduce_invntt_loop
    { start := 0#usize, «end» := consts.RING_DEG } v12 v22 q1_inv_mont_q out

/-- [kopis::arithmetic::ntt::reduce_invntt_to_ring_elem]:
    Source: 'src/arithmetic/ntt.rs', lines 346:0-362:1 -/
def arithmetic.ntt.reduce_invntt_to_ring_elem
  (acc : Array Std.I32 512#usize) : Result arithmetic.ring_arith.RingElem := do
  let b ← backend.avx2.cpu.available
  if b
  then let a ← backend.avx2.ntt.reduce_invntt acc
       ok a
  else let a ← arithmetic.ntt_crt.reduce_invntt acc
       ok a

/-- [kopis::arithmetic::ntt::{kopis::arithmetic::ntt::NttMatrix<X, Y>}::from_uniform_matrix]: loop 1:
    Source: 'src/arithmetic/ntt.rs', lines 369:12-371:13 -/
@[rust_loop]
def arithmetic.ntt.NttMatrix.from_uniform_matrix_loop0_loop0
  {X : Std.Usize} {Y : Std.Usize} (iter : core.ops.range.Range Std.Usize)
  (mat : arithmetic.matrix_arith.Matrix X Y)
  (ret : arithmetic.ntt.NttMatrix X Y) (i : Std.Usize) :
  Result ((arithmetic.matrix_arith.Matrix X Y) × (arithmetic.ntt.NttMatrix X
    Y))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (mat, ret)
  | some j =>
    let a ← Array.index_usize mat i
    let re ← Array.index_usize a j
    let ne ← arithmetic.ntt.NttElem.from_uniform re
    let (a1, index_mut_back) ← Array.index_mut_usize ret i
    let a2 ← Array.update a1 j ne
    let a3 := index_mut_back a2
    arithmetic.ntt.NttMatrix.from_uniform_matrix_loop0_loop0 iter1 mat a3 i
partial_fixpoint

/-- [kopis::arithmetic::ntt::{kopis::arithmetic::ntt::NttMatrix<X, Y>}::from_uniform_matrix]: loop 0:
    Source: 'src/arithmetic/ntt.rs', lines 368:8-372:9 -/
@[rust_loop]
def arithmetic.ntt.NttMatrix.from_uniform_matrix_loop0
  {X : Std.Usize} {Y : Std.Usize} (iter : core.ops.range.Range Std.Usize)
  (mat : arithmetic.matrix_arith.Matrix X Y)
  (ret : arithmetic.ntt.NttMatrix X Y) :
  Result (arithmetic.ntt.NttMatrix X Y)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok ret
  | some i =>
    let (mat1, ret1) ←
      arithmetic.ntt.NttMatrix.from_uniform_matrix_loop0_loop0
        { start := 0#usize, «end» := Y } mat ret i
    arithmetic.ntt.NttMatrix.from_uniform_matrix_loop0 iter1 mat1 ret1
partial_fixpoint

/-- [kopis::arithmetic::ntt::{kopis::arithmetic::ntt::NttMatrix<X, Y>}::from_uniform_matrix]:
    Source: 'src/arithmetic/ntt.rs', lines 366:4-374:5 -/
def arithmetic.ntt.NttMatrix.from_uniform_matrix
  {X : Std.Usize} {Y : Std.Usize} (mat : arithmetic.matrix_arith.Matrix X Y) :
  Result (arithmetic.ntt.NttMatrix X Y)
  := do
  let ret ← arithmetic.ntt.NttMatrix.Insts.CoreDefaultDefault.default X Y
  arithmetic.ntt.NttMatrix.from_uniform_matrix_loop0
    { start := 0#usize, «end» := X } mat ret

/-- [kopis::arithmetic::ntt::{kopis::arithmetic::ntt::NttMatrix<X, Y>}::from_secret_matrix]: loop 1:
    Source: 'src/arithmetic/ntt.rs', lines 380:12-382:13 -/
@[rust_loop]
def arithmetic.ntt.NttMatrix.from_secret_matrix_loop0_loop0
  {X : Std.Usize} {Y : Std.Usize} (iter : core.ops.range.Range Std.Usize)
  (mat : arithmetic.matrix_arith.Matrix X Y)
  (ret : arithmetic.ntt.NttMatrix X Y) (i : Std.Usize) :
  Result ((arithmetic.matrix_arith.Matrix X Y) × (arithmetic.ntt.NttMatrix X
    Y))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (mat, ret)
  | some j =>
    let a ← Array.index_usize mat i
    let re ← Array.index_usize a j
    let ne ← arithmetic.ntt.NttElem.from_secret re
    let (a1, index_mut_back) ← Array.index_mut_usize ret i
    let a2 ← Array.update a1 j ne
    let a3 := index_mut_back a2
    arithmetic.ntt.NttMatrix.from_secret_matrix_loop0_loop0 iter1 mat a3 i
partial_fixpoint

/-- [kopis::arithmetic::ntt::{kopis::arithmetic::ntt::NttMatrix<X, Y>}::from_secret_matrix]: loop 0:
    Source: 'src/arithmetic/ntt.rs', lines 379:8-383:9 -/
@[rust_loop]
def arithmetic.ntt.NttMatrix.from_secret_matrix_loop0
  {X : Std.Usize} {Y : Std.Usize} (iter : core.ops.range.Range Std.Usize)
  (mat : arithmetic.matrix_arith.Matrix X Y)
  (ret : arithmetic.ntt.NttMatrix X Y) :
  Result (arithmetic.ntt.NttMatrix X Y)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok ret
  | some i =>
    let (mat1, ret1) ←
      arithmetic.ntt.NttMatrix.from_secret_matrix_loop0_loop0
        { start := 0#usize, «end» := Y } mat ret i
    arithmetic.ntt.NttMatrix.from_secret_matrix_loop0 iter1 mat1 ret1
partial_fixpoint

/-- [kopis::arithmetic::ntt::{kopis::arithmetic::ntt::NttMatrix<X, Y>}::from_secret_matrix]:
    Source: 'src/arithmetic/ntt.rs', lines 377:4-385:5 -/
def arithmetic.ntt.NttMatrix.from_secret_matrix
  {X : Std.Usize} {Y : Std.Usize} (mat : arithmetic.matrix_arith.Matrix X Y) :
  Result (arithmetic.ntt.NttMatrix X Y)
  := do
  let ret ← arithmetic.ntt.NttMatrix.Insts.CoreDefaultDefault.default X Y
  arithmetic.ntt.NttMatrix.from_secret_matrix_loop0
    { start := 0#usize, «end» := X } mat ret

/-- [kopis::consts::MAX_L]
    Source: 'src/consts.rs', lines 34:0-34:34 -/
@[global_simps, irreducible] def consts.MAX_L : Std.Usize := 4#usize

/-- [kopis::arithmetic::ntt::{kopis::arithmetic::ntt::NttMatrix<X, Y>}::mul]: loop 2:
    Source: 'src/arithmetic/ntt.rs', lines 397:16-399:17 -/
@[rust_loop]
def arithmetic.ntt.NttMatrix.mul_loop0_loop0_loop0
  {X : Std.Usize} {Y : Std.Usize} {Z : Std.Usize}
  (iter : core.ops.range.Range Std.Usize) (self : arithmetic.ntt.NttMatrix X Y)
  (other : arithmetic.ntt.NttMatrix Y Z) (i : Std.Usize) (k : Std.Usize)
  (acc : Array Std.I32 512#usize) :
  Result ((arithmetic.ntt.NttMatrix X Y) × (arithmetic.ntt.NttMatrix Y Z) ×
    (Array Std.I32 512#usize))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (self, other, acc)
  | some j =>
    let a ← Array.index_usize self i
    let ne ← Array.index_usize a j
    let a1 ← Array.index_usize other j
    let ne1 ← Array.index_usize a1 k
    let acc1 ← arithmetic.ntt.pointwise_mul_acc acc ne ne1
    arithmetic.ntt.NttMatrix.mul_loop0_loop0_loop0 iter1 self other i k acc1
partial_fixpoint

/-- [kopis::arithmetic::ntt::{kopis::arithmetic::ntt::NttMatrix<X, Y>}::mul]: loop 1:
    Source: 'src/arithmetic/ntt.rs', lines 395:12-401:13 -/
@[rust_loop]
def arithmetic.ntt.NttMatrix.mul_loop0_loop0
  {X : Std.Usize} {Y : Std.Usize} {Z : Std.Usize}
  (iter : core.ops.range.Range Std.Usize) (self : arithmetic.ntt.NttMatrix X Y)
  (other : arithmetic.ntt.NttMatrix Y Z)
  (result : arithmetic.matrix_arith.Matrix X Z) (i : Std.Usize) :
  Result ((arithmetic.ntt.NttMatrix X Y) × (arithmetic.ntt.NttMatrix Y Z) ×
    (arithmetic.matrix_arith.Matrix X Z))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (self, other, result)
  | some k =>
    let (self1, other1, acc) ←
      arithmetic.ntt.NttMatrix.mul_loop0_loop0_loop0
        { start := 0#usize, «end» := Y } self other i k
        arithmetic.ntt.ACC_ZERO
    let re ← arithmetic.ntt.reduce_invntt_to_ring_elem acc
    let (a, index_mut_back) ← Array.index_mut_usize result i
    let a1 ← Array.update a k re
    let a2 := index_mut_back a1
    arithmetic.ntt.NttMatrix.mul_loop0_loop0 iter1 self1 other1 a2 i
partial_fixpoint

/-- [kopis::arithmetic::ntt::{kopis::arithmetic::ntt::NttMatrix<X, Y>}::mul]: loop 0:
    Source: 'src/arithmetic/ntt.rs', lines 394:8-402:9 -/
@[rust_loop]
def arithmetic.ntt.NttMatrix.mul_loop0
  {X : Std.Usize} {Y : Std.Usize} {Z : Std.Usize}
  (iter : core.ops.range.Range Std.Usize) (self : arithmetic.ntt.NttMatrix X Y)
  (other : arithmetic.ntt.NttMatrix Y Z)
  (result : arithmetic.matrix_arith.Matrix X Z) :
  Result (arithmetic.matrix_arith.Matrix X Z)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok result
  | some i =>
    let (self1, other1, result1) ←
      arithmetic.ntt.NttMatrix.mul_loop0_loop0
        { start := 0#usize, «end» := Z } self other result i
    arithmetic.ntt.NttMatrix.mul_loop0 iter1 self1 other1 result1
partial_fixpoint

/-- [kopis::arithmetic::ntt::{kopis::arithmetic::ntt::NttMatrix<X, Y>}::mul]:
    Source: 'src/arithmetic/ntt.rs', lines 389:4-404:5 -/
def arithmetic.ntt.NttMatrix.mul
  {X : Std.Usize} {Y : Std.Usize} {Z : Std.Usize}
  (self : arithmetic.ntt.NttMatrix X Y) (other : arithmetic.ntt.NttMatrix Y Z)
  :
  Result (arithmetic.matrix_arith.Matrix X Z)
  := do
  massert (Y <= consts.MAX_L)
  let result ←
    arithmetic.matrix_arith.Matrix.Insts.CoreDefaultDefault.default X Z
  arithmetic.ntt.NttMatrix.mul_loop0 { start := 0#usize, «end» := X } self
    other result

/-- [kopis::arithmetic::ntt::{kopis::arithmetic::ntt::NttMatrix<X, Y>}::mul_transpose]: loop 2:
    Source: 'src/arithmetic/ntt.rs', lines 416:16-418:17 -/
@[rust_loop]
def arithmetic.ntt.NttMatrix.mul_transpose_loop0_loop0_loop0
  {X : Std.Usize} {Y : Std.Usize} {Z : Std.Usize}
  (iter : core.ops.range.Range Std.Usize) (self : arithmetic.ntt.NttMatrix X Y)
  (other : arithmetic.ntt.NttMatrix X Z) (j : Std.Usize) (k : Std.Usize)
  (acc : Array Std.I32 512#usize) :
  Result ((arithmetic.ntt.NttMatrix X Y) × (arithmetic.ntt.NttMatrix X Z) ×
    (Array Std.I32 512#usize))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (self, other, acc)
  | some i =>
    let a ← Array.index_usize self i
    let ne ← Array.index_usize a j
    let a1 ← Array.index_usize other i
    let ne1 ← Array.index_usize a1 k
    let acc1 ← arithmetic.ntt.pointwise_mul_acc acc ne ne1
    arithmetic.ntt.NttMatrix.mul_transpose_loop0_loop0_loop0 iter1 self other j
      k acc1
partial_fixpoint

/-- [kopis::arithmetic::ntt::{kopis::arithmetic::ntt::NttMatrix<X, Y>}::mul_transpose]: loop 1:
    Source: 'src/arithmetic/ntt.rs', lines 414:12-420:13 -/
@[rust_loop]
def arithmetic.ntt.NttMatrix.mul_transpose_loop0_loop0
  {X : Std.Usize} {Y : Std.Usize} {Z : Std.Usize}
  (iter : core.ops.range.Range Std.Usize) (self : arithmetic.ntt.NttMatrix X Y)
  (other : arithmetic.ntt.NttMatrix X Z)
  (result : arithmetic.matrix_arith.Matrix Y Z) (j : Std.Usize) :
  Result ((arithmetic.ntt.NttMatrix X Y) × (arithmetic.ntt.NttMatrix X Z) ×
    (arithmetic.matrix_arith.Matrix Y Z))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (self, other, result)
  | some k =>
    let (self1, other1, acc) ←
      arithmetic.ntt.NttMatrix.mul_transpose_loop0_loop0_loop0
        { start := 0#usize, «end» := X } self other j k
        arithmetic.ntt.ACC_ZERO
    let re ← arithmetic.ntt.reduce_invntt_to_ring_elem acc
    let (a, index_mut_back) ← Array.index_mut_usize result j
    let a1 ← Array.update a k re
    let a2 := index_mut_back a1
    arithmetic.ntt.NttMatrix.mul_transpose_loop0_loop0 iter1 self1 other1 a2 j
partial_fixpoint

/-- [kopis::arithmetic::ntt::{kopis::arithmetic::ntt::NttMatrix<X, Y>}::mul_transpose]: loop 0:
    Source: 'src/arithmetic/ntt.rs', lines 413:8-421:9 -/
@[rust_loop]
def arithmetic.ntt.NttMatrix.mul_transpose_loop0
  {X : Std.Usize} {Y : Std.Usize} {Z : Std.Usize}
  (iter : core.ops.range.Range Std.Usize) (self : arithmetic.ntt.NttMatrix X Y)
  (other : arithmetic.ntt.NttMatrix X Z)
  (result : arithmetic.matrix_arith.Matrix Y Z) :
  Result (arithmetic.matrix_arith.Matrix Y Z)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok result
  | some j =>
    let (self1, other1, result1) ←
      arithmetic.ntt.NttMatrix.mul_transpose_loop0_loop0
        { start := 0#usize, «end» := Z } self other result j
    arithmetic.ntt.NttMatrix.mul_transpose_loop0 iter1 self1 other1 result1
partial_fixpoint

/-- [kopis::arithmetic::ntt::{kopis::arithmetic::ntt::NttMatrix<X, Y>}::mul_transpose]:
    Source: 'src/arithmetic/ntt.rs', lines 408:4-423:5 -/
def arithmetic.ntt.NttMatrix.mul_transpose
  {X : Std.Usize} {Y : Std.Usize} {Z : Std.Usize}
  (self : arithmetic.ntt.NttMatrix X Y) (other : arithmetic.ntt.NttMatrix X Z)
  :
  Result (arithmetic.matrix_arith.Matrix Y Z)
  := do
  massert (X <= consts.MAX_L)
  let result ←
    arithmetic.matrix_arith.Matrix.Insts.CoreDefaultDefault.default Y Z
  arithmetic.ntt.NttMatrix.mul_transpose_loop0
    { start := 0#usize, «end» := Y } self other result

/-- [kopis::arithmetic::ring_arith::{impl core::cmp::Eq for kopis::arithmetic::ring_arith::RingElem}::assert_fields_are_eq]:
    Source: 'src/arithmetic/ring_arith.rs', lines 15:9-15:11
    Visibility: public -/
def arithmetic.ring_arith.RingElem.Insts.CoreCmpEq.assert_fields_are_eq
  (self : arithmetic.ring_arith.RingElem) : Result Unit := do
  ok ()

/-- Trait implementation: [kopis::arithmetic::ring_arith::{impl core::cmp::Eq for kopis::arithmetic::ring_arith::RingElem}]
    Source: 'src/arithmetic/ring_arith.rs', lines 15:9-15:11 -/
@[reducible]
def arithmetic.ring_arith.RingElem.Insts.CoreCmpEq : core.cmp.Eq
  arithmetic.ring_arith.RingElem := {
  partialEqInst :=
    arithmetic.ring_arith.RingElem.Insts.CoreCmpPartialEqRingElem
  assert_fields_are_eq :=
    arithmetic.ring_arith.RingElem.Insts.CoreCmpEq.assert_fields_are_eq
}

/-- Trait implementation: [kopis::arithmetic::ring_arith::{impl core::marker::StructuralPartialEq for kopis::arithmetic::ring_arith::RingElem}]
    Source: 'src/arithmetic/ring_arith.rs', lines 15:13-15:22 -/
@[reducible]
def arithmetic.ring_arith.RingElem.Insts.CoreMarkerStructuralPartialEq :
  core.marker.StructuralPartialEq arithmetic.ring_arith.RingElem := {
}

/-- Trait implementation: [kopis::arithmetic::ring_arith::{impl core::marker::Copy for kopis::arithmetic::ring_arith::RingElem}]
    Source: 'src/arithmetic/ring_arith.rs', lines 15:38-15:42 -/
@[reducible]
def arithmetic.ring_arith.RingElem.Insts.CoreMarkerCopy : core.marker.Copy
  arithmetic.ring_arith.RingElem := {
  cloneInst := arithmetic.ring_arith.RingElem.Insts.CoreCloneClone
}

/-- Trait implementation: [kopis::arithmetic::ring_arith::{impl core::default::Default for kopis::arithmetic::ring_arith::RingElem}]
    Source: 'src/arithmetic/ring_arith.rs', lines 18:0-22:1 -/
@[reducible]
def arithmetic.ring_arith.RingElem.Insts.CoreDefaultDefault :
  core.default.Default arithmetic.ring_arith.RingElem := {
  default := arithmetic.ring_arith.RingElem.Insts.CoreDefaultDefault.default
}

/-- [kopis::arithmetic::ring_arith::{kopis::arithmetic::ring_arith::RingElem}::shift_left]: loop 0:
    Source: 'src/arithmetic/ring_arith.rs', lines 105:8-107:9 -/
@[rust_loop]
def arithmetic.ring_arith.RingElem.shift_left_loop
  (iter : core.slice.iter.IterMut Std.U16)
  (back : core.slice.iter.IterMut Std.U16 → core.slice.iter.IterMut Std.U16)
  (shift : Std.Usize) :
  Result (core.slice.iter.IterMut Std.U16)
  := do
  let (o, iter1, next_back) ← core.slice.iter.IteratorIterMut.next iter
  match o with
  | none => ok (let im := next_back iter1 none
                back im)
  | some coeff =>
    let coeff1 ← coeff <<< shift
    arithmetic.ring_arith.RingElem.shift_left_loop iter1
      (fun im => let im1 := next_back im (some coeff1)
                 back im1) shift
partial_fixpoint

/-- [kopis::arithmetic::ring_arith::{kopis::arithmetic::ring_arith::RingElem}::shift_left]:
    Source: 'src/arithmetic/ring_arith.rs', lines 104:4-108:5 -/
def arithmetic.ring_arith.RingElem.shift_left
  (self : arithmetic.ring_arith.RingElem) (shift : Std.Usize) :
  Result arithmetic.ring_arith.RingElem
  := do
  let (s, to_slice_mut_back) ← lift (Array.to_slice_mut self)
  let (iter, iter_mut_back) ← core.slice.Slice.iter_mut s
  let back ←
    arithmetic.ring_arith.RingElem.shift_left_loop iter (fun im => im) shift
  let s1 := iter_mut_back back
  let a := to_slice_mut_back s1
  ok a

/-- [kopis::arithmetic::ring_arith::{impl core::ops::arith::Mul<&'a kopis::arithmetic::ring_arith::RingElem, kopis::arithmetic::ring_arith::RingElem> for &'a kopis::arithmetic::ring_arith::RingElem}::mul]:
    Source: 'src/arithmetic/ring_arith.rs', lines 121:4-125:5
    Visibility: public -/
def SharedARingElem.Insts.CoreOpsArithMulSharedARingElemRingElem.mul
  (self : arithmetic.ring_arith.RingElem)
  (other : arithmetic.ring_arith.RingElem) :
  Result arithmetic.ring_arith.RingElem
  := do
  let ret ← arithmetic.ring_arith.RingElem.Insts.CoreDefaultDefault.default
  arithmetic.ring_arith.ring_mul_acc ret self other

/-- Trait implementation: [kopis::arithmetic::ring_arith::{impl core::ops::arith::Mul<&'a kopis::arithmetic::ring_arith::RingElem, kopis::arithmetic::ring_arith::RingElem> for &'a kopis::arithmetic::ring_arith::RingElem}]
    Source: 'src/arithmetic/ring_arith.rs', lines 118:0-126:1 -/
@[reducible]
def SharedARingElem.Insts.CoreOpsArithMulSharedARingElemRingElem :
  core.ops.arith.Mul arithmetic.ring_arith.RingElem
  arithmetic.ring_arith.RingElem arithmetic.ring_arith.RingElem := {
  mul := SharedARingElem.Insts.CoreOpsArithMulSharedARingElemRingElem.mul
}

/-- Trait implementation: [kopis::arithmetic::ring_arith::{impl core::ops::arith::Add<&'a kopis::arithmetic::ring_arith::RingElem, kopis::arithmetic::ring_arith::RingElem> for &'a kopis::arithmetic::ring_arith::RingElem}]
    Source: 'src/arithmetic/ring_arith.rs', lines 217:0-227:1 -/
@[reducible]
def SharedARingElem.Insts.CoreOpsArithAddSharedARingElemRingElem :
  core.ops.arith.Add arithmetic.ring_arith.RingElem
  arithmetic.ring_arith.RingElem arithmetic.ring_arith.RingElem := {
  add := SharedARingElem.Insts.CoreOpsArithAddSharedARingElemRingElem.add
}

/-- [kopis::arithmetic::ring_arith::{impl core::ops::arith::Sub<&'a kopis::arithmetic::ring_arith::RingElem, kopis::arithmetic::ring_arith::RingElem> for &'a kopis::arithmetic::ring_arith::RingElem}::sub]: loop 0:
    Source: 'src/arithmetic/ring_arith.rs', lines 234:8-236:9
    Visibility: public -/
@[rust_loop]
def SharedARingElem.Insts.CoreOpsArithSubSharedARingElemRingElem.sub_loop
  (iter : core.ops.range.Range Std.Usize)
  (self : arithmetic.ring_arith.RingElem)
  (other : arithmetic.ring_arith.RingElem)
  (ret : arithmetic.ring_arith.RingElem) :
  Result arithmetic.ring_arith.RingElem
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok ret
  | some i =>
    let i1 ← Array.index_usize self i
    let i2 ← Array.index_usize other i
    let i3 ← lift (core.num.U16.wrapping_sub i1 i2)
    let a ← Array.update ret i i3
    SharedARingElem.Insts.CoreOpsArithSubSharedARingElemRingElem.sub_loop iter1
      self other a
partial_fixpoint

/-- [kopis::arithmetic::ring_arith::{impl core::ops::arith::Sub<&'a kopis::arithmetic::ring_arith::RingElem, kopis::arithmetic::ring_arith::RingElem> for &'a kopis::arithmetic::ring_arith::RingElem}::sub]:
    Source: 'src/arithmetic/ring_arith.rs', lines 232:4-238:5
    Visibility: public -/
def SharedARingElem.Insts.CoreOpsArithSubSharedARingElemRingElem.sub
  (self : arithmetic.ring_arith.RingElem)
  (other : arithmetic.ring_arith.RingElem) :
  Result arithmetic.ring_arith.RingElem
  := do
  let ret ← arithmetic.ring_arith.RingElem.Insts.CoreDefaultDefault.default
  SharedARingElem.Insts.CoreOpsArithSubSharedARingElemRingElem.sub_loop
    { start := 0#usize, «end» := consts.RING_DEG } self other ret

/-- Trait implementation: [kopis::arithmetic::ring_arith::{impl core::ops::arith::Sub<&'a kopis::arithmetic::ring_arith::RingElem, kopis::arithmetic::ring_arith::RingElem> for &'a kopis::arithmetic::ring_arith::RingElem}]
    Source: 'src/arithmetic/ring_arith.rs', lines 229:0-239:1 -/
@[reducible]
def SharedARingElem.Insts.CoreOpsArithSubSharedARingElemRingElem :
  core.ops.arith.Sub arithmetic.ring_arith.RingElem
  arithmetic.ring_arith.RingElem arithmetic.ring_arith.RingElem := {
  sub := SharedARingElem.Insts.CoreOpsArithSubSharedARingElemRingElem.sub
}

/-- [kopis::backend::avx2::intrinsics::set1_epi64x]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 79:0-81:1 -/
axiom backend.avx2.intrinsics.set1_epi64x
  : Std.I64 → Result backend.avx2.intrinsics.Vec256

/-- [kopis::backend::avx2::intrinsics::cvtsi32_si128]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 93:0-95:1 -/
axiom backend.avx2.intrinsics.cvtsi32_si128
  : Std.I32 → Result backend.avx2.intrinsics.Vec128

/-- [kopis::backend::avx2::intrinsics::xor_si256]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 107:0-109:1 -/
axiom backend.avx2.intrinsics.xor_si256
  :
  backend.avx2.intrinsics.Vec256 → backend.avx2.intrinsics.Vec256 → Result
    backend.avx2.intrinsics.Vec256

/-- [kopis::backend::avx2::intrinsics::or_si256]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 114:0-116:1 -/
axiom backend.avx2.intrinsics.or_si256
  :
  backend.avx2.intrinsics.Vec256 → backend.avx2.intrinsics.Vec256 → Result
    backend.avx2.intrinsics.Vec256

/-- [kopis::backend::avx2::intrinsics::andnot_si256]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 124:0-126:1 -/
axiom backend.avx2.intrinsics.andnot_si256
  :
  backend.avx2.intrinsics.Vec256 → backend.avx2.intrinsics.Vec256 → Result
    backend.avx2.intrinsics.Vec256

/-- [kopis::backend::avx2::intrinsics::srli_epi16]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 209:0-211:1 -/
axiom backend.avx2.intrinsics.srli_epi16
  (IMM : Std.I32) :
  backend.avx2.intrinsics.Vec256 → Result backend.avx2.intrinsics.Vec256

/-- [kopis::backend::avx2::intrinsics::slli_epi64]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 226:0-228:1 -/
axiom backend.avx2.intrinsics.slli_epi64
  (IMM : Std.I32) :
  backend.avx2.intrinsics.Vec256 → Result backend.avx2.intrinsics.Vec256

/-- [kopis::backend::avx2::intrinsics::srli_epi64]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 235:0-237:1 -/
axiom backend.avx2.intrinsics.srli_epi64
  (IMM : Std.I32) :
  backend.avx2.intrinsics.Vec256 → Result backend.avx2.intrinsics.Vec256

/-- [kopis::backend::avx2::intrinsics::srl_epi16]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 244:0-246:1 -/
axiom backend.avx2.intrinsics.srl_epi16
  :
  backend.avx2.intrinsics.Vec256 → backend.avx2.intrinsics.Vec128 → Result
    backend.avx2.intrinsics.Vec256

/-- [kopis::backend::avx2::intrinsics::load_u8x32]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 459:0-463:1 -/
axiom backend.avx2.intrinsics.load_u8x32
  {N : Std.Usize} :
  Array Std.U8 N → Std.Usize → Result backend.avx2.intrinsics.Vec256

/-- [kopis::backend::avx2::intrinsics::store_u8x32]:
    Source: 'src/backend/avx2/intrinsics.rs', lines 468:0-472:1 -/
axiom backend.avx2.intrinsics.store_u8x32
  {N : Std.Usize} :
  Array Std.U8 N → Std.Usize → backend.avx2.intrinsics.Vec256 → Result
    (Array Std.U8 N)

/-- [kopis::backend::avx2::keccak::PLEN]
    Source: 'src/backend/avx2/keccak.rs', lines 26:0-26:23 -/
@[global_simps, irreducible]
def backend.avx2.keccak.PLEN : Std.Usize := 25#usize

/-- [kopis::backend::avx2::keccak::ROUNDS]
    Source: 'src/backend/avx2/keccak.rs', lines 29:0-29:25 -/
@[global_simps, irreducible]
def backend.avx2.keccak.ROUNDS : Std.Usize := 12#usize

/-- [kopis::backend::avx2::keccak::RC]
    Source: 'src/backend/avx2/keccak.rs', lines 33:0-40:2 -/
@[global_simps, irreducible]
def backend.avx2.keccak.RC : Array Std.U64 24#usize :=
  Array.make 24#usize [
    1#u64, 32898#u64, 9223372036854808714#u64, 9223372039002292224#u64,
    32907#u64, 2147483649#u64, 9223372039002292353#u64,
    9223372036854808585#u64, 138#u64, 136#u64, 2147516425#u64, 2147483658#u64,
    2147516555#u64, 9223372036854775947#u64, 9223372036854808713#u64,
    9223372036854808579#u64, 9223372036854808578#u64, 9223372036854775936#u64,
    32778#u64, 9223372039002259466#u64, 9223372039002292353#u64,
    9223372036854808704#u64, 2147483649#u64, 9223372039002292232#u64
    ]

/-- [kopis::backend::avx2::keccak::rotl]:
    Source: 'src/backend/avx2/keccak.rs', lines 48:0-53:1 -/
def backend.avx2.keccak.rotl
  (L : Std.I32) (R : Std.I32) (v : backend.avx2.intrinsics.Vec256) :
  Result backend.avx2.intrinsics.Vec256
  := do
  let i ← L + R
  massert (i = 64#i32)
  let v1 ← backend.avx2.intrinsics.slli_epi64 L v
  let v2 ← backend.avx2.intrinsics.srli_epi64 R v
  backend.avx2.intrinsics.or_si256 v1 v2

/-- [kopis::backend::avx2::keccak::transpose4x64]:
    Source: 'src/backend/avx2/keccak.rs', lines 64:0-75:1 -/
def backend.avx2.keccak.transpose4x64
  (a : backend.avx2.intrinsics.Vec256) (b : backend.avx2.intrinsics.Vec256)
  (c : backend.avx2.intrinsics.Vec256) (d : backend.avx2.intrinsics.Vec256) :
  Result (backend.avx2.intrinsics.Vec256 × backend.avx2.intrinsics.Vec256 ×
    backend.avx2.intrinsics.Vec256 × backend.avx2.intrinsics.Vec256)
  := do
  let t0 ← backend.avx2.intrinsics.unpacklo_epi64 a b
  let t1 ← backend.avx2.intrinsics.unpackhi_epi64 a b
  let t2 ← backend.avx2.intrinsics.unpacklo_epi64 c d
  let t3 ← backend.avx2.intrinsics.unpackhi_epi64 c d
  let v ← backend.avx2.intrinsics.permute2x128_si256 32#i32 t0 t2
  let v1 ← backend.avx2.intrinsics.permute2x128_si256 32#i32 t1 t3
  let v2 ← backend.avx2.intrinsics.permute2x128_si256 49#i32 t0 t2
  let v3 ← backend.avx2.intrinsics.permute2x128_si256 49#i32 t1 t3
  ok (v, v1, v2, v3)

/-- [kopis::backend::avx2::keccak::load_words]:
    Source: 'src/backend/avx2/keccak.rs', lines 80:0-82:1 -/
def backend.avx2.keccak.load_words
  {RATE : Std.Usize} (block : Array Std.U8 RATE) (word : Std.Usize) :
  Result backend.avx2.intrinsics.Vec256
  := do
  let i ← 8#usize * word
  backend.avx2.intrinsics.load_u8x32 block i

/-- [kopis::backend::avx2::keccak::round_const]:
    Source: 'src/backend/avx2/keccak.rs', lines 87:0-91:1 -/
def backend.avx2.keccak.round_const
  (round : Std.Usize) : Result backend.avx2.intrinsics.Vec256 := do
  massert (round < backend.avx2.keccak.ROUNDS)
  let i ← 24#usize - backend.avx2.keccak.ROUNDS
  let i1 ← i + round
  let i2 ← Array.index_usize backend.avx2.keccak.RC i1
  let i3 ← lift (UScalar.hcast .I64 i2)
  backend.avx2.intrinsics.set1_epi64x i3

/-- [kopis::backend::avx2::keccak::round]:
    Source: 'src/backend/avx2/keccak.rs', lines 128:0-175:1 -/
def backend.avx2.keccak.round
  (src : Array backend.avx2.intrinsics.Vec256 25#usize)
  (dst : Array backend.avx2.intrinsics.Vec256 25#usize)
  (rc : backend.avx2.intrinsics.Vec256) :
  Result (Array backend.avx2.intrinsics.Vec256 25#usize)
  := do
  let v ← Array.index_usize src 0#usize
  let v1 ← Array.index_usize src 5#usize
  let v2 ← backend.avx2.intrinsics.xor_si256 v v1
  let v3 ← Array.index_usize src 10#usize
  let v4 ← Array.index_usize src 15#usize
  let v5 ← backend.avx2.intrinsics.xor_si256 v3 v4
  let v6 ← backend.avx2.intrinsics.xor_si256 v2 v5
  let v7 ← Array.index_usize src 20#usize
  let c0 ← backend.avx2.intrinsics.xor_si256 v6 v7
  let v8 ← Array.index_usize src 1#usize
  let v9 ← Array.index_usize src 6#usize
  let v10 ← backend.avx2.intrinsics.xor_si256 v8 v9
  let v11 ← Array.index_usize src 11#usize
  let v12 ← Array.index_usize src 16#usize
  let v13 ← backend.avx2.intrinsics.xor_si256 v11 v12
  let v14 ← backend.avx2.intrinsics.xor_si256 v10 v13
  let v15 ← Array.index_usize src 21#usize
  let c1 ← backend.avx2.intrinsics.xor_si256 v14 v15
  let v16 ← Array.index_usize src 2#usize
  let v17 ← Array.index_usize src 7#usize
  let v18 ← backend.avx2.intrinsics.xor_si256 v16 v17
  let v19 ← Array.index_usize src 12#usize
  let v20 ← Array.index_usize src 17#usize
  let v21 ← backend.avx2.intrinsics.xor_si256 v19 v20
  let v22 ← backend.avx2.intrinsics.xor_si256 v18 v21
  let v23 ← Array.index_usize src 22#usize
  let c2 ← backend.avx2.intrinsics.xor_si256 v22 v23
  let v24 ← Array.index_usize src 3#usize
  let v25 ← Array.index_usize src 8#usize
  let v26 ← backend.avx2.intrinsics.xor_si256 v24 v25
  let v27 ← Array.index_usize src 13#usize
  let v28 ← Array.index_usize src 18#usize
  let v29 ← backend.avx2.intrinsics.xor_si256 v27 v28
  let v30 ← backend.avx2.intrinsics.xor_si256 v26 v29
  let v31 ← Array.index_usize src 23#usize
  let c3 ← backend.avx2.intrinsics.xor_si256 v30 v31
  let v32 ← Array.index_usize src 4#usize
  let v33 ← Array.index_usize src 9#usize
  let v34 ← backend.avx2.intrinsics.xor_si256 v32 v33
  let v35 ← Array.index_usize src 14#usize
  let v36 ← Array.index_usize src 19#usize
  let v37 ← backend.avx2.intrinsics.xor_si256 v35 v36
  let v38 ← backend.avx2.intrinsics.xor_si256 v34 v37
  let v39 ← Array.index_usize src 24#usize
  let c4 ← backend.avx2.intrinsics.xor_si256 v38 v39
  let v40 ← backend.avx2.keccak.rotl 1#i32 63#i32 c1
  let v41 ← backend.avx2.intrinsics.xor_si256 c4 v40
  let v42 ← backend.avx2.keccak.rotl 1#i32 63#i32 c2
  let v43 ← backend.avx2.intrinsics.xor_si256 c0 v42
  let v44 ← backend.avx2.keccak.rotl 1#i32 63#i32 c3
  let v45 ← backend.avx2.intrinsics.xor_si256 c1 v44
  let v46 ← backend.avx2.keccak.rotl 1#i32 63#i32 c4
  let v47 ← backend.avx2.intrinsics.xor_si256 c2 v46
  let v48 ← backend.avx2.keccak.rotl 1#i32 63#i32 c0
  let v49 ← backend.avx2.intrinsics.xor_si256 c3 v48
  let i ← 0#usize % 5#usize
  let v50 ←
    Array.index_usize (Array.make 5#usize [ v41, v43, v45, v47, v49 ]) i
  let v51 ← backend.avx2.intrinsics.xor_si256 v v50
  let t0 ← backend.avx2.keccak.rotl 0#i32 64#i32 v51
  let i1 ← 6#usize % 5#usize
  let v52 ←
    Array.index_usize (Array.make 5#usize [ v41, v43, v45, v47, v49 ]) i1
  let v53 ← backend.avx2.intrinsics.xor_si256 v9 v52
  let t1 ← backend.avx2.keccak.rotl 44#i32 20#i32 v53
  let i2 ← 12#usize % 5#usize
  let v54 ←
    Array.index_usize (Array.make 5#usize [ v41, v43, v45, v47, v49 ]) i2
  let v55 ← backend.avx2.intrinsics.xor_si256 v19 v54
  let t2 ← backend.avx2.keccak.rotl 43#i32 21#i32 v55
  let i3 ← 18#usize % 5#usize
  let v56 ←
    Array.index_usize (Array.make 5#usize [ v41, v43, v45, v47, v49 ]) i3
  let v57 ← backend.avx2.intrinsics.xor_si256 v28 v56
  let t3 ← backend.avx2.keccak.rotl 21#i32 43#i32 v57
  let i4 ← 24#usize % 5#usize
  let v58 ←
    Array.index_usize (Array.make 5#usize [ v41, v43, v45, v47, v49 ]) i4
  let v59 ← backend.avx2.intrinsics.xor_si256 v39 v58
  let t4 ← backend.avx2.keccak.rotl 14#i32 50#i32 v59
  let v60 ← backend.avx2.intrinsics.andnot_si256 t1 t2
  let v61 ← backend.avx2.intrinsics.xor_si256 t0 v60
  let i5 ← 5#usize * 0#usize
  let dst1 ← Array.update dst i5 v61
  let v62 ← backend.avx2.intrinsics.andnot_si256 t2 t3
  let v63 ← backend.avx2.intrinsics.xor_si256 t1 v62
  let i6 ← i5 + 1#usize
  let dst2 ← Array.update dst1 i6 v63
  let v64 ← backend.avx2.intrinsics.andnot_si256 t3 t4
  let v65 ← backend.avx2.intrinsics.xor_si256 t2 v64
  let i7 ← i5 + 2#usize
  let dst3 ← Array.update dst2 i7 v65
  let v66 ← backend.avx2.intrinsics.andnot_si256 t4 t0
  let v67 ← backend.avx2.intrinsics.xor_si256 t3 v66
  let i8 ← i5 + 3#usize
  let dst4 ← Array.update dst3 i8 v67
  let v68 ← backend.avx2.intrinsics.andnot_si256 t0 t1
  let v69 ← backend.avx2.intrinsics.xor_si256 t4 v68
  let i9 ← i5 + 4#usize
  let dst5 ← Array.update dst4 i9 v69
  let i10 ← 3#usize % 5#usize
  let v70 ←
    Array.index_usize (Array.make 5#usize [ v41, v43, v45, v47, v49 ]) i10
  let v71 ← backend.avx2.intrinsics.xor_si256 v24 v70
  let t01 ← backend.avx2.keccak.rotl 28#i32 36#i32 v71
  let i11 ← 9#usize % 5#usize
  let v72 ←
    Array.index_usize (Array.make 5#usize [ v41, v43, v45, v47, v49 ]) i11
  let v73 ← backend.avx2.intrinsics.xor_si256 v33 v72
  let t11 ← backend.avx2.keccak.rotl 20#i32 44#i32 v73
  let i12 ← 10#usize % 5#usize
  let v74 ←
    Array.index_usize (Array.make 5#usize [ v41, v43, v45, v47, v49 ]) i12
  let v75 ← backend.avx2.intrinsics.xor_si256 v3 v74
  let t21 ← backend.avx2.keccak.rotl 3#i32 61#i32 v75
  let i13 ← 16#usize % 5#usize
  let v76 ←
    Array.index_usize (Array.make 5#usize [ v41, v43, v45, v47, v49 ]) i13
  let v77 ← backend.avx2.intrinsics.xor_si256 v12 v76
  let t31 ← backend.avx2.keccak.rotl 45#i32 19#i32 v77
  let i14 ← 22#usize % 5#usize
  let v78 ←
    Array.index_usize (Array.make 5#usize [ v41, v43, v45, v47, v49 ]) i14
  let v79 ← backend.avx2.intrinsics.xor_si256 v23 v78
  let t41 ← backend.avx2.keccak.rotl 61#i32 3#i32 v79
  let v80 ← backend.avx2.intrinsics.andnot_si256 t11 t21
  let v81 ← backend.avx2.intrinsics.xor_si256 t01 v80
  let i15 ← 5#usize * 1#usize
  let dst6 ← Array.update dst5 i15 v81
  let v82 ← backend.avx2.intrinsics.andnot_si256 t21 t31
  let v83 ← backend.avx2.intrinsics.xor_si256 t11 v82
  let i16 ← i15 + 1#usize
  let dst7 ← Array.update dst6 i16 v83
  let v84 ← backend.avx2.intrinsics.andnot_si256 t31 t41
  let v85 ← backend.avx2.intrinsics.xor_si256 t21 v84
  let i17 ← i15 + 2#usize
  let dst8 ← Array.update dst7 i17 v85
  let v86 ← backend.avx2.intrinsics.andnot_si256 t41 t01
  let v87 ← backend.avx2.intrinsics.xor_si256 t31 v86
  let i18 ← i15 + 3#usize
  let dst9 ← Array.update dst8 i18 v87
  let v88 ← backend.avx2.intrinsics.andnot_si256 t01 t11
  let v89 ← backend.avx2.intrinsics.xor_si256 t41 v88
  let i19 ← i15 + 4#usize
  let dst10 ← Array.update dst9 i19 v89
  let i20 ← 1#usize % 5#usize
  let v90 ←
    Array.index_usize (Array.make 5#usize [ v41, v43, v45, v47, v49 ]) i20
  let v91 ← backend.avx2.intrinsics.xor_si256 v8 v90
  let t02 ← backend.avx2.keccak.rotl 1#i32 63#i32 v91
  let i21 ← 7#usize % 5#usize
  let v92 ←
    Array.index_usize (Array.make 5#usize [ v41, v43, v45, v47, v49 ]) i21
  let v93 ← backend.avx2.intrinsics.xor_si256 v17 v92
  let t12 ← backend.avx2.keccak.rotl 6#i32 58#i32 v93
  let i22 ← 13#usize % 5#usize
  let v94 ←
    Array.index_usize (Array.make 5#usize [ v41, v43, v45, v47, v49 ]) i22
  let v95 ← backend.avx2.intrinsics.xor_si256 v27 v94
  let t22 ← backend.avx2.keccak.rotl 25#i32 39#i32 v95
  let i23 ← 19#usize % 5#usize
  let v96 ←
    Array.index_usize (Array.make 5#usize [ v41, v43, v45, v47, v49 ]) i23
  let v97 ← backend.avx2.intrinsics.xor_si256 v36 v96
  let t32 ← backend.avx2.keccak.rotl 8#i32 56#i32 v97
  let i24 ← 20#usize % 5#usize
  let v98 ←
    Array.index_usize (Array.make 5#usize [ v41, v43, v45, v47, v49 ]) i24
  let v99 ← backend.avx2.intrinsics.xor_si256 v7 v98
  let t42 ← backend.avx2.keccak.rotl 18#i32 46#i32 v99
  let v100 ← backend.avx2.intrinsics.andnot_si256 t12 t22
  let v101 ← backend.avx2.intrinsics.xor_si256 t02 v100
  let i25 ← 5#usize * 2#usize
  let dst11 ← Array.update dst10 i25 v101
  let v102 ← backend.avx2.intrinsics.andnot_si256 t22 t32
  let v103 ← backend.avx2.intrinsics.xor_si256 t12 v102
  let i26 ← i25 + 1#usize
  let dst12 ← Array.update dst11 i26 v103
  let v104 ← backend.avx2.intrinsics.andnot_si256 t32 t42
  let v105 ← backend.avx2.intrinsics.xor_si256 t22 v104
  let i27 ← i25 + 2#usize
  let dst13 ← Array.update dst12 i27 v105
  let v106 ← backend.avx2.intrinsics.andnot_si256 t42 t02
  let v107 ← backend.avx2.intrinsics.xor_si256 t32 v106
  let i28 ← i25 + 3#usize
  let dst14 ← Array.update dst13 i28 v107
  let v108 ← backend.avx2.intrinsics.andnot_si256 t02 t12
  let v109 ← backend.avx2.intrinsics.xor_si256 t42 v108
  let i29 ← i25 + 4#usize
  let dst15 ← Array.update dst14 i29 v109
  let i30 ← 4#usize % 5#usize
  let v110 ←
    Array.index_usize (Array.make 5#usize [ v41, v43, v45, v47, v49 ]) i30
  let v111 ← backend.avx2.intrinsics.xor_si256 v32 v110
  let t03 ← backend.avx2.keccak.rotl 27#i32 37#i32 v111
  let i31 ← 5#usize % 5#usize
  let v112 ←
    Array.index_usize (Array.make 5#usize [ v41, v43, v45, v47, v49 ]) i31
  let v113 ← backend.avx2.intrinsics.xor_si256 v1 v112
  let t13 ← backend.avx2.keccak.rotl 36#i32 28#i32 v113
  let i32 ← 11#usize % 5#usize
  let v114 ←
    Array.index_usize (Array.make 5#usize [ v41, v43, v45, v47, v49 ]) i32
  let v115 ← backend.avx2.intrinsics.xor_si256 v11 v114
  let t23 ← backend.avx2.keccak.rotl 10#i32 54#i32 v115
  let i33 ← 17#usize % 5#usize
  let v116 ←
    Array.index_usize (Array.make 5#usize [ v41, v43, v45, v47, v49 ]) i33
  let v117 ← backend.avx2.intrinsics.xor_si256 v20 v116
  let t33 ← backend.avx2.keccak.rotl 15#i32 49#i32 v117
  let i34 ← 23#usize % 5#usize
  let v118 ←
    Array.index_usize (Array.make 5#usize [ v41, v43, v45, v47, v49 ]) i34
  let v119 ← backend.avx2.intrinsics.xor_si256 v31 v118
  let t43 ← backend.avx2.keccak.rotl 56#i32 8#i32 v119
  let v120 ← backend.avx2.intrinsics.andnot_si256 t13 t23
  let v121 ← backend.avx2.intrinsics.xor_si256 t03 v120
  let i35 ← 5#usize * 3#usize
  let dst16 ← Array.update dst15 i35 v121
  let v122 ← backend.avx2.intrinsics.andnot_si256 t23 t33
  let v123 ← backend.avx2.intrinsics.xor_si256 t13 v122
  let i36 ← i35 + 1#usize
  let dst17 ← Array.update dst16 i36 v123
  let v124 ← backend.avx2.intrinsics.andnot_si256 t33 t43
  let v125 ← backend.avx2.intrinsics.xor_si256 t23 v124
  let i37 ← i35 + 2#usize
  let dst18 ← Array.update dst17 i37 v125
  let v126 ← backend.avx2.intrinsics.andnot_si256 t43 t03
  let v127 ← backend.avx2.intrinsics.xor_si256 t33 v126
  let i38 ← i35 + 3#usize
  let dst19 ← Array.update dst18 i38 v127
  let v128 ← backend.avx2.intrinsics.andnot_si256 t03 t13
  let v129 ← backend.avx2.intrinsics.xor_si256 t43 v128
  let i39 ← i35 + 4#usize
  let dst20 ← Array.update dst19 i39 v129
  let i40 ← 2#usize % 5#usize
  let v130 ←
    Array.index_usize (Array.make 5#usize [ v41, v43, v45, v47, v49 ]) i40
  let v131 ← backend.avx2.intrinsics.xor_si256 v16 v130
  let t04 ← backend.avx2.keccak.rotl 62#i32 2#i32 v131
  let i41 ← 8#usize % 5#usize
  let v132 ←
    Array.index_usize (Array.make 5#usize [ v41, v43, v45, v47, v49 ]) i41
  let v133 ← backend.avx2.intrinsics.xor_si256 v25 v132
  let t14 ← backend.avx2.keccak.rotl 55#i32 9#i32 v133
  let i42 ← 14#usize % 5#usize
  let v134 ←
    Array.index_usize (Array.make 5#usize [ v41, v43, v45, v47, v49 ]) i42
  let v135 ← backend.avx2.intrinsics.xor_si256 v35 v134
  let t24 ← backend.avx2.keccak.rotl 39#i32 25#i32 v135
  let i43 ← 15#usize % 5#usize
  let v136 ←
    Array.index_usize (Array.make 5#usize [ v41, v43, v45, v47, v49 ]) i43
  let v137 ← backend.avx2.intrinsics.xor_si256 v4 v136
  let t34 ← backend.avx2.keccak.rotl 41#i32 23#i32 v137
  let i44 ← 21#usize % 5#usize
  let v138 ←
    Array.index_usize (Array.make 5#usize [ v41, v43, v45, v47, v49 ]) i44
  let v139 ← backend.avx2.intrinsics.xor_si256 v15 v138
  let t44 ← backend.avx2.keccak.rotl 2#i32 62#i32 v139
  let v140 ← backend.avx2.intrinsics.andnot_si256 t14 t24
  let v141 ← backend.avx2.intrinsics.xor_si256 t04 v140
  let i45 ← 5#usize * 4#usize
  let dst21 ← Array.update dst20 i45 v141
  let v142 ← backend.avx2.intrinsics.andnot_si256 t24 t34
  let v143 ← backend.avx2.intrinsics.xor_si256 t14 v142
  let i46 ← i45 + 1#usize
  let dst22 ← Array.update dst21 i46 v143
  let v144 ← backend.avx2.intrinsics.andnot_si256 t34 t44
  let v145 ← backend.avx2.intrinsics.xor_si256 t24 v144
  let i47 ← i45 + 2#usize
  let dst23 ← Array.update dst22 i47 v145
  let v146 ← backend.avx2.intrinsics.andnot_si256 t44 t04
  let v147 ← backend.avx2.intrinsics.xor_si256 t34 v146
  let i48 ← i45 + 3#usize
  let dst24 ← Array.update dst23 i48 v147
  let v148 ← backend.avx2.intrinsics.andnot_si256 t04 t14
  let v149 ← backend.avx2.intrinsics.xor_si256 t44 v148
  let i49 ← i45 + 4#usize
  let dst25 ← Array.update dst24 i49 v149
  let v150 ← Array.index_usize dst25 0#usize
  let v151 ← backend.avx2.intrinsics.xor_si256 v150 rc
  Array.update dst25 0#usize v151

/-- [kopis::backend::avx2::keccak::permute]: loop 0:
    Source: 'src/backend/avx2/keccak.rs', lines 191:4-194:5 -/
@[rust_loop]
def backend.avx2.keccak.permute_loop
  (iter : core.ops.range.Range Std.Usize)
  (state : Array backend.avx2.intrinsics.Vec256 25#usize)
  (scratch : Array backend.avx2.intrinsics.Vec256 25#usize) :
  Result (Array backend.avx2.intrinsics.Vec256 25#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok state
  | some pair =>
    let i ← 2#usize * pair
    let v ← backend.avx2.keccak.round_const i
    let scratch1 ← backend.avx2.keccak.round state scratch v
    let i1 ← i + 1#usize
    let v1 ← backend.avx2.keccak.round_const i1
    let state1 ← backend.avx2.keccak.round scratch1 state v1
    backend.avx2.keccak.permute_loop iter1 state1 scratch1
partial_fixpoint

/-- [kopis::backend::avx2::keccak::permute]:
    Source: 'src/backend/avx2/keccak.rs', lines 179:0-195:1 -/
def backend.avx2.keccak.permute
  (state : Array backend.avx2.intrinsics.Vec256 25#usize) :
  Result (Array backend.avx2.intrinsics.Vec256 25#usize)
  := do
  let v ← backend.avx2.intrinsics.setzero_si256
  let scratch := Array.repeat 25#usize v
  let i ← backend.avx2.keccak.ROUNDS / 2#usize
  backend.avx2.keccak.permute_loop { start := 0#usize, «end» := i } state
    scratch

/-- [kopis::backend::avx2::keccak::pad_block]:
    Source: 'src/backend/avx2/keccak.rs', lines 204:0-214:1 -/
def backend.avx2.keccak.pad_block
  (RATE : Std.Usize) (DS : Std.U8) {S : Std.Usize}
  (prefix1 : Array Std.U8 32#usize) (suffix : Array Std.U8 S) :
  Result (Array Std.U8 RATE)
  := do
  let bytes := Array.repeat RATE 0#u8
  let (s, index_mut_back) ←
    core.array.Array.index_mut (core.ops.index.IndexMutSlice
      (core.slice.index.SliceIndexRangeToUsizeSlice Std.U8)) bytes
      { «end» := 32#usize }
  let s1 ← lift (Array.to_slice prefix1)
  let s2 ← core.slice.Slice.copy_from_slice core.marker.CopyU8 s s1
  let bytes1 := index_mut_back s2
  let i ← 32#usize + S
  let (s3, index_mut_back1) ←
    core.array.Array.index_mut (core.ops.index.IndexMutSlice
      (core.slice.index.SliceIndexRangeUsizeSlice Std.U8)) bytes1
      { start := 32#usize, «end» := i }
  let s4 ← lift (Array.to_slice suffix)
  let s5 ← core.slice.Slice.copy_from_slice core.marker.CopyU8 s3 s4
  let bytes2 := index_mut_back1 s5
  let bytes3 ← Array.update bytes2 i DS
  let i1 ← RATE - 1#usize
  let i2 ← Array.index_usize bytes3 i1
  let i3 ← lift (i2 ||| 128#u8)
  Array.update bytes3 i1 i3

/-- [kopis::backend::avx2::keccak::xof4]: loop 0:
    Source: 'src/backend/avx2/keccak.rs', lines 263:4-277:5 -/
@[rust_loop]
def backend.avx2.keccak.xof4_loop0
  {RATE : Std.Usize} (b0 : Array Std.U8 RATE) (b1 : Array Std.U8 RATE)
  (b2 : Array Std.U8 RATE) (b3 : Array Std.U8 RATE)
  (state : Array backend.avx2.intrinsics.Vec256 25#usize) (words : Std.Usize)
  (word : Std.Usize) :
  Result ((Array backend.avx2.intrinsics.Vec256 25#usize) × Std.Usize)
  := do
  let i ← word + 4#usize
  if i <= words
  then
    let v ← backend.avx2.keccak.load_words b0 word
    let v1 ← backend.avx2.keccak.load_words b1 word
    let v2 ← backend.avx2.keccak.load_words b2 word
    let v3 ← backend.avx2.keccak.load_words b3 word
    let (r0, r1, r2, r3) ← backend.avx2.keccak.transpose4x64 v v1 v2 v3
    let state1 ← Array.update state word r0
    let i1 ← word + 1#usize
    let state2 ← Array.update state1 i1 r1
    let i2 ← word + 2#usize
    let state3 ← Array.update state2 i2 r2
    let i3 ← word + 3#usize
    let a ← Array.update state3 i3 r3
    backend.avx2.keccak.xof4_loop0 b0 b1 b2 b3 a words i
  else ok (state, word)
partial_fixpoint

/-- [kopis::backend::avx2::keccak::xof4]: loop 2:
    Source: 'src/backend/avx2/keccak.rs', lines 282:8-287:9 -/
@[rust_loop]
def backend.avx2.keccak.xof4_loop1_loop0
  {RATE : Std.Usize} (iter : core.ops.range.Range Std.Usize)
  (b0 : Array Std.U8 RATE) (b1 : Array Std.U8 RATE) (b2 : Array Std.U8 RATE)
  (b3 : Array Std.U8 RATE) (word : Std.Usize) (packed : Array Std.U8 32#usize)
  :
  Result (Array Std.U8 32#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok packed
  | some i =>
    let i1 ← 8#usize * word
    let i2 ← i1 + i
    let i3 ← Array.index_usize b0 i2
    let packed1 ← Array.update packed i i3
    let i4 ← i1 + i
    let i5 ← Array.index_usize b1 i4
    let i6 ← 8#usize + i
    let packed2 ← Array.update packed1 i6 i5
    let i7 ← i1 + i
    let i8 ← Array.index_usize b2 i7
    let i9 ← 16#usize + i
    let packed3 ← Array.update packed2 i9 i8
    let i10 ← i1 + i
    let i11 ← Array.index_usize b3 i10
    let i12 ← 24#usize + i
    let a ← Array.update packed3 i12 i11
    backend.avx2.keccak.xof4_loop1_loop0 iter1 b0 b1 b2 b3 word a
partial_fixpoint

/-- [kopis::backend::avx2::keccak::xof4]: loop 1:
    Source: 'src/backend/avx2/keccak.rs', lines 278:4-290:5 -/
@[rust_loop]
def backend.avx2.keccak.xof4_loop1
  {RATE : Std.Usize} (b0 : Array Std.U8 RATE) (b1 : Array Std.U8 RATE)
  (b2 : Array Std.U8 RATE) (b3 : Array Std.U8 RATE)
  (state : Array backend.avx2.intrinsics.Vec256 25#usize) (words : Std.Usize)
  (word : Std.Usize) :
  Result (Array backend.avx2.intrinsics.Vec256 25#usize)
  := do
  if word < words
  then
    let packed := Array.repeat 32#usize 0#u8
    let packed1 ←
      backend.avx2.keccak.xof4_loop1_loop0
        { start := 0#usize, «end» := 8#usize } b0 b1 b2 b3 word packed
    let v ← backend.avx2.intrinsics.load_u8x32 packed1 0#usize
    let a ← Array.update state word v
    let word1 ← word + 1#usize
    backend.avx2.keccak.xof4_loop1 b0 b1 b2 b3 a words word1
  else ok state
partial_fixpoint

/-- [kopis::backend::avx2::keccak::xof4]: loop 4:
    Source: 'src/backend/avx2/keccak.rs', lines 306:8-319:9 -/
@[rust_loop]
def backend.avx2.keccak.xof4_loop2_loop0
  {N : Std.Usize} (out : Array (Array Std.U8 N) 4#usize)
  (state : Array backend.avx2.intrinsics.Vec256 25#usize) (done1 : Std.Usize)
  (words : Std.Usize) (word : Std.Usize) :
  Result ((Array (Array Std.U8 N) 4#usize) × Std.Usize)
  := do
  let i ← word + 4#usize
  if i <= words
  then
    let i1 ← 8#usize * word
    let i2 ← done1 + i1
    let i3 ← i2 + 32#usize
    if i3 <= N
    then
      let v ← Array.index_usize state word
      let i4 ← word + 1#usize
      let v1 ← Array.index_usize state i4
      let i5 ← word + 2#usize
      let v2 ← Array.index_usize state i5
      let i6 ← word + 3#usize
      let v3 ← Array.index_usize state i6
      let (l0, l1, l2, l3) ← backend.avx2.keccak.transpose4x64 v v1 v2 v3
      let start ← done1 + i1
      let (a, index_mut_back) ← Array.index_mut_usize out 0#usize
      let a1 ← backend.avx2.intrinsics.store_u8x32 a start l0
      let out1 := index_mut_back a1
      let (a2, index_mut_back1) ← Array.index_mut_usize out1 1#usize
      let a3 ← backend.avx2.intrinsics.store_u8x32 a2 start l1
      let out2 := index_mut_back1 a3
      let (a4, index_mut_back2) ← Array.index_mut_usize out2 2#usize
      let a5 ← backend.avx2.intrinsics.store_u8x32 a4 start l2
      let out3 := index_mut_back2 a5
      let (a6, index_mut_back3) ← Array.index_mut_usize out3 3#usize
      let a7 ← backend.avx2.intrinsics.store_u8x32 a6 start l3
      let a8 := index_mut_back3 a7
      backend.avx2.keccak.xof4_loop2_loop0 a8 state done1 words i
    else ok (out, word)
  else ok (out, word)
partial_fixpoint

/-- [kopis::backend::avx2::keccak::xof4]: loop 7:
    Source: 'src/backend/avx2/keccak.rs', lines 331:16-333:17 -/
@[rust_loop]
def backend.avx2.keccak.xof4_loop2_loop1_loop0_loop0
  {N : Std.Usize} (iter : core.ops.range.Range Std.Usize)
  (out : Array (Array Std.U8 N) 4#usize) (packed : Array Std.U8 32#usize)
  (lane : Std.Usize) (start : Std.Usize) :
  Result (Array (Array Std.U8 N) 4#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok out
  | some i =>
    let i1 ← 8#usize * lane
    let i2 ← i1 + i
    let i3 ← Array.index_usize packed i2
    let i4 ← start + i
    let (a, index_mut_back) ← Array.index_mut_usize out lane
    let a1 ← Array.update a i4 i3
    let a2 := index_mut_back a1
    backend.avx2.keccak.xof4_loop2_loop1_loop0_loop0 iter1 a2 packed lane start
partial_fixpoint

/-- [kopis::backend::avx2::keccak::xof4]: loop 6:
    Source: 'src/backend/avx2/keccak.rs', lines 327:12-334:13 -/
@[rust_loop]
def backend.avx2.keccak.xof4_loop2_loop1_loop0
  {N : Std.Usize} (iter : core.ops.range.Range Std.Usize)
  (out : Array (Array Std.U8 N) 4#usize) (done1 : Std.Usize) (word : Std.Usize)
  (packed : Array Std.U8 32#usize) :
  Result (Array (Array Std.U8 N) 4#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok out
  | some lane =>
    let i ← 8#usize * word
    let start ← done1 + i
    let i1 ← N - start
    let len ← core.cmp.min core.cmp.OrdUsize 8#usize i1
    let out1 ←
      backend.avx2.keccak.xof4_loop2_loop1_loop0_loop0
        { start := 0#usize, «end» := len } out packed lane start
    backend.avx2.keccak.xof4_loop2_loop1_loop0 iter1 out1 done1 word packed
partial_fixpoint

/-- [kopis::backend::avx2::keccak::xof4]: loop 5:
    Source: 'src/backend/avx2/keccak.rs', lines 323:8-336:9 -/
@[rust_loop]
def backend.avx2.keccak.xof4_loop2_loop1
  {N : Std.Usize} (out : Array (Array Std.U8 N) 4#usize)
  (state : Array backend.avx2.intrinsics.Vec256 25#usize) (done1 : Std.Usize)
  (words : Std.Usize) (word : Std.Usize) :
  Result (Array (Array Std.U8 N) 4#usize)
  := do
  if word < words
  then
    let packed := Array.repeat 32#usize 0#u8
    let v ← Array.index_usize state word
    let packed1 ← backend.avx2.intrinsics.store_u8x32 packed 0#usize v
    let out1 ←
      backend.avx2.keccak.xof4_loop2_loop1_loop0
        { start := 0#usize, «end» := 4#usize } out done1 word packed1
    let word1 ← word + 1#usize
    backend.avx2.keccak.xof4_loop2_loop1 out1 state done1 words word1
  else ok out
partial_fixpoint

/-- [kopis::backend::avx2::keccak::xof4]: loop 3:
    Source: 'src/backend/avx2/keccak.rs', lines 294:4-339:5 -/
@[rust_loop]
def backend.avx2.keccak.xof4_loop2
  (RATE : Std.Usize) {N : Std.Usize} (out : Array (Array Std.U8 N) 4#usize)
  (state : Array backend.avx2.intrinsics.Vec256 25#usize) (done1 : Std.Usize) :
  Result (Array (Array Std.U8 N) 4#usize)
  := do
  if done1 < N
  then
    let state1 ← backend.avx2.keccak.permute state
    let i ← N - done1
    let take ← core.cmp.min core.cmp.OrdUsize RATE i
    let i1 ← take + 7#usize
    let words ← i1 / 8#usize
    let (out1, word) ←
      backend.avx2.keccak.xof4_loop2_loop0 out state1 done1 words 0#usize
    let out2 ←
      backend.avx2.keccak.xof4_loop2_loop1 out1 state1 done1 words word
    let done2 ← done1 + take
    backend.avx2.keccak.xof4_loop2 RATE out2 state1 done2
  else ok out
partial_fixpoint

/-- [kopis::backend::avx2::keccak::xof4]:
    Source: 'src/backend/avx2/keccak.rs', lines 228:0-340:1 -/
def backend.avx2.keccak.xof4
  (RATE : Std.Usize) (DS : Std.U8) {S : Std.Usize} {N : Std.Usize}
  (prefix1 : Array Std.U8 32#usize) (suffixes : Array (Array Std.U8 S) 4#usize)
  (out : Array (Array Std.U8 N) 4#usize) :
  Result (Array (Array Std.U8 N) 4#usize)
  := do
  if RATE = 168#usize
  then ok ()
  else massert (RATE = 136#usize)
  massert (DS >= 1#u8)
  massert (DS <= 127#u8)
  let i ← 32#usize + S
  massert (i < RATE)
  let s0 ← Array.index_usize suffixes 0#usize
  let s1 ← Array.index_usize suffixes 1#usize
  let s2 ← Array.index_usize suffixes 2#usize
  let s3 ← Array.index_usize suffixes 3#usize
  let b0 ← backend.avx2.keccak.pad_block RATE DS prefix1 s0
  let b1 ← backend.avx2.keccak.pad_block RATE DS prefix1 s1
  let b2 ← backend.avx2.keccak.pad_block RATE DS prefix1 s2
  let b3 ← backend.avx2.keccak.pad_block RATE DS prefix1 s3
  let v ← backend.avx2.intrinsics.setzero_si256
  let state := Array.repeat 25#usize v
  let words ← RATE / 8#usize
  let (state1, word) ←
    backend.avx2.keccak.xof4_loop0 b0 b1 b2 b3 state words 0#usize
  let state2 ← backend.avx2.keccak.xof4_loop1 b0 b1 b2 b3 state1 words word
  backend.avx2.keccak.xof4_loop2 RATE out state2 0#usize

/-- [kopis::backend::avx2::sample::RATE_128]
    Source: 'src/backend/avx2/sample.rs', lines 21:0-21:28 -/
@[global_simps, irreducible]
def backend.avx2.sample.RATE_128 : Std.Usize := 168#usize

/-- [kopis::backend::avx2::sample::RATE_256]
    Source: 'src/backend/avx2/sample.rs', lines 23:0-23:28 -/
@[global_simps, irreducible]
def backend.avx2.sample.RATE_256 : Std.Usize := 136#usize

/-- [kopis::backend::avx2::sample::MATRIX_ELEM_BYTES]
    Source: 'src/backend/avx2/sample.rs', lines 26:0-26:63 -/
@[global_simps, irreducible]
def backend.avx2.sample.MATRIX_ELEM_BYTES : Result Std.Usize := do
  let i ← consts.RING_DEG * consts.MODULUS_Q_BITS
  i / 8#usize

/-- [kopis::backend::avx2::sample::Nibbles]
    Source: 'src/backend/avx2/sample.rs', lines 31:0-31:25 -/
@[reducible]
def backend.avx2.sample.Nibbles := Array Std.U8 32#usize

/-- [kopis::backend::avx2::sample::NIBBLE_POPCOUNT]
    Source: 'src/backend/avx2/sample.rs', lines 33:0-36:3 -/
@[global_simps, irreducible]
def backend.avx2.sample.NIBBLE_POPCOUNT : backend.avx2.sample.Nibbles :=
  Array.make 32#usize [
    0#u8, 1#u8, 1#u8, 2#u8, 1#u8, 2#u8, 2#u8, 3#u8, 1#u8, 2#u8, 2#u8, 3#u8,
    2#u8, 3#u8, 3#u8, 4#u8, 0#u8, 1#u8, 1#u8, 2#u8, 1#u8, 2#u8, 2#u8, 3#u8,
    1#u8, 2#u8, 2#u8, 3#u8, 2#u8, 3#u8, 3#u8, 4#u8
    ]

/-- [kopis::backend::avx2::sample::popcount_small]:
    Source: 'src/backend/avx2/sample.rs', lines 45:0-49:1 -/
def backend.avx2.sample.popcount_small
  (v : backend.avx2.intrinsics.Vec256) (lut : backend.avx2.intrinsics.Vec256) :
  Result backend.avx2.intrinsics.Vec256
  := do
  let v1 ← backend.avx2.intrinsics.set1_epi16 15#i16
  let v2 ← backend.avx2.intrinsics.and_si256 v v1
  let low ← backend.avx2.intrinsics.shuffle_epi8 lut v2
  let v3 ← backend.avx2.intrinsics.srli_epi16 4#i32 v
  let v4 ← backend.avx2.intrinsics.set1_epi16 1#i16
  let bit4 ← backend.avx2.intrinsics.and_si256 v3 v4
  backend.avx2.intrinsics.add_epi16 low bit4

/-- [kopis::backend::avx2::sample::shift_right_dynamic]:
    Source: 'src/backend/avx2/sample.rs', lines 57:0-59:1 -/
def backend.avx2.sample.shift_right_dynamic
  (v : backend.avx2.intrinsics.Vec256) (count : Std.Usize) :
  Result backend.avx2.intrinsics.Vec256
  := do
  let i ← lift (UScalar.hcast .I32 count)
  let v1 ← backend.avx2.intrinsics.cvtsi32_si128 i
  backend.avx2.intrinsics.srl_epi16 v v1

/-- [kopis::backend::avx2::sample::cbd]: loop 0:
    Source: 'src/backend/avx2/sample.rs', lines 79:4-86:5 -/
@[rust_loop]
def backend.avx2.sample.cbd_loop
  (i : Std.Usize) (iter : core.ops.range.Range Std.Usize)
  (fields : Array Std.U16 256#usize) (lut : backend.avx2.intrinsics.Vec256)
  (half_mask : backend.avx2.intrinsics.Vec256)
  (out : arithmetic.ring_arith.RingElem) :
  Result arithmetic.ring_arith.RingElem
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok out
  | some i1 =>
    let field ← backend.avx2.intrinsics.load_u16 fields i1
    let low ← backend.avx2.intrinsics.and_si256 field half_mask
    let v ← backend.avx2.sample.shift_right_dynamic field i
    let high ← backend.avx2.intrinsics.and_si256 v half_mask
    let v1 ← backend.avx2.sample.popcount_small low lut
    let v2 ← backend.avx2.sample.popcount_small high lut
    let coeff ← backend.avx2.intrinsics.sub_epi16 v1 v2
    let a ← backend.avx2.intrinsics.store_u16 out i1 coeff
    backend.avx2.sample.cbd_loop i iter1 fields lut half_mask a
partial_fixpoint

/-- [kopis::backend::avx2::sample::cbd]:
    Source: 'src/backend/avx2/sample.rs', lines 72:0-88:1 -/
def backend.avx2.sample.cbd
  (MU : Std.Usize) (buf : Slice Std.U8) :
  Result arithmetic.ring_arith.RingElem
  := do
  let fields ← backend.avx2.ser.deserialize buf MU
  let a := backend.avx2.sample.NIBBLE_POPCOUNT
  let lut ← backend.avx2.intrinsics.load_u8 a 0#usize
  let i ← MU / 2#usize
  let i1 ← 1#u16 <<< i
  let i2 ← i1 - 1#u16
  let i3 ← lift (UScalar.hcast .I16 i2)
  let half_mask ← backend.avx2.intrinsics.set1_epi16 i3
  let out ← arithmetic.ring_arith.RingElem.Insts.CoreDefaultDefault.default
  let i4 ← consts.RING_DEG / 16#usize
  backend.avx2.sample.cbd_loop i { start := 0#usize, «end» := i4 } fields lut
    half_mask out

/-- [kopis::backend::avx2::sample::gen_matrix_from_seed]: loop 1:
    Source: 'src/backend/avx2/sample.rs', lines 112:8-116:9 -/
@[rust_loop]
def backend.avx2.sample.gen_matrix_from_seed_loop0_loop0
  (L : Std.Usize) (iter : core.ops.range.Range Std.Usize) (entries : Std.Usize)
  (first : Std.Usize) (indices : Array (Array Std.U8 2#usize) 4#usize) :
  Result (Array (Array Std.U8 2#usize) 4#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok indices
  | some lane =>
    let i ← first + lane
    let i1 ← entries - 1#usize
    let entry ← core.cmp.min core.cmp.OrdUsize i i1
    let i2 ← entry / L
    let (a, index_mut_back) ← Array.index_mut_usize indices lane
    let i3 ← lift (UScalar.cast .U8 i2)
    let a1 ← Array.update a 0#usize i3
    let i4 ← entry % L
    let indices1 := index_mut_back a1
    let (a2, index_mut_back1) ← Array.index_mut_usize indices1 lane
    let i5 ← lift (UScalar.cast .U8 i4)
    let a3 ← Array.update a2 1#usize i5
    let a4 := index_mut_back1 a3
    backend.avx2.sample.gen_matrix_from_seed_loop0_loop0 L iter1 entries first
      a4
partial_fixpoint

/-- [kopis::backend::avx2::sample::gen_matrix_from_seed]: loop 2:
    Source: 'src/backend/avx2/sample.rs', lines 120:8-128:9 -/
@[rust_loop]
def backend.avx2.sample.gen_matrix_from_seed_loop0_loop1
  {L : Std.Usize} (iter : core.ops.range.Range Std.Usize)
  (mat : arithmetic.matrix_arith.Matrix L L) (entries : Std.Usize)
  (first : Std.Usize) (bufs : Array (Array Std.U8 416#usize) 4#usize) :
  Result (arithmetic.matrix_arith.Matrix L L)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok mat
  | some lane =>
    let entry ← first + lane
    if entry < entries
    then
      let a ← Array.index_usize bufs lane
      let i ← backend.avx2.sample.MATRIX_ELEM_BYTES
      let s ←
        core.array.Array.index (core.ops.index.IndexSlice
          (core.slice.index.SliceIndexRangeToUsizeSlice Std.U8)) a
          { «end» := i }
      let a1 ← backend.avx2.ser.deserialize s consts.MODULUS_Q_BITS
      let i1 ← entry / L
      let i2 ← entry % L
      let (a2, index_mut_back) ← Array.index_mut_usize mat i1
      let a3 ← Array.update a2 i2 a1
      let a4 := index_mut_back a3
      backend.avx2.sample.gen_matrix_from_seed_loop0_loop1 iter1 a4 entries
        first bufs
    else
      backend.avx2.sample.gen_matrix_from_seed_loop0_loop1 iter1 mat entries
        first bufs
partial_fixpoint

/-- [kopis::backend::avx2::sample::gen_matrix_from_seed]: loop 0:
    Source: 'src/backend/avx2/sample.rs', lines 107:4-131:5 -/
@[rust_loop]
def backend.avx2.sample.gen_matrix_from_seed_loop0
  {L : Std.Usize} (seed : Array Std.U8 32#usize)
  (mat : arithmetic.matrix_arith.Matrix L L) (entries : Std.Usize)
  (first : Std.Usize) :
  Result (arithmetic.matrix_arith.Matrix L L)
  := do
  if first < entries
  then
    let a := Array.repeat 2#usize 0#u8
    let indices := Array.repeat 4#usize a
    let indices1 ←
      backend.avx2.sample.gen_matrix_from_seed_loop0_loop0 L
        { start := 0#usize, «end» := 4#usize } entries first indices
    let a1 := Array.repeat 416#usize 0#u8
    let bufs := Array.repeat 4#usize a1
    let bufs1 ← backend.avx2.keccak.xof4 168#usize 2#u8 seed indices1 bufs
    let mat1 ←
      backend.avx2.sample.gen_matrix_from_seed_loop0_loop1
        { start := 0#usize, «end» := 4#usize } mat entries first bufs1
    let first1 ← first + 4#usize
    backend.avx2.sample.gen_matrix_from_seed_loop0 seed mat1 entries first1
  else ok mat
partial_fixpoint

/-- [kopis::backend::avx2::sample::gen_matrix_from_seed]:
    Source: 'src/backend/avx2/sample.rs', lines 102:0-134:1 -/
def backend.avx2.sample.gen_matrix_from_seed
  (L : Std.Usize) (seed : Array Std.U8 32#usize) :
  Result (arithmetic.matrix_arith.Matrix L L)
  := do
  let mat ←
    arithmetic.matrix_arith.Matrix.Insts.CoreDefaultDefault.default L L
  let entries ← L * L
  backend.avx2.sample.gen_matrix_from_seed_loop0 seed mat entries 0#usize

/-- [kopis::backend::avx2::sample::secret]: loop 0:
    Source: 'src/backend/avx2/sample.rs', lines 175:4-177:5 -/
@[rust_loop]
def backend.avx2.sample.secret_loop0
  (L : Std.Usize) (iter : core.ops.range.Range Std.Usize)
  (indices : Array (Array Std.U8 1#usize) 4#usize) :
  Result (Array (Array Std.U8 1#usize) 4#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok indices
  | some lane =>
    let i ← L - 1#usize
    let i1 ← core.cmp.min core.cmp.OrdUsize lane i
    let (a, index_mut_back) ← Array.index_mut_usize indices lane
    let i2 ← lift (UScalar.cast .U8 i1)
    let a1 ← Array.update a 0#usize i2
    let a2 := index_mut_back a1
    backend.avx2.sample.secret_loop0 L iter1 a2
partial_fixpoint

/-- [kopis::backend::avx2::sample::secret]: loop 1:
    Source: 'src/backend/avx2/sample.rs', lines 183:4-185:5 -/
@[rust_loop]
def backend.avx2.sample.secret_loop1
  {L : Std.Usize} (MU : Std.Usize) {N : Std.Usize}
  (iter : core.ops.range.Range Std.Usize)
  (bufs : Array (Array Std.U8 N) 4#usize)
  (secret : arithmetic.matrix_arith.Matrix L 1#usize) :
  Result (arithmetic.matrix_arith.Matrix L 1#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok secret
  | some i =>
    let a ← Array.index_usize bufs i
    let i1 ← consts.RING_DEG * MU
    let i2 ← i1 / 8#usize
    let s ←
      core.array.Array.index (core.ops.index.IndexSlice
        (core.slice.index.SliceIndexRangeToUsizeSlice Std.U8)) a
        { «end» := i2 }
    let re ← backend.avx2.sample.cbd MU s
    let (a1, index_mut_back) ← Array.index_mut_usize secret i
    let a2 ← Array.update a1 0#usize re
    let a3 := index_mut_back a2
    backend.avx2.sample.secret_loop1 MU iter1 bufs a3
partial_fixpoint

/-- [kopis::backend::avx2::sample::secret]:
    Source: 'src/backend/avx2/sample.rs', lines 166:0-187:1 -/
def backend.avx2.sample.secret
  (L : Std.Usize) (MU : Std.Usize) (N : Std.Usize)
  (seed : Array Std.U8 32#usize) :
  Result (arithmetic.matrix_arith.Matrix L 1#usize)
  := do
  massert (L <= 4#usize)
  let a := Array.repeat 1#usize 0#u8
  let indices := Array.repeat 4#usize a
  let indices1 ←
    backend.avx2.sample.secret_loop0 L { start := 0#usize, «end» := 4#usize }
      indices
  let a1 := Array.repeat N 0#u8
  let bufs := Array.repeat 4#usize a1
  let bufs1 ← backend.avx2.keccak.xof4 136#usize 3#u8 seed indices1 bufs
  let secret ←
    arithmetic.matrix_arith.Matrix.Insts.CoreDefaultDefault.default L 1#usize
  backend.avx2.sample.secret_loop1 MU { start := 0#usize, «end» := L } bufs1
    secret

/-- [kopis::backend::avx2::sample::gen_secret_from_seed]:
    Source: 'src/backend/avx2/sample.rs', lines 142:0-155:1 -/
def backend.avx2.sample.gen_secret_from_seed
  (L : Std.Usize) (MU : Std.Usize) (seed : Array Std.U8 32#usize) :
  Result (arithmetic.matrix_arith.Matrix L 1#usize)
  := do
  match MU.val with
  | 10 => backend.avx2.sample.secret L MU 320#usize seed
  | 8 => backend.avx2.sample.secret L MU 256#usize seed
  | 6 => backend.avx2.sample.secret L MU 192#usize seed
  | _ => backend.avx2.sample.secret L MU 320#usize seed

/-- [kopis::backend::avx2::ser::{impl core::clone::Clone for kopis::backend::avx2::ser::Plan}::clone]:
    Source: 'src/backend/avx2/ser.rs', lines 38:9-38:14
    Visibility: public -/
def backend.avx2.ser.Plan.Insts.CoreCloneClone.clone
  (self : backend.avx2.ser.Plan) : Result backend.avx2.ser.Plan := do
  ok self

/-- Trait implementation: [kopis::backend::avx2::ser::{impl core::clone::Clone for kopis::backend::avx2::ser::Plan}]
    Source: 'src/backend/avx2/ser.rs', lines 38:9-38:14 -/
@[reducible]
def backend.avx2.ser.Plan.Insts.CoreCloneClone : core.clone.Clone
  backend.avx2.ser.Plan := {
  clone := backend.avx2.ser.Plan.Insts.CoreCloneClone.clone
}

/-- Trait implementation: [kopis::backend::avx2::ser::{impl core::marker::Copy for kopis::backend::avx2::ser::Plan}]
    Source: 'src/backend/avx2/ser.rs', lines 38:16-38:20 -/
@[reducible]
def backend.avx2.ser.Plan.Insts.CoreMarkerCopy : core.marker.Copy
  backend.avx2.ser.Plan := {
  cloneInst := backend.avx2.ser.Plan.Insts.CoreCloneClone
}

/-- [kopis::consts::KOPIS512_L]
    Source: 'src/consts.rs', lines 16:0-16:39 -/
@[global_simps, irreducible] def consts.KOPIS512_L : Std.Usize := 2#usize

/-- [kopis::consts::KOPIS512_T]
    Source: 'src/consts.rs', lines 17:0-17:39 -/
@[global_simps, irreducible] def consts.KOPIS512_T : Std.Usize := 3#usize

/-- [kopis::consts::KOPIS512_MU]
    Source: 'src/consts.rs', lines 18:0-18:41 -/
@[global_simps, irreducible] def consts.KOPIS512_MU : Std.Usize := 10#usize

/-- [kopis::consts::KOPIS768_L]
    Source: 'src/consts.rs', lines 20:0-20:39 -/
@[global_simps, irreducible] def consts.KOPIS768_L : Std.Usize := 3#usize

/-- [kopis::consts::KOPIS768_T]
    Source: 'src/consts.rs', lines 21:0-21:39 -/
@[global_simps, irreducible] def consts.KOPIS768_T : Std.Usize := 4#usize

/-- [kopis::consts::KOPIS768_MU]
    Source: 'src/consts.rs', lines 22:0-22:40 -/
@[global_simps, irreducible] def consts.KOPIS768_MU : Std.Usize := 8#usize

/-- [kopis::consts::KOPIS1024_L]
    Source: 'src/consts.rs', lines 24:0-24:40 -/
@[global_simps, irreducible] def consts.KOPIS1024_L : Std.Usize := 4#usize

/-- [kopis::consts::KOPIS1024_T]
    Source: 'src/consts.rs', lines 25:0-25:40 -/
@[global_simps, irreducible] def consts.KOPIS1024_T : Std.Usize := 6#usize

/-- [kopis::consts::KOPIS1024_MU]
    Source: 'src/consts.rs', lines 26:0-26:41 -/
@[global_simps, irreducible] def consts.KOPIS1024_MU : Std.Usize := 6#usize

/-- [kopis::consts::MAX_MU]
    Source: 'src/consts.rs', lines 31:0-31:36 -/
@[global_simps, irreducible] def consts.MAX_MU : Std.Usize := 10#usize

/-- [kopis::consts::MAX_T]
    Source: 'src/consts.rs', lines 37:0-37:34 -/
@[global_simps, irreducible] def consts.MAX_T : Std.Usize := 6#usize

/-- [kopis::consts::DOMSEP_KGEXPAND]
    Source: 'src/consts.rs', lines 40:0-40:44 -/
@[global_simps, irreducible] def consts.DOMSEP_KGEXPAND : Std.U8 := 1#u8

/-- [kopis::consts::DOMSEP_GENMAT]
    Source: 'src/consts.rs', lines 41:0-41:42 -/
@[global_simps, irreducible] def consts.DOMSEP_GENMAT : Std.U8 := 2#u8

/-- [kopis::consts::DOMSEP_GENSEC]
    Source: 'src/consts.rs', lines 42:0-42:42 -/
@[global_simps, irreducible] def consts.DOMSEP_GENSEC : Std.U8 := 3#u8

/-- [kopis::consts::DOMSEP_PKHASH]
    Source: 'src/consts.rs', lines 43:0-43:42 -/
@[global_simps, irreducible] def consts.DOMSEP_PKHASH : Std.U8 := 4#u8

/-- [kopis::consts::DOMSEP_FO]
    Source: 'src/consts.rs', lines 44:0-44:38 -/
@[global_simps, irreducible] def consts.DOMSEP_FO : Std.U8 := 5#u8

/-- [kopis::consts::DOMSEP_NOREJECT]
    Source: 'src/consts.rs', lines 45:0-45:44 -/
@[global_simps, irreducible] def consts.DOMSEP_NOREJECT : Std.U8 := 6#u8

/-- [kopis::impls::SharedSecret]
    Source: 'src/impls.rs', lines 15:0-15:34
    Visibility: public -/
@[reducible]
def impls.SharedSecret := Array Std.U8 32#usize

/-- [kopis::impls::{impl zeroize::Zeroize for kopis::impls::SharedSecret}::zeroize]:
    Source: 'src/impls.rs', lines 14:9-14:16
    Visibility: public -/
def impls.SharedSecret.Insts.ZeroizeZeroize.zeroize
  (self : impls.SharedSecret) : Result impls.SharedSecret := do
  let __zeroize_field_0 ←
    Array.Insts.ZeroizeZeroize.zeroize (zeroize.Zeroize.Blanket
      U8.Insts.ZeroizeDefaultIsZeroes) self
  ok __zeroize_field_0

/-- Trait implementation: [kopis::impls::{impl zeroize::Zeroize for kopis::impls::SharedSecret}]
    Source: 'src/impls.rs', lines 14:9-14:16 -/
@[reducible]
def impls.SharedSecret.Insts.ZeroizeZeroize : zeroize.Zeroize
  impls.SharedSecret := {
  zeroize := impls.SharedSecret.Insts.ZeroizeZeroize.zeroize
}

/-- [kopis::impls::{impl core::ops::drop::Drop for kopis::impls::SharedSecret}::drop]:
    Source: 'src/impls.rs', lines 14:18-14:31
    Visibility: public -/
def impls.SharedSecret.Insts.CoreOpsDropDrop.drop
  (self : impls.SharedSecret) : Result impls.SharedSecret := do
  let __zeroize_field_0 ←
    zeroize.__internal.AssertZeroize.Blanket.zeroize_or_on_drop
      (Array.Insts.ZeroizeZeroize 32#usize (zeroize.Zeroize.Blanket
      U8.Insts.ZeroizeDefaultIsZeroes)) self
  ok __zeroize_field_0

/-- Trait implementation: [kopis::impls::{impl core::ops::drop::Drop for kopis::impls::SharedSecret}]
    Source: 'src/impls.rs', lines 14:18-14:31 -/
@[reducible]
def impls.SharedSecret.Insts.CoreOpsDropDrop : core.ops.drop.Drop
  impls.SharedSecret := {
  drop := impls.SharedSecret.Insts.CoreOpsDropDrop.drop
}

/-- Trait implementation: [kopis::impls::{impl zeroize::ZeroizeOnDrop for kopis::impls::SharedSecret}]
    Source: 'src/impls.rs', lines 14:18-14:31 -/
@[reducible]
def impls.SharedSecret.Insts.ZeroizeZeroizeOnDrop : zeroize.ZeroizeOnDrop
  impls.SharedSecret := {
}

/-- [kopis::impls::{kopis::impls::SharedSecret}::as_bytes]:
    Source: 'src/impls.rs', lines 20:4-22:5
    Visibility: public -/
def impls.SharedSecret.as_bytes
  (self : impls.SharedSecret) : Result (Array Std.U8 32#usize) := do
  ok self

/-- [kopis::pke::PkePublicKey]
    Source: 'src/pke.rs', lines 37:0-52:1
    Visibility: public -/
structure pke.PkePublicKey (L : Std.Usize) where
  matrix_seed : Array Std.U8 32#usize
  mat_a_ntt : arithmetic.ntt.NttMatrix L L
  vec_bytes : Array (Array Std.U8 320#usize) L
  vec_ntt : arithmetic.ntt.NttMatrix L 1#usize

/-- [kopis::pke::PkeSecretKey]
    Source: 'src/pke.rs', lines 28:0-28:64 -/
@[reducible]
def pke.PkeSecretKey (L : Std.Usize) := arithmetic.ntt.NttMatrix L 1#usize

/-- [kopis::kem::KemSecretKey]
    Source: 'src/kem.rs', lines 55:0-68:1
    Visibility: public -/
structure kem.KemSecretKey (L : Std.Usize) where
  seed : Array Std.U8 32#usize
  z : Array Std.U8 32#usize
  pke_sk : pke.PkeSecretKey L
  pke_pk : pke.PkePublicKey L
  hash_pke_pk : Array Std.U8 32#usize

/-- [kopis::impls::kopis512::Kopis512SecretKey]
    Source: 'src/impls.rs', lines 44:12-44:65
    Visibility: public -/
@[reducible]
def impls.kopis512.Kopis512SecretKey := kem.KemSecretKey 2#usize

/-- Trait implementation: [kopis::kem::{impl zeroize::ZeroizeOnDrop for kopis::kem::KemSecretKey<L>}]
    Source: 'src/kem.rs', lines 54:9-54:22 -/
@[reducible]
def kem.KemSecretKey.Insts.ZeroizeZeroizeOnDrop (L : Std.Usize) :
  zeroize.ZeroizeOnDrop (kem.KemSecretKey L) := {
}

/-- [kopis::impls::kopis512::{impl core::ops::drop::Drop for kopis::impls::kopis512::Kopis512SecretKey}::drop]:
    Source: 'src/impls.rs', lines 43:21-43:34
    Visibility: public -/
def impls.kopis512.Kopis512SecretKey.Insts.CoreOpsDropDrop.drop
  (self : impls.kopis512.Kopis512SecretKey) :
  Result impls.kopis512.Kopis512SecretKey
  := do
  Shared0Mut1T.Insts.Zeroize__internalAssertZeroizeOnDrop.zeroize_or_on_drop
    (kem.KemSecretKey.Insts.ZeroizeZeroizeOnDrop 2#usize) self
  ok self

/-- Trait implementation: [kopis::impls::kopis512::{impl core::ops::drop::Drop for kopis::impls::kopis512::Kopis512SecretKey}]
    Source: 'src/impls.rs', lines 43:21-43:34 -/
@[reducible]
def impls.kopis512.Kopis512SecretKey.Insts.CoreOpsDropDrop : core.ops.drop.Drop
  impls.kopis512.Kopis512SecretKey := {
  drop := impls.kopis512.Kopis512SecretKey.Insts.CoreOpsDropDrop.drop
}

/-- Trait implementation: [kopis::impls::kopis512::{impl zeroize::ZeroizeOnDrop for kopis::impls::kopis512::Kopis512SecretKey}]
    Source: 'src/impls.rs', lines 43:21-43:34 -/
@[reducible]
def impls.kopis512.Kopis512SecretKey.Insts.ZeroizeZeroizeOnDrop :
  zeroize.ZeroizeOnDrop impls.kopis512.Kopis512SecretKey := {
}

/-- [kopis::impls::kopis768::Kopis768SecretKey]
    Source: 'src/impls.rs', lines 44:12-44:65
    Visibility: public -/
@[reducible]
def impls.kopis768.Kopis768SecretKey := kem.KemSecretKey 3#usize

/-- [kopis::impls::kopis768::{impl core::ops::drop::Drop for kopis::impls::kopis768::Kopis768SecretKey}::drop]:
    Source: 'src/impls.rs', lines 43:21-43:34
    Visibility: public -/
def impls.kopis768.Kopis768SecretKey.Insts.CoreOpsDropDrop.drop
  (self : impls.kopis768.Kopis768SecretKey) :
  Result impls.kopis768.Kopis768SecretKey
  := do
  Shared0Mut1T.Insts.Zeroize__internalAssertZeroizeOnDrop.zeroize_or_on_drop
    (kem.KemSecretKey.Insts.ZeroizeZeroizeOnDrop 3#usize) self
  ok self

/-- Trait implementation: [kopis::impls::kopis768::{impl core::ops::drop::Drop for kopis::impls::kopis768::Kopis768SecretKey}]
    Source: 'src/impls.rs', lines 43:21-43:34 -/
@[reducible]
def impls.kopis768.Kopis768SecretKey.Insts.CoreOpsDropDrop : core.ops.drop.Drop
  impls.kopis768.Kopis768SecretKey := {
  drop := impls.kopis768.Kopis768SecretKey.Insts.CoreOpsDropDrop.drop
}

/-- Trait implementation: [kopis::impls::kopis768::{impl zeroize::ZeroizeOnDrop for kopis::impls::kopis768::Kopis768SecretKey}]
    Source: 'src/impls.rs', lines 43:21-43:34 -/
@[reducible]
def impls.kopis768.Kopis768SecretKey.Insts.ZeroizeZeroizeOnDrop :
  zeroize.ZeroizeOnDrop impls.kopis768.Kopis768SecretKey := {
}

/-- [kopis::impls::kopis1024::Kopis1024SecretKey]
    Source: 'src/impls.rs', lines 44:12-44:65
    Visibility: public -/
@[reducible]
def impls.kopis1024.Kopis1024SecretKey := kem.KemSecretKey 4#usize

/-- [kopis::impls::kopis1024::{impl core::ops::drop::Drop for kopis::impls::kopis1024::Kopis1024SecretKey}::drop]:
    Source: 'src/impls.rs', lines 43:21-43:34
    Visibility: public -/
def impls.kopis1024.Kopis1024SecretKey.Insts.CoreOpsDropDrop.drop
  (self : impls.kopis1024.Kopis1024SecretKey) :
  Result impls.kopis1024.Kopis1024SecretKey
  := do
  Shared0Mut1T.Insts.Zeroize__internalAssertZeroizeOnDrop.zeroize_or_on_drop
    (kem.KemSecretKey.Insts.ZeroizeZeroizeOnDrop 4#usize) self
  ok self

/-- Trait implementation: [kopis::impls::kopis1024::{impl core::ops::drop::Drop for kopis::impls::kopis1024::Kopis1024SecretKey}]
    Source: 'src/impls.rs', lines 43:21-43:34 -/
@[reducible]
def impls.kopis1024.Kopis1024SecretKey.Insts.CoreOpsDropDrop :
  core.ops.drop.Drop impls.kopis1024.Kopis1024SecretKey := {
  drop := impls.kopis1024.Kopis1024SecretKey.Insts.CoreOpsDropDrop.drop
}

/-- Trait implementation: [kopis::impls::kopis1024::{impl zeroize::ZeroizeOnDrop for kopis::impls::kopis1024::Kopis1024SecretKey}]
    Source: 'src/impls.rs', lines 43:21-43:34 -/
@[reducible]
def impls.kopis1024.Kopis1024SecretKey.Insts.ZeroizeZeroizeOnDrop :
  zeroize.ZeroizeOnDrop impls.kopis1024.Kopis1024SecretKey := {
}

/-- [kopis::kem::KemPublicKey]
    Source: 'src/kem.rs', lines 16:0-21:1
    Visibility: public -/
structure kem.KemPublicKey (L : Std.Usize) where
  pke_pk : pke.PkePublicKey L
  hash_pke_pk : Array Std.U8 32#usize

/-- [kopis::impls::kopis512::Kopis512PublicKey]
    Source: 'src/impls.rs', lines 47:12-47:64
    Visibility: public -/
@[reducible]
def impls.kopis512.Kopis512PublicKey := kem.KemPublicKey 2#usize

/-- [kopis::impls::kopis768::Kopis768PublicKey]
    Source: 'src/impls.rs', lines 47:12-47:64
    Visibility: public -/
@[reducible]
def impls.kopis768.Kopis768PublicKey := kem.KemPublicKey 3#usize

/-- [kopis::impls::kopis1024::Kopis1024PublicKey]
    Source: 'src/impls.rs', lines 47:12-47:64
    Visibility: public -/
@[reducible]
def impls.kopis1024.Kopis1024PublicKey := kem.KemPublicKey 4#usize

/-- [kopis::pke::ciphertext_len]:
    Source: 'src/pke.rs', lines 133:0-136:1
    Visibility: public -/
def pke.ciphertext_len (L : Std.Usize) (T : Std.Usize) : Result Std.Usize := do
  let i ← L * consts.MODULUS_P_BITS
  let i1 ← i * consts.RING_DEG
  let i2 ← i1 / 8#usize
  let i3 ← T * consts.RING_DEG
  let i4 ← i3 / 8#usize
  i2 + i4

/-- [kopis::impls::kopis512::KOPIS512_CIPHERTEXT_LEN]
    Source: 'src/impls.rs', lines 50:12-51:69
    Visibility: public -/
@[global_simps, irreducible]
def impls.kopis512.KOPIS512_CIPHERTEXT_LEN : Result Std.Usize :=
  pke.ciphertext_len 2#usize 3#usize

/-- [kopis::impls::kopis768::KOPIS768_CIPHERTEXT_LEN]
    Source: 'src/impls.rs', lines 50:12-51:69
    Visibility: public -/
@[global_simps, irreducible]
def impls.kopis768.KOPIS768_CIPHERTEXT_LEN : Result Std.Usize :=
  pke.ciphertext_len 3#usize 4#usize

/-- [kopis::impls::kopis1024::KOPIS1024_CIPHERTEXT_LEN]
    Source: 'src/impls.rs', lines 50:12-51:69
    Visibility: public -/
@[global_simps, irreducible]
def impls.kopis1024.KOPIS1024_CIPHERTEXT_LEN : Result Std.Usize :=
  pke.ciphertext_len 4#usize 6#usize

/-- [kopis::sample::gen_matrix_from_seed]: loop 1:
    Source: 'src/sample.rs', lines 167:8-175:9 -/
@[rust_loop]
def sample.gen_matrix_from_seed_loop0_loop0
  {L : Std.Usize} (iter : core.ops.range.Range Std.Usize)
  (seed : Array Std.U8 32#usize) (mat : arithmetic.matrix_arith.Matrix L L)
  (buf : Array Std.U8 416#usize) (i : Std.Usize) :
  Result ((arithmetic.matrix_arith.Matrix L L) × (Array Std.U8 416#usize))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (mat, buf)
  | some j =>
    let hasher ←
      turboshake.TurboShake.Insts.CoreDefaultDefault.default 168#usize 2#u8
    let s ← lift (Array.to_slice seed)
    let hasher1 ← turboshake.TurboShake.Insts.DigestUpdate.update hasher s
    let i1 ← lift (UScalar.cast .U8 i)
    let s1 ← lift (Array.to_slice (Array.make 1#usize [ i1 ]))
    let hasher2 ← turboshake.TurboShake.Insts.DigestUpdate.update hasher1 s1
    let i2 ← lift (UScalar.cast .U8 j)
    let s2 ← lift (Array.to_slice (Array.make 1#usize [ i2 ]))
    let hasher3 ← turboshake.TurboShake.Insts.DigestUpdate.update hasher2 s2
    let reader ←
      turboshake.TurboShake.Insts.DigestExtendableOutputTurboShakeReader.finalize_xof
        hasher3
    let (s3, to_slice_mut_back) ← lift (Array.to_slice_mut buf)
    let (_, s4) ←
      turboshake.TurboShakeReader.Insts.DigestXofReader.read reader s3
    let buf1 := to_slice_mut_back s4
    let s5 ← lift (Array.to_slice buf1)
    let re ←
      arithmetic.ring_arith.RingElem.deserialize s5 consts.MODULUS_Q_BITS
    let (a, index_mut_back) ← Array.index_mut_usize mat i
    let a1 ← Array.update a j re
    let a2 := index_mut_back a1
    sample.gen_matrix_from_seed_loop0_loop0 iter1 seed a2 buf1 i
partial_fixpoint

/-- [kopis::sample::gen_matrix_from_seed]: loop 0:
    Source: 'src/sample.rs', lines 166:4-176:5 -/
@[rust_loop]
def sample.gen_matrix_from_seed_loop0
  {L : Std.Usize} (iter : core.ops.range.Range Std.Usize)
  (seed : Array Std.U8 32#usize) (mat : arithmetic.matrix_arith.Matrix L L)
  (buf : Array Std.U8 416#usize) :
  Result (arithmetic.matrix_arith.Matrix L L)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok mat
  | some i =>
    let (mat1, buf1) ←
      sample.gen_matrix_from_seed_loop0_loop0
        { start := 0#usize, «end» := L } seed mat buf i
    sample.gen_matrix_from_seed_loop0 iter1 seed mat1 buf1
partial_fixpoint

/-- [kopis::sample::gen_matrix_from_seed]:
    Source: 'src/sample.rs', lines 151:0-179:1 -/
def sample.gen_matrix_from_seed
  (L : Std.Usize) (seed : Array Std.U8 32#usize) :
  Result (arithmetic.matrix_arith.Matrix L L)
  := do
  let b ← backend.avx2.cpu.available
  if b
  then backend.avx2.sample.gen_matrix_from_seed L seed
  else
    let mat ←
      arithmetic.matrix_arith.Matrix.Insts.CoreDefaultDefault.default L L
    let buf := Array.repeat 416#usize 0#u8
    sample.gen_matrix_from_seed_loop0 { start := 0#usize, «end» := L } seed
      mat buf

/-- [kopis::sample::cbd_diff]:
    Source: 'src/sample.rs', lines 18:0-22:1 -/
def sample.cbd_diff
  (raw1 : Std.U32) (half : Std.U32) (mask : Std.U32) : Result Std.U16 := do
  let i ← lift (raw1 &&& mask)
  let i1 ← core.num.U32.count_ones i
  let a ← lift (UScalar.cast .U16 i1)
  let i2 ← raw1 >>> half
  let i3 ← lift (i2 &&& mask)
  let i4 ← core.num.U32.count_ones i3
  let b ← lift (UScalar.cast .U16 i4)
  ok (core.num.U16.wrapping_sub a b)

/-- [kopis::sample::cbd]: loop 0:
    Source: 'src/sample.rs', lines 34:8-38:9 -/
@[rust_loop]
def sample.cbd_loop0
  (iter : core.iter.adapters.enumerate.Enumerate (core.slice.iter.Iter Std.U8))
  (out : arithmetic.ring_arith.RingElem) :
  Result arithmetic.ring_arith.RingElem
  := do
  let (o, iter1) ←
    core.iter.adapters.enumerate.IteratorEnumerate.next
      (core.iter.traits.iterator.IteratorSliceIter Std.U8) iter
  match o with
  | none => ok out
  | some p =>
    let (i, byte) := p
    let i1 ← lift (byte &&& 15#u8)
    let i2 ← core.num.U8.count_ones i1
    let a ← lift (UScalar.cast .U16 i2)
    let i3 ← byte >>> 4#i32
    let i4 ← core.num.U8.count_ones i3
    let b ← lift (UScalar.cast .U16 i4)
    let i5 ← lift (core.num.U16.wrapping_sub a b)
    let a1 ← Array.update out i i5
    sample.cbd_loop0 iter1 a1
partial_fixpoint

/-- [kopis::sample::cbd]: loop 1:
    Source: 'src/sample.rs', lines 43:8-55:9 -/
@[rust_loop]
def sample.cbd_loop1
  (iter : core.ops.range.Range Std.Usize) (buf : Slice Std.U8)
  (out : arithmetic.ring_arith.RingElem) :
  Result arithmetic.ring_arith.RingElem
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok out
  | some g =>
    let i ← 5#usize * g
    let i1 ← Slice.index_usize buf i
    let b0 ← lift (UScalar.cast .U32 i1)
    let i2 ← i + 1#usize
    let i3 ← Slice.index_usize buf i2
    let b1 ← lift (UScalar.cast .U32 i3)
    let i4 ← i + 2#usize
    let i5 ← Slice.index_usize buf i4
    let b2 ← lift (UScalar.cast .U32 i5)
    let i6 ← i + 3#usize
    let i7 ← Slice.index_usize buf i6
    let b3 ← lift (UScalar.cast .U32 i7)
    let i8 ← i + 4#usize
    let i9 ← Slice.index_usize buf i8
    let b4 ← lift (UScalar.cast .U32 i9)
    let o1 ← 4#usize * g
    let i10 ← b1 <<< 8#i32
    let i11 ← lift (b0 ||| i10)
    let i12 ← sample.cbd_diff i11 5#u32 31#u32
    let a ← Array.update out o1 i12
    let i13 ← b2 <<< 8#i32
    let i14 ← lift (b1 ||| i13)
    let i15 ← i14 >>> 2#i32
    let i16 ← sample.cbd_diff i15 5#u32 31#u32
    let i17 ← o1 + 1#usize
    let a1 ← Array.update a i17 i16
    let i18 ← b3 <<< 8#i32
    let i19 ← lift (b2 ||| i18)
    let i20 ← i19 >>> 4#i32
    let i21 ← sample.cbd_diff i20 5#u32 31#u32
    let i22 ← o1 + 2#usize
    let a2 ← Array.update a1 i22 i21
    let i23 ← b4 <<< 8#i32
    let i24 ← lift (b3 ||| i23)
    let i25 ← i24 >>> 6#i32
    let i26 ← sample.cbd_diff i25 5#u32 31#u32
    let i27 ← o1 + 3#usize
    let a3 ← Array.update a2 i27 i26
    sample.cbd_loop1 iter1 buf a3
partial_fixpoint

/-- [kopis::sample::cbd]: loop 2:
    Source: 'src/sample.rs', lines 59:8-69:9 -/
@[rust_loop]
def sample.cbd_loop2
  (iter : core.ops.range.Range Std.Usize) (buf : Slice Std.U8)
  (out : arithmetic.ring_arith.RingElem) :
  Result arithmetic.ring_arith.RingElem
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok out
  | some g =>
    let i ← 3#usize * g
    let i1 ← Slice.index_usize buf i
    let b0 ← lift (UScalar.cast .U32 i1)
    let i2 ← i + 1#usize
    let i3 ← Slice.index_usize buf i2
    let b1 ← lift (UScalar.cast .U32 i3)
    let i4 ← i + 2#usize
    let i5 ← Slice.index_usize buf i4
    let b2 ← lift (UScalar.cast .U32 i5)
    let o1 ← 4#usize * g
    let i6 ← sample.cbd_diff b0 3#u32 7#u32
    let a ← Array.update out o1 i6
    let i7 ← b1 <<< 8#i32
    let i8 ← lift (b0 ||| i7)
    let i9 ← i8 >>> 6#i32
    let i10 ← sample.cbd_diff i9 3#u32 7#u32
    let i11 ← o1 + 1#usize
    let a1 ← Array.update a i11 i10
    let i12 ← b2 <<< 8#i32
    let i13 ← lift (b1 ||| i12)
    let i14 ← i13 >>> 4#i32
    let i15 ← sample.cbd_diff i14 3#u32 7#u32
    let i16 ← o1 + 2#usize
    let a2 ← Array.update a1 i16 i15
    let i17 ← b2 >>> 2#i32
    let i18 ← sample.cbd_diff i17 3#u32 7#u32
    let i19 ← o1 + 3#usize
    let a3 ← Array.update a2 i19 i18
    sample.cbd_loop2 iter1 buf a3
partial_fixpoint

/-- [kopis::sample::cbd]: loop 3:
    Source: 'src/sample.rs', lines 77:8-95:9 -/
@[rust_loop]
def sample.cbd_loop3
  (MU : Std.Usize) (iter : core.slice.iter.IterMut Std.U16)
  (back : core.slice.iter.IterMut Std.U16 → core.slice.iter.IterMut Std.U16)
  (buf : Slice Std.U8) (half : Std.Usize) (mask : Std.U32)
  (bit_pos : Std.Usize) :
  Result (core.slice.iter.IterMut Std.U16)
  := do
  let (o, iter1, next_back) ← core.slice.iter.IteratorIterMut.next iter
  match o with
  | none => ok (let im := next_back iter1 none
                back im)
  | some _ =>
    let byte_idx ← bit_pos / 8#usize
    let bit_in_byte ← bit_pos % 8#usize
    let i ← Slice.index_usize buf byte_idx
    let raw1 ← lift (UScalar.cast .U32 i)
    let i1 ← byte_idx + 1#usize
    let i2 := Slice.len buf
    let (raw2, back1) ←
      if i1 < i2
      then
        do
        let i3 ← Slice.index_usize buf i1
        let i4 ← lift (UScalar.cast .U32 i3)
        let i5 ← i4 <<< 8#i32
        let raw3 ← lift (raw1 ||| i5)
        ok (raw3, fun i6 im => next_back im (some i6))
      else ok (raw1, fun i3 im => next_back im (some i3))
    let i3 ← byte_idx + 2#usize
    let i4 := Slice.len buf
    let raw3 ←
      if i3 < i4
      then
        do
        let i5 ← Slice.index_usize buf i3
        let i6 ← lift (UScalar.cast .U32 i5)
        let i7 ← i6 <<< 16#i32
        ok (raw2 ||| i7)
      else ok raw2
    let raw4 ← raw3 >>> bit_in_byte
    let i5 ← lift (raw4 &&& mask)
    let i6 ← core.num.U32.count_ones i5
    let a ← lift (UScalar.cast .U16 i6)
    let i7 ← raw4 >>> half
    let i8 ← lift (i7 &&& mask)
    let i9 ← core.num.U32.count_ones i8
    let b ← lift (UScalar.cast .U16 i9)
    let coeff ← lift (core.num.U16.wrapping_sub a b)
    let bit_pos1 ← bit_pos + MU
    sample.cbd_loop3 MU iter1 (fun im => let im1 := back1 coeff im
                                         back im1) buf half mask bit_pos1
partial_fixpoint

/-- [kopis::sample::cbd]:
    Source: 'src/sample.rs', lines 25:0-97:1 -/
def sample.cbd
  (MU : Std.Usize) (buf : Slice Std.U8) (out : arithmetic.ring_arith.RingElem)
  :
  Result arithmetic.ring_arith.RingElem
  := do
  let left_val := Slice.len buf
  let _ ← consts.RING_DEG * MU
  let i ← lift (Std.Usize.wrapping_mul consts.RING_DEG MU)
  let right_val ← i / 8#usize
  massert (left_val = right_val)
  if MU = 8#usize
  then
    let i1 ← core.slice.Slice.iter buf
    let iter ←
      core.iter.traits.iterator.Iterator.enumerate.trait_default
        (core.iter.traits.iterator.IteratorSliceIter Std.U8) i1
    sample.cbd_loop0 iter out
  else
    if MU = 10#usize
    then
      let i1 ← consts.RING_DEG / 4#usize
      sample.cbd_loop1 { start := 0#usize, «end» := i1 } buf out
    else
      if MU = 6#usize
      then
        let i1 ← consts.RING_DEG / 4#usize
        sample.cbd_loop2 { start := 0#usize, «end» := i1 } buf out
      else
        let half ← MU / 2#usize
        let i1 ← 1#u32 <<< half
        let mask ← i1 - 1#u32
        let (s, to_slice_mut_back) ← lift (Array.to_slice_mut out)
        let (iter, iter_mut_back) ← core.slice.Slice.iter_mut s
        let back ←
          sample.cbd_loop3 MU iter (fun im => im) buf half mask 0#usize
        let s1 := iter_mut_back back
        let a := to_slice_mut_back s1
        ok a

/-- [kopis::sample::gen_secret_from_seed]: loop 0:
    Source: 'src/sample.rs', lines 119:4-143:5 -/
@[rust_loop]
def sample.gen_secret_from_seed_loop
  {L : Std.Usize} (MU : Std.Usize) (iter : core.ops.range.Range Std.Usize)
  (seed : Array Std.U8 32#usize)
  (secret : arithmetic.matrix_arith.Matrix L 1#usize) (buf : Slice Std.U8) :
  Result (arithmetic.matrix_arith.Matrix L 1#usize)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok secret
  | some i =>
    let hasher ←
      turboshake.TurboShake.Insts.CoreDefaultDefault.default 136#usize 3#u8
    let s ← lift (Array.to_slice seed)
    let hasher1 ← turboshake.TurboShake.Insts.DigestUpdate.update hasher s
    let i1 ← lift (UScalar.cast .U8 i)
    let s1 ← lift (Array.to_slice (Array.make 1#usize [ i1 ]))
    let hasher2 ← turboshake.TurboShake.Insts.DigestUpdate.update hasher1 s1
    let reader ←
      turboshake.TurboShake.Insts.DigestExtendableOutputTurboShakeReader.finalize_xof
        hasher2
    let (_, buf1) ←
      turboshake.TurboShakeReader.Insts.DigestXofReader.read reader buf
    let (a, index_mut_back) ← Array.index_mut_usize secret i
    let (re, index_mut_back1) ← Array.index_mut_usize a 0#usize
    let re1 ← sample.cbd MU buf1 re
    let a1 := index_mut_back1 re1
    let a2 := index_mut_back a1
    sample.gen_secret_from_seed_loop MU iter1 seed a2 buf1
partial_fixpoint

/-- [kopis::sample::gen_secret_from_seed]:
    Source: 'src/sample.rs', lines 101:0-146:1 -/
def sample.gen_secret_from_seed
  (L : Std.Usize) (MU : Std.Usize) (seed : Array Std.U8 32#usize) :
  Result (arithmetic.matrix_arith.Matrix L 1#usize)
  := do
  let b ← backend.avx2.cpu.available
  if b
  then backend.avx2.sample.gen_secret_from_seed L MU seed
  else
    let secret ←
      arithmetic.matrix_arith.Matrix.Insts.CoreDefaultDefault.default L 1#usize
    let backing_buf := Array.repeat 320#usize 0#u8
    let i ← consts.RING_DEG * MU
    let i1 ← i / 8#usize
    let (buf, _) ←
      core.array.Array.index_mut (core.ops.index.IndexMutSlice
        (core.slice.index.SliceIndexRangeToUsizeSlice Std.U8)) backing_buf
        { «end» := i1 }
    sample.gen_secret_from_seed_loop MU { start := 0#usize, «end» := L } seed
      secret buf

/-- [kopis::pke::{kopis::pke::PkePublicKey<L>}::hash]: loop 0:
    Source: 'src/pke.rs', lines 112:8-114:9 -/
@[rust_loop]
def pke.PkePublicKey.hash_loop
  {L : Std.Usize} (iter : core.ops.range.Range Std.Usize)
  (self : pke.PkePublicKey L) (hasher : turboshake.TurboShake 136#usize 4#u8) :
  Result ((pke.PkePublicKey L) × (turboshake.TurboShake 136#usize 4#u8))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (self, hasher)
  | some i =>
    let a ← Array.index_usize self.vec_bytes i
    let s ← lift (Array.to_slice a)
    let hasher1 ← turboshake.TurboShake.Insts.DigestUpdate.update hasher s
    pke.PkePublicKey.hash_loop iter1 self hasher1
partial_fixpoint

/-- [kopis::pke::{kopis::pke::PkePublicKey<L>}::hash]:
    Source: 'src/pke.rs', lines 109:4-121:5 -/
def pke.PkePublicKey.hash
  {L : Std.Usize} (self : pke.PkePublicKey L) :
  Result (Array Std.U8 32#usize)
  := do
  let hasher ←
    turboshake.TurboShake.Insts.CoreDefaultDefault.default 136#usize 4#u8
  let (self1, hasher1) ←
    pke.PkePublicKey.hash_loop { start := 0#usize, «end» := L } self hasher
  let s ← lift (Array.to_slice self1.matrix_seed)
  let hasher2 ← turboshake.TurboShake.Insts.DigestUpdate.update hasher1 s
  let out := Array.repeat 32#usize 0#u8
  let reader ←
    turboshake.TurboShake.Insts.DigestExtendableOutputTurboShakeReader.finalize_xof
      hasher2
  let (s1, to_slice_mut_back) ← lift (Array.to_slice_mut out)
  let (_, s2) ←
    turboshake.TurboShakeReader.Insts.DigestXofReader.read reader s1
  ok (to_slice_mut_back s2)

/-- [kopis::pke::{impl core::ops::drop::Drop for kopis::pke::PkeSecretKey<L>}::drop]:
    Source: 'src/pke.rs', lines 27:18-27:31
    Visibility: public -/
def pke.PkeSecretKey.Insts.CoreOpsDropDrop.drop
  {L : Std.Usize} (self : pke.PkeSecretKey L) :
  Result (pke.PkeSecretKey L)
  := do
  let __zeroize_field_0 ←
    zeroize.__internal.AssertZeroize.Blanket.zeroize_or_on_drop
      (arithmetic.ntt.NttMatrix.Insts.ZeroizeZeroize L 1#usize) self
  ok __zeroize_field_0

/-- [kopis::pke::H1_VAL]
    Source: 'src/pke.rs', lines 16:0-16:63 -/
@[global_simps, irreducible]
def pke.H1_VAL : Result Std.U16 := do
  let i ← consts.MODULUS_Q_BITS - consts.MODULUS_P_BITS
  let i1 ← i - 1#usize
  1#u16 <<< i1

/-- [kopis::pke::expand_decap_key]: loop 0:
    Source: 'src/pke.rs', lines 190:4-192:5 -/
@[rust_loop]
def pke.expand_decap_key_loop
  {L : Std.Usize} (iter : core.ops.range.Range Std.Usize)
  (b : arithmetic.matrix_arith.Matrix L 1#usize)
  (vec_bytes : Array (Array Std.U8 320#usize) L) :
  Result (Array (Array Std.U8 320#usize) L)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok vec_bytes
  | some i =>
    let a ← Array.index_usize b i
    let re ← Array.index_usize a 0#usize
    let (a1, index_mut_back) ← Array.index_mut_usize vec_bytes i
    let (s, to_slice_mut_back) ← lift (Array.to_slice_mut a1)
    let s1 ←
      arithmetic.ring_arith.RingElem.serialize re s consts.MODULUS_P_BITS
    let a2 := to_slice_mut_back s1
    let a3 := index_mut_back a2
    pke.expand_decap_key_loop iter1 b a3
partial_fixpoint

/-- [kopis::pke::expand_decap_key]:
    Source: 'src/pke.rs', lines 147:0-203:1 -/
def pke.expand_decap_key
  (L : Std.Usize) (MU : Std.Usize) (sk : Array Std.U8 32#usize) :
  Result ((pke.PkeSecretKey L) × (Array Std.U8 32#usize) × (pke.PkePublicKey
    L) × (Array Std.U8 32#usize))
  := do
  let mat_seed := Array.repeat 32#usize 0#u8
  let secret_seed := Array.repeat 32#usize 0#u8
  let z := Array.repeat 32#usize 0#u8
  let hasher ←
    turboshake.TurboShake.Insts.CoreDefaultDefault.default 136#usize 1#u8
  let s ← lift (Array.to_slice sk)
  let hasher1 ← turboshake.TurboShake.Insts.DigestUpdate.update hasher s
  let i ← lift (UScalar.cast .U8 L)
  let s1 ← lift (Array.to_slice (Array.make 1#usize [ i ]))
  let hasher2 ← turboshake.TurboShake.Insts.DigestUpdate.update hasher1 s1
  let xof ←
    turboshake.TurboShake.Insts.DigestExtendableOutputTurboShakeReader.finalize_xof
      hasher2
  let (s2, to_slice_mut_back) ← lift (Array.to_slice_mut mat_seed)
  let (xof1, s3) ←
    turboshake.TurboShakeReader.Insts.DigestXofReader.read xof s2
  let (s4, to_slice_mut_back1) ← lift (Array.to_slice_mut secret_seed)
  let (xof2, s5) ←
    turboshake.TurboShakeReader.Insts.DigestXofReader.read xof1 s4
  let (s6, to_slice_mut_back2) ← lift (Array.to_slice_mut z)
  let (_, s7) ←
    turboshake.TurboShakeReader.Insts.DigestXofReader.read xof2 s6
  let mat_seed1 := to_slice_mut_back s3
  let mat_a ← sample.gen_matrix_from_seed L mat_seed1
  let secret_seed1 := to_slice_mut_back1 s5
  let vec_s ← sample.gen_secret_from_seed L MU secret_seed1
  let mat_a_ntt ← arithmetic.ntt.NttMatrix.from_uniform_matrix mat_a
  let vec_s_ntt ← arithmetic.ntt.NttMatrix.from_secret_matrix vec_s
  let prod ← arithmetic.ntt.NttMatrix.mul_transpose mat_a_ntt vec_s_ntt
  let i1 ← pke.H1_VAL
  let prod1 ← arithmetic.matrix_arith.Matrix.wrapping_add_to_all prod i1
  let i2 ← consts.MODULUS_Q_BITS - consts.MODULUS_P_BITS
  let prod2 ← arithmetic.matrix_arith.Matrix.shift_right prod1 i2
  let vec_ntt ← arithmetic.ntt.NttMatrix.from_uniform_matrix prod2
  let a := Array.repeat 320#usize 0#u8
  let vec_bytes := Array.repeat L a
  let z1 := to_slice_mut_back2 s7
  let vec_bytes1 ←
    pke.expand_decap_key_loop { start := 0#usize, «end» := L } prod2
      vec_bytes
  let pkh ←
    pke.PkePublicKey.hash
      { matrix_seed := mat_seed1, mat_a_ntt, vec_bytes := vec_bytes1, vec_ntt }
  ok (vec_s_ntt, z1,
    { matrix_seed := mat_seed1, mat_a_ntt, vec_bytes := vec_bytes1, vec_ntt },
    pkh)

/-- [kopis::kem::{kopis::kem::KemSecretKey<L>}::expand_from_seed]:
    Source: 'src/kem.rs', lines 72:4-82:5
    Visibility: public -/
def kem.KemSecretKey.expand_from_seed
  (L : Std.Usize) (MU : Std.Usize) (seed : Array Std.U8 32#usize) :
  Result (kem.KemSecretKey L)
  := do
  let (pke_sk, z, pke_pk, hash_pke_pk) ← pke.expand_decap_key L MU seed
  ok { seed, z, pke_sk, pke_pk, hash_pke_pk }

/-- [kopis::pke::{impl zeroize::Zeroize for kopis::pke::PkeSecretKey<L>}::zeroize]:
    Source: 'src/pke.rs', lines 27:9-27:16
    Visibility: public -/
def pke.PkeSecretKey.Insts.ZeroizeZeroize.zeroize
  {L : Std.Usize} (self : pke.PkeSecretKey L) :
  Result (pke.PkeSecretKey L)
  := do
  let __zeroize_field_0 ←
    arithmetic.ntt.NttMatrix.Insts.ZeroizeZeroize.zeroize self
  ok __zeroize_field_0

/-- Trait implementation: [kopis::pke::{impl zeroize::Zeroize for kopis::pke::PkeSecretKey<L>}]
    Source: 'src/pke.rs', lines 27:9-27:16 -/
@[reducible]
def pke.PkeSecretKey.Insts.ZeroizeZeroize (L : Std.Usize) : zeroize.Zeroize
  (pke.PkeSecretKey L) := {
  zeroize := pke.PkeSecretKey.Insts.ZeroizeZeroize.zeroize
}

/-- [kopis::kem::{impl core::ops::drop::Drop for kopis::kem::KemSecretKey<L>}::drop]:
    Source: 'src/kem.rs', lines 54:9-54:22
    Visibility: public -/
def kem.KemSecretKey.Insts.CoreOpsDropDrop.drop
  {L : Std.Usize} (self : kem.KemSecretKey L) :
  Result (kem.KemSecretKey L)
  := do
  let seed ←
    zeroize.__internal.AssertZeroize.Blanket.zeroize_or_on_drop
      (Array.Insts.ZeroizeZeroize 32#usize (zeroize.Zeroize.Blanket
      U8.Insts.ZeroizeDefaultIsZeroes)) self.seed
  let z ←
    zeroize.__internal.AssertZeroize.Blanket.zeroize_or_on_drop
      (Array.Insts.ZeroizeZeroize 32#usize (zeroize.Zeroize.Blanket
      U8.Insts.ZeroizeDefaultIsZeroes)) self.z
  let pke_sk ←
    zeroize.__internal.AssertZeroize.Blanket.zeroize_or_on_drop
      (pke.PkeSecretKey.Insts.ZeroizeZeroize L) self.pke_sk
  ok { self with seed, z, pke_sk }

/-- [kopis::kem::{kopis::kem::KemSecretKey<L>}::generate]:
    Source: 'src/kem.rs', lines 85:4-92:5
    Visibility: public -/
def kem.KemSecretKey.generate
  {T2 : Type} (L : Std.Usize) (MU : Std.Usize) (rand_coreCryptoRngInst :
  rand_core.CryptoRng T2) (rng : T2) :
  Result ((kem.KemSecretKey L) × T2)
  := do
  let seed := Array.repeat 32#usize 0#u8
  let (s, to_slice_mut_back) ← lift (Array.to_slice_mut seed)
  let (rng1, s1) ← rand_coreCryptoRngInst.RngCoreInst.fill_bytes rng s
  let seed1 := to_slice_mut_back s1
  let out ← kem.KemSecretKey.expand_from_seed L MU seed1
  let _ ←
    Array.Insts.ZeroizeZeroize.zeroize (zeroize.Zeroize.Blanket
      U8.Insts.ZeroizeDefaultIsZeroes) seed1
  ok (out, rng1)

/-- [kopis::impls::kopis512::{kopis::impls::kopis512::Kopis512SecretKey}::generate]:
    Source: 'src/impls.rs', lines 61:16-63:17
    Visibility: public -/
def impls.kopis512.Kopis512SecretKey.generate
  {T0 : Type} (rand_coreCryptoRngInst : rand_core.CryptoRng T0) (rng : T0) :
  Result (impls.kopis512.Kopis512SecretKey × T0)
  := do
  let (ksk, rng1) ←
    kem.KemSecretKey.generate 2#usize 10#usize rand_coreCryptoRngInst rng
  ok (ksk, rng1)

/-- [kopis::impls::kopis768::{kopis::impls::kopis768::Kopis768SecretKey}::generate]:
    Source: 'src/impls.rs', lines 61:16-63:17
    Visibility: public -/
def impls.kopis768.Kopis768SecretKey.generate
  {T0 : Type} (rand_coreCryptoRngInst : rand_core.CryptoRng T0) (rng : T0) :
  Result (impls.kopis768.Kopis768SecretKey × T0)
  := do
  let (ksk, rng1) ←
    kem.KemSecretKey.generate 3#usize 8#usize rand_coreCryptoRngInst rng
  ok (ksk, rng1)

/-- [kopis::impls::kopis1024::{kopis::impls::kopis1024::Kopis1024SecretKey}::generate]:
    Source: 'src/impls.rs', lines 61:16-63:17
    Visibility: public -/
def impls.kopis1024.Kopis1024SecretKey.generate
  {T0 : Type} (rand_coreCryptoRngInst : rand_core.CryptoRng T0) (rng : T0) :
  Result (impls.kopis1024.Kopis1024SecretKey × T0)
  := do
  let (ksk, rng1) ←
    kem.KemSecretKey.generate 4#usize 6#usize rand_coreCryptoRngInst rng
  ok (ksk, rng1)

/-- [kopis::kem::{kopis::kem::KemSecretKey<L>}::seed]:
    Source: 'src/kem.rs', lines 95:4-97:5
    Visibility: public -/
def kem.KemSecretKey.impl.seed
  {L : Std.Usize} (self : kem.KemSecretKey L) :
  Result (Array Std.U8 32#usize)
  := do
  ok self.seed

/-- [kopis::impls::kopis512::{kopis::impls::kopis512::Kopis512SecretKey}::seed]:
    Source: 'src/impls.rs', lines 72:16-74:17
    Visibility: public -/
def impls.kopis512.Kopis512SecretKey.seed
  (self : impls.kopis512.Kopis512SecretKey) :
  Result (Array Std.U8 32#usize)
  := do
  kem.KemSecretKey.impl.seed self

/-- [kopis::impls::kopis768::{kopis::impls::kopis768::Kopis768SecretKey}::seed]:
    Source: 'src/impls.rs', lines 72:16-74:17
    Visibility: public -/
def impls.kopis768.Kopis768SecretKey.seed
  (self : impls.kopis768.Kopis768SecretKey) :
  Result (Array Std.U8 32#usize)
  := do
  kem.KemSecretKey.impl.seed self

/-- [kopis::impls::kopis1024::{kopis::impls::kopis1024::Kopis1024SecretKey}::seed]:
    Source: 'src/impls.rs', lines 72:16-74:17
    Visibility: public -/
def impls.kopis1024.Kopis1024SecretKey.seed
  (self : impls.kopis1024.Kopis1024SecretKey) :
  Result (Array Std.U8 32#usize)
  := do
  kem.KemSecretKey.impl.seed self

/-- [kopis::impls::kopis512::{kopis::impls::kopis512::Kopis512SecretKey}::expand_from_seed]:
    Source: 'src/impls.rs', lines 77:16-79:17
    Visibility: public -/
def impls.kopis512.Kopis512SecretKey.expand_from_seed
  (bytes : Array Std.U8 32#usize) :
  Result impls.kopis512.Kopis512SecretKey
  := do
  let ksk ← kem.KemSecretKey.expand_from_seed 2#usize 10#usize bytes
  ok ksk

/-- [kopis::impls::kopis768::{kopis::impls::kopis768::Kopis768SecretKey}::expand_from_seed]:
    Source: 'src/impls.rs', lines 77:16-79:17
    Visibility: public -/
def impls.kopis768.Kopis768SecretKey.expand_from_seed
  (bytes : Array Std.U8 32#usize) :
  Result impls.kopis768.Kopis768SecretKey
  := do
  let ksk ← kem.KemSecretKey.expand_from_seed 3#usize 8#usize bytes
  ok ksk

/-- [kopis::impls::kopis1024::{kopis::impls::kopis1024::Kopis1024SecretKey}::expand_from_seed]:
    Source: 'src/impls.rs', lines 77:16-79:17
    Visibility: public -/
def impls.kopis1024.Kopis1024SecretKey.expand_from_seed
  (bytes : Array Std.U8 32#usize) :
  Result impls.kopis1024.Kopis1024SecretKey
  := do
  let ksk ← kem.KemSecretKey.expand_from_seed 4#usize 6#usize bytes
  ok ksk

/-- [kopis::pke::{impl core::clone::Clone for kopis::pke::PkePublicKey<L>}::clone]:
    Source: 'src/pke.rs', lines 36:9-36:14
    Visibility: public -/
def pke.PkePublicKey.Insts.CoreCloneClone.clone
  {L : Std.Usize} (self : pke.PkePublicKey L) :
  Result (pke.PkePublicKey L)
  := do
  let a ← core.array.CloneArray.clone core.clone.CloneU8 self.matrix_seed
  let nm ← arithmetic.ntt.NttMatrix.Insts.CoreCloneClone.clone self.mat_a_ntt
  let a1 ←
    core.array.CloneArray.clone (core.clone.CloneArray 320#usize
      core.clone.CloneU8) self.vec_bytes
  let nm1 ← arithmetic.ntt.NttMatrix.Insts.CoreCloneClone.clone self.vec_ntt
  ok { matrix_seed := a, mat_a_ntt := nm, vec_bytes := a1, vec_ntt := nm1 }

/-- [kopis::kem::{kopis::kem::KemSecretKey<L>}::public_key]:
    Source: 'src/kem.rs', lines 99:4-104:5 -/
def kem.KemSecretKey.public_key
  {L : Std.Usize} (self : kem.KemSecretKey L) :
  Result (kem.KemPublicKey L)
  := do
  let ppk ← pke.PkePublicKey.Insts.CoreCloneClone.clone self.pke_pk
  ok { pke_pk := ppk, hash_pke_pk := self.hash_pke_pk }

/-- [kopis::impls::kopis512::{kopis::impls::kopis512::Kopis512SecretKey}::public_key]:
    Source: 'src/impls.rs', lines 82:16-84:17
    Visibility: public -/
def impls.kopis512.Kopis512SecretKey.public_key
  (self : impls.kopis512.Kopis512SecretKey) :
  Result impls.kopis512.Kopis512PublicKey
  := do
  let kpk ← kem.KemSecretKey.public_key self
  ok kpk

/-- [kopis::impls::kopis768::{kopis::impls::kopis768::Kopis768SecretKey}::public_key]:
    Source: 'src/impls.rs', lines 82:16-84:17
    Visibility: public -/
def impls.kopis768.Kopis768SecretKey.public_key
  (self : impls.kopis768.Kopis768SecretKey) :
  Result impls.kopis768.Kopis768PublicKey
  := do
  let kpk ← kem.KemSecretKey.public_key self
  ok kpk

/-- [kopis::impls::kopis1024::{kopis::impls::kopis1024::Kopis1024SecretKey}::public_key]:
    Source: 'src/impls.rs', lines 82:16-84:17
    Visibility: public -/
def impls.kopis1024.Kopis1024SecretKey.public_key
  (self : impls.kopis1024.Kopis1024SecretKey) :
  Result impls.kopis1024.Kopis1024PublicKey
  := do
  let kpk ← kem.KemSecretKey.public_key self
  ok kpk

/-- [kopis::pke::{kopis::pke::PkePublicKey<L>}::SERIALIZED_LEN]
    Source: 'src/pke.rs', lines 55:4-55:77
    Visibility: public -/
@[global_simps, irreducible]
def pke.PkePublicKey.SERIALIZED_LEN (L : Std.Usize) : Result Std.Usize := do
  let i ← L * consts.MODULUS_P_BITS
  let i1 ← i * consts.RING_DEG
  let i2 ← i1 / 8#usize
  32#usize + i2

/-- [kopis::kem::{kopis::kem::KemPublicKey<L>}::SERIALIZED_LEN]
    Source: 'src/kem.rs', lines 24:4-24:79 -/
@[global_simps, irreducible]
def kem.KemPublicKey.SERIALIZED_LEN (L : Std.Usize) : Result Std.Usize :=
  pke.PkePublicKey.SERIALIZED_LEN L

/-- [kopis::impls::kopis512::{kopis::impls::kopis512::Kopis512PublicKey}::SERIALIZED_LEN]
    Source: 'src/impls.rs', lines 89:16-89:95
    Visibility: public -/
@[global_simps, irreducible]
def impls.kopis512.Kopis512PublicKey.SERIALIZED_LEN : Result Std.Usize :=
  kem.KemPublicKey.SERIALIZED_LEN 2#usize

/-- [kopis::impls::kopis768::{kopis::impls::kopis768::Kopis768PublicKey}::SERIALIZED_LEN]
    Source: 'src/impls.rs', lines 89:16-89:95
    Visibility: public -/
@[global_simps, irreducible]
def impls.kopis768.Kopis768PublicKey.SERIALIZED_LEN : Result Std.Usize :=
  kem.KemPublicKey.SERIALIZED_LEN 3#usize

/-- [kopis::impls::kopis1024::{kopis::impls::kopis1024::Kopis1024PublicKey}::SERIALIZED_LEN]
    Source: 'src/impls.rs', lines 89:16-89:95
    Visibility: public -/
@[global_simps, irreducible]
def impls.kopis1024.Kopis1024PublicKey.SERIALIZED_LEN : Result Std.Usize :=
  kem.KemPublicKey.SERIALIZED_LEN 4#usize

/-- [kopis::pke::PK_VEC_ELEM_BYTES]
    Source: 'src/pke.rs', lines 20:0-20:63 -/
@[global_simps, irreducible]
def pke.PK_VEC_ELEM_BYTES : Result Std.Usize := do
  let i ← consts.MODULUS_P_BITS * consts.RING_DEG
  i / 8#usize

/-- [kopis::pke::{kopis::pke::PkePublicKey<L>}::serialize]: loop 0:
    Source: 'src/pke.rs', lines 65:8-68:9 -/
@[rust_loop]
def pke.PkePublicKey.serialize_loop
  {L : Std.Usize} (iter : core.ops.range.Range Std.Usize)
  (self : pke.PkePublicKey L) (out_buf : Slice Std.U8) :
  Result ((pke.PkePublicKey L) × (Slice Std.U8))
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok (self, out_buf)
  | some i =>
    let i1 ← pke.PK_VEC_ELEM_BYTES
    let start ← i * i1
    let i2 ← start + i1
    let (s, index_mut_back) ←
      core.slice.index.Slice.index_mut
        (core.slice.index.SliceIndexRangeUsizeSlice Std.U8) out_buf
        { start, «end» := i2 }
    let a ← Array.index_usize self.vec_bytes i
    let s1 ← lift (Array.to_slice a)
    let s2 ← core.slice.Slice.copy_from_slice core.marker.CopyU8 s s1
    let out_buf1 := index_mut_back s2
    pke.PkePublicKey.serialize_loop iter1 self out_buf1
partial_fixpoint

/-- [kopis::pke::{kopis::pke::PkePublicKey<L>}::serialize]:
    Source: 'src/pke.rs', lines 60:4-70:5 -/
def pke.PkePublicKey.serialize
  {L : Std.Usize} (self : pke.PkePublicKey L) (out_buf : Slice Std.U8) :
  Result (Slice Std.U8)
  := do
  let left_val := Slice.len out_buf
  let right_val ← pke.PkePublicKey.SERIALIZED_LEN L
  massert (left_val = right_val)
  let (self1, out_buf1) ←
    pke.PkePublicKey.serialize_loop { start := 0#usize, «end» := L } self
      out_buf
  let i ← pke.PK_VEC_ELEM_BYTES
  let i1 ← L * i
  let (s, index_mut_back) ←
    core.slice.index.Slice.index_mut
      (core.slice.index.SliceIndexRangeFromUsizeSlice Std.U8) out_buf1
      { start := i1 }
  let s1 ← lift (Array.to_slice self1.matrix_seed)
  let s2 ← core.slice.Slice.copy_from_slice core.marker.CopyU8 s s1
  ok (index_mut_back s2)

/-- [kopis::kem::{kopis::kem::KemPublicKey<L>}::serialize]:
    Source: 'src/kem.rs', lines 27:4-29:5 -/
def kem.KemPublicKey.serialize
  {L : Std.Usize} (self : kem.KemPublicKey L) (out_buf : Slice Std.U8) :
  Result (Slice Std.U8)
  := do
  pke.PkePublicKey.serialize self.pke_pk out_buf

/-- [kopis::impls::kopis512::{kopis::impls::kopis512::Kopis512PublicKey}::serialize]:
    Source: 'src/impls.rs', lines 92:16-94:17
    Visibility: public -/
def impls.kopis512.Kopis512PublicKey.serialize
  (self : impls.kopis512.Kopis512PublicKey) (out_buf : Array Std.U8 672#usize)
  :
  Result (Array Std.U8 672#usize)
  := do
  let (s, to_slice_mut_back) ← lift (Array.to_slice_mut out_buf)
  let s1 ← kem.KemPublicKey.serialize self s
  ok (to_slice_mut_back s1)

/-- [kopis::impls::kopis768::{kopis::impls::kopis768::Kopis768PublicKey}::serialize]:
    Source: 'src/impls.rs', lines 92:16-94:17
    Visibility: public -/
def impls.kopis768.Kopis768PublicKey.serialize
  (self : impls.kopis768.Kopis768PublicKey) (out_buf : Array Std.U8 992#usize)
  :
  Result (Array Std.U8 992#usize)
  := do
  let (s, to_slice_mut_back) ← lift (Array.to_slice_mut out_buf)
  let s1 ← kem.KemPublicKey.serialize self s
  ok (to_slice_mut_back s1)

/-- [kopis::impls::kopis1024::{kopis::impls::kopis1024::Kopis1024PublicKey}::serialize]:
    Source: 'src/impls.rs', lines 92:16-94:17
    Visibility: public -/
def impls.kopis1024.Kopis1024PublicKey.serialize
  (self : impls.kopis1024.Kopis1024PublicKey)
  (out_buf : Array Std.U8 1312#usize) :
  Result (Array Std.U8 1312#usize)
  := do
  let (s, to_slice_mut_back) ← lift (Array.to_slice_mut out_buf)
  let s1 ← kem.KemPublicKey.serialize self s
  ok (to_slice_mut_back s1)

/-- [kopis::pke::{kopis::pke::PkePublicKey<L>}::from_bytes]: loop 0:
    Source: 'src/pke.rs', lines 88:8-91:9 -/
@[rust_loop]
def pke.PkePublicKey.from_bytes_loop
  {L : Std.Usize} (iter : core.ops.range.Range Std.Usize)
  (vec_slice : Slice Std.U8) (vec_bytes : Array (Array Std.U8 320#usize) L) :
  Result (Array (Array Std.U8 320#usize) L)
  := do
  let (o, iter1) ←
    core.iter.range.IteratorRange.next core.iter.range.StepUsize iter
  match o with
  | none => ok vec_bytes
  | some i =>
    let i1 ← pke.PK_VEC_ELEM_BYTES
    let start ← i * i1
    let (a, index_mut_back) ← Array.index_mut_usize vec_bytes i
    let (s, to_slice_mut_back) ← lift (Array.to_slice_mut a)
    let i2 ← start + i1
    let s1 ←
      core.slice.index.Slice.index (core.slice.index.SliceIndexRangeUsizeSlice
        Std.U8) vec_slice { start, «end» := i2 }
    let s2 ← core.slice.Slice.copy_from_slice core.marker.CopyU8 s s1
    let a1 := to_slice_mut_back s2
    let a2 := index_mut_back a1
    pke.PkePublicKey.from_bytes_loop iter1 vec_slice a2
partial_fixpoint

/-- [kopis::pke::{kopis::pke::PkePublicKey<L>}::from_bytes]:
    Source: 'src/pke.rs', lines 74:4-101:5 -/
def pke.PkePublicKey.from_bytes
  (L : Std.Usize) (bytes : Slice Std.U8) : Result (pke.PkePublicKey L) := do
  let left_val := Slice.len bytes
  let right_val ← pke.PkePublicKey.SERIALIZED_LEN L
  massert (left_val = right_val)
  let i ← right_val - 32#usize
  let (vec_slice, seed) ← core.slice.Slice.split_at bytes i
  let r ←
    core.array.TryFromArrayCopySlice.try_from 32#usize core.marker.CopyU8 seed
  let matrix_seed ←
    core.result.Result.unwrap core.fmt.DebugTryFromSliceError r
  let vec ← arithmetic.matrix_arith.Matrix.deserialize_10 L 1#usize vec_slice
  let vec_ntt ← arithmetic.ntt.NttMatrix.from_uniform_matrix vec
  let a := Array.repeat 320#usize 0#u8
  let vec_bytes := Array.repeat L a
  let vec_bytes1 ←
    pke.PkePublicKey.from_bytes_loop { start := 0#usize, «end» := L }
      vec_slice vec_bytes
  let mat_a ← sample.gen_matrix_from_seed L matrix_seed
  let mat_a_ntt ← arithmetic.ntt.NttMatrix.from_uniform_matrix mat_a
  ok { matrix_seed, mat_a_ntt, vec_bytes := vec_bytes1, vec_ntt }

/-- [kopis::kem::{kopis::kem::KemPublicKey<L>}::from_bytes]:
    Source: 'src/kem.rs', lines 32:4-40:5 -/
def kem.KemPublicKey.from_bytes
  (L : Std.Usize) (bytes : Slice Std.U8) : Result (kem.KemPublicKey L) := do
  let pke_pk ← pke.PkePublicKey.from_bytes L bytes
  let hash_pke_pk ← pke.PkePublicKey.hash pke_pk
  ok { pke_pk, hash_pke_pk }

/-- [kopis::impls::kopis512::{kopis::impls::kopis512::Kopis512PublicKey}::from_bytes]:
    Source: 'src/impls.rs', lines 97:16-99:17
    Visibility: public -/
def impls.kopis512.Kopis512PublicKey.from_bytes
  (bytes : Array Std.U8 672#usize) :
  Result impls.kopis512.Kopis512PublicKey
  := do
  let s ← lift (Array.to_slice bytes)
  let kpk ← kem.KemPublicKey.from_bytes 2#usize s
  ok kpk

/-- [kopis::impls::kopis768::{kopis::impls::kopis768::Kopis768PublicKey}::from_bytes]:
    Source: 'src/impls.rs', lines 97:16-99:17
    Visibility: public -/
def impls.kopis768.Kopis768PublicKey.from_bytes
  (bytes : Array Std.U8 992#usize) :
  Result impls.kopis768.Kopis768PublicKey
  := do
  let s ← lift (Array.to_slice bytes)
  let kpk ← kem.KemPublicKey.from_bytes 3#usize s
  ok kpk

/-- [kopis::impls::kopis1024::{kopis::impls::kopis1024::Kopis1024PublicKey}::from_bytes]:
    Source: 'src/impls.rs', lines 97:16-99:17
    Visibility: public -/
def impls.kopis1024.Kopis1024PublicKey.from_bytes
  (bytes : Array Std.U8 1312#usize) :
  Result impls.kopis1024.Kopis1024PublicKey
  := do
  let s ← lift (Array.to_slice bytes)
  let kpk ← kem.KemPublicKey.from_bytes 4#usize s
  ok kpk

/-- [kopis::pke::encrypt_deterministic]:
    Source: 'src/pke.rs', lines 238:0-271:1 -/
def pke.encrypt_deterministic
  {L : Std.Usize} (MU : Std.Usize) (T : Std.Usize) (pk : pke.PkePublicKey L)
  (msg : Array Std.U8 32#usize) (randomness : Array Std.U8 32#usize)
  (out_buf : Slice Std.U8) :
  Result (Slice Std.U8)
  := do
  let left_val := Slice.len out_buf
  let right_val ← pke.ciphertext_len L T
  massert (left_val = right_val)
  let vec_sprime ← sample.gen_secret_from_seed L MU randomness
  let sprime_ntt ← arithmetic.ntt.NttMatrix.from_secret_matrix vec_sprime
  let prod ← arithmetic.ntt.NttMatrix.mul pk.mat_a_ntt sprime_ntt
  let i ← pke.H1_VAL
  let prod1 ← arithmetic.matrix_arith.Matrix.wrapping_add_to_all prod i
  let i1 ← consts.MODULUS_Q_BITS - consts.MODULUS_P_BITS
  let prod2 ← arithmetic.matrix_arith.Matrix.shift_right prod1 i1
  let vprime ← arithmetic.ntt.NttMatrix.mul_transpose pk.vec_ntt sprime_ntt
  let a ← Array.index_usize vprime 0#usize
  let vprime1 ← Array.index_usize a 0#usize
  let s ← lift (Array.to_slice msg)
  let a1 ← ser.deserialize_generic 256#usize s 1#usize
  let i2 ← consts.MODULUS_P_BITS - 1#usize
  let msg_polyn ← arithmetic.ring_arith.RingElem.shift_left a1 i2
  let c ←
    SharedARingElem.Insts.CoreOpsArithSubSharedARingElemRingElem.sub vprime1
      msg_polyn
  let c1 ← arithmetic.ring_arith.RingElem.wrapping_add_to_all c i
  let i3 ← consts.MODULUS_P_BITS - T
  let c2 ← arithmetic.ring_arith.RingElem.shift_right c1 i3
  let i4 ← L * consts.MODULUS_P_BITS
  let i5 ← i4 * consts.RING_DEG
  let i6 ← i5 / 8#usize
  let ((bprime_buf, c_buf), split_at_mut_back) ←
    core.slice.Slice.split_at_mut out_buf i6
  let bprime_buf1 ←
    arithmetic.matrix_arith.Matrix.serialize prod2 bprime_buf
      consts.MODULUS_P_BITS
  let c_buf1 ← arithmetic.ring_arith.RingElem.serialize c2 c_buf T
  ok (split_at_mut_back (bprime_buf1, c_buf1))

/-- [kopis::kem::encap_deterministic]:
    Source: 'src/kem.rs', lines 109:0-136:1 -/
def kem.encap_deterministic
  {L : Std.Usize} (MU : Std.Usize) (T : Std.Usize)
  (randomness : Array Std.U8 32#usize) (kem_pk : kem.KemPublicKey L)
  (out_buf : Slice Std.U8) :
  Result ((Array Std.U8 32#usize) × (Slice Std.U8))
  := do
  let k := Array.repeat 32#usize 0#u8
  let r := Array.repeat 32#usize 0#u8
  let hasher ←
    turboshake.TurboShake.Insts.CoreDefaultDefault.default 136#usize 5#u8
  let s ← lift (Array.to_slice randomness)
  let hasher1 ← turboshake.TurboShake.Insts.DigestUpdate.update hasher s
  let s1 ← lift (Array.to_slice kem_pk.hash_pke_pk)
  let hasher2 ← turboshake.TurboShake.Insts.DigestUpdate.update hasher1 s1
  let xof ←
    turboshake.TurboShake.Insts.DigestExtendableOutputTurboShakeReader.finalize_xof
      hasher2
  let (s2, to_slice_mut_back) ← lift (Array.to_slice_mut k)
  let (xof1, s3) ←
    turboshake.TurboShakeReader.Insts.DigestXofReader.read xof s2
  let (s4, to_slice_mut_back1) ← lift (Array.to_slice_mut r)
  let (_, s5) ←
    turboshake.TurboShakeReader.Insts.DigestXofReader.read xof1 s4
  let r1 := to_slice_mut_back1 s5
  let out_buf1 ←
    pke.encrypt_deterministic MU T kem_pk.pke_pk randomness r1 out_buf
  let k1 := to_slice_mut_back s3
  ok (k1, out_buf1)

/-- [kopis::impls::kopis512::{kopis::impls::kopis512::Kopis512PublicKey}::encapsulate_deterministic]:
    Source: 'src/impls.rs', lines 119:16-131:17
    Visibility: public -/
def impls.kopis512.Kopis512PublicKey.encapsulate_deterministic
  (self : impls.kopis512.Kopis512PublicKey)
  (randomness : Array Std.U8 32#usize) :
  Result ((Array Std.U8 736#usize) × impls.SharedSecret)
  := do
  let ct := Array.repeat 736#usize 0#u8
  let (s, to_slice_mut_back) ← lift (Array.to_slice_mut ct)
  let (ss, s1) ← kem.encap_deterministic 10#usize 3#usize randomness self s
  let ct1 := to_slice_mut_back s1
  ok (ct1, ss)

/-- [kopis::impls::kopis512::{kopis::impls::kopis512::Kopis512PublicKey}::encapsulate]:
    Source: 'src/impls.rs', lines 104:16-114:17
    Visibility: public -/
def impls.kopis512.Kopis512PublicKey.encapsulate
  {T0 : Type} (rand_coreCryptoRngInst : rand_core.CryptoRng T0)
  (self : impls.kopis512.Kopis512PublicKey) (rng : T0) :
  Result (((Array Std.U8 736#usize) × impls.SharedSecret) × T0)
  := do
  let randomness := Array.repeat 32#usize 0#u8
  let (s, to_slice_mut_back) ← lift (Array.to_slice_mut randomness)
  let (rng1, s1) ← rand_coreCryptoRngInst.RngCoreInst.fill_bytes rng s
  let randomness1 := to_slice_mut_back s1
  let out ←
    impls.kopis512.Kopis512PublicKey.encapsulate_deterministic self randomness1
  let _ ←
    Array.Insts.ZeroizeZeroize.zeroize (zeroize.Zeroize.Blanket
      U8.Insts.ZeroizeDefaultIsZeroes) randomness1
  ok (out, rng1)

/-- [kopis::impls::kopis768::{kopis::impls::kopis768::Kopis768PublicKey}::encapsulate_deterministic]:
    Source: 'src/impls.rs', lines 119:16-131:17
    Visibility: public -/
def impls.kopis768.Kopis768PublicKey.encapsulate_deterministic
  (self : impls.kopis768.Kopis768PublicKey)
  (randomness : Array Std.U8 32#usize) :
  Result ((Array Std.U8 1088#usize) × impls.SharedSecret)
  := do
  let ct := Array.repeat 1088#usize 0#u8
  let (s, to_slice_mut_back) ← lift (Array.to_slice_mut ct)
  let (ss, s1) ← kem.encap_deterministic 8#usize 4#usize randomness self s
  let ct1 := to_slice_mut_back s1
  ok (ct1, ss)

/-- [kopis::impls::kopis768::{kopis::impls::kopis768::Kopis768PublicKey}::encapsulate]:
    Source: 'src/impls.rs', lines 104:16-114:17
    Visibility: public -/
def impls.kopis768.Kopis768PublicKey.encapsulate
  {T0 : Type} (rand_coreCryptoRngInst : rand_core.CryptoRng T0)
  (self : impls.kopis768.Kopis768PublicKey) (rng : T0) :
  Result (((Array Std.U8 1088#usize) × impls.SharedSecret) × T0)
  := do
  let randomness := Array.repeat 32#usize 0#u8
  let (s, to_slice_mut_back) ← lift (Array.to_slice_mut randomness)
  let (rng1, s1) ← rand_coreCryptoRngInst.RngCoreInst.fill_bytes rng s
  let randomness1 := to_slice_mut_back s1
  let out ←
    impls.kopis768.Kopis768PublicKey.encapsulate_deterministic self randomness1
  let _ ←
    Array.Insts.ZeroizeZeroize.zeroize (zeroize.Zeroize.Blanket
      U8.Insts.ZeroizeDefaultIsZeroes) randomness1
  ok (out, rng1)

/-- [kopis::impls::kopis1024::{kopis::impls::kopis1024::Kopis1024PublicKey}::encapsulate_deterministic]:
    Source: 'src/impls.rs', lines 119:16-131:17
    Visibility: public -/
def impls.kopis1024.Kopis1024PublicKey.encapsulate_deterministic
  (self : impls.kopis1024.Kopis1024PublicKey)
  (randomness : Array Std.U8 32#usize) :
  Result ((Array Std.U8 1472#usize) × impls.SharedSecret)
  := do
  let ct := Array.repeat 1472#usize 0#u8
  let (s, to_slice_mut_back) ← lift (Array.to_slice_mut ct)
  let (ss, s1) ← kem.encap_deterministic 6#usize 6#usize randomness self s
  let ct1 := to_slice_mut_back s1
  ok (ct1, ss)

/-- [kopis::impls::kopis1024::{kopis::impls::kopis1024::Kopis1024PublicKey}::encapsulate]:
    Source: 'src/impls.rs', lines 104:16-114:17
    Visibility: public -/
def impls.kopis1024.Kopis1024PublicKey.encapsulate
  {T0 : Type} (rand_coreCryptoRngInst : rand_core.CryptoRng T0)
  (self : impls.kopis1024.Kopis1024PublicKey) (rng : T0) :
  Result (((Array Std.U8 1472#usize) × impls.SharedSecret) × T0)
  := do
  let randomness := Array.repeat 32#usize 0#u8
  let (s, to_slice_mut_back) ← lift (Array.to_slice_mut randomness)
  let (rng1, s1) ← rand_coreCryptoRngInst.RngCoreInst.fill_bytes rng s
  let randomness1 := to_slice_mut_back s1
  let out ←
    impls.kopis1024.Kopis1024PublicKey.encapsulate_deterministic self
      randomness1
  let _ ←
    Array.Insts.ZeroizeZeroize.zeroize (zeroize.Zeroize.Blanket
      U8.Insts.ZeroizeDefaultIsZeroes) randomness1
  ok (out, rng1)

/-- [kopis::pke::decrypt]:
    Source: 'src/pke.rs', lines 207:0-234:1 -/
def pke.decrypt
  {L : Std.Usize} (T : Std.Usize) (sk : pke.PkeSecretKey L)
  (ciphertext : Slice Std.U8) :
  Result (Array Std.U8 32#usize)
  := do
  let left_val := Slice.len ciphertext
  let right_val ← pke.ciphertext_len L T
  massert (left_val = right_val)
  let i ← L * consts.MODULUS_P_BITS
  let i1 ← i * consts.RING_DEG
  let i2 ← i1 / 8#usize
  let (bprime_bytes, c_bytes) ← core.slice.Slice.split_at ciphertext i2
  let bprime ←
    arithmetic.matrix_arith.Matrix.deserialize_10 L 1#usize bprime_bytes
  let bprime_ntt ← arithmetic.ntt.NttMatrix.from_uniform_matrix bprime
  let c ← arithmetic.ring_arith.RingElem.deserialize c_bytes T
  let i3 ← consts.MODULUS_P_BITS - T
  let c1 ← arithmetic.ring_arith.RingElem.shift_left c i3
  let v ← arithmetic.ntt.NttMatrix.mul_transpose bprime_ntt sk
  let a ← Array.index_usize v 0#usize
  let v1 ← Array.index_usize a 0#usize
  let mprime ←
    SharedARingElem.Insts.CoreOpsArithSubSharedARingElemRingElem.sub v1 c1
  let i4 ← consts.MODULUS_P_BITS - 2#usize
  let i5 ← 1#u16 <<< i4
  let i6 ← i3 - 1#usize
  let i7 ← 1#u16 <<< i6
  let i8 ← i5 - i7
  let i9 ← consts.MODULUS_Q_BITS - consts.MODULUS_P_BITS
  let i10 ← i9 - 1#usize
  let i11 ← 1#u16 <<< i10
  let h2_val ← i8 + i11
  let mprime1 ←
    arithmetic.ring_arith.RingElem.wrapping_add_to_all mprime h2_val
  let i12 ← consts.MODULUS_P_BITS - 1#usize
  let mprime2 ← arithmetic.ring_arith.RingElem.shift_right mprime1 i12
  let m := Array.repeat 32#usize 0#u8
  let (s, to_slice_mut_back) ← lift (Array.to_slice_mut m)
  let s1 ← arithmetic.ring_arith.RingElem.serialize mprime2 s 1#usize
  ok (to_slice_mut_back s1)

/-- [kopis::turboshake256_hash]:
    Source: 'src/lib.rs', lines 41:0-51:1 -/
def turboshake256_hash
  (DS : Std.U8) (input0 : Slice Std.U8) (input1 : Slice Std.U8) :
  Result (Array Std.U8 32#usize)
  := do
  let hasher ←
    turboshake.TurboShake.Insts.CoreDefaultDefault.default 136#usize DS
  let hasher1 ← turboshake.TurboShake.Insts.DigestUpdate.update hasher input0
  let hasher2 ←
    turboshake.TurboShake.Insts.DigestUpdate.update hasher1 input1
  let out := Array.repeat 32#usize 0#u8
  let reader ←
    turboshake.TurboShake.Insts.DigestExtendableOutputTurboShakeReader.finalize_xof
      hasher2
  let (s, to_slice_mut_back) ← lift (Array.to_slice_mut out)
  let (_, s1) ←
    turboshake.TurboShakeReader.Insts.DigestXofReader.read reader s
  ok (to_slice_mut_back s1)

/-- [kopis::kem::decap]:
    Source: 'src/kem.rs', lines 141:0-174:1
    Visibility: public -/
def kem.decap
  {L : Std.Usize} (MU : Std.Usize) (T : Std.Usize) (sk : kem.KemSecretKey L)
  (ciphertext : Slice Std.U8) :
  Result (Array Std.U8 32#usize)
  := do
  let left_val := Slice.len ciphertext
  let right_val ← pke.ciphertext_len L T
  massert (left_val = right_val)
  let randomness ← pke.decrypt T sk.pke_sk ciphertext
  let k := Array.repeat 32#usize 0#u8
  let rprime := Array.repeat 32#usize 0#u8
  let hasher ←
    turboshake.TurboShake.Insts.CoreDefaultDefault.default 136#usize 5#u8
  let s ← lift (Array.to_slice randomness)
  let hasher1 ← turboshake.TurboShake.Insts.DigestUpdate.update hasher s
  let s1 ← lift (Array.to_slice sk.hash_pke_pk)
  let hasher2 ← turboshake.TurboShake.Insts.DigestUpdate.update hasher1 s1
  let xof ←
    turboshake.TurboShake.Insts.DigestExtendableOutputTurboShakeReader.finalize_xof
      hasher2
  let (s2, to_slice_mut_back) ← lift (Array.to_slice_mut k)
  let (xof1, s3) ←
    turboshake.TurboShakeReader.Insts.DigestXofReader.read xof s2
  let (s4, to_slice_mut_back1) ← lift (Array.to_slice_mut rprime)
  let (_, s5) ←
    turboshake.TurboShakeReader.Insts.DigestXofReader.read xof1 s4
  let buf := Array.repeat 1472#usize 0#u8
  let (reconstructed_ct, _) ←
    core.array.Array.index_mut (core.ops.index.IndexMutSlice
      (core.slice.index.SliceIndexRangeToUsizeSlice Std.U8)) buf
      { «end» := right_val }
  let rprime1 := to_slice_mut_back1 s5
  let reconstructed_ct1 ←
    pke.encrypt_deterministic MU T sk.pke_pk randomness rprime1
      reconstructed_ct
  let s6 ← lift (Array.to_slice sk.z)
  let reject_val ← turboshake256_hash 6#u8 s6 ciphertext
  let reconstruction_matched ←
    Slice.Insts.SubtleConstantTimeEq.ct_eq U8.Insts.SubtleConstantTimeEq
      reconstructed_ct1 ciphertext
  let k1 := to_slice_mut_back s3
  Array.Insts.SubtleConditionallySelectable.conditional_select
    U8.Insts.SubtleConditionallySelectable reject_val k1 reconstruction_matched

/-- [kopis::impls::kopis512::{kopis::impls::kopis512::Kopis512SecretKey}::decapsulate]:
    Source: 'src/impls.rs', lines 138:16-144:17
    Visibility: public -/
def impls.kopis512.Kopis512SecretKey.decapsulate
  (self : impls.kopis512.Kopis512SecretKey)
  (encapsulated_key : Array Std.U8 736#usize) :
  Result impls.SharedSecret
  := do
  let s ← lift (Array.to_slice encapsulated_key)
  let a ← kem.decap 10#usize 3#usize self s
  ok a

/-- [kopis::impls::kopis768::{kopis::impls::kopis768::Kopis768SecretKey}::decapsulate]:
    Source: 'src/impls.rs', lines 138:16-144:17
    Visibility: public -/
def impls.kopis768.Kopis768SecretKey.decapsulate
  (self : impls.kopis768.Kopis768SecretKey)
  (encapsulated_key : Array Std.U8 1088#usize) :
  Result impls.SharedSecret
  := do
  let s ← lift (Array.to_slice encapsulated_key)
  let a ← kem.decap 8#usize 4#usize self s
  ok a

/-- [kopis::impls::kopis1024::{kopis::impls::kopis1024::Kopis1024SecretKey}::decapsulate]:
    Source: 'src/impls.rs', lines 138:16-144:17
    Visibility: public -/
def impls.kopis1024.Kopis1024SecretKey.decapsulate
  (self : impls.kopis1024.Kopis1024SecretKey)
  (encapsulated_key : Array Std.U8 1472#usize) :
  Result impls.SharedSecret
  := do
  let s ← lift (Array.to_slice encapsulated_key)
  let a ← kem.decap 6#usize 6#usize self s
  ok a

/-- Trait implementation: [kopis::kem::{impl core::ops::drop::Drop for kopis::kem::KemSecretKey<L>}]
    Source: 'src/kem.rs', lines 54:9-54:22 -/
@[reducible]
def kem.KemSecretKey.Insts.CoreOpsDropDrop (L : Std.Usize) : core.ops.drop.Drop
  (kem.KemSecretKey L) := {
  drop := kem.KemSecretKey.Insts.CoreOpsDropDrop.drop
}

/-- Trait implementation: [kopis::pke::{impl core::ops::drop::Drop for kopis::pke::PkeSecretKey<L>}]
    Source: 'src/pke.rs', lines 27:18-27:31 -/
@[reducible]
def pke.PkeSecretKey.Insts.CoreOpsDropDrop (L : Std.Usize) : core.ops.drop.Drop
  (pke.PkeSecretKey L) := {
  drop := pke.PkeSecretKey.Insts.CoreOpsDropDrop.drop
}

/-- Trait implementation: [kopis::pke::{impl zeroize::ZeroizeOnDrop for kopis::pke::PkeSecretKey<L>}]
    Source: 'src/pke.rs', lines 27:18-27:31 -/
@[reducible]
def pke.PkeSecretKey.Insts.ZeroizeZeroizeOnDrop (L : Std.Usize) :
  zeroize.ZeroizeOnDrop (pke.PkeSecretKey L) := {
}

/-- Trait implementation: [kopis::pke::{impl core::clone::Clone for kopis::pke::PkePublicKey<L>}]
    Source: 'src/pke.rs', lines 36:9-36:14 -/
@[reducible]
def pke.PkePublicKey.Insts.CoreCloneClone (L : Std.Usize) : core.clone.Clone
  (pke.PkePublicKey L) := {
  clone := pke.PkePublicKey.Insts.CoreCloneClone.clone
}

/-- [kopis::pke::max_ciphertext_len]:
    Source: 'src/pke.rs', lines 126:0-129:1
    Visibility: public -/
def pke.max_ciphertext_len : Result Std.Usize := do
  let i ← consts.MAX_T * consts.RING_DEG
  let i1 ← i / 8#usize
  let i2 ← consts.MAX_L * consts.MODULUS_P_BITS
  let i3 ← i2 * consts.RING_DEG
  let i4 ← i3 / 8#usize
  i1 + i4

/-- [kopis::ser::deserialize_13::{impl core::ops::function::FnMut<(usize,), u16> for kopis::ser::deserialize_13::closure<'_0>}::call_mut]:
    Source: 'src/ser.rs', lines 12:16-12:38 -/
def ser.deserialize_13.closure.Insts.CoreOpsFunctionFnMutTupleUsizeU16.call_mut
  (state : ser.deserialize_13.closure) (args : Std.Usize) :
  Result (Std.U16 × ser.deserialize_13.closure)
  := do
  let i ←
    ser.deserialize_13.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call state
      args
  ok (i, state)

/-- [kopis::ser::deserialize_13::{impl core::ops::function::FnOnce<(usize,), u16> for kopis::ser::deserialize_13::closure<'_0>}::call_once]:
    Source: 'src/ser.rs', lines 12:16-12:38 -/
def
  ser.deserialize_13.closure.Insts.CoreOpsFunctionFnOnceTupleUsizeU16.call_once
  (c : ser.deserialize_13.closure) (i : Std.Usize) : Result Std.U16 := do
  let (i1, _) ←
    ser.deserialize_13.closure.Insts.CoreOpsFunctionFnMutTupleUsizeU16.call_mut
      c i
  ok i1

/-- Trait implementation: [kopis::ser::deserialize_13::{impl core::ops::function::FnOnce<(usize,), u16> for kopis::ser::deserialize_13::closure<'_0>}]
    Source: 'src/ser.rs', lines 12:16-12:38 -/
@[reducible]
def ser.deserialize_13.closure.Insts.CoreOpsFunctionFnOnceTupleUsizeU16 :
  core.ops.function.FnOnce ser.deserialize_13.closure Std.Usize Std.U16 := {
  call_once :=
    ser.deserialize_13.closure.Insts.CoreOpsFunctionFnOnceTupleUsizeU16.call_once
}

/-- Trait implementation: [kopis::ser::deserialize_13::{impl core::ops::function::FnMut<(usize,), u16> for kopis::ser::deserialize_13::closure<'_0>}]
    Source: 'src/ser.rs', lines 12:16-12:38 -/
@[reducible]
def ser.deserialize_13.closure.Insts.CoreOpsFunctionFnMutTupleUsizeU16 :
  core.ops.function.FnMut ser.deserialize_13.closure Std.Usize Std.U16 := {
  FnOnceInst :=
    ser.deserialize_13.closure.Insts.CoreOpsFunctionFnOnceTupleUsizeU16
  call_mut :=
    ser.deserialize_13.closure.Insts.CoreOpsFunctionFnMutTupleUsizeU16.call_mut
}

/-- Trait implementation: [kopis::ser::deserialize_13::{impl core::ops::function::Fn<(usize,), u16> for kopis::ser::deserialize_13::closure<'_0>}]
    Source: 'src/ser.rs', lines 12:16-12:38 -/
@[reducible]
def ser.deserialize_13.closure.Insts.CoreOpsFunctionFnTupleUsizeU16 :
  core.ops.function.Fn ser.deserialize_13.closure Std.Usize Std.U16 := {
  FnMutInst :=
    ser.deserialize_13.closure.Insts.CoreOpsFunctionFnMutTupleUsizeU16
  call := ser.deserialize_13.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call
}

/-- [kopis::ser::deserialize_10::{impl core::ops::function::FnMut<(usize,), u16> for kopis::ser::deserialize_10::closure<'_0>}::call_mut]:
    Source: 'src/ser.rs', lines 34:16-34:38 -/
def ser.deserialize_10.closure.Insts.CoreOpsFunctionFnMutTupleUsizeU16.call_mut
  (state : ser.deserialize_10.closure) (args : Std.Usize) :
  Result (Std.U16 × ser.deserialize_10.closure)
  := do
  let i ←
    ser.deserialize_10.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call state
      args
  ok (i, state)

/-- [kopis::ser::deserialize_10::{impl core::ops::function::FnOnce<(usize,), u16> for kopis::ser::deserialize_10::closure<'_0>}::call_once]:
    Source: 'src/ser.rs', lines 34:16-34:38 -/
def
  ser.deserialize_10.closure.Insts.CoreOpsFunctionFnOnceTupleUsizeU16.call_once
  (c : ser.deserialize_10.closure) (i : Std.Usize) : Result Std.U16 := do
  let (i1, _) ←
    ser.deserialize_10.closure.Insts.CoreOpsFunctionFnMutTupleUsizeU16.call_mut
      c i
  ok i1

/-- Trait implementation: [kopis::ser::deserialize_10::{impl core::ops::function::FnOnce<(usize,), u16> for kopis::ser::deserialize_10::closure<'_0>}]
    Source: 'src/ser.rs', lines 34:16-34:38 -/
@[reducible]
def ser.deserialize_10.closure.Insts.CoreOpsFunctionFnOnceTupleUsizeU16 :
  core.ops.function.FnOnce ser.deserialize_10.closure Std.Usize Std.U16 := {
  call_once :=
    ser.deserialize_10.closure.Insts.CoreOpsFunctionFnOnceTupleUsizeU16.call_once
}

/-- Trait implementation: [kopis::ser::deserialize_10::{impl core::ops::function::FnMut<(usize,), u16> for kopis::ser::deserialize_10::closure<'_0>}]
    Source: 'src/ser.rs', lines 34:16-34:38 -/
@[reducible]
def ser.deserialize_10.closure.Insts.CoreOpsFunctionFnMutTupleUsizeU16 :
  core.ops.function.FnMut ser.deserialize_10.closure Std.Usize Std.U16 := {
  FnOnceInst :=
    ser.deserialize_10.closure.Insts.CoreOpsFunctionFnOnceTupleUsizeU16
  call_mut :=
    ser.deserialize_10.closure.Insts.CoreOpsFunctionFnMutTupleUsizeU16.call_mut
}

/-- Trait implementation: [kopis::ser::deserialize_10::{impl core::ops::function::Fn<(usize,), u16> for kopis::ser::deserialize_10::closure<'_0>}]
    Source: 'src/ser.rs', lines 34:16-34:38 -/
@[reducible]
def ser.deserialize_10.closure.Insts.CoreOpsFunctionFnTupleUsizeU16 :
  core.ops.function.Fn ser.deserialize_10.closure Std.Usize Std.U16 := {
  FnMutInst :=
    ser.deserialize_10.closure.Insts.CoreOpsFunctionFnMutTupleUsizeU16
  call := ser.deserialize_10.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call
}

/-- [kopis::ser::serialize_10::{impl core::ops::function::FnMut<(usize,), u16> for kopis::ser::serialize_10::closure<'_0, '_1>}::call_mut]:
    Source: 'src/ser.rs', lines 87:16-87:46 -/
def ser.serialize_10.closure.Insts.CoreOpsFunctionFnMutTupleUsizeU16.call_mut
  (state : ser.serialize_10.closure) (args : Std.Usize) :
  Result (Std.U16 × ser.serialize_10.closure)
  := do
  let i ←
    ser.serialize_10.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call state
      args
  ok (i, state)

/-- [kopis::ser::serialize_10::{impl core::ops::function::FnOnce<(usize,), u16> for kopis::ser::serialize_10::closure<'_0, '_1>}::call_once]:
    Source: 'src/ser.rs', lines 87:16-87:46 -/
def ser.serialize_10.closure.Insts.CoreOpsFunctionFnOnceTupleUsizeU16.call_once
  (c : ser.serialize_10.closure) (i : Std.Usize) : Result Std.U16 := do
  let (i1, _) ←
    ser.serialize_10.closure.Insts.CoreOpsFunctionFnMutTupleUsizeU16.call_mut c
      i
  ok i1

/-- Trait implementation: [kopis::ser::serialize_10::{impl core::ops::function::FnOnce<(usize,), u16> for kopis::ser::serialize_10::closure<'_0, '_1>}]
    Source: 'src/ser.rs', lines 87:16-87:46 -/
@[reducible]
def ser.serialize_10.closure.Insts.CoreOpsFunctionFnOnceTupleUsizeU16 :
  core.ops.function.FnOnce ser.serialize_10.closure Std.Usize Std.U16 := {
  call_once :=
    ser.serialize_10.closure.Insts.CoreOpsFunctionFnOnceTupleUsizeU16.call_once
}

/-- Trait implementation: [kopis::ser::serialize_10::{impl core::ops::function::FnMut<(usize,), u16> for kopis::ser::serialize_10::closure<'_0, '_1>}]
    Source: 'src/ser.rs', lines 87:16-87:46 -/
@[reducible]
def ser.serialize_10.closure.Insts.CoreOpsFunctionFnMutTupleUsizeU16 :
  core.ops.function.FnMut ser.serialize_10.closure Std.Usize Std.U16 := {
  FnOnceInst :=
    ser.serialize_10.closure.Insts.CoreOpsFunctionFnOnceTupleUsizeU16
  call_mut :=
    ser.serialize_10.closure.Insts.CoreOpsFunctionFnMutTupleUsizeU16.call_mut
}

/-- Trait implementation: [kopis::ser::serialize_10::{impl core::ops::function::Fn<(usize,), u16> for kopis::ser::serialize_10::closure<'_0, '_1>}]
    Source: 'src/ser.rs', lines 87:16-87:46 -/
@[reducible]
def ser.serialize_10.closure.Insts.CoreOpsFunctionFnTupleUsizeU16 :
  core.ops.function.Fn ser.serialize_10.closure Std.Usize Std.U16 := {
  FnMutInst := ser.serialize_10.closure.Insts.CoreOpsFunctionFnMutTupleUsizeU16
  call := ser.serialize_10.closure.Insts.CoreOpsFunctionFnTupleUsizeU16.call
}

end RustKopisAvx2
